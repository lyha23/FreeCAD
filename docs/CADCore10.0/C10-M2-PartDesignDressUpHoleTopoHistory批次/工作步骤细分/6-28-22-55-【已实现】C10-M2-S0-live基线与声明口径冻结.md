# 【已实现】C10-M2-S0 live 基线与声明口径冻结

## 目标

冻结 C10-M2 起点，明确允许声明、禁止声明、状态词典和验证命令。S0 只做 docs / matrix 口径冻结，不采 oracle，不改 C++。

## 输入

- `docs/CADCore10.0/README.md`
- `docs/CADCore10.0/C10-M1-SketchOpenWireInternalFaceStableSelector批次/`
- `docs/CADCore方案/细化方案/09-P6-TopoNaming主路径.md`
- `docs/CADCore方案/细化方案/10-P7-PartDesign常用生态.md`
- current `cad-core capabilities`

## 允许声明

| 声明 | 证据 |
| --- | --- |
| C10-M1 queue 已关闭 | C10-M1 S6 已实现，工作步骤目录无待执行 S0-S6。 |
| C10-M2 关注 DressUp / Hole topo history 第二阶段 | current capability 显示 DressUp / Hole producer matrix 已有 first slice。 |
| 本包默认不实现 CopyOnChange | CopyOnChange 是 SubShapeBinder retained known gap / oracle-blocked。 |
| S0 执行前 S0-S6 初始均为待执行 | 工作步骤总入口只是索引；S0 完成后只把队列推进到 S1，不打开 C++ gate。 |

## 禁止声明

- 不声明 DressUp stale `ReferenceShadow` / Base recovery 已 supported。
- 不声明 C10-M2 已发现 backend gap；backend gap 必须由 S3-S5 证明。
- 不声明 raw `FaceN`、bbox、面积、顺序、source index、fixture 名称可用于 stable selector。
- 不声明 Hole / DressUp 的 full universe 已支持。
- 不声明 CAD Core 可以保存跨请求 shape / BREP / NamedShape / ElementMap cache。

## 状态词典

| 状态 | 用法 |
| --- | --- |
| `baseline_frozen_s0` | S0 冻结起点和口径。 |
| `closed_s0` | 只表示 S0 文档 / 矩阵基线冻结已完成；不表示 DressUp / Hole backend gap 或 supported 范围扩大。 |
| `source_audit_pending` | S1 待读源码和 current coverage。 |
| `oracle_candidate` | 需要 native expected 或 checked-in expected。 |
| `current_mismatch_candidate` | 待比较 expected 与 current。 |
| `backend_gap_candidate` | S3-S5 证明 mismatch 后，S6 才能消费。 |
| `diagnostic_retained` | 明确保留诊断，不写 supported。 |
| `notCollected` | 没有 oracle 或不可观察，不是实现任务。 |
| `release_gate` | S6 发布实现、no-gap 或 retained diagnostic。 |

## 必须回写的矩阵行

- `C10M2-BLOCKER-000`：S0 完成后应从 `S0` 改为 `closed_s0`。
- `C10M2-SCOPE-001`：S0 完成后应从 `baseline_frozen_s0` 保持 release baseline，不能打开 C++ gate。
- `C10M2-VAL-000` 到 `C10M2-VAL-004`：确认通用验证命令可运行或说明当前环境限制。

## S0 冻结结果

- live baseline：`pwd=/home/user/Chili3DProject/FreeCAD`，起点 `HEAD=2420e3b842`（`docs: 完成 C10-M1 S6 发布闸门`）。
- 起点 dirty boundary：C10-M2 docs / matrices 为 in-scope untracked context，`docs/CADCore10.0/README.md` 为 in-scope modified context；无 `cad-core/src`、tests、fixtures、expected 或 capability 改动。
- `C10M2-BLOCKER-000` 已关闭为 `closed_s0`。
- `C10M2-SCOPE-001` 保持 `baseline_frozen_s0`，作为 S6 复核的 docs-only release baseline；不打开 C++ gate。
- `C10M2-VAL-000` 到 `C10M2-VAL-004` 均为当前环境可运行的 docs / matrix 验证命令；其中 trailing whitespace rg 退出码 1 表示无匹配，不是失败。

## 验收标准

```bash
cd /home/user/Chili3DProject/FreeCAD
git status --short -uall
git rev-parse --short=10 HEAD
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore10.0/C10-M2-PartDesignDressUpHoleTopoHistory批次/工作步骤细分 --format markdown
awk -F '\t' 'FNR==1{n=NF; next} NF!=n{print FILENAME ":" FNR ": expected " n " fields, got " NF; bad=1} END{exit bad}' docs/CADCore10.0/C10-M2-PartDesignDressUpHoleTopoHistory批次/矩阵/*.tsv
rg -n 'raw `FaceN`|bbox|source index|copy_on_change_full_temporary_document_cache' docs/CADCore10.0/C10-M2-PartDesignDressUpHoleTopoHistory批次
rg -n '[ \t]$' docs/CADCore10.0/C10-M2-PartDesignDressUpHoleTopoHistory批次 docs/CADCore10.0/README.md
git diff --check
```

`raw FaceN` / `bbox` / `source index` / `copy_on_change_full_temporary_document_cache` 检索用于确认 forbidden / non-goal 文字仍是禁止口径，不要求零匹配。

验收通过后，S0 文件已重命名为 `6-28-22-55-【已实现】C10-M2-S0-live基线与声明口径冻结.md`，并同步更新总入口、工作步骤总入口和交叉链接。

## 非目标

- 不运行 FreeCADCmd。
- 不采集或修改 expected。
- 不修改 `cad-core/src`、tests、fixtures 或 capability。
