# 【已实现】C10-M2 工作步骤总入口

## 目标

把 `PartDesign DressUp / Hole TopoHistory` 批次拆成 S0-S6。当前收口状态：S0 已完成 baseline 冻结，S1 到 S6 待执行。本文只是索引，创建后标 `【已实现】`，避免 goal 队列把索引当成实现步骤。

## 步骤索引

| 步骤 | 文件 | 当前状态 | 解决的问题 |
| --- | --- | --- | --- |
| S0 | `6-28-22-55-【已实现】C10-M2-S0-live基线与声明口径冻结.md` | 已实现 | 冻结 C10-M1 queue-empty、current capability、forbidden claims 和验证命令。 |
| S1 | `6-28-22-56-C10-M2-S1-FreeCAD源码与current覆盖候选矩阵.md` | 待执行 | 复核 FreeCAD source authority 与 current cad-core coverage。 |
| S2 | `6-28-22-57-C10-M2-S2-范围准入与blocker矩阵.md` | 待执行 | 对 oracle、backend gap、diagnostic 和 non-goal 做范围准入。 |
| S3 | `6-28-22-58-C10-M2-S3-DressUp生产者History专项复审.md` | 待执行 | 复核 DressUp AddSubShape slot、selection、Draft / Thickness history 和 chain consumption。 |
| S4 | `6-28-22-59-C10-M2-S4-Hole生产者History专项复审.md` | 待执行 | 复核 Hole `findHoles()`、profile-source、ModelThread、head-cut 和 Body cut history。 |
| S5 | `6-28-23-00-C10-M2-S5-跨特征旧引用恢复与diagnostic边界专项复审.md` | 待执行 | 复核 DressUp / Hole 旧引用恢复、split / deleted 诊断和跨特征传播边界。 |
| S6 | `6-28-23-01-C10-M2-S6-Oracle实现与发布闸门.md` | 待执行 | 消费 S3-S5 evidence，落 C++ / tests 或发布 no-code release gate。 |

## 执行顺序

1. 先做 S0，冻结 live baseline、允许声明和 forbidden claims。
2. S1 只读 FreeCAD source、current cad-core 和 tests，不采 oracle，不改 C++。
3. S2 完成 scope / blocker / non-goal / backend-gap 初始路由。
4. S3 复核 DressUp producer history。
5. S4 复核 Hole producer history。
6. S5 复核跨特征旧引用恢复和 diagnostic 边界。
7. S6 只消费已证明的 implementation row、releaseGate row 或 retained diagnostic row。

## 当前矩阵闸门

| 矩阵 | 当前用途 | 当前结论 |
| --- | --- | --- |
| `c10m2_dressup_hole_topohistory_source_candidates.tsv` | source authority seed | 待 S1 复核 FreeCAD authority 与 current cad-core coverage。 |
| `c10m2_dressup_hole_topohistory_scope_review_matrix.tsv` | scope route seed | 待 S2 路由；seed 不等于 supported。 |
| `c10m2_dressup_hole_topohistory_blocker_queue.tsv` | blocker queue seed | `C10M2-BLOCKER-000=closed_s0`，其余待 S1-S6 逐步关闭。 |
| `c10m2_dressup_hole_topohistory_non_goal_registry.tsv` | forbidden claims | CopyOnChange、full DressUp universe、GUI、persistent state 和 geometry guessing 均为 non-goal seed。 |
| `c10m2_dressup_hole_topohistory_backend_gap_classification.tsv` | S6 gate 分类 | 当前只有 candidate / release gate seed，无已证明 backend gap。 |
| `c10m2_dressup_hole_topohistory_validation_matrix.tsv` | 分层验收 | 每步按 required_at 执行。 |

## 状态纪律

- S0-S6 只有在各自 `验收标准` 通过后才能重命名为 `【已实现】`。
- S0 只关闭 docs / matrix 基线 blocker；`C10M2-SCOPE-001` 仍是 `baseline_frozen_s0` release baseline，不打开 C++ gate。
- `backend_gap_candidate` 不能直接变成 C++ 任务；必须先有 FreeCAD authority 和 current mismatch evidence。
- `notCollected` 不是实现任务；它只能触发 oracle 补采或 retained diagnostic。
- 不允许把 raw `FaceN`、bbox、面积、source index、split order、输出排序或 fixture 名称写成 stable selector。

## 通用验收

```bash
cd /home/user/Chili3DProject/FreeCAD
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore10.0/C10-M2-PartDesignDressUpHoleTopoHistory批次/工作步骤细分 --format markdown
awk -F '\t' 'FNR==1{n=NF; next} NF!=n{print FILENAME ":" FNR ": expected " n " fields, got " NF; bad=1} END{exit bad}' docs/CADCore10.0/C10-M2-PartDesignDressUpHoleTopoHistory批次/矩阵/*.tsv
rg -n '[ \t]$' docs/CADCore10.0/C10-M2-PartDesignDressUpHoleTopoHistory批次 docs/CADCore10.0/README.md
git diff --check
```

## 非目标

- 不重开 SubShapeBinder CopyOnChange。
- 不实现完整 DressUp universe 或 GUI 交互。
- 不引入跨请求 shape / BREP / NamedShape / ElementMap cache。
- 不把 stale `ReferenceShadow` / Base recovery 的 oracle-blocked 行声明为 supported。
- 不把 downstream Rust、frontend selection UI 或 adapter protocol cleanup 混进本包。
