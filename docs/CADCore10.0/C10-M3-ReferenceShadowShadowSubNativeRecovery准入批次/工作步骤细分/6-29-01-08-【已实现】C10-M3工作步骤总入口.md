# 【已实现】C10-M3 工作步骤总入口

## 目标

把 `ReferenceShadow / ShadowSub Native Recovery` 准入批次拆成 S0-S6。当前收口状态：S0-S1 已完成，S2 到 S6 均为待执行。本文只是索引，创建后标 `【已实现】`，避免 goal 队列把索引当成实现步骤。

## 步骤索引

| 步骤 | 文件 | 当前状态 | 解决的问题 |
| --- | --- | --- | --- |
| S0 | `6-29-01-09-【已实现】C10-M3-S0-live基线与声明口径冻结.md` | 已完成 | 冻结 C10-M2 queue-empty、current retained diagnostic 和 forbidden claims。 |
| S1 | `6-29-01-10-【已实现】C10-M3-S1-FreeCAD源码与current覆盖候选矩阵.md` | 已完成 | 复核 FreeCAD source authority 与 current cad-core recovery coverage。 |
| S2 | `6-29-01-11-C10-M3-S2-范围准入与blocker矩阵.md` | 待执行 | 对 native oracle、backend gap、diagnostic 和 non-goal 做范围准入。 |
| S3 | `6-29-01-12-C10-M3-S3-native可观测性与oracle采集专项复审.md` | 待执行 | 判断 FCStd / XML restore 后 ShadowSub / ReferenceShadow 是否 native observable。 |
| S4 | `6-29-01-13-C10-M3-S4-cad-core恢复路径与current mismatch专项复审.md` | 待执行 | 比较 cad-core recovery path 与 native evidence，必要时打开 backend gap。 |
| S5 | `6-29-01-14-C10-M3-S5-前端协议诊断与non-goal边界专项复审.md` | 待执行 | 复核 elementReferenceUpdates、ReferenceShadow.brep 单 subshape 例外和禁止 shortcut。 |
| S6 | `6-29-01-15-C10-M3-S6-Oracle实现与发布闸门.md` | 待执行 | 消费 S3-S5 evidence，落 C++ / tests 或发布 no-code release gate。 |

## 执行顺序

1. S0 已冻结 live baseline、允许声明和 forbidden claims。
2. S1 只读 FreeCAD source、current cad-core 和 tests，不采 oracle，不改 C++。
3. S2 完成 scope / blocker / non-goal / backend-gap 初始路由。
4. S3 复核 native 可观测性和 oracle 采集路线。
5. S4 复核 cad-core recovery path 与 current mismatch。
6. S5 复核前端协议、diagnostic 和 non-goal 边界。
7. S6 只消费已证明的 implementation row、releaseGate row 或 retained diagnostic row。

## 当前矩阵闸门

| 矩阵 | 当前用途 | 当前结论 |
| --- | --- | --- |
| `c10m3_reference_shadow_recovery_source_candidates.tsv` | source authority seed | S1 已复核 FreeCAD authority、replacement path 风险与 current cad-core coverage。 |
| `c10m3_reference_shadow_recovery_scope_review_matrix.tsv` | scope route seed | 待 S2 路由；seed 不等于 supported。 |
| `c10m3_reference_shadow_recovery_blocker_queue.tsv` | blocker queue seed | `C10M3-BLOCKER-000=closed_s0`，`C10M3-BLOCKER-101=closed_s1`；S2-S6 待逐步关闭。 |
| `c10m3_reference_shadow_recovery_non_goal_registry.tsv` | forbidden claims | raw geometry selector、cross-request cache、CopyOnChange、full BREP 和 GUI 均为 non-goal seed。 |
| `c10m3_reference_shadow_recovery_backend_gap_classification.tsv` | S6 gate 分类 | 当前只有 candidate / release gate seed，无已证明 backend gap。 |
| `c10m3_reference_shadow_recovery_validation_matrix.tsv` | 分层验收 | 每步按 required_at 执行。 |

## 状态纪律

- S0-S6 只有在各自 `验收标准` 通过后才能重命名为 `【已实现】`。
- `native_oracle_candidate` 不能直接变成 C++ 任务；必须先有 native observable evidence 和 current mismatch。
- `notCollected` 不是实现任务；它只能触发 oracle 补采或 retained diagnostic。
- 不允许把 raw `FaceN`、bbox、面积、source index、split order、输出排序或 fixture 名称写成 stable selector。

## 通用验收

```bash
cd /home/user/Chili3DProject/FreeCAD
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore10.0/C10-M3-ReferenceShadowShadowSubNativeRecovery准入批次/工作步骤细分 --format markdown
awk -F '\t' 'FNR==1{n=NF; next} NF!=n{print FILENAME ":" FNR ": expected " n " fields, got " NF; bad=1} END{exit bad}' docs/CADCore10.0/C10-M3-ReferenceShadowShadowSubNativeRecovery准入批次/矩阵/*.tsv
rg -n '[ \t]$' docs/CADCore10.0/C10-M3-ReferenceShadowShadowSubNativeRecovery准入批次 docs/CADCore10.0/README.md
git diff --check
```

## 非目标

- 不重开 SubShapeBinder CopyOnChange full temporary-document cache。
- 不实现不可观察 stale ReferenceShadow / Base recovery。
- 不引入跨请求 shape / BREP / NamedShape / ElementMap cache。
- 不把 split one-to-many、deleted 或 source evidence 不足的旧引用声明为 supported。
- 不把 downstream Rust、frontend selection UI 或 adapter protocol cleanup 混进本包。
