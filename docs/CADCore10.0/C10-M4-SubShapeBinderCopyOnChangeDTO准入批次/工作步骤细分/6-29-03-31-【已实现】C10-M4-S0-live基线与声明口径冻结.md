# 【已实现】C10-M4 S0 live 基线与声明口径冻结

## 目标

冻结 C10-M4 起点：当前仓库、HEAD、C10-M1/M2/M3 队列状态、live capability 的 CopyOnChange retained gap，以及本包 allowed / forbidden claim。S0 只改文档和矩阵，不采 oracle，不改 C++。

## 必做动作

1. 运行 `git status --short -uall` 和 `git rev-parse --short=10 HEAD`，把起点写入 README / scope matrix。
2. 运行 C10-M1、C10-M2、C10-M3、C10-M4 queue 检查，确认旧队列为空且 C10-M4 下一步为 S0。
3. 用 `rg` 复核 `cad-core/src/runtime/capability_contract.cpp` 中 `copy_on_change_full_temporary_document_cache_not_supported`、`remaining_gaps` 和 retained diagnostic 口径。
4. 更新 `c10m4_copy_on_change_dto_scope_review_matrix.tsv`、`blocker_queue.tsv`、`non_goal_registry.tsv` 的 S0 状态。
5. 将本文件通过验收后重命名为 `【已实现】`。

## 执行结果

- `pwd=/home/user/Chili3DProject/FreeCAD`
- S0 起点 HEAD：`cd8cd95fa2`（`cd8cd95fa2 docs: 新增 C10-M4 CopyOnChange DTO 准入方案`）。
- S0 起点 dirty state：`git -c core.quotepath=false status --short -uall` 无输出，工作区干净。
- 队列状态：C10-M1 / C10-M2 / C10-M3 队列为空；C10-M4 在 S0 执行前下一步为 S0。
- retained gap：`copy_on_change_full_temporary_document_cache` 仍是 `known_gap_diagnostic` / `oracle_blocked`，diagnostic 为 `copy_on_change_full_temporary_document_cache_not_supported`，`remaining_gaps=["copy_on_change_full_temporary_document_cache"]`。
- forbidden claims 已冻结：不声明 full temporary-document cache supported；不声明 backend session、persistent copied object、cross-request BREP / TopoDS / NamedShape / ElementMap cache supported；不把 App::Link CopyOnChange current DTO 自动等同为 SubShapeBinder CopyOnChange supported。
- 本步骤未采 oracle，未改 C++，未提前执行 S1-S6。

## 允许声明

- C10-M4 是 SubShapeBinder CopyOnChange request-local DTO 准入包。
- 当前 live gap 仍是 known gap diagnostic / oracle blocked。
- S6 只有在 S3-S5 同时证明 native evidence、产品批准 DTO 和 current mismatch 时才改 C++。

## 禁止声明

- 不声明 full temporary-document cache supported。
- 不声明 backend session、persistent copied object、cross-request BREP / TopoDS / NamedShape / ElementMap cache supported。
- 不把 App::Link CopyOnChange current DTO 自动等同为 SubShapeBinder CopyOnChange supported。

## 验收标准

```bash
cd /home/user/Chili3DProject/FreeCAD
git status --short -uall
git rev-parse --short=10 HEAD
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore10.0/C10-M4-SubShapeBinderCopyOnChangeDTO准入批次/工作步骤细分 --format markdown
rg -n "copy_on_change_full_temporary_document_cache|known_gap_diagnostic|oracle_blocked|remaining_gaps" cad-core/src/runtime/capability_contract.cpp
awk -F '\t' 'FNR==1{n=NF; next} NF!=n{print FILENAME ":" FNR ": expected " n " fields, got " NF; bad=1} END{exit bad}' docs/CADCore10.0/C10-M4-SubShapeBinderCopyOnChangeDTO准入批次/矩阵/*.tsv
rg -n '[ \t]$' docs/CADCore10.0/C10-M4-SubShapeBinderCopyOnChangeDTO准入批次 docs/CADCore10.0/README.md
git diff --check
```

`rg -n '[ \t]$'` 退出码 1 表示没有 trailing whitespace，是通过。
