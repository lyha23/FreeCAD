# C10-M4 S2 范围准入与 blocker 矩阵

## 目标

把 C10-M4 的 candidate rows 路由成可执行范围：native oracle、request-local DTO product decision、current mismatch、diagnostic retained、non-goal 和 S6 release gate。S2 不采 oracle，不改 C++。

## 必做动作

1. 复核 `scope_review_matrix.tsv` 的每个 `current_status` 是否属于本包词汇：`baseline_pending`、`native_oracle_candidate`、`product_decision_needed`、`current_retained_diagnostic`、`dto_boundary_candidate`、`diagnostic_retained`、`backend_gap_candidate`、`release_gate`。
2. 为每个 scope 指定 owner step、fixture/test route 和 close condition。
3. 确认 `backend_gap_classification.tsv` 不直接把 `known_gap_diagnostic` 升级为 implementation row。
4. 确认 `non_goal_registry.tsv` 覆盖 backend session、persistent temp doc/cache、cross-request geometry state、GUI、adapter repair 和 unsupported claim。
5. 通过验收后重命名本文件为 `【已实现】`。

## 准入规则

- `native_oracle_candidate` 只能进入 S3，不能进入 C++。
- `product_decision_needed` 只能进入 DTO 边界确认，不能进入 C++。
- `backend_gap_candidate` 必须由 S3-S5 共同产生。
- `diagnostic_retained` 在 S6 只能发布 retained diagnostic，除非新增 evidence 改变分类。

## 验收标准

```bash
cd /home/user/Chili3DProject/FreeCAD
awk -F '\t' 'FNR==1{n=NF; next} NF!=n{print FILENAME ":" FNR ": expected " n " fields, got " NF; bad=1} END{exit bad}' docs/CADCore10.0/C10-M4-SubShapeBinderCopyOnChangeDTO准入批次/矩阵/*.tsv
rg -n "backend_gap_candidate|product_decision_needed|diagnostic_retained|release_gate|backend session|persistent temporary|cross-request|adapter" docs/CADCore10.0/C10-M4-SubShapeBinderCopyOnChangeDTO准入批次
rg -n '[ \t]$' docs/CADCore10.0/C10-M4-SubShapeBinderCopyOnChangeDTO准入批次 docs/CADCore10.0/README.md
git diff --check
```
