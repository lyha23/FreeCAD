# 【已实现】C10-M4 工作步骤总入口

## 目标

把 SubShapeBinder CopyOnChange request-local DTO 准入批次拆成 S0-S6。本文只是索引，创建后标 `【已实现】`，避免 goal 队列把索引当成实现步骤。

## 步骤索引

| 步骤 | 文件 | 当前状态 | 解决的问题 |
| --- | --- | --- | --- |
| S0 | `6-29-03-31-【已实现】C10-M4-S0-live基线与声明口径冻结.md` | 已实现 | 已冻结 C10-M1/M2/M3 queue-empty、current retained gap 和 forbidden claims。 |
| S1 | `6-29-03-32-C10-M4-S1-FreeCAD源码与current覆盖候选矩阵.md` | 待执行 | 复核 FreeCAD ShapeBinder / Link CopyOnChange source authority 与 current cad-core coverage。 |
| S2 | `6-29-03-33-C10-M4-S2-范围准入与blocker矩阵.md` | 待执行 | 对 native oracle、DTO product decision、backend gap、diagnostic 和 non-goal 做范围准入。 |
| S3 | `6-29-03-34-C10-M4-S3-CopyOnChangeNativeProbe与DTO证据专项复审.md` | 待执行 | 复审 native copied-object evidence 能否转为 request-local DTO。 |
| S4 | `6-29-03-35-C10-M4-S4-cad-core请求DTO与documentObjectUpdates专项复审.md` | 待执行 | 比较 current diagnostic、App::Link DTO 和 SubShapeBinder DTO 可实现边界。 |
| S5 | `6-29-03-36-C10-M4-S5-临时文档禁用与non-goal边界专项复审.md` | 待执行 | 明确禁止 backend session、persistent temp doc/cache、full BREP / TopoDS persistence 和 GUI lifecycle。 |
| S6 | `6-29-03-37-C10-M4-S6-Oracle实现与发布闸门.md` | 待执行 | 消费 S3-S5 evidence；有 implementation row 才落 C++，否则发布 no-code retained diagnostic。 |

## 状态纪律

- S0-S6 只有在各自验收标准通过后才能重命名为 `【已实现】`。
- `native_oracle_candidate` 不能直接变成实现任务；必须先有 native copied-object evidence、产品批准 DTO 和 current mismatch。
- `product_decision_needed` 不是 C++ 任务；它只能触发 DTO 边界确认。
- 不允许把 FreeCAD temporary document cache、backend session 或 cross-request shape cache 写成 cad-core supported 范围。

## 通用验收

```bash
cd /home/user/Chili3DProject/FreeCAD
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore10.0/C10-M4-SubShapeBinderCopyOnChangeDTO准入批次/工作步骤细分 --format markdown
awk -F '\t' 'FNR==1{n=NF; next} NF!=n{print FILENAME ":" FNR ": expected " n " fields, got " NF; bad=1} END{exit bad}' docs/CADCore10.0/C10-M4-SubShapeBinderCopyOnChangeDTO准入批次/矩阵/*.tsv
rg -n '[ \t]$' docs/CADCore10.0/C10-M4-SubShapeBinderCopyOnChangeDTO准入批次 docs/CADCore10.0/README.md
git diff --check
```
