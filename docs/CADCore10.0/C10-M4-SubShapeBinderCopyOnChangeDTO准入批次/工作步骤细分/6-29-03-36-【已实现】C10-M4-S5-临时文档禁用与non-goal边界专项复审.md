# 【已实现】C10-M4 S5 临时文档禁用与 non-goal 边界专项复审

## 目标

把 SubShapeBinder CopyOnChange 的不可实现边界写清：后端不能持有 FreeCAD temporary document、backend session、persistent copied object、cross-request BREP / TopoDS / NamedShape / ElementMap cache，也不能由 adapter 或前端 mock 替代核心语义。

## 必做动作

1. 复核 `non_goal_registry.tsv` 是否覆盖所有 forbidden claim。
2. 复核 S3/S4 是否引入任何 backend state、full object BREP persistence、adapter output repair 或 GUI lifecycle 表述。
3. 对每个 non-goal 写明 reopen condition：必须是架构批准或 native evidence + product DTO + current mismatch，不允许模糊重开。
4. 更新 `backend_gap_classification.tsv`：不满足实现条件的行必须回到 `diagnostic_retained` 或 `release_gate`。
5. 通过验收后重命名本文件为 `【已实现】`。

## 必保留边界

- `ReferenceShadow.brep` 的 single-subshape evidence 例外不能扩展成 full object BREP persistence。
- cad-core 请求结束后不保留 copied-object graph 或 shape cache。
- adapter 只能透传 `documentObjectUpdates` / diagnostics，不能生成业务语义。
- unsupported `BindCopyOnChange` / `PartialLoad` 必须继续有结构化 diagnostic。

## S5 执行结果

- 起点基线：`pwd=/home/user/Chili3DProject/FreeCAD`，`HEAD=6963ff7a4e`（`6963ff7a4e docs: 完成 C10-M4 S4 DTO 复审`），`git -c core.quotepath=false status --short -uall` 无输出。
- 已复核 S3/S4 文档：S3 只证明 `BindCopyOnChange=Disabled/Enabled/Mutated` 与 `PartialLoad` property/session evidence，未导出 request-local copied-object DTO；S4 只把 App::Link `documentObjectUpdates` 作为 reference path，未把 SubShapeBinder CopyOnChange 升级为 supported。
- S3/S4 未引入 backend state、full object BREP persistence、adapter output repair 或 GUI lifecycle 作为 cad-core 行为；`backend_gap_candidate` 仍未打开。
- `non_goal_registry.tsv` 已覆盖 backend session、persistent temporary document/cache、persistent copied object、cross-request BREP / TopoDS / NamedShape / ElementMap cache、GUI lifecycle、adapter repair、frontend mock、unsupported claim、App::Link lifecycle 非等价和 `ReferenceShadow.brep` full-object persistence 禁止项。
- 每个 non-goal 已写清 reopen condition：persistent backend state 只能由明确架构批准重开；request-local CopyOnChange 替代方案必须同时具备 native copied-object evidence、product DTO approval 和 current cad-core mismatch。
- `backend_gap_classification.tsv` 已把未满足实现条件的行保持在 `diagnostic_retained` / `release_gate`；unsupported `BindCopyOnChange` / `PartialLoad` 继续要求结构化 diagnostic。
- `scope_review_matrix.tsv`、`blocker_queue.tsv`、`validation_matrix.tsv` 和 README 已对齐 S5 结论；`C10M4-BLOCKER-501` 关闭为 `closed_s5_diagnostic_retained`，`C10M4-BLOCKER-601` / S6 继续 pending。
- 本步骤未改 C++，未运行 FreeCADCmd，未打开 backend_gap_candidate。

## 验收标准

```bash
cd /home/user/Chili3DProject/FreeCAD
rg -n "backend session|persistent temporary|cross-request|full object BREP|TopoDS|NamedShape|ElementMap|GUI|adapter|diagnostic_retained|release_gate|ReferenceShadow" docs/CADCore10.0/C10-M4-SubShapeBinderCopyOnChangeDTO准入批次 cad-core/src/runtime/capability_contract.cpp
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore10.0/C10-M4-SubShapeBinderCopyOnChangeDTO准入批次/工作步骤细分 --format markdown
awk -F '\t' 'FNR==1{n=NF; next} NF!=n{print FILENAME ":" FNR ": expected " n " fields, got " NF; bad=1} END{exit bad}' docs/CADCore10.0/C10-M4-SubShapeBinderCopyOnChangeDTO准入批次/矩阵/*.tsv
rg -n '[ \t]$' docs/CADCore10.0/C10-M4-SubShapeBinderCopyOnChangeDTO准入批次 docs/CADCore10.0/README.md
git diff --check
```

`rg -n '[ \t]$'` 退出码 1 表示没有 trailing whitespace，是通过。

## 最终验收结果

- S5 non-goal `rg`：已运行，退出码 0，命中 backend session、persistent temporary、cross-request、full object BREP、TopoDS、NamedShape、ElementMap、GUI、adapter、`diagnostic_retained`、`release_gate` 与 `ReferenceShadow` 口径。
- queue：已运行，S5 被跳过，下一项为 S6。
- TSV 列数：已运行，退出码 0。
- trailing whitespace：已运行，退出码 1，表示无匹配。
- `git diff --check`：已运行，退出码 0。
