# 【已实现】C10-M4 S6 Oracle 实现与发布闸门

## 目标

消费 S3-S5 的证据和矩阵分类。若存在 `backend_gap_candidate`，按 FreeCAD 调用链、request-local DTO 和 current mismatch 落 C++ / fixtures / tests / capability；若不存在 implementation row，发布 no-code retained diagnostic / release gate。

## 实现条件

S6 改 C++ 前必须同时满足：

1. S3 有 native copied-object evidence，且不依赖 backend session。
2. S4 有产品批准的 request-local DTO 输入 / 输出 / writeback 边界。
3. S4 证明 current cad-core 与 native expected 存在 mismatch。
4. S5 确认实现不引入 persistent temporary document/cache、cross-request geometry state 或 adapter output repair。

## 可改落点

- `cad-core/src/part_design/feature_shape_binder.cpp`
- `cad-core/include/cad_core/part_design/feature_shape_binder.h`
- `cad-core/src/app/copy_on_change.cpp`
- `cad-core/include/cad_core/app/copy_on_change.h`
- `cad-core/src/runtime/capability_contract.cpp`
- `cad-core/tests/test_p7_features.py`
- `cad-core/tests/test_adapters.py`
- 必要时新增 `cad-core/fixtures/c10m4` 和 collector / probe。

## 发布规则

- 有 implementation row：先写 FreeCAD 依据注释，再实现 DTO / diagnostics / tests / capability；验证通过后关闭 row。
- 无 implementation row：不改 C++，把 retained diagnostic / release gate 写入 README、scope、blocker、backend-gap matrix，并关闭队列。
- 无论哪种结果，S6 都必须刷新 `step_goal_queue.py` 输出并证明队列状态。

## S6 执行结果

- 起点基线：`pwd=/home/user/Chili3DProject/FreeCAD`，`HEAD=376e3dba31`（`376e3dba31 docs: 完成 C10-M4 S5 non-goal 边界复审`），`git -c core.quotepath=false status --short -uall` 无输出。
- 已消费 S3-S5 证据：S3 没有 request-local copied-object evidence，copied-object lifecycle 为 `notCollected`；S4 只确认 App::Link `documentObjectUpdates` 是 reference-only DTO 路径，SubShapeBinder 仍是 `product_decision_needed` / `current_retained_diagnostic`；S5 确认 no backend session、no persistent temporary document/cache、no cross-request geometry state、no adapter repair。
- 矩阵列级检查确认没有任何 `route` / `current_status` / `status` 列被分类为 `backend_gap_candidate`。当前出现的 `backend_gap_candidate` 仅是未来 reopen condition 或状态词汇说明，不是 implementation row。
- 结论：S6 走 docs-only no-code retained diagnostic / release gate；不改 C++，不新增 fixture / probe / collector，不运行 build / focused tests。
- capability contract 保持不变：`copy_on_change_full_temporary_document_cache` 仍是 `known_gap_diagnostic` / `oracle_blocked` / `remaining_gaps`；unsupported `BindCopyOnChange=Enabled/Mutated` 与 `PartialLoad=True` 继续由 `copy_on_change_full_temporary_document_cache_not_supported` 结构化 diagnostic 表达。
- 已更新 README、总入口、scope review matrix、backend gap classification、blocker queue、validation matrix 和父级 `docs/CADCore10.0/README.md`；`C10M4-SCOPE-401=release_closed`，`C10M4-CAT-105=release_closed`，`C10M4-BLOCKER-601=closed_s6`。

## 验收标准

Docs-only release gate：

```bash
cd /home/user/Chili3DProject/FreeCAD
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore10.0/C10-M4-SubShapeBinderCopyOnChangeDTO准入批次/工作步骤细分 --format markdown
awk -F '\t' 'FNR==1{n=NF; next} NF!=n{print FILENAME ":" FNR ": expected " n " fields, got " NF; bad=1} END{exit bad}' docs/CADCore10.0/C10-M4-SubShapeBinderCopyOnChangeDTO准入批次/矩阵/*.tsv
rg -n '[ \t]$' docs/CADCore10.0/C10-M4-SubShapeBinderCopyOnChangeDTO准入批次 docs/CADCore10.0/README.md
git diff --check
```

Implementation gate 额外执行：

```bash
cd /home/user/Chili3DProject/FreeCAD/cad-core
cmake --build build
python3 -m unittest tests.test_p7_features tests.test_adapters
```

若新增 collector / expected，还必须记录 collector 命令、FreeCAD / LibPack / OCCT 版本和 expected 更新结论。

## 最终验收结果

- capability `rg`：已运行，退出码 0；`cad-core/src/runtime/capability_contract.cpp` 仍命中 `copy_on_change_full_temporary_document_cache`、`known_gap_diagnostic`、`oracle_blocked`、`copy_on_change_full_temporary_document_cache_not_supported` 和 `remaining_gaps={"copy_on_change_full_temporary_document_cache"}`。
- backend-gap 当前列检查：已运行，退出码 0，无输出，表示 `route` / `current_status` / `status` 列没有当前 `backend_gap_candidate` 行。
- queue：已运行，退出码 0，只输出表头，表示 C10-M4 工作步骤队列为空。
- TSV 列数：已运行，退出码 0。
- trailing whitespace：已运行，退出码 1，表示无匹配。
- `git diff --check`：已运行，退出码 0。
