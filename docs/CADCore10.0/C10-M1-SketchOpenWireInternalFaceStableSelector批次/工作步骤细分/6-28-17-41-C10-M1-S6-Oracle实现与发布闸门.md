# C10-M1-S6 Oracle 实现与发布闸门

## 目标

根据 S3-S5 证据完成 C10-M1 release gate。S6 只消费已经明确的 `backend_gap_requires_implementation`、`backend_gap_candidate`、`diagnostic_retained`、`notCollected` 或 `release_gate` row，不新增业务范围。

## 入场条件

- S0-S5 均已按验收通过并重命名为 `【已实现】`。
- `c10m1_sketch_openwire_scope_review_matrix.tsv` 中每个 scope 都有最终 route。
- `c10m1_sketch_openwire_backend_gap_classification.tsv` 明确说明是否存在 `backend_gap_requires_implementation`。
- S3/S4 oracle evidence 与 S5 stable selector 裁决一致。

## 本轮代码实现目标

只有存在 `backend_gap_requires_implementation` 时，S6 才允许落代码。

| blocker / scope | C++ 落点 | FreeCAD authority | 测试 | 成功标准 |
| --- | --- | --- | --- | --- |
| `C10M1-BLOCKER-601` / `C10M1-SCOPE-101` | `cad-core/src/part/face_maker.cpp`、`cad-core/src/sketcher/sketch_internal_builder.cpp` | `SketchObject::buildInternals()`、`FaceMakerBuildFace::Build_Essence()` | `cad-core/tests/test_p5_sketch.py`、新增 `cad-core/fixtures/c10m1` | near-tangent / coincident expected-backed InternalShape parity。 |
| `C10M1-BLOCKER-601` / `C10M1-SCOPE-103` | `cad-core/src/part/wire_joiner.cpp`、`cad-core/src/part/internal_shape_history_publisher.cpp` | `WireJoinerP::build()`、`getOpenWires()` | `cad-core/tests/test_p5_sketch.py`、`cad-core/tests/test_adapters.py` | complex open-wire unique evidence 进入 mapper events / ElementMap；多解保持 diagnostic。 |
| `C10M1-BLOCKER-601` / `C10M1-SCOPE-201` | `cad-core/src/part_design/profile_resolver.cpp`、`cad-core/src/runtime/element_reference_update.cpp` | `ProfileBased::getTopoShapeVerifiedFace()`、Sketch internal element map | `cad-core/tests/test_p6_topology.py`、`cad-core/tests/test_adapters.py` | `Profile.StableSubList=InternalFaceN` without `ReferenceShadow` 只在唯一 request-local evidence 下解析，缺证据或多解时发 diagnostic。 |
| `C10M1-BLOCKER-601` / `C10M1-SCOPE-202` | `cad-core/src/runtime/capability_contract.cpp` | current capability `wire_joiner` / `topo_history` / `part_design.pad_pocket` | `cad-core/tests/test_adapters.py` | capability 发布 supported / diagnostic / retained blocker 与 tests 一致。 |

禁止路径：

- 不按 fixture 名、bbox、面积、长度、几何类型或输出排序猜 InternalFace。
- 不把 raw `FaceN` 当成 stable `InternalFaceN`。
- 不在 adapter/output JSON 末端补业务逻辑。
- 不保存跨请求 Sketch document、InternalShape、BREP、TopoDS_Shape、NamedShape、ElementMap 或 open-wire cache。
- 不让 open wire 产生假 `profile_ready=true`。

## no-code release gate

若 S5 没有批准 implementation gate，S6 必须关闭为 no-code release gate：

- 保持 without-ReferenceShadow stable selector 为 diagnostic / retained blocker。
- 更新 README、总入口、方案、步骤索引和矩阵，记录 S3-S5 证据。
- 保持 reopen condition：只有 FreeCAD oracle 和 current evidence 证明唯一 request-local ElementMap-backed selector 后才替换。

## 验收

文档 / 队列验收：

```bash
cd /home/user/Chili3DProject/FreeCAD
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore10.0/C10-M1-SketchOpenWireInternalFaceStableSelector批次/工作步骤细分 --format markdown
awk -F '\t' 'FNR==1{n=NF; next} NF!=n{print FILENAME ":" FNR ": expected " n " fields, got " NF; bad=1} END{exit bad}' docs/CADCore10.0/C10-M1-SketchOpenWireInternalFaceStableSelector批次/矩阵/*.tsv
rg -n '[ \t]$' docs/CADCore10.0
git diff --check
```

实现 gate 验收，仅在 S6 改 C++ 时执行：

```bash
cd /home/user/Chili3DProject/FreeCAD/cad-core
cmake --build build
python3 -m unittest tests.test_p5_sketch tests.test_p6_topology tests.test_adapters
```

## 验收标准

- 若 no-code，队列为空，矩阵记录 retained diagnostic / notCollected 裁决，capability 不伪装支持。
- 若 code gate，新增或修改的 fixture / tests / capability 与 FreeCAD oracle 和 current comparison 同步。
- S6 完成后所有本包链接无 stale basename。

## 非目标

- 不执行全量 FreeCAD CI。
- 不把 downsteam Rust 同步混进本 repo C++ gate。
