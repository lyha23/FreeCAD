# 【已实现】C10-M1-S2 范围准入与 blocker 矩阵

## 目标

把 S1 的 source/current evidence 分类到明确路线：`native_oracle_required`、`current_mismatch_candidate`、`backend_gap_candidate`、`diagnostic_retained`、`diagnostic_non_goal`、`release_gate` 或 `notCollected`。S2 不采 oracle、不改 C++。

## live 基线

| 命令 | S2 记录 |
| --- | --- |
| `pwd` | `/home/user/Chili3DProject/FreeCAD` |
| `git rev-parse --short HEAD` | `b53dd572ad` |
| `git log -1 --oneline` | `b53dd572ad docs: 完成 C10-M1 S1 源码覆盖矩阵审计` |
| `git -c core.quotepath=false status --short -uall` | 无输出，工作区干净。 |
| C10-M1 queue | 下一项为 S2。 |

## 分类规则

| 状态 | 使用条件 | 下一步 |
| --- | --- | --- |
| `native_oracle_required` | 需要 FreeCADCmd / native probe 证明行为 | 交 S3 或 S4。 |
| `current_mismatch_candidate` | 已有 oracle 或明确 current mismatch 线索 | 交 S6 前复核。 |
| `backend_gap_candidate` | 可能实现，但证据尚不足 | S3-S5 前不得落代码。 |
| `diagnostic_retained` | 多解 / 缺证据 / not serializable，需要保持显式诊断 | 交 S5/S6 发布。 |
| `diagnostic_non_goal` | Solver、GUI、cache、guessing 等明确排除 | 写 non-goal registry。 |
| `release_gate` | capability、diagnostic、test、docs 发布一致性 | 交 S6。 |
| `notCollected` | oracle 尚未采集或环境阻断 | 只能补 oracle 或保留 blocker。 |

## 必须回写的矩阵行

- `C10M1-SCOPE-001` 和 `C10M1-SCOPE-101` 到 `C10M1-SCOPE-105`
- `C10M1-BLOCKER-201`
- `C10M1-CAT-101` 到 `C10M1-CAT-104`
- `C10M1-NG-001` 到 `C10M1-NG-006`

## S2 准入结论

| 行 | S2 route | owner step | 结论 |
| --- | --- | --- | --- |
| `C10M1-SCOPE-001` | `release_gate` | S6 | S0 的 `baseline_frozen_s0` 作为发布闸门保留；不从 baseline 打开 C++ gate。 |
| `C10M1-SCOPE-101` | `native_oracle_required` | S3 | near-tangent / coincident 仍需 FreeCAD oracle，当前 `cad-core/fixtures/c10m1` 视为 `notCollected`。 |
| `C10M1-SCOPE-102` | `backend_gap_candidate` | S3 -> S6 | 只有 S3 证明 expected-backed `current_mismatch_candidate` 后，S6 才能消费 FaceMaker producer history gap。 |
| `C10M1-SCOPE-103` | `native_oracle_required` | S4 | complex open-wire / WireJoiner ledger 仍需 native evidence，不能从 current 输出倒推 expected。 |
| `C10M1-SCOPE-104` | `diagnostic_retained` | S4 -> S6 | ambiguous one-to-many open-wire history、缺 child-wire evidence 或 summary-only history 保持 diagnostic。 |
| `C10M1-SCOPE-105` | `backend_gap_candidate` | S5 | without `ReferenceShadow` 的 `StableSubList=InternalFaceN` 只保持产品边界待裁决；S2 不写 supported。 |

## 矩阵回写结果

- `scope_review_matrix.tsv` 已为 `C10M1-SCOPE-001`、`101..105` 写入 S2 route、owner step 和 close condition。
- `backend_gap_classification.tsv` 已为 `C10M1-CAT-101..104` 写入 `route` 与 close condition：S3/S4 负责 native oracle 或 `notCollected`，S5 负责产品边界，S6 负责 release gate。
- `blocker_queue.tsv` 已把 `C10M1-BLOCKER-201` 标为 `closed_s2`；S3/S4/S5/S6 blocker 保持 pending。
- `non_goal_registry.tsv` 已把 `C10M1-NG-001..006` 统一为 `diagnostic_non_goal`，覆盖 full solver、GUI、cross-request cache、raw FaceN alias、geometry guessing 和 downstream Rust。
- 未新增 `backend_gap_requires_implementation`，未采 oracle，未新增 fixture / expected / tests，未修改 C++。

## 验收

```bash
cd /home/user/Chili3DProject/FreeCAD
rg -n 'C10M1-SCOPE|C10M1-BLOCKER|C10M1-CAT|native_oracle_required|current_mismatch_candidate|backend_gap_candidate|diagnostic_retained|diagnostic_non_goal|release_gate|notCollected' docs/CADCore10.0/C10-M1-SketchOpenWireInternalFaceStableSelector批次
awk -F '\t' 'FNR==1{n=NF; next} NF!=n{print FILENAME ":" FNR ": expected " n " fields, got " NF; bad=1} END{exit bad}' docs/CADCore10.0/C10-M1-SketchOpenWireInternalFaceStableSelector批次/矩阵/*.tsv
rg -n '[ \t]$' docs/CADCore10.0
git diff --check
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore10.0/C10-M1-SketchOpenWireInternalFaceStableSelector批次/工作步骤细分 --format markdown
```

## 验收标准

- 每个 scope 都有合法状态、cad-core landing 或 exclusion reason。
- 每个 blocker / nonGoal / backend-gap category 都能追到 scope id。
- 没有无 FreeCAD authority 和 current mismatch evidence 的 `backend_gap_requires_implementation`。
- S2 后 S3/S4/S5/S6 owner step 清楚。
- 队列下一项为 S3。

## 验收记录

- `rg -n 'C10M1-SCOPE|C10M1-BLOCKER|C10M1-CAT|native_oracle_required|current_mismatch_candidate|backend_gap_candidate|diagnostic_retained|diagnostic_non_goal|release_gate|notCollected' docs/CADCore10.0/C10-M1-SketchOpenWireInternalFaceStableSelector批次`：通过，route vocabulary、scope、blocker 和 category 均可定位。
- `awk -F '\t' ... docs/CADCore10.0/C10-M1-SketchOpenWireInternalFaceStableSelector批次/矩阵/*.tsv`：通过，TSV 字段数一致。
- `rg -n '[ \t]$' docs/CADCore10.0`：无输出。
- `git diff --check`：通过。
- `step_goal_queue.py docs/CADCore10.0/C10-M1-SketchOpenWireInternalFaceStableSelector批次/工作步骤细分 --format markdown`：下一项为 S3。

## 非目标

- 不把 `notCollected` 或 oracle-blocked case 直接改成 C++ 任务。
- 不运行 FreeCADCmd。
- 不运行 cad-core build。
