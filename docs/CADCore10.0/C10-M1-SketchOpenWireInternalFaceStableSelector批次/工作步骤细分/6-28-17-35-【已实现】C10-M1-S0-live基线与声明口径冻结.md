# 【已实现】C10-M1-S0 live 基线与声明口径冻结

## 目标

冻结 C10-M1 起点：当前 HEAD、dirty state、C9-M5 队列状态、P5b next-step、允许声明、禁止声明和状态词典。S0 不采 oracle、不改 C++、不新增 fixture。

## live 基线

| 命令 | S0 记录 |
| --- | --- |
| `pwd` | `/home/user/Chili3DProject/FreeCAD` |
| `git rev-parse --short HEAD` | `382539f170` |
| `git log -1 --oneline` | `382539f170 docs: 关闭 C9-M5 S6 发布闸门` |
| `git -c core.quotepath=false status --short -uall` | 仅 C10-M1 seed 文档、矩阵和 `docs/CADCore10.0/README.md` 未跟踪；无 `cad-core/src`、fixtures、expected 或 tests 改动。 |
| C9-M5 queue | 输出只有 Markdown 表头，确认 queue-empty。 |

## 输入

- `docs/CADCore10.0/README.md`
- `docs/CADCore9.0/README.md`
- `docs/CADCore方案/细化方案/12-P5b-Sketch-open-wire-WireJoiner完整迁移方案.md`
- `cad-core/src/runtime/capability_contract.cpp`
- `cad-core/src/part_design/profile_resolver.cpp`
- `cad-core/src/part/wire_joiner.cpp`
- `cad-core/src/part/internal_shape_history_publisher.cpp`

## 声明口径

| 项 | S0 口径 |
| --- | --- |
| C9-M5 状态 | 队列为空，CopyOnChange 保持 no-code retained known gap。 |
| C10-M1 目标 | 推进 P5b Sketch open-wire / WireJoiner / InternalFace stable selector。 |
| 当前实现前提 | `Profile.SubList=InternalFaceN`、ReferenceShadow-backed recovery 和 recoverable WireJoiner 子集已存在。 |
| 当前待准入 | 近切线、重合边、复杂 open-wire、without ReferenceShadow stable selector。 |
| S6 code gate | 仅在 S3-S5 证明 oracle / current mismatch / 产品边界后打开。 |

## 状态词典

| 状态词 | S0 口径 |
| --- | --- |
| `baseline_frozen_s0` | live baseline、allowed claims、forbidden claims 已冻结。 |
| `native_oracle_required` | 需要 FreeCAD oracle 或 native blocker，不转成实现。 |
| `backend_gap_candidate` | 只表示候选，必须等 S3-S5 证据后由 S6 消费。 |
| `diagnostic_retained` | 保留稳定诊断或 known gap，不写 supported。 |
| `diagnostic_non_goal` | 本包排除，不能在 C10-M1 写成 supported。 |
| `release_gate` | S6 才能关闭实现或 no-code retained diagnostic。 |

## live source 依据

- `capability_contract.cpp` 仍把 `copy_on_change_full_temporary_document_cache` 发布为 `known_gap_diagnostic` / `oracle_blocked` 和 `remaining_gaps`，因此 CopyOnChange 只保留为 C9 retained known gap。
- `profile_resolver.cpp` 已支持 explicit `InternalFaceN` profile selection，并要求 request-local `StableSubList=InternalFaceN` 必须有当前 `Sketch.InternalShape` `NamedShape` / `ElementMap` evidence；非 internal stable subname 需要 `ReferenceShadow`。
- `wire_joiner.cpp` 与 `internal_shape_history_publisher.cpp` 已有 EdgeInfo / WireInfo、openWireCompound child-wire、MapperHistory、split / deleted / generated history 和唯一 ElementMap alias 子集；一对多或 vertex multiplicity 不可证明时保持 diagnostic。

## 禁止声明

- 不得声明 `copy_on_change_full_temporary_document_cache` 是 C10-M1 实现范围。
- 不得声明 full Sketcher solver、GUI、TaskPanel 或 cross-request Sketch session supported。
- 不得把 raw `FaceN`、source index、split order、bbox、面积或输出排序作为 `InternalFaceN` stable selector。
- 不得让 open wire 生成假 `profile_ready=true`。
- 不得引入持久 BREP、TopoDS_Shape、NamedShape、ElementMap 或 InternalShape cache。

## 必须回写的矩阵行

- `C10M1-SCOPE-001`
- `C10M1-SCOPE-101`
- `C10M1-SCOPE-102`
- `C10M1-BLOCKER-000`
- `C10M1-NG-001` 到 `C10M1-NG-006`

## 回写结果

- `C10M1-SCOPE-001` 标记为 `baseline_frozen_s0`。
- `C10M1-SCOPE-101` 保持 `native_oracle_required`，下一步仍是 S3 oracle。
- `C10M1-SCOPE-102` 保持 `backend_gap_candidate`，必须等 S3 expected-backed mismatch 后才能进 S6。
- `C10M1-BLOCKER-000` 标记 `closed_s0`。
- `C10M1-NG-001..006` 标记 `frozen_non_goal_s0`，禁止声明保持有效。

## 验收

```bash
cd /home/user/Chili3DProject/FreeCAD
git status --short -uall
git log -1 --oneline
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore9.0/C9-M5-SubShapeBinderCopyOnChangeRequestLocalDTO准入复审包/工作步骤细分 --format markdown
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore10.0/C10-M1-SketchOpenWireInternalFaceStableSelector批次/工作步骤细分 --format markdown
awk -F '\t' 'FNR==1{n=NF; next} NF!=n{print FILENAME ":" FNR ": expected " n " fields, got " NF; bad=1} END{exit bad}' docs/CADCore10.0/C10-M1-SketchOpenWireInternalFaceStableSelector批次/矩阵/*.tsv
rg -n '[ \t]$' docs/CADCore10.0
git diff --check
```

## 验收标准

- C9-M5 队列输出只有 Markdown 表头。
- C10-M1 队列下一项为 S1。
- README、总入口、方案和矩阵没有把 CopyOnChange 或 Sketcher solver 写成本批 supported。
- `C10M1-BLOCKER-000` 已关闭；S1-S6 未被标记已实现。

## 非目标

- 不运行 FreeCADCmd。
- 不修改 `cad-core/src`、fixtures、expected 或 tests。
