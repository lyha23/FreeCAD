# C8-M2-S6 实现准入与发布闸门

## 目标

完成 C8-M2 发布闸门：队列、矩阵、docs、必要 focused tests、capability smoke、实现准入裁决和最终状态同步。S6 只消费已经明确的 `sync_required`、`backend_gap_candidate`、`known_gap_retained`、`oracle_blocked` 和 `diagnostic_non_goal` row，不新增业务范围。

## release audit

必须确认：

- S0-S5 均已按验收标准完成并重命名为 `【已实现】`。
- `step_goal_queue.py` 对本目录输出空 pending 表。
- `c8m2_copyonchange_backend_gap_classification.tsv` 没有无证据的 `backend_gap_requires_implementation`。
- 所有 `oracle_blocked` / `known_gap_retained` 都有 delete / reopen condition。
- 下游同步合同不被误写成 FreeCAD C++ supported。

## 下一轮代码落点

| blocker / scope | C++ 或下游落点 | FreeCAD authority | 测试 | 成功标准 |
| --- | --- | --- | --- | --- |
| `C8M2-BG-101` / `C8M2-SCOPE-201` | `cad-core/src/part_design/feature_shape_binder.cpp`、`cad-core/src/app/copy_on_change.cpp` | `SubShapeBinder::setupCopyOnChange()`、`checkCopyOnChange()` | `cad-core/tests/test_c8_shapebinder.py` | 仅在 S3 证明 request-local DTO 后实现 |
| `C8M2-BG-102` / `C8M2-SCOPE-202` | `cad-core/src/part_design/feature_shape_binder.cpp` | `SubShapeBinder::onChanged()`、`PartialLoad` | `tests.test_diagnostics` | PartialLoad diagnostic / writeback 明确 |
| `C8M2-SYNC-101..103` | `opencascade-rs` document_graph / capability / blackbox tests | C8-M1 capability / fixtures | 下游 cargo focused tests | 另开下游包执行 |

禁止路径：

- 不按 fixture 名、bbox、面积、长度或几何类型猜 CopyOnChange。
- 不在 adapter/output JSON 末端补业务逻辑。
- 不保存跨请求 FreeCAD document、temporary document、BREP、TopoDS_Shape、NamedShape 或 ElementMap cache。

## 验收分层

本轮短跑：

```bash
cd /home/user/Chili3DProject/FreeCAD
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore8.0/C8-M2-SubShapeBinderCopyOnChange下游同步与DTO准入主线/工作步骤细分 --format markdown
awk -F '\t' 'FNR==1{n=NF; next} NF!=n{print FILENAME ":" FNR ": expected " n " fields, got " NF; bad=1} END{exit bad}' docs/CADCore8.0/C8-M2-SubShapeBinderCopyOnChange下游同步与DTO准入主线/矩阵/*.tsv
rg -n '[ \t]$' docs/CADCore8.0/C8-M2-SubShapeBinderCopyOnChange下游同步与DTO准入主线 docs/CADCore8.0/README.md
git diff --check
```

实现回归只在 S6 打开 C++ implementation gate 时运行：

```bash
cd /home/user/Chili3DProject/FreeCAD/cad-core
cmake --build build
python3 -m unittest tests.test_c8_shapebinder tests.test_diagnostics
```

## 完成规则

S6 完成后必须：

- 重命名本文件为 `6-26-22-27-【已实现】C8-M2-S6-实现准入与发布闸门.md`。
- 更新 `docs/CADCore8.0/README.md`、本包 README、总入口和方案状态。
- 运行 `git status --short -uall`，确认只有本轮相关变更或工作区干净。

## 非目标

- 不开新 feature scope。
- 不执行全量 FreeCAD build。
- 不直接修改下游 Rust。
