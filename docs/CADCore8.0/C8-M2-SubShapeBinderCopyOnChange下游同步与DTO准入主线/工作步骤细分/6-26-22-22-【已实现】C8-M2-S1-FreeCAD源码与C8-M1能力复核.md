# 【已实现】C8-M2-S1 FreeCAD 源码与 C8-M1 能力复核

## 目标

复核 `SubShapeBinder BindCopyOnChange` / `PartialLoad` 的 FreeCAD source authority、C8-M1 capability/tests/fixtures、current `cad-core` diagnostic 边界和下游同步合同输入。S1 不采 oracle，不改 C++。

## live 基线

- `pwd=/home/user/Chili3DProject/FreeCAD`
- `HEAD=e7e07663d9`（`e7e07663d9 docs: 完成 C8-M2 S0 live 基线冻结`）
- S1 开始时 `git -c core.quotepath=false status --short -uall` 为空，未发现无关 dirty 文件。
- 队列首项是本 S1 文件；后续 pending 为 S2-S6。

## FreeCAD source authority

| source | symbol | S1 结论 |
| --- | --- | --- |
| `/home/user/Chili3DProject/FreeCAD/src/Mod/PartDesign/App/ShapeBinder.cpp:397` | `PartialLoad` property | 字段说明是 `Enable partial loading...external bound object`，并设置 `PartialTrigger`；它是 allow-partial 行为入口，不是 copied-object cache 本身。 |
| `/home/user/Chili3DProject/FreeCAD/src/Mod/PartDesign/App/ShapeBinder.cpp:417` | `BindCopyOnChange` enum | 枚举值为 `Disabled`、`Enabled`、`Mutated`；说明中 `Enabled` 复制 `CopyOnChange` 属性，`Mutated` 表示 Binder 已因 `CopyOnChange` 属性改变而变异。 |
| `/home/user/Chili3DProject/FreeCAD/src/Mod/PartDesign/App/ShapeBinder.cpp:529` | `SubShapeBinder::setupCopyOnChange()` | Disabled 或非单 Support 会清理动态 CopyOnChange 属性；单 Support 且非 Disabled 时调用 `App::LinkBaseExtension::setupCopyOnChange()` 并监听源对象变更以清 `_CopiedObjs` cache。 |
| `/home/user/Chili3DProject/FreeCAD/src/Mod/PartDesign/App/ShapeBinder.cpp:593` | `SubShapeBinder::update()` | `BindCopyOnChange == 2` 且单 Support 时读取 `_CopiedObjs`；无有效 copy 时创建 `"_tmp_binder"` temporary document、`copyObject({obj}, true, true)`、先 `recomputeFeature(true)`，再把 `_CopiedLink` 指到 copied object。 |
| `/home/user/Chili3DProject/FreeCAD/src/Mod/PartDesign/App/ShapeBinder.cpp:1095` | `SubShapeBinder::onChanged()` | Support 变化会 `clearCopiedObjects()` 并重新 `setupCopyOnChange()`；`BindCopyOnChange` 变化只重新 setup；`BindMode=Detached` 清 Support；`PartialLoad` 触发 `checkPropertyStatus()`；普通属性变更走 `checkCopyOnChange()`。 |
| `/home/user/Chili3DProject/FreeCAD/src/Mod/PartDesign/App/ShapeBinder.cpp:1144` | `SubShapeBinder::checkCopyOnChange()` | 只有 `BindCopyOnChange == 1`、非 transaction、动态 CopyOnChange 属性、单 Support 时检查源属性；若同名同类型属性不相同，则 `BindCopyOnChange.setValue(2)` 进入 Mutated。 |
| `/home/user/Chili3DProject/FreeCAD/src/Mod/PartDesign/App/ShapeBinder.cpp:1162` | `SubShapeBinder::checkPropertyStatus()` | 关键行为是 `Support.setAllowPartial(PartialLoad.getValue())`；S1 只确认入口，不把 allow-partial 内部状态提升为 supported。 |
| `/home/user/Chili3DProject/FreeCAD/src/App/Link.cpp:778` | `LinkBaseExtension::setupCopyOnChange()` | 遍历 linked object 的 `Property::CopyOnChange` 属性，给 parent 添加带 group prefix 的动态属性并连接同步回调；这是 App::Link 对照基线，不等同 SubShapeBinder full cache 已支持。 |
| `/home/user/Chili3DProject/FreeCAD/src/App/Link.cpp:894` | `LinkBaseExtension::checkCopyOnChange()` | App::Link 在属性分歧时可 `makeCopyOnChange()` 并把属性 paste 到 copied object；cad-core 的 App::Link request-local helper可作 DTO 思路，不可直接搬成 Binder supported。 |

## current cad-core coverage

| path | current evidence | S1 状态 |
| --- | --- | --- |
| `cad-core/src/part_design/feature_shape_binder.cpp` | `addLifecycleMetadata()` 读取 `BindMode`、`BindCopyOnChange` 和 `PartialLoad`；Detached 只返回 Support clear writeback；Enabled / Mutated / PartialLoad 均发 `copy_on_change_full_temporary_document_cache_not_supported`。 | current boundary 是 request-local shape recompute + diagnostic；没有实现 FreeCAD `_tmp_binder` / `_CopiedObjs` cache。 |
| `cad-core/src/app/copy_on_change.cpp` | `buildCopyOnChangeLifecycleUpdates()` 为 App::Link `LinkCopyOnChange` 生成 group sync、deep-copy lifecycle 和 link writeback 的 `documentObjectUpdates`。 | 可作为下游 DTO 设计输入；它处理 `LinkCopyOnChange*`，不是 SubShapeBinder `BindCopyOnChange` 支持证明。 |
| `cad-core/src/runtime/capability_contract.cpp` | `part_design.sub_shape_binder.status=supported_c8m1_expected_backed_request_local_with_copy_on_change_known_gap`；`remaining_gaps=["copy_on_change_full_temporary_document_cache"]`；known gap 的 status 是 `known_gap_diagnostic`，route 是 `oracle_blocked`。 | S1 保持 known_gap，不改 supported 发布口径。 |
| `cad-core/tests/test_c8_shapebinder.py` | CopyOnChange fixture 断言三个 `copy_on_change_full_temporary_document_cache_not_supported`；capability test 断言 known_gap status、route、diagnostic、delete_condition 和 reopen_condition。 | C8-M1 focused tests 已锁当前边界，是 S4/S5 下游同步输入。 |
| `cad-core/tests/test_diagnostics.py` | 全局 diagnostics suite 没有 `SubShapeBinder` / `BindCopyOnChange` / `PartialLoad` 专项断言。 | CopyOnChange known_gap 由 C8 focused tests 与 capability contract 锁定，不能误认为 generic diagnostics 已发布额外语义。 |
| `cad-core/fixtures/c8m1/subshape-binder-copy-on-change-disabled-enabled-mutated-partialload.json` | 输入含 `CopyOnChangeDisabled`、`CopyOnChangeEnabled`、`CopyOnChangeMutated`、`PartialLoadEnabled` 四个 SubShapeBinder。 | 下游 blackbox 同步应保留这些 property-state 种子。 |
| `cad-core/fixtures/c8m1/expected/subshape-binder-copy-on-change-disabled-enabled-mutated-partialload.freecad.json` | expected 记录 `property_states_collected=["Disabled","Enabled","Mutated","PartialLoad=True"]`，并明确 `_CopiedObjs temporary document cache contents`、mutation trigger 和 `Support.setAllowPartial` 内部 flag 未采集。 | 这是 native lifecycle blocker 证据，不是 CopyOnChange full cache supported expected。 |

## 下游同步输入

- TypeIds：`PartDesign::ShapeBinder`、`PartDesign::SubShapeBinder`、`PartDesign::SubShapeBinderPython`。
- Capability：ShapeBinder `remaining_gaps=[]`；SubShapeBinder 保持 `copy_on_change_full_temporary_document_cache` known_gap。
- Diagnostics：`copy_on_change_full_temporary_document_cache_not_supported` 必须同步为 known_gap diagnostic，不可降级成普通 success 或 supported。
- Fixtures：`cad-core/fixtures/c8m1` 当前有 12 个 input JSON 和 12 个 FreeCAD expected JSON；CopyOnChange property-state fixture 是 S4/S5 同步种子之一。
- cad-core 落点：Binder executor 仍在 `cad-core/src/part_design/feature_shape_binder.cpp`；App::Link CopyOnChange DTO 思路在 `cad-core/src/app/copy_on_change.cpp`；发布口径在 `cad-core/src/runtime/capability_contract.cpp`。

## 矩阵回写

- `c8m2_copyonchange_source_candidates.tsv`：补全 `LinkBaseExtension::setupCopyOnChange()`、`SubShapeBinder::update()` 和 `test_diagnostics.py` 边界行，并把 `PartialLoad` 行改成 S1 live 结论。
- `c8m2_copyonchange_scope_review_matrix.tsv`：把 C8-M1 capability / diagnostics / fixtures 同步行从 S0 seed 更新为 S1 verified；保持 CopyOnChange DTO 为 `backend_gap_candidate`，PartialLoad 为 `oracle_candidate`，full temporary cache 为 `known_gap_retained`。
- `c8m2_copyonchange_non_goal_registry.tsv`：记录 S1 不做 oracle / fixture / test / C++ / Rust，且 full temporary document cache 不归 backend persistent state。
- `c8m2_copyonchange_blocker_queue.tsv`：`C8M2-BLOCKER-101` 关闭为 `closed_S1_source_authority_and_current_capability_reviewed`，不关闭 S2/S3/S6 gate。

## 下一步

进入 S2：把 S1 source / current evidence 转成 DTO 准入、oracle 候选和下游同步分类。S2 仍不能采 oracle、不能改 C++，也不能把 `known_gap_diagnostic` 改成 supported。

## 验收

```bash
cd /home/user/Chili3DProject/FreeCAD
rg -n 'setupCopyOnChange|checkCopyOnChange|BindCopyOnChange|PartialLoad|Mutated|SubShapeBinder' src/Mod/PartDesign/App src/App cad-core/src cad-core/tests cad-core/fixtures/c8m1 docs/CADCore8.0/C8-M2-SubShapeBinderCopyOnChange下游同步与DTO准入主线
awk -F '\t' 'FNR==1{n=NF; next} NF!=n{print FILENAME ":" FNR ": expected " n " fields, got " NF; bad=1} END{exit bad}' docs/CADCore8.0/C8-M2-SubShapeBinderCopyOnChange下游同步与DTO准入主线/矩阵/*.tsv
git diff --check
```

验证通过后，本文件按原时间前缀重命名为 `6-26-22-22-【已实现】C8-M2-S1-FreeCAD源码与C8-M1能力复核.md`，步骤索引链接同步更新。

## 非目标

- 不采 FreeCAD oracle。
- 不新增 fixture / expected / tests / collector。
- 不修改 C++ 或下游 Rust。
- 不把 `known_gap_diagnostic` 改 supported。
- 不打开 S2/S3/S6 的实现结论。
