# 【已实现】C8-M2-S6 实现准入与发布闸门

## 目标

完成 C8-M2 发布闸门：队列、矩阵、docs、必要 focused tests、capability smoke、实现准入裁决和最终状态同步。S6 只消费已经明确的 `sync_required`、`backend_gap_candidate`、`known_gap_retained`、`oracle_blocked` 和 `diagnostic_non_goal` row，不新增业务范围。

## release audit 结果

- S0-S5 均已完成并按原时间前缀重命名为 `【已实现】`。
- S6 入场基线：`pwd=/home/user/Chili3DProject/FreeCAD`；`HEAD=8994732678`（`8994732678 docs: 完成 C8-M2 S5 capability 发布`）；入场工作区干净；队列只剩本 S6。
- `c8m2_copyonchange_backend_gap_classification.tsv` 没有 `backend_gap_requires_implementation`；`C8M2-BG-101` / `102` 仍只是 product decision input，`C8M2-BG-301` 仍是 retained known gap。
- `C8M2-ORACLE-101` / `102` 只有 property-state evidence；`C8M2-ORACLE-103` full temporary-document cache 仍为 `oracle_blocked`，并保留 delete / reopen condition。
- 下游同步合同保持 `sync_required`，只作为 `opencascade-rs` / 前端另包输入；没有写成 FreeCAD C++ supported。
- `C8M2-BLOCKER-601` 关闭为 `closed_S6_no_code_release_gate_queue_empty_and_scope_synced`。

## release gate 裁决

S6 关闭为 no-code release gate，不打开 C++ implementation gate。

裁决依据：

- S3 / S5 只证明 `BindCopyOnChange` Disabled / Enabled / Mutated、动态 CopyOnChange 属性、Enabled -> Mutated 触发和 `PartialLoad=True` 等 Python-visible property-state。
- S3 / S5 没有证明 `_CopiedObjs`、`_tmp_binder copyObject` dependency order、`recomputeFeature(true)` ElementMap lifecycle 可以导出为稳定 request-local DTO。
- 当前 capability 与 focused tests 仍把 `copy_on_change_full_temporary_document_cache` 发布为 `known_gap_diagnostic` / `oracle_blocked`，并保留 `delete_condition` / `reopen_condition`。
- 本仓库无产品批准把 property-state 子集升级为新的 request-local CopyOnChange DTO。

## 下一轮代码落点

| blocker / scope | C++ 或下游落点 | FreeCAD authority | 测试 | 成功标准 |
| --- | --- | --- | --- | --- |
| `C8M2-BG-101` / `C8M2-SCOPE-201` | `cad-core/src/part_design/feature_shape_binder.cpp`、`cad-core/src/app/copy_on_change.cpp` | `SubShapeBinder::setupCopyOnChange()`、`checkCopyOnChange()` | `cad-core/tests/test_c8_shapebinder.py` | 仅在未来 native probe 导出稳定 request-local DTO 且 product approval 明确后实现 |
| `C8M2-BG-102` / `C8M2-SCOPE-202` | `cad-core/src/part_design/feature_shape_binder.cpp` | `SubShapeBinder::onChanged()`、`PartialLoad` | `tests.test_diagnostics` | PartialLoad diagnostic / writeback 明确 |
| `C8M2-SYNC-101..103` | `opencascade-rs` document_graph / capability / blackbox tests | C8-M1 capability / fixtures | 下游 cargo focused tests | 另开下游包执行 |

禁止路径：

- 不按 fixture 名、bbox、面积、长度或几何类型猜 CopyOnChange。
- 不在 adapter/output JSON 末端补业务逻辑。
- 不保存跨请求 FreeCAD document、temporary document、BREP、TopoDS_Shape、NamedShape 或 ElementMap cache。

## 验收分层

本轮短跑已执行：

```bash
cd /home/user/Chili3DProject/FreeCAD
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore8.0/C8-M2-SubShapeBinderCopyOnChange下游同步与DTO准入主线/工作步骤细分 --format markdown
awk -F '\t' 'FNR==1{n=NF; next} NF!=n{print FILENAME ":" FNR ": expected " n " fields, got " NF; bad=1} END{exit bad}' docs/CADCore8.0/C8-M2-SubShapeBinderCopyOnChange下游同步与DTO准入主线/矩阵/*.tsv
rg -n '[ \t]$' docs/CADCore8.0/C8-M2-SubShapeBinderCopyOnChange下游同步与DTO准入主线 docs/CADCore8.0/README.md
git diff --check
```

S5 focused checks 保留为本 release gate 的代码/能力证明：`./cad-core capabilities > /tmp/c8m2-capabilities.json` 通过；`python3 -m unittest tests.test_c8_shapebinder tests.test_diagnostics` 通过，18 个测试 OK。S6 未修改 C++ 或 tests，因此不重跑 build。

实现回归只在 S6 打开 C++ implementation gate 时运行：

```bash
cd /home/user/Chili3DProject/FreeCAD/cad-core
cmake --build build
python3 -m unittest tests.test_c8_shapebinder tests.test_diagnostics
```

## 完成规则

S6 完成后已执行：

- 本文件重命名为 `6-26-22-27-【已实现】C8-M2-S6-实现准入与发布闸门.md`。
- `docs/CADCore8.0/README.md`、本包 README、总入口、方案和步骤索引已同步为 C8-M2 已收口。
- 收口后 `step_goal_queue.py` 队列为空；最终 `git status --short -uall` 只应包含本轮提交前相关变更，提交后为空。

## 非目标

- 不开新 feature scope。
- 不执行全量 FreeCAD build。
- 不直接修改下游 Rust。
