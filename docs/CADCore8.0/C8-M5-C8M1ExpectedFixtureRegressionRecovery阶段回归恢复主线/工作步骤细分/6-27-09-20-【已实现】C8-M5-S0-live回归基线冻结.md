# 【已实现】C8-M5-S0 live 回归基线冻结

## 目标

冻结 C8-M5 开始时的 live repo、能力和阶段回归失败基线，确保后续 S1-S6 不从旧文档或记忆倒推结论。

## 输入

- 包入口：`docs/CADCore8.0/C8-M5-C8M1ExpectedFixtureRegressionRecovery阶段回归恢复主线/`
- C8-M4 S6 记录：`docs/CADCore8.0/C8-M4-PartGeomPlateCurveConstraintCriteriaRequestLocal批量收口主线/工作步骤细分/6-27-02-40-【已实现】C8-M4-S6-实现与发布闸门.md`
- current capability：`cd cad-core && ./cad-core capabilities`

## 执行

1. 记录 `pwd`、`git rev-parse --short=10 HEAD`、`git log -1 --oneline`、`git status --short -uall`。
2. 运行 C8-M1 到 C8-M4 的 `step_goal_queue.py`，确认历史主线队列为空。
3. 导出 current capability，确认 active `remaining_gaps` 只有 C8-M2 保留的 `copy_on_change_full_temporary_document_cache`，并把它标为 C8-M5 non-goal。
4. 重新运行或复核 C8-M4 S6 stage regression 失败，只记录两个 C8-M1 drift 的 fixture 名、expected 文件和 current code。
5. 更新 `README.md`、`矩阵/c8m5_expected_fixture_regression_blocker_queue.tsv` 和 validation matrix 的 S0 状态。

## S0 完成记录

- live baseline：`pwd=/home/user/Chili3DProject/FreeCAD`，`HEAD=e93ddd8746`，`git log -1 --oneline=e93ddd8746 feat: 完成 C8-M4 GeomPlate 曲线 criteria 发布闸门`。
- 开始工作区：`docs/CADCore8.0/README.md` 已修改，本 C8-M5 包为未跟踪文件；未触碰 `src/`、`cad-core/src`、fixture 或 expected 文件。
- C8-M1、C8-M2、C8-M3、C8-M4 工作步骤队列均为空。
- capability：唯一非空 active `remaining_gaps` 为 `part_design.sub_shape_binder.remaining_gaps=["copy_on_change_full_temporary_document_cache"]`，本轮保持 C8-M5 non-goal。
- stage regression 重跑结果：266 tests、35 skipped、1 error、1 failure；仅复现 `shape-binder-subshape-binder-element-map-namedshape-body-replay` 的 `BodyBaseFeature` 缺失和 `subshape-binder-setlinks-normalization-diagnostics` 的 current `cycle_dependency`。
- `C8M5-BLOCKER-000` 已关闭；下一队列首项为 S1。

## 验收

```bash
cd /home/user/Chili3DProject/FreeCAD
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore8.0/C8-M5-C8M1ExpectedFixtureRegressionRecovery阶段回归恢复主线/工作步骤细分 --format markdown
awk -F '\t' 'FNR==1{n=NF; next} NF!=n{print FILENAME ":" FNR ": expected " n " fields, got " NF; bad=1} END{exit bad}' docs/CADCore8.0/C8-M5-C8M1ExpectedFixtureRegressionRecovery阶段回归恢复主线/矩阵/*.tsv
git diff --check
```

## 退出标准

- `C8M5-BLOCKER-000` 关闭。
- `C8M5-NG-001` 明确说明 CopyOnChange full temporary-document cache 不是本轮实现项。
- 两个 drift 的 raw evidence 可从文档和矩阵追溯。
