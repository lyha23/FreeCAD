# C8-M5-S1 失败清单与 owner 分类矩阵

## 目标

把两个 C8-M1 expected drift 拆成可实施 owner：expected 文件、current runtime 输出、focused assertion、source authority 和发布闸门，避免在 S3/S4 直接改 fixture。

## 输入

- `cad-core/fixtures/c8m1/expected/shape-binder-subshape-binder-element-map-namedshape-body-replay.freecad.json`
- `cad-core/fixtures/c8m1/expected/subshape-binder-setlinks-normalization-diagnostics.freecad.json`
- `cad-core/tests/test_c8_shapebinder.py`
- `cad-core/tests/test_diagnostics.py`
- `cad-core/src/part_design/body.cpp`
- `cad-core/src/graph/recompute_plan.cpp`

## 执行

1. 用 `rg` 收集 `BodyBaseFeature`、`cycle_rejected_by_property_link`、`cycle_dependency` 在 fixtures、tests、src 中的全部本轮相关引用。
2. 更新 `source_candidates.tsv`，给每个证据行绑定 source authority 与 cad-core landing。
3. 更新 `scope_review_matrix.tsv`，把 drift 分类为 `expected_authority_pending`、`implementation_regression_candidate` 或 `diagnostic_policy_pending`。
4. 更新 `fixture_oracle_matrix.tsv`，标出是否需要 native FreeCAD re-collection。
5. 不改 C++、fixture expected 或测试断言。

## 验收

```bash
cd /home/user/Chili3DProject/FreeCAD
awk -F '\t' 'FNR==1{n=NF; next} NF!=n{print FILENAME ":" FNR ": expected " n " fields, got " NF; bad=1} END{exit bad}' docs/CADCore8.0/C8-M5-C8M1ExpectedFixtureRegressionRecovery阶段回归恢复主线/矩阵/*.tsv
rg -n '[ \t]$' docs/CADCore8.0/C8-M5-C8M1ExpectedFixtureRegressionRecovery阶段回归恢复主线
git diff --check
```

## 退出标准

- `C8M5-BLOCKER-101` 关闭。
- 每个 drift 至少有 expected、current implementation、focused test 和 FreeCAD source authority 四类证据。
- 后续 S3/S4 可直接按 owner 处理，不需要重新做全仓库搜索。
