# C8-M5-S4 cycle 诊断代码漂移专项复审

## 目标

裁决 `subshape-binder-setlinks-normalization-diagnostics` 的诊断 code 应为 FreeCAD setter-level `cycle_rejected_by_property_link`，还是 cad-core graph-level `cycle_dependency`。

## Source authority

- FreeCAD：`/home/user/Chili3DProject/FreeCAD/src/Mod/PartDesign/App/ShapeBinder.cpp::SubShapeBinder::setLinks()`，当 linked object 位于 `getInListEx(true)` / self set 时抛出 `Cyclic reference to ...`。
- FreeCAD：`/home/user/Chili3DProject/FreeCAD/src/App/PropertyLinks.h::PropertyLinkBase` 表达 dependency-bearing property link 基类。
- cad-core：`/home/user/Chili3DProject/FreeCAD/cad-core/src/graph/recompute_plan.cpp::visitObject()` 当前在 graph 拓扑阶段输出 `cycle_dependency`。
- cad-core：`/home/user/Chili3DProject/FreeCAD/cad-core/tests/test_c8_shapebinder.py` 当前 focused assertion 接受 `cycle_dependency`。

## 执行

1. 读取目标 fixture input，判断 cycle 是否在 JSON normalization / property setter 阶段可被无状态重放。
2. 若能稳定重放 setter-level reject，则在 document/reference lifecycle 层输出 `cycle_rejected_by_property_link`，并同步 diagnostics vocabulary / tests。
3. 若不能稳定重放 setter-level reject，则把 expected 更新为 `cycle_dependency`，并在 matrix 中说明 request-local 无状态后端只能观察 graph cycle。
4. 不允许在 CLI adapter 或 expected comparator 中把 `cycle_dependency` 改名为 `cycle_rejected_by_property_link`。
5. 更新 non-goal 与 backend gap classification，避免把 full FreeCAD property editor setter lifecycle 写成 supported。

## 验收

Focused 命令：

```bash
cd /home/user/Chili3DProject/FreeCAD/cad-core
python3 -m unittest tests.test_c8_shapebinder tests.test_diagnostics tests.test_adapters
python3 -m unittest tests.test_expected_fixtures.CadCoreExpectedFixtureTest.test_expected_fixtures_match_recompute_results
```

文档检查：

```bash
cd /home/user/Chili3DProject/FreeCAD
git diff --check
```

## 退出标准

- `C8M5-BLOCKER-401` 关闭。
- `C8M5-GAP-201` 分类为 `approved_expected_refresh` 或 `code_fix_required`。
- diagnostics vocabulary、focused tests 和 expected fixture 不再互相矛盾。
