# 【已实现】C8-M5-S6 阶段回归发布闸门

## 目标

确认 C8-M5 已恢复 C8 阶段回归闸门，并把结果发布到 `docs/CADCore8.0/README.md`、矩阵和 capability / tests 文档边界。

## 执行

1. 确认 S0-S5 blocker 全部关闭，工作步骤队列只剩 S6。
2. 运行 focused expected fixture、C8 shapebinder、diagnostics、adapter tests。
3. 运行阶段回归命令。
4. 如本轮修改 C++ 主路径，执行 `cmake --build build`；若只刷新 expected / tests / docs，可记录不需要构建。
5. 更新 README 与矩阵，写清最终裁决和剩余 non-goals。
6. 将本 S6 文件重命名为 `【已实现】` 后再关闭队列。

## 验收

Focused：

```bash
cd /home/user/Chili3DProject/FreeCAD/cad-core
python3 -m unittest tests.test_c8_shapebinder tests.test_diagnostics tests.test_adapters
python3 -m unittest tests.test_expected_fixtures.CadCoreExpectedFixtureTest.test_expected_fixtures_match_recompute_results
```

Stage regression：

```bash
cd /home/user/Chili3DProject/FreeCAD/cad-core
python3 -m unittest tests.test_p8_features tests.test_expected_fixtures tests.test_adapters tests.test_diagnostics
```

Docs / queue：

```bash
cd /home/user/Chili3DProject/FreeCAD
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore8.0/C8-M5-C8M1ExpectedFixtureRegressionRecovery阶段回归恢复主线/工作步骤细分 --format markdown
awk -F '\t' 'FNR==1{n=NF; next} NF!=n{print FILENAME ":" FNR ": expected " n " fields, got " NF; bad=1} END{exit bad}' docs/CADCore8.0/C8-M5-C8M1ExpectedFixtureRegressionRecovery阶段回归恢复主线/矩阵/*.tsv
rg -n '[ \t]$' docs/CADCore8.0/C8-M5-C8M1ExpectedFixtureRegressionRecovery阶段回归恢复主线 docs/CADCore8.0/README.md
git diff --check
```

## 退出标准

- `C8M5-BLOCKER-601` 关闭。
- C8-M5 队列为空。
- stage regression 通过，或只剩明确记录且与 C8-M5 无关的外部环境问题。
- C8-M5 不新增 active remaining gap。

## S6 完成记录

- 执行基线：`pwd=/home/user/Chili3DProject/FreeCAD`，`HEAD=89dacb11fd`，`git log -1 --oneline=89dacb11fd docs: 完成 C8-M5 S5 准入收口`，开始工作区干净。
- S0-S5 blocker 已全部关闭；执行前队列只剩本 S6 文件。
- `cmake --build build` 通过，覆盖 S4 修改过的 C++ 主路径。
- `python3 -m unittest tests.test_c8_shapebinder tests.test_diagnostics tests.test_adapters` 通过，47 tests。
- `python3 -m unittest tests.test_expected_fixtures.CadCoreExpectedFixtureTest.test_expected_fixtures_match_recompute_results` 通过，35 skipped。
- `python3 -m unittest tests.test_p8_features tests.test_expected_fixtures tests.test_adapters tests.test_diagnostics` 通过，266 tests，35 skipped。
- `C8M5-BLOCKER-601` 已关闭；C8-M5 队列为空。
- `copy_on_change_full_temporary_document_cache` 继续保持 C8-M2 `known_gap` / `oracle_blocked` / C8-M5 non-goal；C8-M5 不新增 active remaining gap。
