# C8-M6 S1 源头合同与能力面复核

## 目标

把下游同步合同的源头证据从旧 C8-M2 状态更新到 C8-M5 之后的 live 状态，确保每个合同都有 FreeCAD source、cad-core landing、fixture 或 test 证据。

## FreeCAD 依据

- `src/Mod/PartDesign/App/ShapeBinder.cpp::ShapeBinder::updatedShape()`
- `src/Mod/PartDesign/App/ShapeBinder.cpp::SubShapeBinder::update()`
- `src/Mod/PartDesign/App/ShapeBinder.cpp::SubShapeBinder::setLinks()`
- `src/Mod/PartDesign/App/Body.cpp::Body::onChanged()`
- `src/App/PropertyLinks.cpp` 的 property-link cycle / dependency 语义。

## cad-core 依据

- `cad-core/src/part_design/feature_shape_binder.cpp`
- `cad-core/src/part_design/body.cpp`
- `cad-core/src/runtime/capability_contract.cpp`
- `cad-core/src/runtime/reference_lifecycle.cpp`
- `cad-core/tests/test_c8_shapebinder.py`
- `cad-core/tests/test_expected_fixtures.py`

## 必须回写的矩阵行

- `C8M6-SRC-101` 到 `C8M6-SRC-403`
- `C8M6-BLOCKER-101`
- `C8M6-SCOPE-101` 到 `C8M6-SCOPE-104`

## 分类规则

- 已由 C8-M1/C8-M5 expected 和 focused tests 覆盖的项，标为 `sync_required_source_contract`。
- 仍由 C8-M2 裁决为 `known_gap` 的项，保持 `known_gap_retained`。
- 若发现 source / capability / test 证据互相矛盾，标为 `unexpected_mismatch` 并交给 S6 决定是否进入代码落点。

## 验收

```bash
cd /home/user/Chili3DProject/FreeCAD
rg -n "PartDesign::ShapeBinder|PartDesign::SubShapeBinder|SubShapeBinderPython|copy_on_change_full_temporary_document_cache|cycle_rejected_by_property_link" cad-core/src cad-core/tests cad-core/fixtures/c8m1 docs/CADCore8.0/C8-M2-SubShapeBinderCopyOnChange下游同步与DTO准入主线 docs/CADCore8.0/C8-M5-C8M1ExpectedFixtureRegressionRecovery阶段回归恢复主线
git diff --check
```

## 非目标

- 不扩大到其它 PartDesign feature。
- 不把 old C8-M2 contract 直接复制为最终结论；必须复核 C8-M5 delta。
