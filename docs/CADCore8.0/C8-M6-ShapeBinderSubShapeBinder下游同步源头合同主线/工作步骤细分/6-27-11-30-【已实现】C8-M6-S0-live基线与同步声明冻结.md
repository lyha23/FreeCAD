# 【已实现】C8-M6 S0 live 基线与同步声明冻结

## 目标

冻结 C8-M6 的 live 起点，确认本包只做 FreeCAD 仓库内的 ShapeBinder/SubShapeBinder 下游同步源头合同，不修改下游仓库。

## 输入

- C8-M5 release gate：`docs/CADCore8.0/C8-M5-C8M1ExpectedFixtureRegressionRecovery阶段回归恢复主线/README.md`
- C8-M2 downstream contract：`docs/CADCore8.0/C8-M2-SubShapeBinderCopyOnChange下游同步与DTO准入主线/矩阵/c8m2_downstream_sync_contract.tsv`
- 当前 package 矩阵：`../矩阵/*.tsv`

## 必须复核

- `pwd` 是 `/home/user/Chili3DProject/FreeCAD`。
- `git rev-parse --short=10 HEAD` 与 `git log -1 --oneline` 写回 README 和 blocker 矩阵。
- C8-M1 到 C8-M5 工作步骤队列为空。
- 当前 capability 中 `shape_binder` 仍为 supported，`sub_shape_binder` 仍只保留 `copy_on_change_full_temporary_document_cache` known gap。
- C8-M5 已裁决的 `BodyBaseFeature` expected refresh 和 `cycle_rejected_by_property_link` 诊断不被旧 C8-M2 合同覆盖掉。

## 必须回写的矩阵行

- `C8M6-BLOCKER-000`
- `C8M6-SCOPE-001`
- `C8M6-NG-001` 到 `C8M6-NG-006`
- `C8M6-VAL-001` 到 `C8M6-VAL-004`

## S0 冻结结论

- live 基线：`pwd=/home/user/Chili3DProject/FreeCAD`，`HEAD=9361ddc83a`，`git log -1 --oneline=9361ddc83a docs: 新增 C8-M6 下游同步源头合同包`。
- S0 开始工作区干净：`git -c core.quotepath=false status --short -uall` 无输出。
- C8-M1 到 C8-M5 工作步骤队列均为空；C8-M6 S0 执行前队列首项为 S0，S0 重命名后队列应跳到 S1。
- capability smoke：`shape_binder` 为 `supported_c8m1_expected_backed_request_local` 且 `remaining_gaps=[]`；`sub_shape_binder` 为 `supported_c8m1_expected_backed_request_local_with_copy_on_change_known_gap` 且唯一 `remaining_gaps=["copy_on_change_full_temporary_document_cache"]`。
- C8-M5 最终口径保持生效：`shape-binder-subshape-binder-element-map-namedshape-body-replay` 不再要求无输入 `Body.BaseFeature` 时出现 `BodyBaseFeature`；`subshape-binder-setlinks-normalization-diagnostics` 使用 `cycle_rejected_by_property_link`，不是旧 generic `cycle_dependency`。
- 本步只更新 C8-M6 文档、README 和 TSV 矩阵，不修改 `cad-core/src`、fixtures、expected、测试或下游仓库。

## 验收

```bash
cd /home/user/Chili3DProject/FreeCAD
git status --short -uall
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore8.0/C8-M5-C8M1ExpectedFixtureRegressionRecovery阶段回归恢复主线/工作步骤细分 --format markdown
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore8.0/C8-M6-ShapeBinderSubShapeBinder下游同步源头合同主线/工作步骤细分 --format markdown
cd cad-core
./cad-core capabilities > /tmp/c8m6-s0-capabilities.json
```

## 非目标

- 不跑重型阶段回归。
- 不刷新 fixture expected。
- 不修改 `cad-core/src`。
