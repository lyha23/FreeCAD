# 【已实现】C8-M6 S6 发布闸门与下游交接清单

## 目标

执行 C8-M6 发布闸门，关闭文档队列，并输出下游同步时应使用的合同清单。

## S6 完成记录

- 执行基线：`pwd=/home/user/Chili3DProject/FreeCAD`，`HEAD=f0739ca2ba`，`git log -1 --oneline=f0739ca2ba docs: 完成 C8-M6 S5 消费边界发布`，开始工作区干净。
- 范围裁决：S0-S5 未发现 current / unexpected mismatch；S6 是文档发布闸门，不进入 C++ 修改，未修改 `cad-core/src`、fixtures、expected、tests 或下游仓库。
- 短跑验证：S6 重命名前队列只剩 S6；C8-M6 TSV 字段数检查通过；C8-M6 包和 `docs/CADCore8.0/README.md` 尾随空白无匹配；`git diff --check` 通过。
- 阶段合同复核：`./cad-core capabilities > /tmp/c8m6-capabilities.json` 通过；`python3 -m unittest tests.test_c8_shapebinder tests.test_diagnostics tests.test_adapters` 通过，47 tests OK；expected fixture gate 通过，1 test OK，35 skipped known-gap fixtures。
- 重型收口：`cmake --build build` 通过；`python3 -m unittest tests.test_p8_features tests.test_expected_fixtures tests.test_adapters tests.test_diagnostics` 通过，266 tests OK，35 skipped known-gap fixtures。
- `C8M6-BLOCKER-601` 已关闭；S6 重命名后 C8-M6 工作步骤队列应为空。

## 发布闸门

本轮短跑：

```bash
cd /home/user/Chili3DProject/FreeCAD
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore8.0/C8-M6-ShapeBinderSubShapeBinder下游同步源头合同主线/工作步骤细分 --format markdown
awk -F '\t' 'FNR==1{n=NF; next} NF!=n{print FILENAME ":" FNR ": expected " n " fields, got " NF; bad=1} END{exit bad}' docs/CADCore8.0/C8-M6-ShapeBinderSubShapeBinder下游同步源头合同主线/矩阵/*.tsv
rg -n '[ \t]$' docs/CADCore8.0/C8-M6-ShapeBinderSubShapeBinder下游同步源头合同主线 docs/CADCore8.0/README.md
git diff --check
```

阶段合同复核：

```bash
cd /home/user/Chili3DProject/FreeCAD/cad-core
./cad-core capabilities > /tmp/c8m6-capabilities.json
python3 -m unittest tests.test_c8_shapebinder tests.test_diagnostics tests.test_adapters
python3 -m unittest tests.test_expected_fixtures.CadCoreExpectedFixtureTest.test_expected_fixtures_match_recompute_results
```

重型收口：

```bash
cd /home/user/Chili3DProject/FreeCAD/cad-core
cmake --build build
python3 -m unittest tests.test_p8_features tests.test_expected_fixtures tests.test_adapters tests.test_diagnostics
```

## 下游交接清单

- TypeIds：`PartDesign::ShapeBinder`、`PartDesign::SubShapeBinder`、`PartDesign::SubShapeBinderPython`。
- Capability：以 `cad-core/src/runtime/capability_contract.cpp` 当前输出和 `/tmp/c8m6-capabilities.json` 为准；`part_design.shape_binder.remaining_gaps=[]`，`part_design.sub_shape_binder.remaining_gaps=["copy_on_change_full_temporary_document_cache"]`。
- Fixture seeds：`cad-core/fixtures/c8m1` 下 12 个 input fixture 和 `cad-core/fixtures/c8m1/expected` 下 12 个 C8-M5-current expected。
- Diagnostics：`cycle_rejected_by_property_link` 用于 SubShapeBinder Support self-link setter rejection；generic graph cycle 继续是 `cycle_dependency`；CopyOnChange full temporary-document cache known gap 继续是 `copy_on_change_full_temporary_document_cache_not_supported`。
- Request-local output：mesh / subshapes / full subname、ElementMap / NamedShape evidence、maker history、reference update evidence、`documentObjectUpdates`、`elementReferenceUpdates` 均只作为单次 recompute 的结果或建议。
- Evidence：`shape-binder-subshape-binder-element-map-namedshape-body-replay` 使用 C8-M5-current expected，不要求无 input `Body.BaseFeature` 时合成 `BodyBaseFeature`；`subshape-binder-setlinks-normalization-diagnostics` 使用 `cycle_rejected_by_property_link`，不是 adapter 字符串重写。
- Non-goal：full CopyOnChange temporary-document cache、持久 BREP / TopoDS_Shape / NamedShape / ElementMap / geometry cache、request 结束后的 shape cache；`ReferenceShadow.brep` 仍只是单个旧 subshape snapshot 例外。

## 下一轮代码落点

如果 S0-S5 没有发现 current mismatch，S6 是文档发布闸门，不进入 C++ 修改。

如果发现 implementable mismatch，必须按下列落点处理后再关闭：

| mismatch | 允许落点 | focused test |
| --- | --- | --- |
| capability payload drift | `cad-core/src/runtime/capability_contract.cpp` | `cad-core/tests/test_adapters.py`、`cad-core/tests/test_c8_shapebinder.py` |
| SubShapeBinder self-link diagnostic drift | `cad-core/src/runtime/reference_lifecycle.cpp` | `cad-core/tests/test_c8_shapebinder.py`、`cad-core/tests/test_diagnostics.py` |
| Body replay drift | `cad-core/src/part_design/body.cpp` | `cad-core/tests/test_c8_shapebinder.py`、`cad-core/tests/test_expected_fixtures.py` |
| Binder output contract drift | `cad-core/src/part_design/feature_shape_binder.cpp` | `cad-core/tests/test_c8_shapebinder.py`、`cad-core/tests/test_expected_fixtures.py` |

禁止用 adapter 字符串重写、fixture 名称分支、输出排序、几何猜测或跨请求 shape cache 关闭 mismatch。

## 非目标

- 不把本包当作下游仓库实现完成证明。
- 不把 C8-M2 known gap 改写成 supported。
