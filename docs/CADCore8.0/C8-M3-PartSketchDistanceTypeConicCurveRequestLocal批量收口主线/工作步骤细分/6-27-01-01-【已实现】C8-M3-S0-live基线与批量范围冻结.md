# 【已实现】C8-M3-S0 live 基线与批量范围冻结

## 目标

冻结 C8-M3 的声明口径、live 基线、批量语义范围、禁止声明和状态词典。S0 不采 oracle，不改 C++。

## live 基线

- `pwd=/home/user/Chili3DProject/FreeCAD`
- `HEAD=c6a848b69c`
- `git log -1 --oneline`：`c6a848b69c docs: 完成 C8-M2 S6 发布闸门`
- S0 开始时 `git -c core.quotepath=false status --short -uall` 只显示 `docs/CADCore8.0/README.md` 已修改，以及本 C8-M3 文档包 / 矩阵 / 工作步骤未跟踪；未见 C++、Rust、fixture、expected、collector 或无关源码 dirty 文件。
- 队列首项是本 S0 文件；S0 完成后下一 pending 必须是 S1。

## S0 输入

- C8-M2 队列已在上一包 S6 关闭，当前 HEAD 是 C8-M2 S6 发布闸门提交。
- `part_workbench.conic_curves.status=done_part_geometry_curve_edge_consumer`。
- `part_workbench.conic_curves.remaining_gaps=["gui_conic_edit","full_sketcher_solver_conic_constraints","distance_type_default_todo"]` 作为 S0 输入保留；S0 不删除、不重命名、不声明已支持。
- 本包只冻结 conic request-local API 批量范围，后续是否实现由 S1-S6 证据决定。

## 范围冻结

| 类型 | 纳入 C8-M3 | S0 结论 |
| --- | --- | --- |
| `PartConicCurveDTO` producer / consumer current coverage | 是 | `C8M3-SCOPE-101..103` 纳入批量复核，避免重复单 fixture |
| Sketcher ArcOfHyperbola / ArcOfParabola input / external-reference | 是 | solver-facing 输入边界，不是 full solver |
| `distance_type_default_todo` | 是 | S5 必须实现、重分类或明确关闭条件 |
| GUI conic edit | 否 | non-goal |
| full Sketcher solver conic constraints | 否 | non-goal / solver-facing boundary |
| fake `Part::Hyperbola` / `Part::Parabola` DocumentObject | 否 | non-goal |
| 跨请求 geometry cache / BREP / NamedShape / ElementMap cache | 否 | non-goal |

## 状态词典

- `already_supported`：已有 expected-backed 或 focused-test-backed support。
- `oracle_candidate`：需要 FreeCAD native / current expected 复核。
- `backend_gap_candidate`：有 FreeCAD authority 与 current mismatch，S6 可实现。
- `capability_publication_gap`：代码已有支撑但 capability / tests / docs 仍保留 stale gap。
- `non_goal`：明确超出无状态 CAD Core。
- `split_required`：同轮批量风险过大，必须记录下一批范围和拆分理由。

## 必须回写的矩阵行

- `C8M3-SCOPE-101..103`
- `C8M3-SCOPE-201..203`
- `C8M3-SCOPE-301`
- `C8M3-NG-001..005`
- `C8M3-BLOCKER-000`

S0 已回写上述矩阵行。`C8M3-BLOCKER-000` 关闭为 `closed_S0_live_baseline_and_batch_scope_frozen`；S1-S6 队列保持 pending。

## 验收标准与结果

- 记录 live baseline：`pwd`、`git rev-parse --short HEAD`、`git log -1 --oneline`、`git status --short -uall`。
- `docs/CADCore8.0/README.md` 和本包 README / 总入口包含 C8-M3 当前状态与禁止声明。
- 矩阵中存在上述 scope / non-goal / blocker seed row。
- `part_workbench.conic_curves.remaining_gaps` 当前三项被列为 S0 输入，不被提前删除。
- 已按本步骤运行以下文档 / 矩阵验证；未运行 FreeCAD oracle、fixture、collector、C++ 或 Rust 验证。

```bash
cd /home/user/Chili3DProject/FreeCAD
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore8.0/C8-M3-PartSketchDistanceTypeConicCurveRequestLocal批量收口主线/工作步骤细分 --format markdown
rg -n 'C8-M3|PartConicCurveDTO|distance_type_default_todo|full_sketcher_solver_conic_constraints|gui_conic_edit|non_goal|backend_gap_candidate' docs/CADCore8.0/C8-M3-PartSketchDistanceTypeConicCurveRequestLocal批量收口主线 docs/CADCore8.0/README.md
awk -F '\t' 'FNR==1{n=NF; next} NF!=n{print FILENAME ":" FNR ": expected " n " fields, got " NF; bad=1} END{exit bad}' docs/CADCore8.0/C8-M3-PartSketchDistanceTypeConicCurveRequestLocal批量收口主线/矩阵/*.tsv
rg -n '[ \t]$' docs/CADCore8.0/C8-M3-PartSketchDistanceTypeConicCurveRequestLocal批量收口主线 docs/CADCore8.0/README.md
git diff --check
```

本文件已按原时间前缀重命名为 `6-27-01-01-【已实现】C8-M3-S0-live基线与批量范围冻结.md`，索引链接已更新。

## 非目标

- 不采 FreeCAD oracle。
- 不新增 fixture / expected / tests / collector。
- 不修改 C++ 或下游 Rust。
