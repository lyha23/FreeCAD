# 【已实现】C8-M3-S6 实现与发布闸门

## 目标

完成 C8-M3 发布闸门：队列、矩阵、docs、oracle / implementation 裁决、focused tests、capability publication 和最终状态同步。S6 只消费 S0-S5 明确的 `backend_gap_candidate`、`capability_publication_gap`、`oracle_blocked`、`split_required` 和 `non_goal` row，不新增业务范围。

## live 基线

- `pwd=/home/user/Chili3DProject/FreeCAD`
- `HEAD=eb26957cfd`
- `git log -1 --oneline`：`eb26957cfd docs: 完成 C8-M3 S5 DistanceType capability 准入`
- S6 开始时 `git -c core.quotepath=false status --short -uall` 无输出，工作区干净。
- 队列首项是本 S6 文件；S6 完成后队列应为空。

## release audit

已确认：

- S0-S5 均已按验收标准完成并重命名为 `【已实现】`。
- `step_goal_queue.py` 对本目录输出空 pending 表。
- `c8m3_conic_requestlocal_backend_gap_classification.tsv` 没有无证据的 `backend_gap_requires_implementation`。
- `distance_type_default_todo` 不再作为无解释 active gap。
- GUI conic edit 与 full Sketcher solver conic constraints 有 non-goal / reopen condition。
- 批量实现或发布没有退化成单 fixture 特判。

## S6 裁决

S6 裁决为 `capability_publication_sync`，不是 Assembly solver implementation gate。

- `C8M3-BG-101` / `C8M3-SCOPE-301`：`distance_type_default_todo` 从 `part_workbench.conic_curves.remaining_gaps` 移除，并在 conic capability 中链接到 Assembly `distance_type_extended_geometry` 的 `PointCurve` diagnostic 与 `default_or_todo_boundaries`。
- `C8M3-BG-201` / `C8M3-SCOPE-401`：`gui_conic_edit` 与 `full_sketcher_solver_conic_constraints` 发布为 explicit `non_goals` / reopen conditions，不作为 supported runtime。
- `C8M3-BG-301` / `C8M3-SCOPE-501`：`part_workbench.conic_curves.remaining_gaps=[]`，并把 S3 Part producer / consumer evidence、S4 Sketcher request-local evidence、S5 DistanceType publication evidence 同步到 `cad-core/tests/test_adapters.py` assertion。
- 未修改 `cad-core/src/assembly/joint_solver.cpp` 或 `cad-core/src/assembly/assembly_utils.cpp`。
- 未注册 fake `Part::Hyperbola` / `Part::Parabola` DocumentObject。

## S6 落点

| blocker / scope | C++ 或测试落点 | FreeCAD authority | 测试 | 成功标准 |
| --- | --- | --- | --- | --- |
| `C8M3-BG-101` / `C8M3-SCOPE-301` | `cad-core/src/runtime/capability_contract.cpp`、`cad-core/tests/test_adapters.py`、package docs | `AssemblyUtils.cpp::getDistanceType()`、`AssemblyObject.cpp::makeMbdJointOfType()` | existing `cad-core/tests/test_p8_features.py` coverage plus `test_adapters.py` capability assertion sync | S5 已裁决为 `capability_publication_gap`；S6 不改 Assembly solver/classifier，只删除 `distance_type_default_todo` active gap |
| `C8M3-BG-102` / `C8M3-SCOPE-201` | capability publication / `cad-core/tests/test_adapters.py` | `SketchObjectExternal.cpp`、`GeoList.cpp`、`SolverGeometryExtension.h` | `cad-core/tests/test_p5_sketch.py` | ArcOfHyperbola / ArcOfParabola request-local input / profile / external evidence 已发布 |
| `C8M3-BG-103` / `C8M3-SCOPE-101..103` | capability publication / `cad-core/tests/test_adapters.py` | `Geometry.cpp::GeomArcOf*::Save/Restore()` | `cad-core/tests/test_p8_features.py`、`test_expected_fixtures.py` | Part producer / consumer 批量证据与 capability 发布一致 |

禁止路径：

- 不按 fixture 名、bbox、面积、长度或几何类型猜业务逻辑。
- 不在 adapter/output JSON 末端补业务逻辑。
- 不保存跨请求 BREP、TopoDS_Shape、NamedShape 或 ElementMap cache。
- 不注册 fake `Part::Hyperbola` / `Part::Parabola` DocumentObject。
- 不把 full Sketcher solver conic constraints 写成 supported。

## 验收分层

本轮短跑：

```bash
cd /home/user/Chili3DProject/FreeCAD
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore8.0/C8-M3-PartSketchDistanceTypeConicCurveRequestLocal批量收口主线/工作步骤细分 --format markdown
awk -F '\t' 'FNR==1{n=NF; next} NF!=n{print FILENAME ":" FNR ": expected " n " fields, got " NF; bad=1} END{exit bad}' docs/CADCore8.0/C8-M3-PartSketchDistanceTypeConicCurveRequestLocal批量收口主线/矩阵/*.tsv
rg -n '[ \t]$' docs/CADCore8.0/C8-M3-PartSketchDistanceTypeConicCurveRequestLocal批量收口主线 docs/CADCore8.0/README.md
git diff --check
```

实现回归：

```bash
cd /home/user/Chili3DProject/FreeCAD/cad-core
cmake --build build
python3 -m unittest tests.test_p8_features tests.test_p5_sketch tests.test_adapters tests.test_diagnostics
```

重型收口只在修改 expected collector、批量 expected、Sketcher profile shared path、DistanceType shared classifier 或 Part conic producer 主路径时执行。

## 验收结果

已按本步骤运行以下验证；`rg -n '[ \t]$' ...` 无输出，表示无 trailing whitespace 命中。

```bash
cd /home/user/Chili3DProject/FreeCAD/cad-core
cmake --build build
python3 -m unittest tests.test_p8_features tests.test_p5_sketch tests.test_adapters tests.test_diagnostics

cd /home/user/Chili3DProject/FreeCAD
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore8.0/C8-M3-PartSketchDistanceTypeConicCurveRequestLocal批量收口主线/工作步骤细分 --format markdown
awk -F '\t' 'FNR==1{n=NF; next} NF!=n{print FILENAME ":" FNR ": expected " n " fields, got " NF; bad=1} END{exit bad}' docs/CADCore8.0/C8-M3-PartSketchDistanceTypeConicCurveRequestLocal批量收口主线/矩阵/*.tsv
rg -n '[ \t]$' docs/CADCore8.0/C8-M3-PartSketchDistanceTypeConicCurveRequestLocal批量收口主线 docs/CADCore8.0/README.md
git diff --check
```

结果：

- `cmake --build build` 通过。
- `python3 -m unittest tests.test_p8_features tests.test_p5_sketch tests.test_adapters tests.test_diagnostics` 通过：`Ran 413 tests in 57.753s`，`OK`。
- `step_goal_queue.py ... --format markdown` 输出空 pending 表。
- TSV column check 通过。
- trailing whitespace scan 无匹配。
- `git diff --check` 通过。

## 完成规则

S6 完成项：

- 本文件已重命名为 `6-27-01-07-【已实现】C8-M3-S6-实现与发布闸门.md`。
- `docs/CADCore8.0/README.md`、本包 README、总入口、方案、步骤索引和矩阵状态已同步。
- 提交前需确认只包含本包相关变更。
- 最终运行 `git status --short -uall`，确认工作区干净。

## 非目标

- 不开新 feature scope。
- 不执行全量 FreeCAD build。
- 不直接修改下游 Rust。
