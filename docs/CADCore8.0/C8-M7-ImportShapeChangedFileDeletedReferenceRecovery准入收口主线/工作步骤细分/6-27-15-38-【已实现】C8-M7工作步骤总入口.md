# 【已实现】C8-M7 工作步骤总入口

## 总目标

处理 current capability 中 `topo_history.producer_matrix.import_shape.remaining=["changed_file_deleted_reference_recovery"]` 的残留行，并在 C8-M7 内给出可验证裁决：已覆盖发布收口、request-local backend gap 实现，或 oracle-blocked / non-goal 发布边界。

## 执行顺序

1. S0：live 基线与 residual 声明冻结。
2. S1：FreeCAD 源码与 current 覆盖复核。
3. S2：准入路由与 blocker 矩阵。
4. S3：import 文件生命周期 oracle 复审。
5. S4：ElementMap 与 ReferenceShadow 恢复边界复审。
6. S5：capability 残留与 non-goal 发布准入。
7. S6：实现或 no-code 发布闸门。

## 全局纪律

- 每一步开始先看 `git -c core.quotepath=false status --short -uall`，只改本步骤相关文件。
- 不重开 C7-M7 的完整 Link / ShowElement / cross-document lifecycle。
- 不引入跨请求 backend cache、full BREP、TopoDS_Shape、NamedShape 或 ElementMap 持久状态。
- 若发现实现缺口，先写 FreeCAD 调用链和 cad-core 分层映射，再改代码。
- 每一步完成后将本步骤文件重命名为带 `【已实现】` 的文件名，并更新 README / 矩阵中对应关闭记录。

## 队列命令

```bash
cd /home/user/Chili3DProject/FreeCAD
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore8.0/C8-M7-ImportShapeChangedFileDeletedReferenceRecovery准入收口主线/工作步骤细分 --format markdown
```
