# C8-M7 S6 实现或 no-code 发布闸门

## 目标

按 S5 裁决关闭 C8-M7：如果存在 `request_local_backend_gap`，完成受限实现和 focused tests；否则执行 no-code publication closure。最终必须清空 C8-M7 队列。

## S6 分支

### no-code closure

适用条件：

- S0-S5 未发现 implementable current mismatch。
- changed-file 已由当前请求重导入覆盖，或 deleted-file 被裁为 non-goal / oracle-blocked。
- capability residual 只需文档解释或发布口径调整。

允许改动：

- C8-M7 README、方案、工作步骤、矩阵。
- `docs/CADCore8.0/README.md`。
- 如 S5 明确要求能力文案收口，可改 `cad-core/src/runtime/capability_contract.cpp` 与 `cad-core/tests/test_adapters.py`。

### implementation gate

适用条件：

- S5 标出 `request_local_backend_gap`。
- 缺口可由当前请求 `FileName`、current imported shape、stable subname、`ReferenceShadow` 和 ElementMap 解决。
- 不需要跨请求 backend cache。

允许落点：

- `cad-core/src/part/part_import.cpp`
- `cad-core/src/part/topo_shape_expansion.cpp`
- `cad-core/src/part/topo_shape.cpp`
- `cad-core/src/runtime/reference_resolution.cpp`
- `cad-core/src/runtime/element_reference_update.cpp`
- `cad-core/src/runtime/capability_contract.cpp`
- `cad-core/tests/test_p6_topology.py`
- `cad-core/tests/test_p8_features.py`
- `cad-core/tests/test_adapters.py`

## S6 结论

S6 按 S5 裁决走 capability publication closure，不打开 runtime backend gate。`topo_history.producer_matrix.import_shape` 保持 `status=done_first_slice` 和 `covered=["step","brep","iges","owner_qualified_alias"]`，`remaining=[]`；`changed_file_deleted_reference_recovery` 不再出现在 capability JSON。

deleted-file old geometry recovery 不发布为 supported / covered，仍保持 diagnostic / non-goal 边界。本轮仅允许并执行 `cad-core/src/runtime/capability_contract.cpp`、`cad-core/tests/test_adapters.py`、C8-M7 README / 矩阵、`docs/CADCore8.0/README.md` 和 S6 文件名收口；不改 runtime import、reference resolution、elementReferenceUpdates、fixtures、expected、adapter 输出改写或下游仓库。

## 发布闸门

本轮短跑：

```bash
cd /home/user/Chili3DProject/FreeCAD
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore8.0/C8-M7-ImportShapeChangedFileDeletedReferenceRecovery准入收口主线/工作步骤细分 --format markdown
awk -F '\t' 'FNR==1{n=NF; next} NF!=n{print FILENAME ":" FNR ": expected " n " fields, got " NF; bad=1} END{exit bad}' docs/CADCore8.0/C8-M7-ImportShapeChangedFileDeletedReferenceRecovery准入收口主线/矩阵/*.tsv
rg -n '[ \t]$' docs/CADCore8.0/C8-M7-ImportShapeChangedFileDeletedReferenceRecovery准入收口主线 docs/CADCore8.0/README.md
git diff --check
```

若改 capability 或 runtime：

```bash
cd /home/user/Chili3DProject/FreeCAD/cad-core
cmake --build build
./cad-core capabilities > /tmp/c8m7-capabilities.json
python3 -m unittest tests.test_p6_topology tests.test_p8_features tests.test_adapters
```

## 关闭条件

- `C8M7-BLOCKER-601` 关闭。
- C8-M7 S6 文件重命名为 `【已实现】` 后，队列为空。
- README 记录最终 route、验证命令和任何 remaining / non-goal 状态。
- 如果进入代码 gate，相关 focused tests 和 build 通过。
