# C8-M7 S2 准入路由与 blocker 矩阵

## 目标

把 `changed_file_deleted_reference_recovery` 拆成可裁决行：changed-file 当前请求重导入、deleted-file 不可读诊断、ReferenceShadow request-local 恢复、persistent cache 非目标和 capability publication drift。

## 输入

- S0/S1 README 结论。
- `矩阵/c8m7_import_shape_recovery_source_candidates.tsv`
- `矩阵/c8m7_import_shape_recovery_scope_review_matrix.tsv`
- `矩阵/c8m7_import_shape_recovery_backend_gap_classification.tsv`
- `矩阵/c8m7_import_shape_recovery_non_goal_registry.tsv`

## 路由分类

- `already_covered`: current code / tests 已覆盖，S5/S6 只需发布收口。
- `oracle_candidate`: 需要 S3 native evidence 或现有 expected 复审。
- `request_local_backend_gap_candidate`: 可能通过当前请求数据实现，禁止依赖跨请求 cache。
- `known_gap_retained`: 确认可诊断但不可声明 supported。
- `diagnostic_non_goal`: 不属于无状态 CAD Core。
- `unexpected_mismatch`: current evidence 与发布口径矛盾，S6 必须进入代码或能力修复。

## 必须回写

- `C8M7-SCOPE-101` 到 `C8M7-SCOPE-302`
- `C8M7-BG-101` 到 `C8M7-BG-302`
- `C8M7-NG-001` 到 `C8M7-NG-007`
- `C8M7-BLOCKER-201`

## 验收

```bash
cd /home/user/Chili3DProject/FreeCAD
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore8.0/C8-M7-ImportShapeChangedFileDeletedReferenceRecovery准入收口主线/工作步骤细分 --format markdown
awk -F '\t' 'FNR==1{n=NF; next} NF!=n{print FILENAME ":" FNR ": expected " n " fields, got " NF; bad=1} END{exit bad}' docs/CADCore8.0/C8-M7-ImportShapeChangedFileDeletedReferenceRecovery准入收口主线/矩阵/*.tsv
rg -n 'request_local_backend_gap_candidate|diagnostic_non_goal|known_gap_retained|unexpected_mismatch|already_covered' docs/CADCore8.0/C8-M7-ImportShapeChangedFileDeletedReferenceRecovery准入收口主线
git diff --check
```

## 非目标

- 不实现代码。
- 不采 heavy oracle。
- 不修改 C7-M7 结论。
