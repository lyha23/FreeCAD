# 【已实现】C13-M4 工作步骤总入口

## 目标

按小步队列把 FreeCADCmd ledger sidecar 证明过的 public topoNamingState 投影接入 `cad-core` runtime，并优先关闭 `c4m6/topo-state-link-compound-child-maps` 的 child path projection diff。

## 步骤

| 步骤 | 文件 | 关闭条件 |
| --- | --- | --- |
| S0 | `7-9-18-42-C13-M4-S0-live基线与ledger闭包冻结.md` | 【已实现】live baseline、ledger gate、首个 runtime diff 和非目标冻结。 |
| S1 | `7-9-18-43-C13-M4-S1-Compound-child-path投影发布.md` | `Compound.Child0.Face1` projection subshape、projection child map 和顶层 entry 发布。 |
| S2 | `7-9-18-44-C13-M4-S2-c4m6-focused-parity转绿.md` | `tests.test_topo_naming_state_response` 普通通过，c4m6 strict ledger gate 仍通过。 |
| S3 | `7-9-18-45-C13-M4-S3-门禁收口与C13索引更新.md` | README/矩阵/索引收口，记录是否回流 C13-M2/C13-M3。 |

## 队列验证

```bash
cd /Users/li/Chili3DProject/FreeCAD
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore13.0/C13-M4-FreeCADExpectedLedgerTopoState投影闭环批次/工作步骤细分 --format markdown
```

## 入口关闭条件

- 包结构齐备：README、方案、总入口、S0-S3、矩阵。
- TSV 字段数校验通过。
- 本入口关闭后队列从 S0 开始。

## 入口关闭结果

- 包结构已齐备：README、方案、总入口、S0-S3、6 个 TSV 矩阵文件。
- `step_goal_queue.py` 可渲染本目录队列。
- TSV 字段数校验通过。
- `git diff --check` 通过。
- 本文件标记为已实现后，后续队列从 S0 开始。

## 验收

```bash
cd /Users/li/Chili3DProject/FreeCAD
awk -F '\t' 'FNR==1{n=NF; next} NF!=n{print FILENAME ":" FNR ": expected " n " fields, got " NF; bad=1} END{exit bad}' docs/CADCore13.0/C13-M4-FreeCADExpectedLedgerTopoState投影闭环批次/矩阵/*.tsv
git diff --check
```
