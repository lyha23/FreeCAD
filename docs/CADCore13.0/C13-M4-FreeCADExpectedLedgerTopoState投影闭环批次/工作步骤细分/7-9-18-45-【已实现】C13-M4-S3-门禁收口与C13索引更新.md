# 【已实现】C13-M4 S3 门禁收口与 C13 索引更新

## 目标

在 S2 转绿后收口文档、矩阵和 CADCore13.0 索引，明确 C13-M4 是否只关闭 c4m6 projection 闭环，还是需要把新 blocker 回流 C13-M2/C13-M3。

## 必读文件

- S2 输出
- `docs/CADCore13.0/README.md`
- `docs/CADCore13.0/C13-M4-FreeCADExpectedLedgerTopoState投影闭环批次/README.md`
- `docs/CADCore13.0/C13-M4-FreeCADExpectedLedgerTopoState投影闭环批次/矩阵/*.tsv`

## 操作

1. 更新 README 当前结论与矩阵状态。
2. 如果 C13-M4 关闭，按仓库规则把方案文件重命名为 `7-9-18-40-【已实现】C13-M4-FreeCADExpectedLedgerTopoState投影闭环批次方案.md`。
3. 如果 S2 暴露非 projection blocker，记录到 blocker matrix，并保持方案未标已实现。
4. 更新 `docs/CADCore13.0/README.md` 中 C13-M4 状态。
5. 最后运行队列、TSV 和 diff 检查。

## 关闭条件

- `C13M4-BLOCKER-301` 关闭，或明确记录 open blocker 和回流批次。
- 工作步骤队列状态与 README 一致。
- TSV 字段数校验通过。
- `git diff --check` 通过。

## S3 实现结果

- C13-M4 包状态已收口为 closed / completed，只关闭 `c4m6` public projection 闭环。
- C13M4-BLOCKER-301、C13M4-IMPL-301、C13M4-VAL-001、C13M4-VAL-002、C13M4-VAL-003 已关闭。
- C13-M4 README、矩阵和 `docs/CADCore13.0/README.md` 已同步：ledger validator 9/9 green，focused topoNamingState runtime 14 tests OK，S1 projection 已发布。
- 未发现新的 C13-M2 / C13-M3 回流 blocker；既有 C13-M2 S4 producer ledger blocker 与 C13-M3 前置实现边界保持不变。
- 本步未修改 C++、expected/ledger JSON，未重跑或重采 FreeCADCmd oracle。

## 验收

```bash
cd /Users/li/Chili3DProject/FreeCAD
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore13.0/C13-M4-FreeCADExpectedLedgerTopoState投影闭环批次/工作步骤细分 --format markdown
awk -F '\t' 'FNR==1{n=NF; next} NF!=n{print FILENAME ":" FNR ": expected " n " fields, got " NF; bad=1} END{exit bad}' docs/CADCore13.0/C13-M4-FreeCADExpectedLedgerTopoState投影闭环批次/矩阵/*.tsv
git diff --check
```
