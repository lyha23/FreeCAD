# C13-M3 S4 runtime 消费与 S3 红线转绿

## 目标

让 `runtime/topo_naming_state.cpp` 消费 producer ledger 输出 `mappedName.raw/canonical`，删除 C13-M2 S3 的 guarded expectedFailure。

## 必读文件

- S3 输出
- `cad-core/src/runtime/topo_naming_state.cpp`
- `cad-core/tests/test_topo_naming_state_response.py`
- `docs/CADCore13.0/C13-M2-FreeCADMappedNameParity实现批次/工作步骤细分/7-8-20-20-【已实现】C13-M2-S3-focused-red-tests.md`

## 操作

1. runtime 只消费 source-backed ledger。
2. 无 ledger evidence 时不要发布 fake raw mappedName。
3. 删除 p2/c4m6/p6/p5/p8 五个 expectedFailure。
4. 保留 S5 空证据守卫，除非同时补了非空 key/id parity。

## 关闭条件

- `C13M3-BLOCKER-401` 关闭。
- `python3 -m unittest tests.test_topo_naming_state_response` 普通通过，无 expectedFailure。
- C13-M1 adapter channel 不回退。

## 本轮阻塞结果

- 已实现 runtime 消费边界：`cad-core/src/runtime/topo_naming_state.cpp` 只发布 `NamedShape::mappedNameProvenance` 中 `source_backed` 且 raw/canonical 非空的 ledger entry；无 ledger evidence 时不再用 stable/display token 合成 `mappedName.raw/canonical`。
- p5 `sketch-internal-face` / `Sketch` 与 p8 `app-link-box-face` / `BoxLink` 已从 C13-M2 S3 expectedFailure 转为普通通过，验证 indexed-only no-fake-raw 边界。
- S4 不能关闭：p2 `rect-pad-pocket` / `Body` 的 S3 ledger 仍只有 request-local source-backed names，例如 `Pad.Edge1;CUT...`，runtime 会跳过这类没有 FreeCAD `#` encoded element token 的 evidence，actual 为 `indexed_only` / `0 entries`，expected 需要 `Pocket.#...` FreeCAD raw keys；c4m6 `topo-state-body-tip-stable-recovery` / `Body` 同理，S3 ledger 仍是 `Sketch.Edge1;XTR...`，expected 需要 `Pad.#...` FreeCAD raw keys。
- p6 `up-to-face-stable-body-history` / `ProbePad` 仍无 object-local producer ledger，actual `topoNamingState.objects.ProbePad.elementMap` 为 `indexed_only` / `0 entries`，expected 为 `history_partial` / `26 entries`；按本步红线不得 fake。
- `python3 -m unittest tests.test_topo_naming_state_response` 当前结果为 `OK (expected failures=3)`；剩余 expectedFailure 只保留 p2/c4m6/p6 三个 parity blocker。

## 验收

```bash
cd /Users/li/Chili3DProject/FreeCAD/cad-core
cmake --build build
python3 -m unittest tests.test_topo_naming_state_response
python3 -m unittest tests.test_adapters.CadCoreAdapterTest.test_c13m1_cli_c_api_worker_wasm_share_topo_naming_state_channel
cd ..
git diff --check
```
