# C13-M3 S4 runtime 消费与 S3 红线转绿

## 目标

让 `runtime/topo_naming_state.cpp` 消费 producer ledger 输出 `mappedName.raw/canonical`，删除 C13-M2 S3 的 guarded expectedFailure。

## 必读文件

- S3 输出
- `cad-core/src/runtime/topo_naming_state.cpp`
- `cad-core/tests/test_topo_naming_state_response.py`
- `docs/CADCore13.0/C13-M2-FreeCADMappedNameParity实现批次/工作步骤细分/7-8-20-20-【已实现】C13-M2-S3-focused-red-tests.md`

## 操作

1. runtime 只消费 source-backed ledger。
2. 无 ledger evidence 时不要发布 fake raw mappedName。
3. 删除 p2/c4m6/p6/p5/p8 五个 expectedFailure。
4. 保留 S5 空证据守卫，除非同时补了非空 key/id parity。

## 关闭条件

- `C13M3-BLOCKER-401` 关闭。
- `python3 -m unittest tests.test_topo_naming_state_response` 普通通过，无 expectedFailure。
- C13-M1 adapter channel 不回退。

## 验收

```bash
cd /Users/li/Chili3DProject/FreeCAD/cad-core
cmake --build build
python3 -m unittest tests.test_topo_naming_state_response
python3 -m unittest tests.test_adapters.CadCoreAdapterTest.test_c13m1_cli_c_api_worker_wasm_share_topo_naming_state_channel
cd ..
git diff --check
```
