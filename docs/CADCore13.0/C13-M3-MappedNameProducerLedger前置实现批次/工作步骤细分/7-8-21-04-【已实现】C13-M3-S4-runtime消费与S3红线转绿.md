# 【已实现】C13-M3 S4 runtime 消费与 S3 红线转绿

## 目标

让 `runtime/topo_naming_state.cpp` 只消费 producer ledger 输出 `mappedName.raw/canonical`，删除 C13-M2 S3 的 guarded expectedFailure，并保持无 ledger evidence 时不发布 fake raw mappedName。

## 必读文件

- `7-8-21-03-【已实现】C13-M3-S3-PartDesign-focused-producer接线.md`
- `cad-core/src/runtime/topo_naming_state.cpp`
- `cad-core/src/part/topo_shape.cpp`
- `cad-core/tests/test_topo_naming_state_response.py`
- `docs/CADCore13.0/C13-M2-FreeCADMappedNameParity实现批次/工作步骤细分/7-8-20-20-【已实现】C13-M2-S3-focused-red-tests.md`

## 关闭结果

- live baseline：`pwd=/Users/li/Chili3DProject/FreeCAD`，起点 `HEAD=645f505d1d`（`645f505d1d docs: 关闭 C13-M4 门禁收口`），起点只有无关脏文件 `DESIGN.md` 与 `docs/框架/7-9-15-53-FreeCADCmd权威账本与topoNamingState裁剪原则.md`，本步未暂存、未回退。
- `topoNamingState` runtime publisher 已保持 S4 边界：只发布 `NamedShape::mappedNameProvenance` 中 `source_backed` 且 raw/canonical 非空，并带 FreeCAD `#` encoded element token 的 entries；缺 producer ledger evidence 时保持 indexed-only，不从 stable/display/fullSubname 伪造 raw mapped name。
- `cad-core/src/part/topo_shape.cpp::requestLocalProducerTagForShape()` 修复 C13-M1 adapter channel 漂移：不再使用 `std::hash<TopoDS_Shape>` 这类进程局部 identity，而是从当前 producer shape 的稳定 topology / geometry fingerprint 得到 request-local tag evidence，保证 CLI / C API / worker / wasm 发布同一 topoNamingState。
- `cad-core/tests/test_topo_naming_state_response.py` 已无 `unittest.expectedFailure`，p2 `rect-pad-pocket` / `Body`、c4m6 `topo-state-body-tip-stable-recovery` / `Body` 的 `mappedName.raw/canonical` parity 普通通过；p5 `sketch-internal-face` / `Sketch` 与当前 p8 `app-link-box` / `BoxLink` 的 indexed-only no-fake-raw 边界普通通过。
- 旧 focused fixture `p6/up-to-face-stable-body-history` 与 `p8/app-link-box-face` 已在 `718267783c chore: 刷新 FreeCAD expected 账本基线` 中删除，提交说明为“移除已迁移或废弃的 fixture、cad-core-res 和测试断言”。本步不手工恢复或手改 expected；若后续重新纳入 p6 `ProbePad` parity，必须重新采集 native expected 并另排步骤。
- `C13M3-BLOCKER-401` 关闭；S5 发布闸门与 C13-M2 回流仍未关闭。

## 剩余风险

- S4 只关闭 runtime mappedName publication 与 focused response/adapter channel；不做全量 topo state expected parity。
- p6 旧 `ProbePad` focused pair 当前不在 live fixture/expected 树内；不能把旧文件缺席当作 FreeCAD parity 证据。
- `childElementMapKey` / `mapperHistoryIds` 的发布闸门仍属于 S5。

## 验收

```bash
cd /Users/li/Chili3DProject/FreeCAD/cad-core
cmake --build build
python3 -m unittest tests.test_topo_naming_state_response
python3 -m unittest tests.test_adapters.CadCoreAdapterTest.test_c13m1_cli_c_api_worker_wasm_share_topo_naming_state_channel

cd /Users/li/Chili3DProject/FreeCAD
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore13.0/C13-M3-MappedNameProducerLedger前置实现批次/工作步骤细分 --format markdown
awk -F '\t' 'FNR==1{n=NF; next} NF!=n{print FILENAME ":" FNR ": expected " n " fields, got " NF; bad=1} END{exit bad}' docs/CADCore13.0/C13-M3-MappedNameProducerLedger前置实现批次/矩阵/*.tsv
git diff --check
```

验收结果：`cmake --build build` 通过；`tests.test_topo_naming_state_response` 为 `Ran 15 tests OK`；C13-M1 adapter channel 单测为 `Ran 1 test OK`；队列、TSV 字段数和 `git diff --check` 由本步最终验收记录。
