# 【已实现】C13-M3 S5 发布闸门与 C13-M2 回流

## 目标

收口 C13-M3，并明确 C13-M2 S4 可以恢复执行。

## 必读文件

- S0-S4 输出
- `../README.md`
- `../../README.md`
- `../../C13-M2-FreeCADMappedNameParity实现批次/README.md`
- C13-M3 矩阵

## 操作

1. 更新 C13-M3 README、矩阵和工作步骤状态。
2. 若 S4 已普通通过，更新 C13-M2 的 S4 blocker 描述为可恢复。
3. 若仍有 producer-side 缺口，拆出 narrowed blocker，不得假装 C13-M2 S4 可关闭。
4. 记录验收命令和剩余非目标。

## 关闭条件

- `C13M3-BLOCKER-501` 关闭。
- C13-M3 queue 为空，或明确写出剩余 blocker 和下一批次。

## 关闭结果

- live baseline：`pwd=/Users/li/Chili3DProject/FreeCAD`，起点 `HEAD=f385c2b5e5`（`f385c2b5e5 fix: 关闭 C13-M3 S4 runtime 消费`）。起点已有外部脏文件 `DESIGN.md`、`docs/框架/7-9-15-53-FreeCADCmd权威账本与topoNamingState裁剪原则.md` 和 3 个 `cad-core/fixtures/c4m6/cad-core-res/*.json`，本步未暂存、未回退。
- `C13M3-BLOCKER-501` 已关闭：C13-M3 S1-S4 已解除 C13-M2 S4 的 producer-ledger 前置阻塞，S5 只做发布闸门和文档回流。
- C13-M3 队列在 S5 文件重命名为 `【已实现】` 后为空；C13-M2 队列仍从 S4/S5/S6 继续。
- C13-M2 S4 可恢复/继续执行，但本步不替 C13-M2 关闭 S4；`childElementMapKey` / `mapperHistoryIds` 仍按 C13-M2 S5/S6 队列处理，未标成 supported。
- S4 live evidence 可作为回流依据：`tests.test_topo_naming_state_response` 为 `Ran 15 OK` 且无 expectedFailure；adapter channel 单测为 `Ran 1 OK`；p2/c4m6 raw/canonical parity 和 p5/current p8 no-fake-raw 普通通过。
- 旧 `p6/up-to-face-stable-body-history` 与 `p8/app-link-box-face` fixture 已由 `718267783c chore: 刷新 FreeCAD expected 账本基线` 删除，不能作为 live parity 证据；后续若恢复 p6 `ProbePad` 必须重新采集 native expected。

## 验收

```bash
cd /Users/li/Chili3DProject/FreeCAD
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore13.0/C13-M3-MappedNameProducerLedger前置实现批次/工作步骤细分 --format markdown
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore13.0/C13-M2-FreeCADMappedNameParity实现批次/工作步骤细分 --format markdown
awk -F '\t' 'FNR==1{n=NF; next} NF!=n{print FILENAME ":" FNR ": expected " n " fields, got " NF; bad=1} END{exit bad}' docs/CADCore13.0/C13-M3-MappedNameProducerLedger前置实现批次/矩阵/*.tsv
awk -F '\t' 'FNR==1{n=NF; next} NF!=n{print FILENAME ":" FNR ": expected " n " fields, got " NF; bad=1} END{exit bad}' docs/CADCore13.0/C13-M2-FreeCADMappedNameParity实现批次/矩阵/*.tsv
git diff --check
```

验收结果：本步最终验证通过；C13-M3 queue 只剩表头，C13-M2 queue 仍列出 S4/S5/S6。
