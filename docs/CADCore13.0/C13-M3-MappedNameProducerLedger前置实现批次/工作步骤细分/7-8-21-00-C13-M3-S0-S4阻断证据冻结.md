# C13-M3 S0 S4 阻断证据冻结

## 目标

冻结 C13-M2 S4 的真实 blocker：当前 cad-core 没有 producer-side FreeCAD raw mapped-name ledger，不能安全实现 runtime codec。

## 必读文件

- `../README.md`
- `../7-8-20-58-C13-M3-MappedNameProducerLedger前置实现批次方案.md`
- `../../C13-M2-FreeCADMappedNameParity实现批次/README.md`
- `../../C13-M2-FreeCADMappedNameParity实现批次/工作步骤细分/7-8-20-20-【已实现】C13-M2-S3-focused-red-tests.md`
- `../../C13-M2-FreeCADMappedNameParity实现批次/工作步骤细分/7-8-20-21-C13-M2-S4-mappedName-codec实现.md`
- `cad-core/tests/test_topo_naming_state_response.py`

## 操作

1. 记录当前 `git status --short`、HEAD 和 C13-M2 / C13-M3 队列。
2. 复核 S3 expectedFailure redline。
3. 记录 S4 blocker：缺 `TopoShape.Tag` / `ElementMap::encodeElementName()` 等价 producer ledger。
4. 更新 scope / blocker / validation matrix。

## 关闭条件

- `C13M3-BLOCKER-001` 关闭。
- `C13M3-SCOPE-001` 关闭。
- README 记录 C13-M3 是 C13-M2 S4 前置批次。

## 验收

```bash
cd /Users/li/Chili3DProject/FreeCAD
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore13.0/C13-M3-MappedNameProducerLedger前置实现批次/工作步骤细分 --format markdown
awk -F '\t' 'FNR==1{n=NF; next} NF!=n{print FILENAME ":" FNR ": expected " n " fields, got " NF; bad=1} END{exit bad}' docs/CADCore13.0/C13-M3-MappedNameProducerLedger前置实现批次/矩阵/*.tsv
git diff --check
```
