# 【已实现】C13-M5 工作步骤总入口

## 目标

按小步队列把 `cad-core` release output 对齐到 `fixtures/<phase>/expected/*.freecad.json`。先建立 expected discovery 与 strict comparator，再用 `c4m6` 关闭 topoNamingState strict lane，最后按 phase 家族扩展。

## 步骤

| 步骤 | 文件 | 关闭条件 |
| --- | --- | --- |
| S0 | `7-10-00-17-【已实现】C13-M5-S0-inventory与比较边界冻结.md` | expected inventory、field policy、首批 lane 和非目标已冻结。 |
| S1 | `7-10-00-18-【已实现】C13-M5-S1-strict-comparator与生成入口.md` | 已实现：可按 phase 生成 cad-core-res，并输出 canonical strict diff report。 |
| S2 | `7-10-00-19-【已实现】C13-M5-S2-c4m6-strict-public-parity红灯基线.md` | 已实现：c4m6 strict diff 全部机器化归类，测试从最低合同升级到 strict expected 红灯基线。 |
| S3 | `7-10-00-20-【已实现】C13-M5-S3-topoNamingState发布策略对齐.md` | 已由 S5 覆盖 hash-recompute 旧结论：request integrity 在 recompute 前 hard fail，transport 仅精确 registry 接受。 |
| S4 | `7-10-00-21-【已实现】C13-M5-S4-phase-family扩展.md` | 已实现：按家族记录 red classified known gaps，不把它们伪装成 green。 |
| S5 | `7-10-00-22-【已实现】C13-M5-S5-release-gate收口.md` | 已实现：role/ledger/registry/live freshness/contract-test gate 闭合；known gaps 继续可追踪。 |

## 队列验证

```bash
cd /Users/li/Chili3DProject/FreeCAD
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore13.0/C13-M5-FreeCADExpected发布对齐批次/工作步骤细分 --format markdown
```

## 入口关闭条件

- 包结构齐备：README、方案、总入口、S0-S5、矩阵。
- TSV 字段数校验通过。
- 顶层 C13 README 已加入 M5。
- S0-S5 均已关闭，队列为空；S4 family red surface 由后续实现批次处理。

## 验收

```bash
cd /Users/li/Chili3DProject/FreeCAD
awk -F '\t' 'FNR==1{n=NF; next} NF!=n{print FILENAME ":" FNR ": expected " n " fields, got " NF; bad=1} END{exit bad}' docs/CADCore13.0/C13-M5-FreeCADExpected发布对齐批次/矩阵/*.tsv
git diff --check -- docs/CADCore13.0/C13-M5-FreeCADExpected发布对齐批次 docs/CADCore13.0/README.md
```
