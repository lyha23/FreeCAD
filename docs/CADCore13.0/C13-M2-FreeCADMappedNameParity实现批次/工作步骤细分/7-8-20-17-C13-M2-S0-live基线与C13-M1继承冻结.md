# C13-M2 S0 live 基线与 C13-M1 继承冻结

## 目标

冻结 C13-M2 起点：C13-M1 已发布 response state，C13-M2 只继承该闭环并处理 mapped-name evidence parity。

## 必读文件

- `../README.md`
- `../7-8-20-15-C13-M2-FreeCADMappedNameParity实现批次方案.md`
- `../../C13-M1-TopoNamingState输出发布闭环批次/README.md`
- `../../C13-M1-TopoNamingState输出发布闭环批次/矩阵/c13m1_topo_state_contract_matrix.tsv`
- `../../C13-M1-TopoNamingState输出发布闭环批次/矩阵/c13m1_topo_state_non_goal_registry.tsv`
- `cad-core/src/runtime/topo_naming_state.cpp`
- focused `cad-core/fixtures/<phase>/cad-core-res/*.cad-core.json`

## 操作

1. 记录 `git status --short`、`git rev-parse --short=10 HEAD`、`git log -1 --oneline`。
2. 确认 C13-M1 队列为空。
3. 抽样 focused `cad-core-res` 与 expected 的 C13-M2 gap。
4. 冻结非目标：不改 frontend、不做全量 parity、不复制 expected 字符串。

## 关闭条件

- `矩阵/c13m2_mapped_name_scope_matrix.tsv` 中 S0 行更新为 `closed`。
- blocker queue 中 S0 blocker 关闭。
- validation matrix 记录队列、TSV、diff check。

## 验收

```bash
cd /Users/li/Chili3DProject/FreeCAD
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore13.0/C13-M2-FreeCADMappedNameParity实现批次/工作步骤细分 --format markdown
git diff --check
```
