# 【已实现】C13-M2 S0 live 基线与 C13-M1 继承冻结

## 目标

冻结 C13-M2 起点：C13-M1 已发布 response state，C13-M2 只继承该闭环并处理 mapped-name evidence parity。

## 必读文件

- `../README.md`
- `../7-8-20-15-C13-M2-FreeCADMappedNameParity实现批次方案.md`
- `../../C13-M1-TopoNamingState输出发布闭环批次/README.md`
- `../../C13-M1-TopoNamingState输出发布闭环批次/矩阵/c13m1_topo_state_contract_matrix.tsv`
- `../../C13-M1-TopoNamingState输出发布闭环批次/矩阵/c13m1_topo_state_non_goal_registry.tsv`
- `cad-core/src/runtime/topo_naming_state.cpp`
- focused `cad-core/fixtures/<phase>/cad-core-res/*.cad-core.json`

## 操作

1. 记录 `git status --short`、`git rev-parse --short=10 HEAD`、`git log -1 --oneline`。
2. 确认 C13-M1 队列为空。
3. 抽样 focused `cad-core-res` 与 expected 的 C13-M2 gap。
4. 冻结非目标：不改 frontend、不做全量 parity、不复制 expected 字符串。

## 关闭条件

- `矩阵/c13m2_mapped_name_scope_matrix.tsv` 中 S0 行更新为 `closed`。
- blocker queue 中 S0 blocker 关闭。
- validation matrix 记录队列、TSV、diff check。

## 验收

```bash
cd /Users/li/Chili3DProject/FreeCAD
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore13.0/C13-M1-TopoNamingState输出发布闭环批次/工作步骤细分 --format markdown
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore13.0/C13-M2-FreeCADMappedNameParity实现批次/工作步骤细分 --format markdown
awk -F '\t' 'FNR==1{n=NF; next} NF!=n{print FILENAME ":" FNR ": expected " n " fields, got " NF; bad=1} END{exit bad}' docs/CADCore13.0/C13-M2-FreeCADMappedNameParity实现批次/矩阵/*.tsv
git diff --check
```

## 关闭结果

- live baseline：`pwd=/Users/li/Chili3DProject/FreeCAD`，`HEAD=907a9264d7`（`907a9264d7 docs: 关闭 C13-M2 工作步骤总入口`），起点 `git status --short -uall` 无输出。
- C13-M1 队列为空：`step_goal_queue.py docs/CADCore13.0/C13-M1-TopoNamingState输出发布闭环批次/工作步骤细分 --format markdown` 只输出表头，无 pending 步骤。
- C13-M2 队列在本步关闭前从 S0 开始；本步关闭后应从 S1 `FreeCAD MappedName 源码调用链复核` 继续。
- 本步未修改 `cad-core/src/runtime/topo_naming_state.cpp`、fixtures、expected、frontend 或 S1-S6 步骤文件。

## C13-M1 继承能力

- C13-M1 已发布顶层 `topoNamingState`，schema 为 `cad-core.topo-state.v1`。
- `document.topoNamingState` 可被下一次请求消费，`c4m6/topo-state-body-tip-stable-recovery` 继续作为 Body/Tip stable reference recovery 基线。
- CLI / C API / worker / wasm channel 已共享正式 topo state response。
- 当前 publisher 仍从 `NamedShape.elementMap`、`child_element_maps`、`mapper_history` 投影 state；`mappedName.raw/canonical` 仍是 cad-core stable token，不是 FreeCAD raw mapped name。

## focused baseline

| fixture | current cad-core topoNamingState | expected evidence | S0 gap 分类 |
| --- | --- | --- | --- |
| `p2/rect-pad-pocket` | `Body/Pad/Pocket/Sketch*` 等 7 个对象，111 个 stable-token entries，evidence 为 `mapperHistoryIndexes+source` | expected 只有 `Body`，50 个 FreeCAD raw entries，evidence 为 `childElementMapKey+mapperHistoryIds+source` | `freecad_mapped_name_encoding_gap`、`child_element_map_key_gap`、`mapper_history_id_gap` |
| `c4m6/topo-state-body-tip-stable-recovery` | `Body/Pad/Sketch/Sketch.InternalShape`，57 个 stable-token entries | expected 只有 `Body`，26 个 FreeCAD raw entries | `freecad_mapped_name_encoding_gap`、`child_element_map_key_gap`、`mapper_history_id_gap`，并保持 round-trip recovery 不回退 |
| `p5/sketch-internal-face` | `Sketch` 与 `Sketch.InternalShape`，13 个 stable-token entries | expected 只有 `Sketch` indexed-only，0 entries | `indexed_only_boundary`，不得为 InternalShape 伪造 FreeCAD raw name |
| `p6/up-to-face-stable-body-history` | `Body/Pad/Pocket/ProbePad/Sketch*` 等 10 个对象，133 个 stable-token entries | expected 只有 `ProbePad`，26 个 FreeCAD raw entries | `freecad_mapped_name_encoding_gap`、`child_element_map_key_gap`、`mapper_history_id_gap` |
| `p8/app-link-box-face` | `Box` 与 `BoxLink`，`BoxLink` 有 `Box.Box.Face1` / `Box.Face1` stable-token entries | expected 只有 `BoxLink` indexed-only，0 entries | `child_path_identity_boundary`，Link display path 不等于 durable identity |

## 继承非目标

- 不改 frontend / my-chili3d consumer。
- 不做全量 expected fixture parity。
- 不从 `fixtures/<phase>/expected/*.freecad.json` 复制 raw mappedName 字符串进 runtime。
- 不修改 `subname/fullSubname/stableSubname` 既有 response 语义。
- 不引入 backend session topology cache；CAD Core 继续保持无状态，topo state 只随 request/response 传递。

## S0 验证记录

- `C13M2-SCOPE-001`、`C13M2-BLOCKER-101`、`C13M2-IMPL-001` 已更新为 closed。
- `c13m2_mapped_name_validation_matrix.tsv` 已记录 C13-M1 queue、C13-M2 queue、TSV field count、`git diff --check` 为 `passed_s0`。
- focused fixture 行只标记为 `baseline_frozen_s0`，未写成 supported，也未关闭 S1-S6。
