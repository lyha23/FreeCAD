# P6 复杂引用恢复工作步骤总入口【已实现】

## 目标

执行 `P6MH-SCOPE-005` 的 oracle-first 收口：先确认 ShapeFix / DressUp / taper 造成 split / deleted 后的旧稳定引用 FreeCAD 行为，再决定是否进入 C++。

## 步骤索引

| 步骤 | 文件 | 目标 |
| --- | --- | --- |
| S0 | `6-19-11-15-【已实现】P6CR-S0-声明口径与live基线复核.md` | 已完成：冻结 live 基线，确认不直接写 C++ |
| S1 | `6-19-11-16-【已实现】P6CR-S1-FreeCAD源码与oracle候选矩阵.md` | 已完成：选出 ShapeFix / DressUp / taper 三类 oracle 候选 |
| S2 | `6-19-11-17-【已实现】P6CR-S2-Oracle场景设计与采集闸门.md` | 已完成：采集 DressUp/Refine 与 two-sided taper expected，ShapeFix 关闭为 collectorGap |
| S3 | `6-19-11-18-【已实现】P6CR-S3-cad-core-fixture与mismatch分类.md` | 已完成：DressUp/Refine 与 two-sided taper 为 supported，ShapeFix 保持 notCollected |
| S4 | `6-19-11-19-【已实现】P6CR-S4-实现与发布闸门.md` | 已完成：无 backendGap C++ 行；同步旧 P6 文档和能力发布并关闭队列 |

## 完成结论

- `P6CR-CAND-004` DressUp / Refine 与 `P6CR-CAND-006` two-sided taper 由 checked-in FreeCAD expected 加 focused cad-core tests 发布为 `supported`。
- `P6CR-CAND-003` ShapeFix 保持 `notCollected` / `collectorGap`，后续需要 ShapeFix/removeSmallEdges collector 或 FreeCADCmd probe 后才能做 mismatch 分类。
- S4 没有产品 C++、adapter capability 或 expected 输出端改动；旧 P6 `P6MH-SCOPE-005` 同步为“部分 oracle 已关闭，ShapeFix collectorGap 仍保留，当前无 backendGap”。

## 执行纪律

- `notCollected` 不能直接变成 C++。
- backendGap 必须同时有 FreeCAD source、checked-in expected 和 cad-core mismatch。
- 一次最小批次必须覆盖 ShapeFix、DressUp / Refine、taper 三类 producer 的 oracle 决策；无法采集的类保留 notCollected 并说明原因。
- 不按几何猜测或输出修正恢复旧引用。

## 轻量验收

```bash
git diff --check -- docs/FreeCAD几何生态迁移工程-细分/P6-ComplexReferenceRecovery-ShapeFix-DressUp-Taper收口主线
awk -F '\t' 'FNR==1{n=NF; next} NF!=n{print FILENAME ":" FNR ": expected " n " fields, got " NF; bad=1} END{exit bad}' docs/FreeCAD几何生态迁移工程-细分/P6-ComplexReferenceRecovery-ShapeFix-DressUp-Taper收口主线/矩阵/*.tsv
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/FreeCAD几何生态迁移工程-细分/P6-ComplexReferenceRecovery-ShapeFix-DressUp-Taper收口主线/工作步骤细分 --format markdown
```

## 产物索引

| 类型 | 路径 |
| --- | --- |
| source / oracle 候选矩阵 | `../矩阵/p6_complex_reference_recovery_source_candidates.tsv` |
| oracle 采集裁决矩阵 | `../矩阵/p6_complex_reference_recovery_oracle_collection.tsv` |
| backend / mismatch 分类矩阵 | `../矩阵/p6_complex_reference_recovery_backend_mismatch_classification.tsv` |
