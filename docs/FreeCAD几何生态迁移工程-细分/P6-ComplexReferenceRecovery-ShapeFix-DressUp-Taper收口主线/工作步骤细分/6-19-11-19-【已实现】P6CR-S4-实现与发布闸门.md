# P6CR-S4 实现与发布闸门【已实现】

## 目标

发布 S3 结论并回写旧 `P6MH-SCOPE-005`：本轮没有 backendGap C++ 行，只同步 P6CR / old-P6 文档和矩阵。

## 发布结论

- `P6CR-CAND-004` DressUp / Refine 与 `P6CR-CAND-006` two-sided taper：checked-in FreeCAD expected 存在，cad-core expected / topology / capability focused tests 通过，发布为 `supported`。
- `P6CR-CAND-003` ShapeFix：没有 checked-in expected，保持 `notCollected` / `collectorGap`，后续先补 ShapeFix/removeSmallEdges collector 或 FreeCADCmd probe。
- S4 没有产品 C++、adapter capability、fixture expected 或输出端修正；旧 P6 同步为部分 oracle closure，当前无 backendGap。

## 实施边界

1. 只有 checked-in expected mismatch 才能进入 producer / topo / runtime C++；本轮 backend mismatch matrix 为 0 个 backendGap。
2. capability 只通过既有 adapter focused test 校验，不改 C ABI capability 口径。
3. old P6 只回写 `P6MH-SCOPE-005` / `P6MH-BLOCK-005` / `P6MH-BG-005` 的部分 oracle closure 口径。

## 禁止路径

- 不按面积、长度、bbox、fixture 名、输出顺序或 geometry type 猜恢复。
- 不把 ambiguous split 改成 resolved。
- 不把完整 BREP 或 NamedShape / ElementMap 跨请求持久化。
- 不扩大到 GUI 重选生命周期。

## 验收

```bash
cd cad-core
python3 -m unittest tests.test_expected_fixtures.CadCoreExpectedFixtureTest
python3 -m unittest tests.test_p6_topology.CadCoreP6TopologyTest
python3 -m unittest tests.test_adapters.CadCoreAdapterTest -k capabilities
cd ..
git diff --check -- docs/FreeCAD几何生态迁移工程-细分/P6-ComplexReferenceRecovery-ShapeFix-DressUp-Taper收口主线 docs/FreeCAD几何生态迁移工程-细分/P6-MakerHistory-ShapeFix-DressUp-Taper收敛主线 cad-core/fixtures/p6 cad-core/tests/test_p6_topology.py
awk -F '\t' 'FNR==1{n=NF; next} NF!=n{print FILENAME ":" FNR ": expected " n " fields, got " NF; bad=1} END{exit bad}' docs/FreeCAD几何生态迁移工程-细分/P6-ComplexReferenceRecovery-ShapeFix-DressUp-Taper收口主线/矩阵/*.tsv docs/FreeCAD几何生态迁移工程-细分/P6-MakerHistory-ShapeFix-DressUp-Taper收敛主线/矩阵/*.tsv
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/FreeCAD几何生态迁移工程-细分/P6-ComplexReferenceRecovery-ShapeFix-DressUp-Taper收口主线/工作步骤细分 --format markdown
```
