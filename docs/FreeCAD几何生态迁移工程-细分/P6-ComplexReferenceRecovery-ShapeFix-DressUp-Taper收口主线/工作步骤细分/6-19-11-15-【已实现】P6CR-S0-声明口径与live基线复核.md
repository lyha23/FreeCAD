# P6CR-S0 声明口径与 live 基线复核【已实现】

## 目标

确认本包只消费 `P6MH-SCOPE-005`，不重开已 supported 的 ShapeFix / DressUp / taper producer baseline，不直接写 C++。

## 必读

- `docs/FreeCAD几何生态迁移工程-细分/P6-MakerHistory-ShapeFix-DressUp-Taper收敛主线/6-17-17-51-【已实现】P6-MakerHistory-ShapeFix-DressUp-Taper收敛主线总入口.md`
- `docs/FreeCAD几何生态迁移工程-细分/P6-MakerHistory-ShapeFix-DressUp-Taper收敛主线/矩阵/p6_maker_history_scope_review_matrix.tsv`
- `docs/FreeCAD几何生态迁移工程-细分/P6-MakerHistory-ShapeFix-DressUp-Taper收敛主线/矩阵/p6_maker_history_blocker_queue.tsv`
- `docs/FreeCAD几何生态迁移工程-细分/P6-ComplexReferenceRecovery-ShapeFix-DressUp-Taper收口主线/矩阵/*.tsv`

## 输出

- 记录 HEAD、dirty 边界和当前 `P6MH-SCOPE-005` 状态。
- 回写本包矩阵中 S0 相关行，但不新增 C++。
- 若发现旧 P6 矩阵已有 active backendGap，先停止并改写本包范围。

## 完成结论

- 2026-06-19 S0 live 基线已冻结：`pwd=/Users/li/Chili3DProject/FreeCAD`，HEAD 为 `6b1962d5a0`，最新提交为 `assembly: 完成P8扩展DistanceType S6发布闸门`。
- 启动时 dirty 边界为既有 `AGENTS.md` 修改、P8 DistanceTypeExtendedGeometry 包文档 / 矩阵修改，以及本 P6CR 包未跟踪文件；S0 只更新本 P6CR 包内文档和矩阵。
- 旧 P6 主线支持行保持只读：`P6MH-SCOPE-002` ShapeFix、`P6MH-SCOPE-003` DressUp / Refine / transformed、`P6MH-SCOPE-004` taper、`P6MH-SCOPE-006` publication 均为 `supported`。
- 唯一被本包承接的是 `P6MH-SCOPE-005`，状态仍为 `notCollected`；缺口是缺少 checked-in FreeCAD oracle 场景，不是当前 C++ backendGap。
- 当前 C++ capability / test 证据没有 active backendGap：`shape_fix.remaining=[]`、`dressup.remaining=[]`、`transformed.remaining=[]`、`taper_history.remaining_gaps=[]`，且 `known_gaps=[]`。
- `P6CR-BLOCK-001` 和 `P6CR-BLOCK-002` 已按 S0 证据关闭；S1-S3 仍必须先采 ShapeFix / DressUp / taper 三类 oracle，不能直接写 C++。

## 验收

```bash
git diff --check -- docs/FreeCAD几何生态迁移工程-细分/P6-ComplexReferenceRecovery-ShapeFix-DressUp-Taper收口主线
awk -F '\t' 'FNR==1{n=NF; next} NF!=n{print FILENAME ":" FNR ": expected " n " fields, got " NF; bad=1} END{exit bad}' docs/FreeCAD几何生态迁移工程-细分/P6-ComplexReferenceRecovery-ShapeFix-DressUp-Taper收口主线/矩阵/*.tsv
```
