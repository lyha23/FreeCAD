# P5/P6 ExternalGeometry / TopoNaming 工作步骤总入口

## 目标

本目录用于把 P5 Sketcher 外部几何 / 内部元素与 P6 TopoNaming / 引用恢复合并成一条可执行主线。它不是直接开始写 C++，而是先按 L2 的方式建立“声明口径 -> 源码候选 -> 范围矩阵 -> 专项复审 -> oracle / 实现 -> 发布闸门”的准入链条。

主线入口是 `docs/CADCore方案/细化方案/13-ExternalGeometry-TopoNaming下一阶段主线.md`。本目录只负责把该方案拆成可执行步骤和矩阵，不替代 P5/P6 原方案文档。

当前收口状态：P5P6-S0 已实现，只完成声明口径与 live 基线复核；P5P6-S1 到 P5P6-S6 仍为待执行。已建立初始矩阵骨架，但尚未完成 oracle、实现或发布闸门；不得把整个 P5/P6 主线写成“已实现”。

## 步骤索引

| 步骤 | 文件 | 当前状态 | 解决的问题 |
| --- | --- | --- | --- |
| P5P6-S0 | [声明口径与 live 基线复核](6-17-11-57-P5P6-S0-声明口径与live基线复核.md) | 已实现 | 已冻结本主线到底纳入什么、排除什么、哪些能力只能进入候选或专项复审 |
| P5P6-S1 | [FreeCAD 源码候选矩阵](6-17-11-58-P5P6-S1-FreeCAD源码候选矩阵.md) | 待执行 | 从本地 FreeCAD 源码生成候选清单，候选不等于 supported 或 blocker |
| P5P6-S2 | [范围准入与 blocker 矩阵](6-17-11-59-P5P6-S2-范围准入与blocker矩阵.md) | 待执行 | 把候选压成 scope review、nonGoal registry、blocker queue 和 backendGap 分类 |
| P5P6-S3 | [ExternalGeometry 状态机专项复审](6-17-12-00-P5P6-S3-ExternalGeometry状态机专项复审.md) | 待执行 | 单独裁决 Defining / Frozen / Detached / Missing / Sync 与完整 solver / live editing 边界 |
| P5P6-S4 | [MapperHistory 与 ElementMap 生命周期专项复审](6-17-12-01-P5P6-S4-MapperHistory与ElementMap生命周期专项复审.md) | 待执行 | 单独裁决 request-local MapperHistory / ElementMap lifecycle 与跨请求持久状态边界 |
| P5P6-S5 | [FaceMaker / WireJoiner history producer 专项复审](6-17-12-02-P5P6-S5-FaceMaker-WireJoiner-history-producer专项复审.md) | 待执行 | 单独裁决 InternalShape、open wire、split / deleted history 的 producer 主路径和 fallback 删除边界 |
| P5P6-S6 | [Oracle、实现与发布闸门](6-17-12-03-P5P6-S6-Oracle实现与发布闸门.md) | 待执行 | 消费 S2-S5 产生的 `notCollected` / `backendGap` 队列，完成实现、验证和台账回写 |

## 执行顺序

1. P5P6-S0 已完成，只冻结声明口径和红线，不进入实现。
2. P5P6-S1 只做 FreeCAD 源码候选扫描，产出候选矩阵。
3. P5P6-S2 对候选项做状态分类；只有 `notCollected`、`backendGap`、`unsupported` 才进入 blocker 队列。
4. P5P6-S3、S4、S5 分别关闭三个高风险面：ExternalGeometry 状态机、MapperHistory / ElementMap 生命周期、FaceMaker / WireJoiner history producer。
5. P5P6-S6 只消费 S2-S5 留下的队列，按 FreeCAD 调用链补 oracle、实现和验证。
6. 发布前必须清空 blocker queue，回写 P5 / P6 / 13 方案，不复用早期 P5/P6 fixture 通过作为本主线通过证明。

## 当前矩阵闸门

当前矩阵为初始骨架，不是发布状态；S0 只冻结状态词典和范围红线，不把 seed row 直接升级为 supported：

| 矩阵 | 当前用途 | 当前结论 |
| --- | --- | --- |
| `p5p6_source_candidates.tsv` | FreeCAD 源码候选 | 已有 seed rows，后续 S1 可扩充 |
| `p5p6_scope_review_matrix.tsv` | 范围准入主矩阵 | 已有 seed rows，后续 S2/S3/S4/S5 必须回写状态 |
| `p5p6_non_goal_registry.tsv` | 公开排除项 | 已有 solver、GUI、持久 cache 等初始排除项 |
| `p5p6_blocker_queue.tsv` | 可执行队列 | 已有 seed blockers，但尚未采 oracle 或实现 |
| `p5p6_backend_gap_classification.tsv` | backendGap 聚合 | 已按 topo / part / runtime / sketcher 初步分类 |

发布时必须重新统计 live TSV，不得沿用本文的初始描述。

## P5P6 状态纪律

- 除 P5P6-S0 外，新步骤完成前不加 `【已实现】`。
- `notCollected` 只表示缺 FreeCAD expected 或 focused evidence，不能直接写 C++。
- `backendGap` 必须已有 FreeCAD 依据和 cad-core 不匹配证据，不能凭感觉生成。
- `nonGoal` 必须写入 registry，并写清用户/协议表现和重新打开条件。
- 一对多 split、deleted target、Missing object、ambiguous source 都必须输出稳定 diagnostic，不按面积、长度、索引或 geometry type 猜唯一目标。
- 代码实现必须先补 topo / part / runtime 主路径，再切 sketcher consumer；不得在 `sketch_object.cpp` 或 adapter 中继续堆 fixture 特判。

## 通用验收

文档与矩阵阶段：

```bash
git diff --check
python3 - <<'PY'
import csv
from pathlib import Path
root = Path('docs/FreeCAD几何生态迁移工程-细分/P5P6-ExternalGeometry-TopoNaming联合主线/矩阵')
for p in sorted(root.glob('*.tsv')):
    with p.open(newline='') as f:
        rows = list(csv.DictReader(f, delimiter='\t'))
    print(p.name, len(rows))
PY
```

实现阶段按 touched 范围选择：

```bash
cd cad-core
cmake --build build
python3 -m unittest tests/test_mvp.py
```

涉及 FreeCAD oracle、ExternalGeometry 状态机或 WireJoiner probe 时，优先在本机非 sandbox 环境运行 `FreeCADCmd`，再把输出作为 expected。

## 非目标

- 不在 S0 未冻结前新增 C++ 实现。
- 不把完整 Sketcher solver、GUI、Workbench、ViewProvider 或 Python session API 写入 supported。
- 不把 BREP、NamedShape、ElementMap、topomap 或 mesh 作为跨请求后端状态保存。
- 不用 cad-core 当前输出倒推 FreeCAD oracle。
- 不把命名顺序差异直接当成硬失败；几何内容、数量、稳定引用和诊断语义才是硬边界。
