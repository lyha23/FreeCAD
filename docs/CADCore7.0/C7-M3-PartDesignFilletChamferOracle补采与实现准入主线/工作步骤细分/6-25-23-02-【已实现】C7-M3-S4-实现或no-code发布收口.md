# 【已实现】C7-M3 S4 实现或 no-code 发布收口

## 目标

按 S3 gate 执行实现或 no-code publication closure。若 S3 没有 `backend_gap_requires_implementation`，S4 不改 C++，只同步 docs/matrix/capability 发布口径。

## 必读

- S3 完成后的本包 README、总入口、方案和 `矩阵/*.tsv`
- S3 明确列出的 FreeCAD 源码和 cad-core 修改目标
- `AGENTS.md` 中 FreeCAD 迁移实现纪律、OpenCascade 使用规则和测试指南

## 已完成动作

1. 已记录 live baseline：`pwd=/Users/li/Chili3DProject/FreeCAD`，`HEAD=364ae7a093`（`364ae7a093 测试：完成 C7-M3 S3 parity gate`），开始时 `git -c core.quotepath=false status --short -uall` 无输出。
2. 已刷新队列：S4 为当前 pending，S5 在后。
3. 已读取 S3 gate：没有 `backend_gap_requires_implementation`，因此走 no-code closure，不改 C++。
4. 已把 5 个 Fillet / Chamfer oracle rows 发布为 expected-backed：`p7/fillet-pad-multi-edge`、`p7/fillet-pad-use-all-edges`、`p7/chamfer-pad-edge-flip-true`、`c3m5/chamfer-two-distances-edge-flip-true`、`c3m5/chamfer-distance-angle-edge-flip-true`。
5. 已保留 `c3m5/dressup-reference-shadow-base-recovery` 为 `oracle_blocked` / not supported；没有把 geometry-only StableSubList 成功发布为 `ShadowSub` / `ReferenceShadow` 原生恢复证据。
6. 已同步 root README、本包 README/总入口/方案、P7 细化文档和 C7-M3 矩阵；没有修改 C++ executor/runtime/topo/adapter/capability_contract、fixtures/expected/tests。
7. 已把本文件文件名和一级标题标记为 `【已实现】`，队列推进到 S5。

## 非目标

- 不扩大到 S3 未授权 scope。
- 不靠 fixture 名、边顺序、source edge 形态猜测实现。
- 不运行全量 FreeCAD build。

## 验收

```bash
cd /Users/li/Chili3DProject/FreeCAD/cad-core
python3 -m unittest tests.test_p7_features.CadCoreP7FeatureTest.test_c7m3_fillet_oracle_rows_match_expected tests.test_p7_features.CadCoreP7FeatureTest.test_c7m3_chamfer_flip_direction_oracle_rows_match_expected tests.test_p7_features.CadCoreP7FeatureTest.test_c7m3_reference_shadow_recovery_oracle_remains_blocked

cd /Users/li/Chili3DProject/FreeCAD
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore7.0/C7-M3-PartDesignFilletChamferOracle补采与实现准入主线/工作步骤细分 --format markdown
awk -F '\t' 'FNR==1{n=NF; next} NF!=n{print FILENAME ":" FNR ": expected " n " fields, got " NF; bad=1} END{exit bad}' docs/CADCore7.0/C7-M3-PartDesignFilletChamferOracle补采与实现准入主线/矩阵/*.tsv
rg -n '[ \t]$' docs/CADCore7.0/C7-M3-PartDesignFilletChamferOracle补采与实现准入主线 docs/CADCore7.0/README.md docs/CADCore方案/细化方案/10-P7-PartDesign常用生态.md
git diff --check
```

本轮是 no-code closure；focused unittest 只复跑 S3 已新增的 C7-M3 gate tests，未新增或修改 tests。

## 通过条件

- S3 gate 被落实，没有扩大 scope。
- 如有代码改动，FreeCAD 依据注释和 focused tests 同步完成。
- 队列推进到 S5。
