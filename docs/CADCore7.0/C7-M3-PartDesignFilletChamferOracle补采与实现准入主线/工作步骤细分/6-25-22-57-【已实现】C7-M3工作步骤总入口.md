# 【已实现】C7-M3 工作步骤总入口

## 目标

建立 C7-M3 的有序执行队列。后续步骤围绕 C7-M2 留下的 3 个 `oracle_pending_collect` rows：Fillet multi-edge / `UseAllEdges`、Chamfer `FlipDirection=true`、DressUp chain stale `ReferenceShadow` / Base recovery。

## 当前顺序

1. `6-25-22-58-【已实现】C7-M3-S0-live基线与C7-M2-pending-row冻结.md`
2. `6-25-22-59-【已实现】C7-M3-S1-oracle-fixture设计与collector路径.md`
3. `6-25-23-00-【已实现】C7-M3-S2-FreeCAD-oracle采集与expected固化.md`
4. `6-25-23-01-【已实现】C7-M3-S3-cad-core-parity与implementation-gate.md`
5. `6-25-23-02-C7-M3-S4-实现或no-code发布收口.md`
6. `6-25-23-03-C7-M3-S5-release-gate.md`

## 总规则

- 每一步开始前先执行 live baseline 命令：`pwd`、`git rev-parse --short HEAD`、`git log -1 --oneline`、`git -c core.quotepath=false status --short -uall`。
- 每一步都先运行本目录的 `step_goal_queue.py`，跳过已经标记 `【已实现】` 的文件。
- S0-S1 只允许文档和矩阵，不新增 fixtures/expected/tests，不改 C++。
- S2 可以按 S1 设计新增 fixtures 并采 FreeCAD expected；expected 必须来自 FreeCAD oracle 或明确 diagnostic blocker。
- S3 只做 parity 和准入裁决；S3 已确认没有 `backend_gap_requires_implementation`，S4 不改 C++。
- 不允许 adapter/output 层引用恢复猜测、fixture 名称分支、边编号猜测或从 `cad-core` 输出倒推 expected。

## 队列检查

```bash
cd /Users/li/Chili3DProject/FreeCAD
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore7.0/C7-M3-PartDesignFilletChamferOracle补采与实现准入主线/工作步骤细分 --format markdown
```
