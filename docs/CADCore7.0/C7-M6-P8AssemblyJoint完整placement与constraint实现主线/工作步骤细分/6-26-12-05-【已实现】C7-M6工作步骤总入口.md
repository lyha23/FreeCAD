# 【已实现】C7-M6 工作步骤总入口

本文是 C7-M6 的步骤索引，创建时标记为 `【已实现】`，避免 `goal-step-runner` 把索引当作独立实现步骤。本次只建立队列，不执行 S0-S6。

## 目标

建立 C7-M6 的有序执行队列。后续步骤围绕 P8 Assembly Joint 完整 placement / constraint、复杂 placement chain、remaining JointType 和 request-local writeback，先证明 FreeCAD native 行为和 current `cad-core` mismatch，再决定是否实现。

## 当前顺序

| step | file | status |
| --- | --- | --- |
| S0 | `6-26-12-06-【已实现】C7-M6-S0-live基线与P8已覆盖边界冻结.md` | done |
| S1 | `6-26-12-07-【已实现】C7-M6-S1-FreeCAD源码与current-cad-core-solver能力复核.md` | done |
| S2 | `6-26-12-08-【已实现】C7-M6-S2-Joint完整placement与constraint-oracle候选矩阵.md` | done |
| S3 | `6-26-12-09-【已实现】C7-M6-S3-native-oracle采集与expected固化.md` | done |
| S4 | `6-26-12-10-【已实现】C7-M6-S4-cad-core-parity与implementation-gate.md` | done |
| S5 | `6-26-12-11-【已实现】C7-M6-S5-实现或no-code发布收口.md` | done: no-code publication closure |
| S6 | `6-26-12-12-【已实现】C7-M6-S6-release-gate.md` | done: release gate |

## 总规则

- 每一步开始前先执行 live baseline 命令：`pwd`、`git rev-parse --short HEAD`、`git log -1 --oneline`、`git -c core.quotepath=false status --short -uall`。
- 每一步都先运行本目录的 `step_goal_queue.py`，跳过已经标记 `【已实现】` 的文件。
- S0/S1 只允许文档和矩阵，不新增 fixtures/expected/tests，不改 C++。
- S2 只形成 oracle 候选与最小完整语义批次，不采 oracle，不改 C++。
- S3 可以新增 oracle fixture / expected / known_gap，但不能改 runtime C++ 主路径。
- S4 只做 parity 和准入裁决；只有 S4 写出 `backend_gap_requires_implementation` 才允许 S5 改 C++。
- S4 已裁决 `C7M6-ORACLE-202=already_closed_expected_backed`，`C7M6-ORACLE-302/203=oracle_blocked`，S5 implementation gate closed，只允许 no-code publication closure。
- S5 已完成 no-code publication closure：未改 C++、adapter、tests、fixtures、expected、collector 或生成输出；`C7M6-BLOCKER-501` 已关闭，队列推进到 S6 release gate。
- S6 已完成 release gate：`C7M6-BLOCKER-601` / `C7M6-GATE-701` 已关闭，C7-M6 队列为空；没有新增 C++、adapter、tests、fixtures、expected、collector、capability 或生成输出改动。
- 不允许 adapter/output 层 placement 猜测、fixture 名称分支、solver result 后处理特判或从 `cad-core` 输出倒推 expected。

## 队列检查

```bash
cd /Users/li/Chili3DProject/FreeCAD
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore7.0/C7-M6-P8AssemblyJoint完整placement与constraint实现主线/工作步骤细分 --format markdown
```
