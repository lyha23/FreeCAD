# 【已实现】C7-M6 S0 live 基线与 P8 已覆盖边界冻结

## 目标

冻结 C7-M6 live 起点，确认 C7-M1/C7-M2/C7-M3/C7-M4/C7-M5 队列为空，并把 P8 Assembly / Joint 已覆盖的 request-local solver、DistanceType、marker placement、native expected 和 diagnostic boundary 复制到 C7-M6 矩阵。S0 不采 oracle，不新增 fixture/expected/test，不改 C++。

## 完成结论

- live 起点：`pwd=/Users/li/Chili3DProject/FreeCAD`，`HEAD=60876b2f6c`（`60876b2f6c docs: 完成 C7-M5 S6 release gate`）。
- 开始状态：`git status --short -uall` 只包含 root `docs/CADCore7.0/README.md` modified 和本 C7-M6 文档包 untracked 文件；未发现无关 dirty work。
- C7-M1/C7-M2/C7-M3/C7-M4/C7-M5 队列均为空；C7-M6 队列从 S0 起步，本文件标记后下一 pending 为 S1。
- `cad-core/fixtures/c3m6/expected` 当前有 51 个 JSON，其中 50 个 Assembly expected；45 个 Assembly expected 无 `known_gap` / `backendGap`，5 个 DistanceType default / TODO / PointCurve expected 保持 `DTE-NG-003` diagnostic boundary；34 个 expected 带 `native_marker_oracle`。
- `cad-core/tests/test_p8_features.py` 已覆盖 grounded JointType matrix、DistanceType basic / extended / default diagnostic boundary、marker native oracle expected batch、single / multi-component `assembly_set_placement` writeback、invalid grounded、ungrounded、unsupported diagnostics、RackPinion / Screw sliding precondition 与 marker rewrite。
- `C7M6-BLOCKER-000` / `C7M6-GATE-000` 已关闭；S0 未采 oracle、未新增 fixture/expected/test、未改 C++。

## 必读文件

- `docs/CADCore7.0/README.md`
- `docs/CADCore方案/细化方案/00-CAD-Core完整抽取执行总览.md`
- `docs/CADCore方案/细化方案/11-P8-Part导入导出与Assembly后续.md`
- `cad-core/fixtures/c3m6/expected`
- `cad-core/tests/test_p8_features.py`
- `docs/CADCore7.0/C7-M6-P8AssemblyJoint完整placement与constraint实现主线/矩阵/*.tsv`

## 执行要点

1. 记录 live baseline：`pwd`、`git rev-parse --short HEAD`、`git log -1 --oneline`、`git -c core.quotepath=false status --short -uall`。
2. 跑 C7-M1/C7-M2/C7-M3/C7-M4/C7-M5/C7-M6 队列，确认前置队列为空，C7-M6 从 S0 开始。
3. 复核 P8 live 文档中 Assembly solver、Joint input metadata、c3m6 expected、DistanceType extended geometry 和 marker placement 的发布口径。
4. 复核哪些 rows 是 already covered / expected-backed，哪些仍是 remaining / diagnostic / not collected。
5. 更新 C7-M6 README/总入口/方案/矩阵中的 S0 状态，只记录后续执行依赖的基线结论。
6. 标记本文件标题和文件名为 `【已实现】`，队列推进到 S1。

## 验收

```bash
cd /Users/li/Chili3DProject/FreeCAD
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore7.0/C7-M6-P8AssemblyJoint完整placement与constraint实现主线/工作步骤细分 --format markdown
awk -F '\t' 'FNR==1{n=NF; next} NF!=n{print FILENAME ":" FNR ": expected " n " fields, got " NF; bad=1} END{exit bad}' docs/CADCore7.0/C7-M6-P8AssemblyJoint完整placement与constraint实现主线/矩阵/*.tsv
rg -n '[ \t]$' docs/CADCore7.0/C7-M6-P8AssemblyJoint完整placement与constraint实现主线 docs/CADCore7.0/README.md
git diff --check
```

## 完成标准

- C7-M6 live 起点、前置队列、P8 already-covered / expected-backed / diagnostic boundary 已冻结。
- S0 没有修改 C++、fixtures、expected 或 tests。
- 队列推进到 S1。
