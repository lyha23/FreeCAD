# 【已实现】C7-M6 S5 实现或 no-code 发布收口

## 目标

按 S4 route 执行：如果 S4 打开 `backend_gap_requires_implementation`，实现正式 Assembly Joint placement / constraint gap；否则做 no-code blocked / diagnostic 发布收口。S5 不允许扩大到 full Link lifecycle、Worker / WASM / Web、Part surface full family 或 output-side guessing。

## 完成结论

- live 起点：`pwd=/Users/li/Chili3DProject/FreeCAD`，`HEAD=2d1b4673d7`（`2d1b4673d7 文档：完成 C7-M6 S4 parity 准入裁决`），开始状态干净。
- S4 未打开 `backend_gap_requires_implementation`。S5 未做 C++ implementation，未修改 `cad-core/src`、adapter、tests、fixtures、expected、collector 或生成输出。
- `C7M6-ORACLE-202` 保持 `already_closed_expected_backed`：S4 已证明 current `cad-core` legacy recompute 与 S3 FreeCAD expected 匹配，S3 expected 里的 marker `backendGap` 只保留为历史 collector 元数据。
- `C7M6-ORACLE-302` 保持 `oracle_blocked`：仍待 zero Angle fallback `solver_joint_class` / fallback evidence；不得从 signed current value probe 推导 backend gap。
- `C7M6-ORACLE-203` 保持 `oracle_blocked`：仍待 dedicated native `preDrag()` / bundled fixed lifecycle probe 证明非 identity `offsetPlc` marker / writeback 链。
- `C7M6-BLOCKER-501` 已关闭，`C7M6-GATE-601` 发布为 S5 no-code closure；`C7M6-GATE-701` / `C7M6-BLOCKER-601` 仍等待 S6 release gate。

## 必读文件

- S4 完成后的 C7-M6 README、方案和矩阵。
- S3/S4 fixture / expected / blocker JSON。
- S4 指定的 FreeCAD source authority。
- `cad-core/src/assembly/assembly_object.cpp`
- `cad-core/src/assembly/joint_solver.cpp`
- `cad-core/src/assembly/assembly_utils.cpp`
- `cad-core/include/cad_core/assembly/joint_solver.h`
- `cad-core/tests/test_p8_features.py`
- `cad-core/tests/test_adapters.py`

## 执行要点

1. 记录 live baseline 和 C7-M6 queue。
2. 若 S4 route=`backend_gap_requires_implementation`，只改 S4 指定的 C++ / test / expected 范围。
3. 实现必须落在正式 assembly solver / marker / DTO / writeback 路径，不得在 adapter 或输出 JSON 末端修正。
4. 若 S4 没打开 code gate，只更新 README、方案、矩阵和 P8 发布口径。
5. 标记本文件标题和文件名为 `【已实现】`，队列推进到 S6。

## 验收

```bash
cd /Users/li/Chili3DProject/FreeCAD
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore7.0/C7-M6-P8AssemblyJoint完整placement与constraint实现主线/工作步骤细分 --format markdown
awk -F '\t' 'FNR==1{n=NF; next} NF!=n{print FILENAME ":" FNR ": expected " n " fields, got " NF; bad=1} END{exit bad}' docs/CADCore7.0/C7-M6-P8AssemblyJoint完整placement与constraint实现主线/矩阵/*.tsv
rg -n '[ \t]$' docs/CADCore7.0/C7-M6-P8AssemblyJoint完整placement与constraint实现主线 docs/CADCore7.0/README.md
git diff --check
```

S5 是 no-code publication closure，不运行 build、focused unittest、FreeCADCmd、collector、fixture comparator 或 adapter test；这些留给需要代码 / fixture / expected / capability 改动的步骤。

## 完成标准

- S5 route 已落实为实现收口或 no-code publication closure。
- README / 方案 / 总入口 / 矩阵 / 工作步骤口径一致。
- 队列推进到 S6。
