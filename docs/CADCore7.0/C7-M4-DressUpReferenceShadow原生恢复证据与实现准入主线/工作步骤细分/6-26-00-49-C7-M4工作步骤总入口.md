# 【已实现】C7-M4 工作步骤总入口

## 目标

建立 C7-M4 的有序执行队列。后续步骤只围绕 C7-M3 留下的 `dressup-reference-shadow-base-recovery` oracle blocker，先证明 FreeCAD native restore / update 行为，再裁决 cad-core 是否实现。

## 当前顺序

1. `6-26-00-50-C7-M4-S0-live基线与C7-M3-blocked-row冻结.md`
2. `6-26-00-51-C7-M4-S1-FreeCAD原生ReferenceShadow调用链与probe设计.md`
3. `6-26-00-52-C7-M4-S2-native-oracle-probe与collector补证据.md`
4. `6-26-00-53-C7-M4-S3-cad-core-parity与实现准入裁决.md`
5. `6-26-00-54-C7-M4-S4-ReferenceRecovery实现或blocked发布收口.md`
6. `6-26-00-55-C7-M4-S5-release-gate.md`

## 总规则

- 每一步开始前先执行 live baseline 命令：`pwd`、`git rev-parse --short HEAD`、`git log -1 --oneline`、`git -c core.quotepath=false status --short -uall`。
- 每一步都先运行本目录的 `step_goal_queue.py`，跳过已经标记 `【已实现】` 的文件。
- S0/S1 只允许文档和矩阵，不新增 fixtures/expected/tests，不改 C++。
- S2 可以新增 native oracle probe / collector 证据，但不能改 runtime C++ 主路径。
- S3 只做 parity 和准入裁决；只有 S3 写出 `backend_gap_requires_implementation` 才允许 S4 改 C++。
- 不允许 adapter/output 层引用恢复猜测、fixture 名称分支、边编号猜测或从 `cad-core` 输出倒推 expected。

## 队列检查

```bash
cd /Users/li/Chili3DProject/FreeCAD
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore7.0/C7-M4-DressUpReferenceShadow原生恢复证据与实现准入主线/工作步骤细分 --format markdown
```
