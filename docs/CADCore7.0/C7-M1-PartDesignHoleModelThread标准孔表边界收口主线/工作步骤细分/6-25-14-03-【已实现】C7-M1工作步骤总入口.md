# 【已实现】C7-M1 工作步骤总入口

本文是 C7-M1 的步骤索引，创建时标 `【已实现】`，避免 `goal-step-runner` 把索引当作独立实现步骤。

## 队列

| step | file | status |
| --- | --- | --- |
| S0 | `6-25-14-04-【已实现】C7-M1-S0-live基线与Hole残余gap冻结.md` | done |
| S1 | `6-25-14-05-【已实现】C7-M1-S1-FreeCAD源码与oracle候选矩阵.md` | done |
| S2 | `6-25-14-06-【已实现】C7-M1-S2-ModelThread与标准孔表准入裁决.md` | done |
| S3 | `6-25-14-07-【已实现】C7-M1-S3-ModelThread与标准孔表发布收口.md` | done |
| S4 | `6-25-14-08-【已实现】C7-M1-S4-fixtures-tests-capability发布.md` | done |
| S5 | `6-25-14-09-【已实现】C7-M1-S5-阶段回归与release-gate.md` | done |

## 顺序规则

1. 每次只执行当前 pending step。
2. 执行前刷新 `step_goal_queue.py`，不要一次生成所有 prompt 后盲跑。
3. S2 之前不得改 C++、fixtures、expected 或 tests。
4. S1 必须批量覆盖 supported native oracle、legacy pending standard rows、ModelThread + head cut、point/circle/arc profile source。
5. S2 必须逐 row 裁决 route；只有 `backend_gap_requires_implementation` 才能进入 S3 代码实现。
6. 每步完成后更新矩阵、README / 总入口状态，并把步骤文件重命名为 `【已实现】`。
7. S3 已按 S2 裁决完成 no-code publication closure。
8. S4 已按 no-code 边界完成 fixtures/tests/capability/docs 发布同步。
9. S5 已通过 release gate：`cmake --build build` 通过，focused unittest 5 tests OK；队列应为空。
