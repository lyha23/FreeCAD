# 【已实现】C7-M5 S5 实现或 no-code 发布收口

## 目标

按 S4 route 执行：如果 S4 打开 `backend_gap_requires_implementation`，实现正式 transformed / pattern ownership 或 MapperHistory gap；否则做 no-code blocked / diagnostic 发布收口。S5 不允许扩大到 full MapperHistory、full DressUp universe、P8 Link / Assembly 或 output-side guessing。

## 必读文件

- S4 完成后的 C7-M5 README、方案和矩阵。
- S3/S4 fixture / expected / blocker JSON。
- S4 指定的 FreeCAD source authority。
- `cad-core/src/part_design/feature_transformed.cpp`
- `cad-core/src/part_design/feature_dress_up.cpp`
- `cad-core/src/part/topo_shape.cpp`
- `cad-core/tests/test_p7_features.py`
- `cad-core/tests/test_adapters.py`

## 执行要点

1. 记录 live baseline 和 C7-M5 queue。
2. 若 S4 route=`backend_gap_requires_implementation`，只改 S4 指定的 C++ / test / expected 范围。
3. 实现必须落在正式 transformed / dress_up / topo 账本路径，不得在 adapter 或输出 JSON 末端修正。
4. 若 S4 没打开 code gate，只更新 README、方案、矩阵和 P7 发布口径。
5. 标记本文件标题和文件名为 `【已实现】`，队列推进到 S6。

## 验收

```bash
cd /Users/li/Chili3DProject/FreeCAD/cad-core
cmake --build build
python3 -m unittest tests.test_p7_features.CadCoreP7FeatureTest
python3 -m unittest tests.test_adapters.CadCoreAdapterTest.test_c_api_capabilities_exposes_web_contract_facts
cd /Users/li/Chili3DProject/FreeCAD
awk -F '\t' 'FNR==1{n=NF; next} NF!=n{print FILENAME ":" FNR ": expected " n " fields, got " NF; bad=1} END{exit bad}' docs/CADCore7.0/C7-M5-TransformedPatternMapperHistory复杂ownership实现准入主线/矩阵/*.tsv
git diff --check
```

如果 S5 只做 no-code publication closure，不运行 build，只运行文档短跑和 relevant focused unittest。

## S5 完成结论

- live baseline：`pwd=/Users/li/Chili3DProject/FreeCAD`，`HEAD=77b7903f76`（`77b7903f76 docs: 完成 C7-M5 S4 parity gate 裁决`），开始状态 `git status --short -uall` 无输出；C7-M5 队列首项为 S5。
- S4 route 已执行为 no-code publication closure：current `cad-core` 与 S3 confirmed expected 匹配，`C7M5-ORACLE-201` / `C7M5-ORACLE-301` 为 `already_closed_expected_backed`，`C7M5-ORACLE-401` 为 `diagnostic_non_goal` carry-forward，S5 code gate 不打开。
- S4 parity 结果作为 S5 发布依据引用：focused 5 tests 通过（`Ran 5 tests in 1.601s` / `OK`），完整 `tests.test_p7_features.CadCoreP7FeatureTest` 通过（`Ran 148 tests in 27.855s` / `OK`）。S5 未重跑 build 或 unittest，因为本轮只改文档和矩阵。
- 已同步 C7-M5 README、主线总入口、方案和矩阵，关闭 `C7M5-BLOCKER-501`，发布口径为 expected-backed closed / no backendGap。无需实现、无需改 C++、fixtures、expected、tests、adapter 或 capability。
- P7 live / 总览只记录 C7-M5 expected-backed closed / no backendGap，不扩大 transformed / pattern 支持范围，不重开旧 P7T rows，不把 standalone geometry-equivalent Whole shape 升格为 native golden。
- S5 未修改 `cad-core/src`、fixtures、expected、tests、adapter、collector 或 capability；队列推进到 S6。

## 完成标准

- S5 route 已落实为实现收口或 no-code publication closure。
- docs / tests / expected / capability 口径一致。
- 队列推进到 S6。
