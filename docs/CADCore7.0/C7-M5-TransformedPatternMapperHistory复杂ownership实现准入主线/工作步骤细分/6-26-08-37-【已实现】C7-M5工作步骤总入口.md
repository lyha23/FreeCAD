# 【已实现】C7-M5 工作步骤总入口

## 目标

建立 C7-M5 的有序执行队列。后续步骤围绕 P7 transformed / pattern 完整 MapperHistory 与复杂 ownership，先证明 FreeCAD native ownership / history 行为和 current `cad-core` mismatch，再决定是否实现。

本文是 C7-M5 的步骤索引，已按 C7-M1/C7-M2/C7-M3 约定标记为 `【已实现】`，避免 `goal-step-runner` 把索引当作独立实现步骤。本次只关闭索引，不执行 S0-S6。

## 当前顺序

| step | file | status |
| --- | --- | --- |
| S0 | `6-26-08-38-【已实现】C7-M5-S0-live基线与P7已关闭边界冻结.md` | done |
| S1 | `6-26-08-39-C7-M5-S1-FreeCAD源码与当前cad-core历史能力复核.md` | pending |
| S2 | `6-26-08-40-C7-M5-S2-复杂ownership-oracle候选矩阵与最小批次.md` | pending |
| S3 | `6-26-08-41-C7-M5-S3-native-oracle采集与expected固化.md` | pending |
| S4 | `6-26-08-42-C7-M5-S4-cad-core-parity与implementation-gate.md` | pending |
| S5 | `6-26-08-43-C7-M5-S5-实现或no-code发布收口.md` | pending |
| S6 | `6-26-08-44-C7-M5-S6-release-gate.md` | pending |

## 总规则

- 每一步开始前先执行 live baseline 命令：`pwd`、`git rev-parse --short HEAD`、`git log -1 --oneline`、`git -c core.quotepath=false status --short -uall`。
- 每一步都先运行本目录的 `step_goal_queue.py`，跳过已经标记 `【已实现】` 的文件。
- S0 已冻结 live 基线、C7-M1..M4 空队列、旧 P7T `P7T-SCOPE-001..007` supported、`P7T-BG-*` / `P7T-BLOCK-*` closed 和 standalone Whole shape nonGoal 边界，队列推进到 S1。
- S0/S1 只允许文档和矩阵，不新增 fixtures/expected/tests，不改 C++。
- S2 只形成 oracle 候选与最小完整语义批次，不采 oracle，不改 C++。
- S3 可以新增 oracle fixture / expected / known_gap，但不能改 runtime C++ 主路径。
- S4 只做 parity 和准入裁决；只有 S4 写出 `backend_gap_requires_implementation` 才允许 S5 改 C++。
- 不允许 adapter/output 层 ownership 猜测、fixture 名称分支、source index / transform index 猜测或从 `cad-core` 输出倒推 expected。

## 队列检查

```bash
cd /Users/li/Chili3DProject/FreeCAD
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore7.0/C7-M5-TransformedPatternMapperHistory复杂ownership实现准入主线/工作步骤细分 --format markdown
```
