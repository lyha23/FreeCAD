# 【已实现】C7-M5 S0 live 基线与 P7 已关闭边界冻结

## 目标

冻结 C7-M5 live 起点，确认 C7-M1/C7-M2/C7-M3/C7-M4 队列为空，并把旧 P7 Transformed 主线已关闭的 supported / covered / nonGoal 边界复制到 C7-M5 矩阵。S0 不采 oracle，不新增 fixture/expected/test，不改 C++。

## 必读文件

- `docs/CADCore7.0/README.md`
- `docs/CADCore方案/细化方案/00-CAD-Core完整抽取执行总览.md`
- `docs/CADCore方案/细化方案/10-P7-PartDesign常用生态.md`
- `docs/FreeCAD几何生态迁移工程-细分/P7-Transformed-Topology-MakerHistory收敛主线/6-17-15-42-P7-Transformed-Topology-MakerHistory收敛主线总入口.md`
- `docs/FreeCAD几何生态迁移工程-细分/P7-Transformed-Topology-MakerHistory收敛主线/矩阵/*.tsv`
- `docs/CADCore7.0/C7-M5-TransformedPatternMapperHistory复杂ownership实现准入主线/矩阵/*.tsv`

## 执行要点

1. 记录 live baseline：`pwd`、`git rev-parse --short HEAD`、`git log -1 --oneline`、`git -c core.quotepath=false status --short -uall`。
2. 跑 C7-M1/C7-M2/C7-M3/C7-M4/C7-M5 队列，确认前置队列为空，C7-M5 从 S0 开始。
3. 复核旧 P7T `P7T-SCOPE-001..007`、`P7T-BG-*`、`P7T-BLOCK-*` 的最终 route。
4. 复核 P7 live 文档中 transformed family 的 supported baseline、non-goal 和后续专项口径。
5. 更新 C7-M5 README/总入口/方案/矩阵中的 S0 状态，只记录后续执行依赖的基线结论。
6. 标记本文件标题和文件名为 `【已实现】`，队列推进到 S1。

## S0 完成结论

- live 起点：`pwd=/Users/li/Chili3DProject/FreeCAD`，`HEAD=a2cc93a1ee`（`a2cc93a1ee 文档：收口 C7-M5 工作步骤总入口索引`），开始状态 `git status --short -uall` 无输出。
- 队列状态：C7-M1/C7-M2/C7-M3/C7-M4 `工作步骤细分` 队列均为空；旧 P7 Transformed 主线 `工作步骤细分` 队列为空；C7-M5 从 S0 起步，S0 完成后推进到 S1。
- 旧 P7T closed baseline：`P7T-SCOPE-001..007` 保持 `supported`；`P7T-BG-001` 为 collected oracle supported closed，`P7T-BG-002` 为 ownership / fallback supported covered，`P7T-BG-003` 为 standalone lifecycle boundary；`P7T-BLOCK-001..005` 均已 closed。
- 旧 P7T non-goal baseline：`P7T-NG-005` standalone geometry-equivalent native golden 保持 nonGoal；C7-M5 只能使用 support-backed Body / BaseFeature lifecycle 证据提出 native ownership oracle，不能把 standalone geometry-equivalent 用例升格为 native golden。
- S0 已把上述边界同步到 C7-M5 README、主线总入口、方案和矩阵；未采 oracle、未运行 FreeCADCmd、未新增或修改 fixtures/expected/tests、未改 C++、collector、runtime、topo 或 adapter。

## 验收

```bash
cd /Users/li/Chili3DProject/FreeCAD
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore7.0/C7-M5-TransformedPatternMapperHistory复杂ownership实现准入主线/工作步骤细分 --format markdown
awk -F '\t' 'FNR==1{n=NF; next} NF!=n{print FILENAME ":" FNR ": expected " n " fields, got " NF; bad=1} END{exit bad}' docs/CADCore7.0/C7-M5-TransformedPatternMapperHistory复杂ownership实现准入主线/矩阵/*.tsv
rg -n '[ \t]$' docs/CADCore7.0/C7-M5-TransformedPatternMapperHistory复杂ownership实现准入主线 docs/CADCore7.0/README.md
git diff --check
```

## 完成标准

- C7-M5 live 起点、前置队列、旧 P7 supported / covered / nonGoal 和当前 P7 live 口径已冻结。
- S0 没有修改 C++、fixtures、expected 或 tests。
- 队列推进到 S1。
