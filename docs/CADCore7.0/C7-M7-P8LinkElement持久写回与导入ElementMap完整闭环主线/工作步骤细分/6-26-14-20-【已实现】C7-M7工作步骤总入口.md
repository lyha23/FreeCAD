# 【已实现】C7-M7 工作步骤总入口

本文是 C7-M7 的步骤索引，创建时标记为 `【已实现】`，避免 `goal-step-runner` 把索引当作独立实现步骤。本次只建立队列，不执行 S0-S6。

## 目标

建立 C7-M7 的有序执行队列。后续步骤围绕 P8 LinkElement 持久写回、导入 shape 完整 ElementMap、cross-document hash / postfix 生命周期和复杂 LinkSub 链，先证明 FreeCAD native 行为和 current `cad-core` mismatch，再决定是否实现。

## 当前顺序

| step | file | status |
| --- | --- | --- |
| S0 | `6-26-14-21-【已实现】C7-M7-S0-live基线与P8引用闭环边界冻结.md` | 已实现 |
| S1 | `6-26-14-22-【已实现】C7-M7-S1-FreeCAD源码与current-cad-core-Link覆盖复核.md` | 已实现 |
| S2 | `6-26-14-23-【已实现】C7-M7-S2-LinkElement写回与Import-ElementMap-oracle候选矩阵.md` | 已实现 |
| S3 | `6-26-14-24-【已实现】C7-M7-S3-native-oracle采集与expected固化.md` | 已实现 |
| S4 | `6-26-14-25-【已实现】C7-M7-S4-cad-core-parity与implementation-gate.md` | 已实现 |
| S5 | `6-26-14-26-【已实现】C7-M7-S5-实现或no-code发布收口.md` | 已实现 |
| S6 | `6-26-14-27-C7-M7-S6-release-gate.md` | pending |

## 总规则

- 每一步开始前先执行 live baseline 命令：`pwd`、`git rev-parse --short HEAD`、`git log -1 --oneline`、`git -c core.quotepath=false status --short -uall`。
- 每一步都先运行本目录的 `step_goal_queue.py`，跳过已经标记 `【已实现】` 的文件。
- S0/S1 只允许文档和矩阵，不新增 fixtures/expected/tests，不改 C++。
- S2 只形成 oracle 候选与最小完整语义批次，不采 oracle，不改 C++。
- S3 可以新增 oracle fixture / expected / known_gap，但不能改 runtime C++ 主路径。
- S4 只做 parity 和准入裁决；只有 S4 写出 `backend_gap_requires_implementation` 才允许 S5 改 C++。
- S4 已裁决 ORACLE-202 / 302 / 402 均因缺 source-backed native expected 保持 `oracle_blocked`，ORACLE-203 保持 STL `oracle_blocker`；S5 implementation gate closed，只允许 no-code publication closure。
- S5 已按 no-code publication closure 完成；already-covered rows 继续关闭，ORACLE-202 / 302 / 402 发布为 `oracle_blocked`，ORACLE-203 发布为 STL `oracle_blocker`，GUI / frontend / cache / Worker 保持 `diagnostic_non_goal`。S5 未改 C++、fixtures、expected、tests、collector、capability 或生成输出；下一项只剩 S6 release gate。
- 不允许 adapter/output 层 LinkSub 猜测、fixture 名称分支、后处理特判或从 `cad-core` 输出倒推 expected。

## 队列检查

```bash
cd /Users/li/Chili3DProject/FreeCAD
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore7.0/C7-M7-P8LinkElement持久写回与导入ElementMap完整闭环主线/工作步骤细分 --format markdown
```
