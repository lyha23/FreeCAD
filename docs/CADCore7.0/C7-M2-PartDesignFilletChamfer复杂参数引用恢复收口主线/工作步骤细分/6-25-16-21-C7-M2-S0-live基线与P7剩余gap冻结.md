# C7-M2 S0 live 基线与 P7 剩余 gap 冻结

## 目标

冻结 C7-M2 live 起点，确认 C7-M1 队列已关闭，记录 P7 Fillet / Chamfer 当前 docs、fixtures、tests、capability 和 known gap。S0 是文档/矩阵步骤，不改 C++、fixtures、expected 或 tests。

## 必读

- `docs/CADCore7.0/README.md`
- `docs/CADCore7.0/C7-M2-PartDesignFilletChamfer复杂参数引用恢复收口主线/README.md`
- `docs/CADCore7.0/C7-M2-PartDesignFilletChamfer复杂参数引用恢复收口主线/6-25-16-20-C7-M2-PartDesignFilletChamfer复杂参数引用恢复收口方案.md`
- `docs/CADCore方案/细化方案/10-P7-PartDesign常用生态.md`
- `cad-core/src/part_design/feature_dress_up.cpp`
- `cad-core/tests/test_p7_features.py`
- `cad-core/fixtures/p7/`

## 动作

1. 记录 `pwd`、`git rev-parse --short HEAD`、`git log -1 --oneline`、`git -c core.quotepath=false status --short -uall`。
2. 运行 C7-M1 和 C7-M2 `工作步骤细分` 的 `step_goal_queue.py`。
3. grep P7 文档中的 Fillet / Chamfer supported baseline 和 known gap。
4. 列出当前 Fillet / Chamfer / DressUp / SupportTransform fixtures、expected 和 focused test names。
5. 更新本包 README、总入口、方案和矩阵中的 S0 baseline 行。
6. 把本文件文件名和一级标题标记为 `【已实现】`，队列推进到 S1。

## 非目标

- 不做 oracle 采集。
- 不改 cad-core C++、fixtures、expected 或 tests。
- 不裁决 complex parameter 是否已支持，S1/S2 再处理。

## 验收

```bash
cd /Users/li/Chili3DProject/FreeCAD
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore7.0/C7-M2-PartDesignFilletChamfer复杂参数引用恢复收口主线/工作步骤细分 --format markdown
rg -n 'Fillet|Chamfer|SupportTransform|复杂参数|稳定恢复|UseAllEdges|ChamferType|FlipDirection' docs/CADCore方案/细化方案/10-P7-PartDesign常用生态.md cad-core/src/part_design/feature_dress_up.cpp cad-core/tests/test_p7_features.py
find cad-core/fixtures/p7 -maxdepth 2 -type f | rg 'fillet|chamfer|dressup|support-transform|support_transform'
awk -F '\t' 'FNR==1{n=NF; next} NF!=n{print FILENAME ":" FNR ": expected " n " fields, got " NF; bad=1} END{exit bad}' docs/CADCore7.0/C7-M2-PartDesignFilletChamfer复杂参数引用恢复收口主线/矩阵/*.tsv
rg -n '[ \t]$' docs/CADCore7.0/C7-M2-PartDesignFilletChamfer复杂参数引用恢复收口主线
git diff --check
```

## 通过条件

- C7-M1 队列为空的事实已记录。
- P7 Fillet / Chamfer supported baseline 与 known gap 已写入本包文档/矩阵。
- 本文件标记后，C7-M2 队列推进到 S1。
