# 【已实现】C7-M2 工作步骤总入口

## 目标

建立 C7-M2 的有序执行队列。后续步骤围绕 `PartDesign::Fillet` / `PartDesign::Chamfer` 复杂参数组合和复杂引用变更后的稳定恢复推进，先裁决、再实现、最后发布。

## 当前顺序

1. `6-25-16-21-【已实现】C7-M2-S0-live基线与P7剩余gap冻结.md`
2. `6-25-16-22-【已实现】C7-M2-S1-FreeCAD源码与oracle候选矩阵.md`
3. `6-25-16-23-【已实现】C7-M2-S2-复杂参数与引用恢复准入裁决.md`
4. `6-25-16-24-【已实现】C7-M2-S3-实现或diagnostic边界收口.md`
5. `6-25-16-25-C7-M2-S4-fixtures-tests-capability发布.md`
6. `6-25-16-26-C7-M2-S5-阶段回归与release-gate.md`

## 当前状态

- S0 已实现：live 起点 `HEAD=b5767e2391`（`b5767e2391 docs: 新增 C7-M2 Fillet Chamfer 收口方案`），开始时 `git status --short -uall` 无输出；C7-M1 队列为空。
- S0 只冻结现有 P7 Fillet / Chamfer supported baseline、known gap、fixtures、expected 和 focused tests；没有改 C++、fixtures、expected 或 tests。
- S1 已实现：读取 FreeCAD/cad-core 源码和 fixtures/tests，补齐 source / oracle / input contract 矩阵；Chamfer Two distances 与 Distance and Angle 归为现有 c3m5 expected-backed。
- S2 已实现：Chamfer Two distances、Distance and Angle、SupportTransform regression 为 `already_closed_expected_backed`；Fillet multi-edge/UseAllEdges、Chamfer FlipDirection=true、DressUp chain stale ReferenceShadow/Base recovery 为 `oracle_pending_collect`；publication drift 为 `publication_closure_only`；GUI/full DressUp/full MapperHistory 为 `diagnostic_non_goal`。
- S3 已实现：S2 没有产生 `backend_gap_requires_implementation`，Code edit gate 关闭；S3 已做 no-code diagnostic/publication boundary，未改 C++、fixtures、expected 或 tests，队列推进到 S4。

## 总规则

- 每一步开始前先执行 live baseline 命令：`pwd`、`git rev-parse --short HEAD`、`git log -1 --oneline`、`git -c core.quotepath=false status --short -uall`。
- 每一步都先运行本目录的 `step_goal_queue.py`，跳过已经标记 `【已实现】` 的文件。
- S0-S2 只允许文档、矩阵和裁决，不改 C++、fixtures、expected 或 tests。
- S3 只有在 S2 明确产生 `backend_gap_requires_implementation` 时才能改代码；当前 S2 未打开代码闸门，S3 已按 no-code 路径关闭。
- 不允许输出端修剪、fixture 名称分支、边编号猜测或 adapter 层引用恢复猜测。
- 每步结束按本步骤验收命令验证，并按仓库规则处理本轮相关变更。

## 队列检查

```bash
cd /Users/li/Chili3DProject/FreeCAD
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore7.0/C7-M2-PartDesignFilletChamfer复杂参数引用恢复收口主线/工作步骤细分 --format markdown
```
