# 【已实现】C4-S0 M0 目标范围审计与矩阵重建

## 目标

把当前 CADCore4.0 矩阵从初稿审计成后续实现的权威入口。该步骤不实现 C++，只修正范围、优先级、source、oracle、non-goal、validation 和 blocker queue。

## 必读文件

- `docs/CADCore4.0/README.md`
- `docs/CADCore4.0/C4-M0-目标范围审计与矩阵重建主线/6-19-23-53-C4-M0目标范围审计与矩阵重建方案.md`
- `docs/CADCore3.0/00-总览.md`
- `docs/CADCore3.0/06-C3-M8后续收口清单.md`
- `docs/CADCore3.0/FreeCAD语义矩阵.md`
- `docs/CADCore3.0/capabilities-gap对照表.md`
- `docs/CADCore3.0/oracle-fixture队列.md`
- `cad-core/src/adapters/c_api/c_api.cpp`

## 产物

- 更新 `docs/CADCore4.0/矩阵/cadcore4_scope_review_matrix.tsv`。
- 更新 `docs/CADCore4.0/矩阵/cadcore4_blocker_queue.tsv`。
- 更新 `docs/CADCore4.0/矩阵/cadcore4_source_candidates.tsv`。
- 更新 `docs/CADCore4.0/矩阵/cadcore4_fixture_oracle_matrix.tsv`。
- 更新 `docs/CADCore4.0/矩阵/cadcore4_non_goal_registry.tsv`。
- 必要时同步各 C4-M* 方案中的范围说明。

## 非目标

- 不改 `cad-core` 源码。
- 不新增 fixture expected。
- 不把完整 FreeCAD 产品功能搬进 4.0。

## 验收

```bash
cd /Users/li/Chili3DProject/FreeCAD
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore4.0/C4-M0-目标范围审计与矩阵重建主线/工作步骤细分 --format markdown
git diff --check -- docs/CADCore4.0
```

## 完成口径

每个 C4 blocker 都有明确 source、landing、oracle/diagnostic、validation 和 next owner；没有 broad `known_gap`、`complete_*` 或无法执行的宽泛项。

## 完成记录

- 根级 `cadcore4_scope_review_matrix.tsv`、`cadcore4_blocker_queue.tsv`、`cadcore4_source_candidates.tsv`、`cadcore4_fixture_oracle_matrix.tsv`、`cadcore4_non_goal_registry.tsv` 和 `cadcore4_validation_matrix.tsv` 已补齐闭环 owner 口径。
- C4-M0..M6 各专题包本地 `scope`、`blocker_queue` 和 package index 矩阵已补齐 source authority、cad-core landing、oracle / diagnostic、validation 和 next owner 字段。
- C4-S0 只关闭 docs/matrix 审计，不声明任何 C++ capability、fixture expected 或 runtime support 已新增。
