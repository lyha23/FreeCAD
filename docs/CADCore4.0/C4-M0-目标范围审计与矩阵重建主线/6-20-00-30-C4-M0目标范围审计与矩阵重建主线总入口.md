# C4-M0 目标范围审计与矩阵重建主线总入口

## 目标

把 CADCore4.0 的总范围、分包边界、oracle / fixture / blocker / non-goal 矩阵整理成可执行基线。C4-S0 已完成后，后续实现包只能从已列明 source、cad-core landing、oracle / diagnostic、validation 和 owner 的 blocker 领取，不得把 draft 状态直接改成 supported。

## 必读文件

- `docs/CADCore4.0/6-19-23-52-CADCore4.0总览方案.md`
- `docs/CADCore4.0/C4-M0-目标范围审计与矩阵重建主线/6-19-23-53-C4-M0目标范围审计与矩阵重建方案.md`
- `docs/CADCore4.0/C4-M0-目标范围审计与矩阵重建主线/矩阵/c4m0_scope.tsv`
- `docs/CADCore4.0/C4-M0-目标范围审计与矩阵重建主线/矩阵/c4m0_blocker_queue.tsv`
- `docs/CADCore4.0/矩阵/cadcore4_scope_review_matrix.tsv`
- `docs/CADCore4.0/矩阵/cadcore4_validation_matrix.tsv`

## 队列

```bash
cd /Users/li/Chili3DProject/FreeCAD
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore4.0/C4-M0-目标范围审计与矩阵重建主线/工作步骤细分 --format markdown
```

## 完成口径

- 根矩阵和 M0 本地矩阵对齐，不再指向已废弃的根 `工作步骤细分/`。
- 每个后续专题包都有独立入口、矩阵和队列路径。
- C4-M0 的步骤文件可被 queue script 独立消费。
- 每个 C4 blocker 都有 source authority、cad-core landing、oracle / diagnostic、validation 和 next owner；不恢复 broad `known_gap`、`complete_*` 或 representative fallback/full support 口径。
