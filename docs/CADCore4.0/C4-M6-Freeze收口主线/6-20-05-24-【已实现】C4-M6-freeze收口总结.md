# C4-M6 freeze 收口总结

## 基线

- 仓库：`/Users/li/Chili3DProject/FreeCAD`
- HEAD：`d5011feb9d docs: 收口 Part Surface Family 冻结验证`
- 任务：关闭 `C4M6-FR-BLK-001` / `C4-BLK-601`，只做 freeze 同步、验证、发布和边界清理。
- 工作区边界：S1-S11 已有大量未提交改动；本步骤不提交、不 reset、不回退他人改动。

## 收口项

- `freeze_scope.tsv`：`C4M6-FR-001..004` 已从 pending 同步为 freeze done，分别覆盖 capability、fixture/expected、diagnostics 和 heavy validation。
- `freeze_blocker_queue.tsv` 与全局 `cadcore4_blocker_queue.tsv`：`C4M6-FR-BLK-001` / `C4-BLK-601` 已关闭。
- `cadcore4_scope_review_matrix.tsv`：`C4-SCOPE-601` 已标记为 `done_freeze_closed`。
- `cad_core_capabilities_json()`：移除 `full_part_surface_family` catchall remaining gap；Part surface 剩余项改为具体 feature-family/helper/contract 边界。
- `cad-core/tests/test_adapters.py`：同步 capability assertions，并新增 freeze invariant，确保 capabilities JSON 不再发布 surface-family catchall gap。
- `sketcher_external_package_index.tsv`：修正 C4-S6 已实现后仍显示 pending 的索引漂移。

## 队列

逐个运行全部 11 个 `docs/CADCore4.0/C4-M*/工作步骤细分` 队列目录后，输出均为空表，仅保留表头。S0-S12 step 文件均已带 `【已实现】`。

## 验证记录

```bash
cd /Users/li/Chili3DProject/FreeCAD/cad-core
cmake --build build
```

结果：通过。

```bash
cd /Users/li/Chili3DProject/FreeCAD/cad-core
python3 -m unittest tests.test_mvp tests.test_diagnostics tests.test_feature_flows tests.test_p5_sketch tests.test_p6_topology tests.test_p7_features tests.test_p8_features tests.test_expected_fixtures tests.test_adapters
```

结果：通过，`Ran 508 tests in 138.624s`，`OK (skipped=14)`。

## Remaining boundary

- Part surface：`ProjectOnSurface` Faces/All、height/offset solid、multi-projection；`Part::Loft` complex profile；`Part::Sweep` AuxiliarySpine / SupportMode / Binormal / LocationMode / Tolerance contract；`Part.makeFilledFace` Surface / Supports / Orders / non-default params / non-boundary constraints / compound boundary；`Part.GeomPlate.BuildPlateSurface` InitialSurface、2D/projected constraints、custom criteria、PlateSurface.Curves wrapper。
- PartDesign：Revolution/Groove TwoAngles/UpTo/Profile subshape/FuseOrder；Boolean LinkStage3-only Type、AllowCompound multi-solid policy、multi-body ownership pressure；Loft/Pipe closed/multisection/multi-wire/sewing history and orientation/scale-law branches；Datum AttachmentSupport/MapMode AttachEngine solver。
- Sketcher：完整 GCS solver、rank/QR analysis、GUI/live edit solver feedback仍不进入 CAD Core；unsupported relation 走稳定 diagnostic。
- Topo/reference：split/deleted/ambiguous terminal history 仍必须保持 needs_reselect 或 diagnostic_only，不用输出顺序、bbox、fixture 名称或几何相似度猜唯一别名。
- Assembly/runtime/adapter：Assembly full Ondsel solver 不是本轮目标；representative solver 仍是 fallback metadata。CLI / C ABI / Worker / WASM 只发布 protocol/schema/resource contract，不承载建模语义。

## 风险

- C4 expected fixture 仍以采集 expected 时的 FreeCAD / LibPack / OCCT 基线为准；非同基线 OCCT 的 BREP topology、bbox 或 exchange 差异只作为兼容性探测，不作为语义修改依据。
- 后续新增能力必须先进入具体 feature-family / lifecycle / producer / adapter contract 行，再采集 oracle 或补稳定 diagnostic，不能恢复 broad gap。
