# C3-M8 后续收口清单

本文件只记录 C3-M8 taper-history cleanup 后的冻结状态和真实剩余项，不记录执行流水账。状态依据以当前 `cad-core/src/adapters/c_api/c_api.cpp::capabilitiesJson()`、相关 fixture expected 和 `docs/CADCore3.0` 矩阵为准。

## C3-M8 后最终状态表

### 已 `covered_full`

| 项 | 当前状态 | 依据 | 边界 |
| --- | --- | --- | --- |
| `object_metadata.local_history.taper_history` | `covered_full` | `part_design.pad`、`part_design.pocket`、`part.extrusion` 的 taper fixture 输出 `topo_naming_history=maker_history:taper_thru_sections`；`NamedShape.element_map_status=history_partial`，`mapper_history.maker_stage=maker_history` | 只代表当前 Pad / Pocket / Part::Extrusion taper ThruSections history metadata 已收口；不恢复旧 local-history gap 清单 |
| `assembly.ondsel_solver_adapter` | `covered_full` | grounded Fixed / Revolute / Slider / Ball / Distance / Angle 已走 request-local `ASMTAssembly::runPreDrag()`；invalid grounded placement fixture 拒绝 writeback | full 依据只来自真实 Ondsel solver path，不来自 representative fallback |
| `assembly.placement_writeback` | `covered_full` | `documentObjectUpdates.action=assembly_set_placement` 覆盖 solver placement delta、unchanged no-op、invalid grounded validation、应用到下一次 request graph 后稳定 recompute、多 component 写回顺序和 `Placement` 目标字段 | full 依据只代表无状态写回建议的前端持久应用生命周期，不表示 CAD Core 保存跨请求 Assembly session |
| `link_transaction.copy_on_change_deep_copy_lifecycle` | `covered_full` | CopyOnChange property tree deep copy、child/group copy、internal relink、dependency rewrite、ReferenceShadow / stable subname evidence preserve 与 touched sync 已由 fixture 固定 | `copy_on_change_writeback_contract.status=covered` 仍只是 transport 口径 |

### 仍非 `covered_full`

| 项 | 当前状态 | 为什么不能升级 | 下一步候选 |
| --- | --- | --- | --- |
| `wire_joiner.generated_open_export_bridge` | `covered_main_path` | 已把 `openWireCompound` child-wire 自身的 source lineage、splitter lineage、noOriginal shared-source 匹配结果输出到 ledger/history，并让 `getOpenWires()` 与 `topo_shape.cpp` 的 NamedShape / ElementMap producer evidence 优先消费 child-wire source/noOriginal verdict；`splitEdges()` fragment-to-source ledger 已区分 Modified / Generated / input EdgeInfo source sidecar，并透传到 runtime history、`Sketch.InternalShape` history 和 topo evidence；split source lineage 已改为使用原始 `sourceEdgeArray` identity，mutable `sourceEdgeLedgerEdges_` 只作为 vmap vertex ledger，不再冒充 source ownership；本轮拆分 `split_fragment_source_identity_fallback_*` 与 `split_fragment_history_shape_geometry_bridge_*` 诊断，并通过 wire edge -> sourceEdgeArray index sidecar 将 P5 through/branch/cross/segmented/T/arc-lens 的旧 `split_fragment_identity_fallback_edge_info_count` 固定为 0；producer child-wire 输出已优先使用 `sourceEdgeLedgerEdges_` + split fragment producer ledger 的 vmap vertex ledger，`CurrentMemberChildWire` 已改由 root/current-member child ledger 输出并用 `open_wire_compound_root_current_member_producer_output_wire_info_count` 固定，独立 result-wire producer plan 类型已删除，剩余 history materialization binding 仍是 producer bridge 债务；closed-cycle generated open-export 预判已从 bounded-face 结果边采样改为读取 Modified split fragment ledger。已由 child-wire `ExportedWithoutTransitionalSlot` 导出的 entry 不再输出 active `helper_open_export_override`，legacy helper reason string 已从代码路径删除，child-wire 层已无 helper 字段，EdgeInfo 与 child-wire 的 producer identity candidate、source-edge producer 输出与 root/current-member producer lifecycle 均改为 result-wire producer ledger 字段，history materialization binding 不再把 result-slot endpoint evidence 作为 producer classification 输入，`resultWireProducerLocatorEdge()` 已删除，分类路径直接使用 `resultWireProducerOpenExportEdge()` 读取 history materialization per-edge entry / EdgeInfo edge fallback，`resultWireProducerOpenExportEdge()` / `openExportWire()` 不再读取 result-slot endpoint evidence 作为 child-wire 输出，current-member blocker 诊断已从 child-wire ledger 读数，物化侧 result-slot endpoint evidence 已删除；producer identity 改为不依赖 helper flag 消费；本轮已删除 `getOpenWires()` 无 child-wire ledger 时从 EdgeInfo 重导出 open wire 的 legacy 分支；open-export history 与 topo evidence 现在直接记录 `openWireCompound` child-wire 输出，child-wire 通过 `producerLedgerWire` 持有已 materialized 的 producer wire，并只用 `open_wire_compound_producer_ledger_wire_built` / `open_wire_compound_producer_ledger_wire_built_wire_info_count` 固定该消费边界；公开 `open_wire_compound_source_edge_producer_output` 与 summary count 已移入 deleted_fields。若 exportable EdgeInfo 缺少 child-wire，只记录 `missing_open_wire_compound_child_wire` / `open_wire_compound_missing_child_wire_history_edge_info_count` 诊断，不再从 `EdgeInfo::openExportWire()` 重导出。本轮保留 `open_wire_compound_producer_ledger_wire_from_source_vmap_wire_info_count` 验证 FreeCAD vmap/sourceEdges/split-fragment ledger 路径，并删除公开 `open_wire_compound_producer_ledger_wire_from_result_slot_evidence*` 旧 0 值诊断；P5 T/cross/segmented 已固定为 source-vmap producer ledger，three-overlap current-member producer 输出已归零到 multiplicity blocked 0、candidate matched 6，source-vmap producer 计数为 3，并以 `CurrentMemberChildWire / ExportedWithoutTransitionalSlot` 进入 mapper history；公开 `result_slot_vertex_evidence_output` / `open_wire_compound_result_slot_vertex_evidence_wire_info_count` 已删除，result-slot 不再有输出形状诊断面。ledger 根部 `openExportProducerEdges` 已删除并并入 history materialization per-edge `openExportProducerEdge` / `historyProducerChildWireCandidate`，公开 `edgeInfo_resultWireProducerCandidate_internal` 已移入 deleted_fields；但仍保留 `WireJoinerHistoryMaterializationLedger` binding 与 history materialization per-edge `openExportProducerEdge` / `historyProducerChildWireCandidate` 这类内部建账字段，还不是完整 FreeCAD `WireJoinerP` ledger 生命周期 | 沿 `WireJoinerP::EdgeInfo`、`WireInfo`、`myShapesToReturn`、`MapperHistory`、`ElementMap` 继续补正式账本，直到 materialized child-slot bridge 可删除 |
| `wire_joiner.purge_as_original_bridge` | `covered_main_path` | `purgeAsOriginalOpenEdge` 与公开 `purge_bridge` history/detail 字段已删除；noOriginal 公开 candidate bridge 已删除并进入 `purge_as_original_bridge.deleted_fields`，shared-source 匹配收敛到 `OpenWireCompoundWireInfo` child-wire ledger，NamedShape / ElementMap 的 deleted / no_original_purge 也只认 child-wire actual purged verdict，ledgered main path 不再走最终输出 pruning；`getOpenWires()` 已删除无 child-wire ledger 时的最终 shared-source 输出 pruning；`splitFromInputEdge` 已来自 splitter history fragment ledger，不再用端点几何比较判定 split fragment。actual purge 现在先按 materialized child slot 重组最终 wire，再按 FreeCAD shared-source 谓词整组删除；仍不能升级为 full，因为 child-wire ownership 仍是 cad-core 重组账本，MapperHistory / ElementMap lifecycle 未完整接管 | 补完整 vmap replacement、补齐 `openWireCompound` child-wire ownership 和 `WireInfo / EdgeInfo` lifecycle；删除条件是 noOriginal 决策只依赖真实 FreeCAD child-wire ownership，且 P5 open-cutter/cross/T/segmented cutter 全部保持稳定 |
| `assembly.representative_solver_adapter` | `covered_representative` | 只代表 no-grounded 或未链接 Ondsel 时的 stateless fallback DTO；不能替代真实 solver full path | 只有需要扩展 fallback 语义时才拆新项；不要用它证明 Assembly full solver |

最新 WireJoiner gate：three-overlap 的 `open_wire_compound_current_member_split_ledger_vertex_multiplicity_blocked_wire_info_count=0`，3 个 current-member child 均以 `CurrentMemberChildWire / ExportedWithoutTransitionalSlot` 输出；summary gate 当前固定 `open_wire_compound_current_member_split_ledger_output_unmatched_vertex_count=0` 与 `open_wire_compound_current_member_split_ledger_output_candidate_matched_vertex_count=6`。source-vmap producer 计数已为 3，旧 `wire_joiner_current_member_vertex_multiplicity_blocked` mapper diagnostic、result-slot evidence producer count、result-slot-only 诊断和内部 endpoint materialization sidecar 均已归零或删除；three-overlap `InternalVertex` oracle 固定为 19，不退化到 16。

最新 vertex debt ledger：three-overlap 的 current-member path 已把 `memberLedgerVertices`、`candidateVertices` 与每个 output vertex 的 `matched_member_split_ledger` / `matched_candidate_ledger` 记录到 `OpenWireCompoundWireInfo` child-wire；`wire_joiner_ledger`、`wire_joiner_history_detail` 与 `Sketch.InternalShape.mapper_history` 暴露 `open_wire_compound_current_member_split_ledger_vertex_debt_recorded`、`member_vertex_count`、`output_vertex_ledger_count`、`output_matched_vertex_count` 与 `output_candidate_matched_vertex_count`。当前 `vertex_debt_wire_info_count=3`、`output_vertex_ledger_count=6`、`output_matched_vertex_count=1`、`output_candidate_matched_vertex_count=6`；旧 `result_slot_only_identity` 已进入 capability `deleted_fields`，这是 sidecar 删除后的正式诊断账本，但仍不是 `covered_full`。

最新边界更新：`EdgeInfo::resultSlotVertexEvidenceEdge`、独立 result-wire producer plan result-slot endpoint edge seed、内部 `OpenWireCompoundWireInfo::endpointMaterializationEvidenceVertices`、`producerLedgerWireFromEndpointMaterializationEvidence` 与 `WireJoinerEndpointMaterializationLedger` 已删除。公开 endpoint-materialization 兼容诊断字段也已移入 capability `deleted_fields`，不能作为 producer 输出或 vertex identity 输入；`generated_open_export_bridge` 仍不能升级到 `covered_full`。

最新内部字段边界：`OpenWireCompoundWireInfo::producerLedgerWireFromEndpointMaterializationEvidence` 已删除；公开 `open_wire_compound_producer_ledger_wire_from_result_slot_evidence*` 也已删除并移入 capability `deleted_fields`，不再作为 0 值诊断名保留。

最新 source/vmap endpoint ledger：`OpenWireCompoundWireInfo` 增加 child-wire 级 `sourceVmapEndpointLedger*` 读数，按 FreeCAD `WireJoinerP::add()` 的 vmap/sourceEdges 顶点替换语义，统计当前输出 wire 的 endpoint `TopoDS_Vertex` 身份是否已由 request-local source/vmap/split ledger 覆盖。该 ledger 不改变输出 wire：P5 cross / segmented / T 的 source-vmap producer 计数仍为 4，旧 result-slot evidence 公开字段已删除，matched endpoint 分别为 12 / 12 / 11；three-overlap 为 `wire_info_count=15`、`output_vertex_count=30`、`matched_vertex_count=6`，且 current-member candidate endpoint 已覆盖 6 个输出端点。

最新 vmap replacement event ledger：`WireJoinerVmapReplacementEvent` 已把 `WireJoinerP::add()` 中 old vertex、new shared vertex、affected source edge / child-wire endpoint、replacement source edge index 推进到 `OpenWireCompoundWireInfo` child-wire provenance。当前 P5 cross / segmented 均为 `open_wire_compound_vmap_replacement_event_count=8`、`open_wire_compound_endpoint_provenance_vmap_replacement_matched_vertex_count=8`；three-overlap 仍为 event 0，但 candidate matched 6。

最新 endpoint provenance ledger：`OpenWireCompoundWireInfo::EndpointProvenance` 已按最终 child-wire 输出顶点记录 source/vmap、vmap replacement event、current-member candidate 三类 `TopoDS_Vertex` identity 命中。该 ledger 由 `recordOpenWireCompoundLedger()` 在 child-wire 边界写入，并通过 `wire_joiner_ledger`、`wire_joiner_history_detail`、`Sketch.InternalShape` history 和 `topo_shape.cpp` evidence 转发；`generated_open_export_bridge.covered` 新增 `child_wire_endpoint_provenance_ledger` 和 `child_wire_vmap_replacement_event_ledger`。当前 three-overlap 固定 `open_wire_compound_endpoint_provenance_wire_info_count=15`、`output_vertex_count=30`、`source_vmap_matched=1`、`vmap_replacement_matched=0`、`candidate_matched=6`、`unmatched=24`。公开 endpoint-materialization 诊断已删除；该字段是 per-endpoint child-wire 账本，不能把 `generated_open_export_bridge` 升级为 `covered_full`。

最新 noOriginal 边界：`sourceEdgeArrayOriginalOpenEdgeCandidate()` 已删除，公开 `open_wire_compound_no_original_purge_candidate*` / `source_identity_no_original_purge_candidate*` 字段已移入 `purge_as_original_bridge.deleted_fields`，并以 `openWireCompound_noOriginal_candidate_public_bridge_removed` 固定；`updateOpenWireCompoundNoOriginalPurgeVerdict()` 在 child-wire 层计算 shared-source match，`updateOpenWireCompoundNoOriginalGroupPurgeVerdicts()` 先按 endpoint connectivity 重组最终 wire，再写 `noOriginalPurgedByLedger` actual purge verdict；`getOpenWires(noOriginal)` 只通过 `openWireCompoundChildWirePurgedByNoOriginal()` 消费该组级 verdict。删除条件仍是 P5 open-cutter / cross / T / segmented cutter 不因 child-level shared-source purge 退化。

最新 noOriginal shared-source edge ledger：`OpenWireCompoundWireInfo` 已按 FreeCAD `WireJoinerP::getOpenWires(noOriginal=true)` 的 `source.findSubShapesWithSharedVertex(TopoShape(edge, -1)).empty()` 谓词记录每条 child-wire 的 edge / matched / unmatched 计数，并公开为 `open_wire_compound_no_original_shared_source_*` summary 与 per-entry history 字段。actual purge 已改为先重组最终 wire、再按组内 all-matched verdict 删除；这修正了 T / segmented 中 exact-source child 被提前删掉的问题。`purge_as_original_bridge.status` 仍保持 `covered_main_path`，因为重组粒度和 MapperHistory / ElementMap full lifecycle 仍未完全等价 FreeCAD。

最新 producer ledger 边界：`result_wire_producer_ledger_entries` 现在由 `OpenWireCompoundWireInfo` child-wire slot 生成，不再在 summary 阶段从 EdgeInfo candidate 重新组装公开 entry；child-wire 层已删除 `resultWireProducerCandidate` 拷贝，producer entry 现在消费已分类的 `ResultWireProducerIdentity`，不再直接读取 raw EdgeInfo candidate bit，noOriginal candidate gate 公开字段也已删除；child-wire 层继续删除内部 `sourceEdgeProducerOutput` sidecar，公开 `openWireCompoundSourceEdgeProducerOutput` / JSON `open_wire_compound_source_edge_producer_output` 与 summary count 已删除并移入 deleted_fields，内部 readiness helper 只私有服务 producer identity；ledger 根部 `openExportProducerEdges` 已删除并并入 history materialization per-edge `openExportProducerEdge` / `historyProducerChildWireCandidate`，该字段仍是临时 bridge，但不再复制成 child-wire-local `producerLedgerEdge`，`recordOpenWireCompoundLedger()` 只用该 scoped edge 物化 child-wire `producerLedgerWire`，也不再把 per-edge `historyProducerChildWireCandidate` 直接复制成 public child-wire source；公开 `open_wire_compound_export_source` 已改为 final child-wire pass 从 open-leaf / unowned / root-current-member / materialized producer-wire ledger 推导，C ABI 用 `open_wire_compound_export_source_from_child_wire_final_identity` 固定，旧 `open_wire_compound_export_source_from_materialization_entry` 进入 deleted_fields；后续 producer wire 物化、open-export history 和 topo evidence 均读取 child-wire ledger，并输出 `open_wire_compound_producer_ledger_wire_built` 证明 child-wire producer wire 物化，不再回读 EdgeInfo producer shape（该 EdgeInfo shape 字段已删除）。Endpoint materialization 内部 sidecar 已删除；EdgeInfo `partialSharedClosedWireProducer` 与 `resultWireProducerSourceEdgeInfo*`/eligible/full-aHistory-evidence 与 `resultWireProducerSuperEdge*` 缓存已删除，binding 内 openWireCompound eligible 候选缓存、per-edge source EdgeInfo bool/index 恒等缓存、per-edge open-export gate 缓存、full AHistory 缓存和 aHistory/source-lineage 复制字段也已删除，分类直接读取 history materialization binding / per-edge entry，并按 EdgeInfo 当前 `iteration / wireInfo / buildClosedWire / aHistory Remove` 状态即时判断 open-export gate、full AHistory evidence 与 source-lineage sidecar；raw `edgeInfo_resultWireProducerCandidate_internal` 已移入 deleted_fields，`open_wire_compound_producer_ledger_edge_materialized` 与 child-wire producer edge copy gate 已移入 deleted_fields，history materialization per-edge `openExportProducerEdge` / `historyProducerChildWireCandidate` 仍是内部临时建账入口；删除条件仍是 `WireJoinerP::EdgeInfo / WireInfo / myShapesToReturn / MapperHistory / ElementMap` 生命周期能直接产生 openWireCompound child ownership。

最新 producer entry gate：公开 `result_wire_producer_ledger_entries` 的发布 gate 已从 history materialization per-edge identity 后移到 `OpenWireCompoundWireInfo::resultWireProducer` final child-wire identity；C ABI 用 `result_wire_producer_entry_gate_after_child_wire_identity` 固定该边界，并把 `result_wire_producer_entry_gate_from_materialization_entry_identity` 放入 deleted_fields。该项只删除 public entry 对 per-edge pre-classification 的直接依赖，不删除内部 `openExportProducerEdge` / `historyProducerChildWireCandidate` bridge。

最新 producer state 边界：空 `ResultWireProducerIdentity` 的默认状态已从旧 `TransitionalResultSlotCandidate` 改为 `Unpublished`；`Unpublished` 只允许表示没有 producer identity 的 open-export history / mapper evidence，不发布新的 `result_wire_producer_ledger_entries`。旧 `TransitionalResultSlotCandidate` 已列入 `generated_open_export_bridge.deleted_fields`，不得再作为公开 state 输出。

最新 producer gate：edge-level `ProducerLedgerReady` 不再由非空 history materialization per-edge `openExportProducerEdge` / `historyProducerChildWireCandidate` 推导；`source_shape_ready_derived_from_history_materialization_ledger_open_export_edge` 与 `edge_level_producer_ledger_ready_from_history_materialization_ledger` 已进入 deleted_fields，`producer_ledger_ready_gate_direct_history_materialization_ledger` 已从 remaining gap 删除。旧公开 state `SourceShapeReady` 已进入 deleted_fields；openWireCompound child-wire materialized 之后的输出 readiness 改由 child-wire `producerLedgerWire` / `currentMemberProducerOutput` 发布，不再作为 EdgeInfo 分类缓存或 child-wire producer edge copy gate。`child_wire_producer_ledger_edge_copy_gate_from_history_materialization_ledger` 已进入 deleted_fields；`edge_info_identity_source_edge_output_gate_removed` / `result_wire_producer_identity_source_shape_ready_removed` 固定 child-wire producer edge 不再读取 EdgeInfo identity source-shape-ready 状态；`EdgeInfo::resultWireProducerSourceEdgeExportShape` 已删除。该改动把 producer-ready 从 edge-level materialization bridge 收束到 child-wire materialization，不改变 `generated_open_export_bridge.status=covered_main_path`，删除条件仍是补齐 FreeCAD `WireJoinerP::EdgeInfo / WireInfo / myShapesToReturn / MapperHistory` 生命周期后移除 history materialization per-edge result-wire producer candidate 字段。

最新 identity classifier 边界：`ResultWireProducerIdentity` 已删除 `hasSourceLineage`、`hasStrictRemoveSource`、`hasRemovedTarget`、`hasSameSourceRemoveLineage`、`hasFullAHistoryEvidence`、`finalGateEligible` 这类分类 scratch 字段；相同判断只在 `classifyResultWireProducerSlot()` 内作为局部谓词使用。identity 发布面收敛为 kind / state / blocker、producer index、child-wire index 与 `childWireBuilt`。

最新 EdgeInfo 输出 helper 边界：`EdgeInfo::openExportWire()` 已删除，缺 child-wire 的 open-export history 只能报 `missing_open_wire_compound_child_wire` 诊断，不能再从 EdgeInfo wire helper 重导出，也不能复制 EdgeInfo source lineage、split fragment、vertex replacement 或 producer identity payload；`resultWireProducerOpenExportEdge()` 仍是 producer 分类和 child-wire 建账的内部 evidence，不代表 WireJoiner full ledger。

最新 WireJoiner relation 边界：open-export relation 现在先在 `cad-core/src/part/wire_joiner.cpp` 由 `OpenWireCompoundWireInfo` child-wire ledger 分类为 `preserved / split / generated / deleted`，再由 `Sketch.InternalShape` history 和 `topo_shape.cpp` evidence 通过 `wire_joiner_history_relation_from_child_wire_ledger` 消费；`topo_open_export_relation_fallback_removed` 固定 topo 层从 source indices、split flags、producer kind 反推 relation 的 legacy fallback 已删除。该切片删除的是 topo evidence 的 relation 拼装 debt；仍未删除 history materialization per-edge `openExportProducerEdge` / `historyProducerChildWireCandidate`，且 noOriginal 仍依赖 cad-core child-wire 重组账本而不是完整 FreeCAD child-wire ownership，因此 `generated_open_export_bridge` 和 `purge_as_original_bridge` 仍保持 `covered_main_path`。

最新 WireJoiner history event 边界：`WireJoinerHistoryRelation` 已进一步写入 request-local `WireJoinerHistoryEvent` 列表，runtime `wire_joiner_history_detail` 与 `Sketch.InternalShape.sketch_internal_history` 输出 `wire_joiner_history_events`、`wire_joiner_history_event_count` 与 `wire_joiner_history_event_from_child_wire_ledger_count`；open-export entry 和 topo evidence 只携带 `wire_joiner_history_event_index` / `wire_joiner_history_event_from_child_wire_ledger`。本轮 `topo_shape.cpp` 的 open-export mapper evidence 已通过 event index 查找 `SketchInternalHistoryContext::wireJoinerHistoryEvents`，并优先消费 event relation/source edge indices，公开 evidence 用 `wire_joiner_history_event_consumed_by_topo=true` 固定该边界。该 event 记录 relation、source lineage、splitter lineage、actual noOriginal purge 和 Modified / Generated fragment 标记，仍只证明 child-wire ledger 到 topo evidence 的可枚举账本边界；删除条件仍是补完 `WireJoinerP::aHistory -> MapperHistory -> ElementMap` 生命周期并移除 history materialization binding / per-edge result-wire producer debt。

最新 WireJoiner mapper-history 边界：`topo_shape.cpp` 在已有 concrete `wire_joiner:open_export` source->InternalEdge mapper event 且 evidence 同时消费 `wire_joiner_history_event_consumed_by_topo` 与 `open_wire_compound_child_wire_ownership_consumed_by_topo` 时，不再追加 `summary_only:wire_joiner_history:open_export` 兜底。C ABI capability 用 `wire_joiner_open_export_mapper_history_concrete_events` 固定该边界；这只是删除 open-export summary-only 解释层，remaining gap 仍是 history materialization per-edge `openExportProducerEdge / historyProducerChildWireCandidate` 与完整 `MapperHistory(aHistory)` / ElementMap 生命周期。

最新 topo mapper evidence 边界：`Sketch.InternalShape.mapper_history` 的 concrete WireJoiner open-export evidence 不再输出 `result_wire_producer_*` identity 字段，mapper diagnostic 也不再从 producer blocker 派生 `producer_blocker:*`，缺 source lineage 时输出 `missing_open_wire_compound_source_lineage` 而不是 `missing_producer_identity`；C ABI capability 用 `topo_mapper_evidence_result_wire_producer_identity_removed` / `topo_mapper_diagnostic_result_wire_producer_blocker_removed` / `topo_mapper_diagnostic_missing_producer_identity_removed` 和对应 deleted_fields 固定。producer audit 仍保留在 producer ledger、history detail 和 sketch internal history entry 中。这只是删除 mapper event 的 producer identity 转发，不改变 `generated_open_export_bridge.status=covered_main_path`。

最新 topo identity counter 边界：`named_shape_history_missing_result_wire_identity_count` 与 `element_map_result_wire_identity_mismatch_count` 已从 `Sketch.InternalShape.sketch_internal_history` 删除并进入 capability deleted_fields；`topo_result_wire_identity_counters_removed` 固定 topo 不再用 result-wire producer identity 生成 NamedShape / ElementMap 计数。producer identity 仍只作为 producer ledger / history detail audit，不作为 topo 消费 gate。

最新 WireJoiner ElementMap 边界：`topo_shape.cpp` 已把唯一 child-wire-consumed source->InternalEdge history 写入 `Sketch.InternalShape.element_map`，并用 `wire_joiner_history:element_map` 标记该第一片；P5 branch open-cutter 固定 `Edge5 -> InternalEdge10`。该路径要求 history event 与 open-export child-wire slot 对齐、child shape identity 已记录、source 只指向一个 target 且不覆盖既有 alias；一对多 split、deleted relation 和已有 alias 仍保留在 terminal mapper history 中，不能把状态升级为 full。

最新 topo child-wire ownership gate：`topo_shape.cpp` 的 WireJoiner open-export mapper evidence 现在额外输出 `open_wire_compound_child_wire_ownership_consumed_by_topo=true`，只有 history event 与 open-export entry 指向同一个 `openWireCompound` child-wire slot，且 child shape identity / edge / vertex inventory 均存在时才成立。C ABI capability `generated_open_export_bridge.covered` 新增 `topo_consumes_openWireCompound_child_wire_ownership_ledger`；`wire_joiner_history_materialization_ledger_open_export_producer_edge` 仍列入 `remaining_gaps`，不作为 covered 成功项；公开 source-edge producer output/count、edge-level producer-ready gate 与 child-wire producer edge copy gate 已删除，不再作为 gap 单独追踪。

最新 binding 收束：`WireJoinerHistoryMaterializationBinding` 不再保存 source EdgeInfo candidate list，`computeWireJoinerHistoryMaterializationLedger()` 在 partial shared closed-wire、consumed open-cutter graph 与 closed-cycle generated export 三条 binding 路径都直接记录当前最终 `EdgeInfo` row；`applyWireJoinerHistoryMaterialization()` 不再按等价几何候选排序，只消费该 `edgeInfoIndex`。该改动对齐 FreeCAD `WireJoinerP::build()` 从最终 EdgeInfo states 执行 `builder.Add(openWireCompound, info.wire())` 的边界；剩余删除条件仍是移除 per-edge `openExportProducerEdge` / `historyProducerChildWireCandidate` staging，并补齐 `MapperHistory(aHistory)` / ElementMap 正式消费。

最新 child-wire ownership 边界：`OpenWireCompoundWireInfo` 已记录 `OpenLeafIterationMinus3 / UnownedOpenEdge / AHistoryProducerChildWire / RootCurrentMemberChildProducer` export source、EdgeInfo iteration / iteration2、owner WireInfo / wireInfo2、child shape edge/vertex inventory、history event index 和 source edge indices；公开 source 现在只从 final child-wire identity 推导，不再从 materialization edge entry 复制；`result_wire_producer_ledger_entries` 直接消费这些 child-wire ownership 字段，并同步到 `wire_joiner_ledger`、`wire_joiner_history_detail`、`Sketch.InternalShape` history 和 topo evidence。该边界对齐 FreeCAD `WireJoinerP::build()` 的 open export 条件，但 history materialization per-edge `openExportProducerEdge` / `historyProducerChildWireCandidate` 仍作为内部建账入口，因此 `generated_open_export_bridge.status` 仍保持 `covered_main_path`。

最新 per-endpoint vertex debt 边界：three-overlap 的 current-member path 已输出 `open_wire_compound_current_member_split_ledger_output_vertex_debt`，逐端点记录 member/split ledger 和 candidate ledger。当前 3 个 current-member child 的 6 个输出端点均为 candidate 命中，其中 1 个端点同时命中 member/split ledger；旧 `result_slot_only_identity` 字段已删除，three-overlap `InternalVertex` 固定为 19，不退化到 16。

### Diagnostic-only 边界

| 项 | 当前边界 | 诊断 |
| --- | --- | --- |
| Pad / Pocket `UpToShape` multi-face | 仍保留 diagnostic-only unsupported boundary，不和 taper history 混为一个 gap | `unsupported_subshape_kind` |
| Assembly unsupported JointType | RackPinion / Screw / Gears / Belt / Cylindrical 仍只暴露稳定 unsupported diagnostic | unsupported JointType diagnostics |

## 下一阶段候选任务

1. WireJoiner full ledger：只沿 FreeCAD `WireJoinerP::EdgeInfo / WireInfo / myShapesToReturn / MapperHistory / ElementMap` 路径推进，不用输出 pruning、fixture 特判或几何猜测当主路径。
   - 最新推进：`OpenWireCompoundWireInfo` 已承接 child-wire source lineage、splitter lineage、noOriginal shared-source match；公开 noOriginal candidate bridge 已删除并进入 C ABI `deleted_fields`。`splitEdges()` fragment-to-source ledger 已区分 Modified / Generated / input EdgeInfo source sidecar，source lineage 使用原始 `sourceEdgeArray` identity，vmap replacement 已从 `sourceEdgeLedgerEdges_` 统计推进为 `WireJoinerVmapReplacementEvent` typed ledger。P5 cross / segmented 通过 event ledger 固定非 current-member producer 不再使用 endpoint materialization evidence；旧 result-slot evidence 与 result-slot-only 公开字段已删除，three-overlap current-member vertex debt 已固定 `vertex_multiplicity_blocked=0`、`output_unmatched_vertex_count=0`、`output_candidate_matched_vertex_count=6`。WireJoiner ElementMap 已有唯一 child-wire alias 第一片，branch open-cutter 固定 `Edge5 -> InternalEdge10`；下一步只应收敛 history materialization per-edge producer bridge 与完整 MapperHistory / ElementMap 消费，不恢复输出端 pruning。
2. UpToShape multi-face：若要从 diagnostic-only 进入实现，先拆 Pad / Pocket UpToShape 多面选择的 FreeCAD 源码依据、fixture 和 diagnostics，不复用已收口的 taper history gap 名称。
3. 后续新增缺口必须拆成具体 producer、lifecycle、feature-family、JointType 或 adapter contract 项；不得恢复旧 local-history gap 清单、旧 taper known-gap topo marker 或 broad `known_gaps`。
