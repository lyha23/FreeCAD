# C3-M8 后续收口清单

本文件只记录 C3-M8 后的当前状态、真实剩余项和下一步入口。历史过程、逐轮字段删除和中间诊断变更不再写入本文件；需要实现细节时看源码、fixture 和 `07-WireJoiner-MapperHistory接管方案.md`。

## 状态依据

- `cad-core/src/adapters/c_api/c_api.cpp::capabilitiesJson()`
- `cad-core/include/cad_core/part/wire_joiner.h::WireJoinerHistoryMaterializationEdgeEntry`
- `cad-core/src/part/wire_joiner.cpp`
- `cad-core/src/part/topo_shape.cpp`
- `cad-core/fixtures/p5`、`cad-core/fixtures/p6`、`cad-core/fixtures/c3m*`

## 已冻结为 `covered_full`

| 项 | 当前状态 | 边界 |
| --- | --- | --- |
| `object_metadata.local_history.taper_history` | `covered_full` | 只覆盖当前 Pad / Pocket / Part::Extrusion taper ThruSections history metadata；不恢复旧 local-history gap |
| `assembly.ondsel_solver_adapter` | `covered_full` | grounded Fixed / Revolute / Slider / Ball / Distance / Angle 走真实 Ondsel path；representative fallback 不作为 full 依据 |
| `assembly.placement_writeback` | `covered_full` | 只代表 `documentObjectUpdates.action=assembly_set_placement` 可被前端应用到下一次 request graph；不表示 CAD Core 保存跨请求 Assembly session |
| `link_transaction.copy_on_change_deep_copy_lifecycle` | `covered_full` | CopyOnChange property tree copy、child/group copy、internal relink、dependency rewrite、ReferenceShadow / stable subname evidence preserve 与 touched sync 已有 fixture |

## 仍非 `covered_full`

| 项 | 当前状态 | 真实剩余项 |
| --- | --- | --- |
| `wire_joiner.generated_open_export_bridge` | `covered_main_path` | `openWireCompound` child-wire source lineage、splitter lineage、history event、topo evidence、unique child-wire ElementMap alias 已进入主路径；`historyProducerChildWireCandidate` 已删除并由 binding final `EdgeInfo` row 推导 candidate；`openExportProducerEdge` 读取已集中到 resolver/gate，但它仍作为 history materialization staged producer edge 存在，尚未由正式 `MapperHistory(aHistory) -> ElementMap` 生命周期接管 |
| `wire_joiner.purge_as_original_bridge` | `covered_main_path` | noOriginal 公开 candidate bridge 已删除，actual purge 已改为按 child-wire shared-source edge ledger 和 wire group verdict 执行；但 group verdict 仍依赖 cad-core 重组账本，尚未完全等价 FreeCAD `WireJoinerP::getOpenWires(noOriginal=true)` 之后的 `MapperHistory(aHistory)` / ElementMap 消费 |
| `assembly.representative_solver_adapter` | `covered_representative` | 只作为 no-grounded 或未链接 Ondsel 时的 fallback DTO；不是未完成 blocker |
| Pad / Pocket `UpToShape` multi-face | diagnostic-only boundary | 当前保留 `unsupported_subshape_kind` 诊断；若后续实现，必须另拆 Pad / Pocket UpToShape 多面选择源码依据和 fixture，不复用 taper history 名称 |

## WireJoiner 当前不可回退边界

- 不恢复 `openExportOverride`、`purgeAsOriginalOpenEdge`、公开 noOriginal candidate 字段、公开 source-edge producer output/count、result-slot output-shape diagnostics、endpoint-materialization 公开诊断或 EdgeInfo open-wire 重导出 fallback。
- 不在 `topo_shape.cpp`、adapter 或 expected JSON 中按 source index、几何形态、输出顺序、fixture 名称重新推断 open-export relation。
- `Sketch.InternalShape.mapper_history` 的 WireJoiner open-export 只能消费 concrete `wire_joiner:open_export` event、child-wire ownership 和 ElementMap / terminal history evidence；producer identity 只保留为 audit，不作为 mapper event 解释层。
- one-to-one alias 才能进入 `ElementMap`；split / deleted / ambiguous 关系保留在 `MapperHistory` 和 diagnostics 中，直到有 FreeCAD 等价的唯一 target 解析。

## 下一阶段任务

下一阶段只推进 WireJoiner full ledger：

1. 用正式 MapperHistory/ElementMap producer evidence 替换 `wireJoinerHistoryMaterializationAHistoryProducerEdge()` / `wireJoinerHistoryMaterializationEntryHasAHistoryProducerChildWire()` 当前读取的 `openExportProducerEdge`；`historyProducerChildWireCandidate` 字段已删除。
2. 让 `MapperHistory(aHistory) -> ElementMap` 正式接管 open-export / noOriginal / split / deleted 关系。
3. 验收 P5 open-cutter / cross / T / segmented cutter 不退化，three-overlap `InternalVertex` 保持 19，branch open-cutter 的唯一 child-wire alias 继续成立。

具体方案见 `07-WireJoiner-MapperHistory接管方案.md`。
