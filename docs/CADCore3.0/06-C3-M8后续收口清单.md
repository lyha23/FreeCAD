# C3-M8 后续收口清单

本文件只记录 C3-M8 taper-history cleanup 后的冻结状态和真实剩余项，不记录执行流水账。状态依据以当前 `cad-core/src/adapters/c_api/c_api.cpp::capabilitiesJson()`、相关 fixture expected 和 `docs/CADCore3.0` 矩阵为准。

## C3-M8 后最终状态表

### 已 `covered_full`

| 项 | 当前状态 | 依据 | 边界 |
| --- | --- | --- | --- |
| `object_metadata.local_history.taper_history` | `covered_full` | `part_design.pad`、`part_design.pocket`、`part.extrusion` 的 taper fixture 输出 `topo_naming_history=maker_history:taper_thru_sections`；`NamedShape.element_map_status=history_partial`，`mapper_history.maker_stage=maker_history` | 只代表当前 Pad / Pocket / Part::Extrusion taper ThruSections history metadata 已收口；不恢复旧 local-history gap 清单 |
| `assembly.ondsel_solver_adapter` | `covered_full` | grounded Fixed / Revolute / Slider / Ball / Distance / Angle 已走 request-local `ASMTAssembly::runPreDrag()`；invalid grounded placement fixture 拒绝 writeback | full 依据只来自真实 Ondsel solver path，不来自 representative fallback |
| `assembly.placement_writeback` | `covered_full` | `documentObjectUpdates.action=assembly_set_placement` 覆盖 solver placement delta、unchanged no-op、invalid grounded validation、应用到下一次 request graph 后稳定 recompute、多 component 写回顺序和 `Placement` 目标字段 | full 依据只代表无状态写回建议的前端持久应用生命周期，不表示 CAD Core 保存跨请求 Assembly session |
| `link_transaction.copy_on_change_deep_copy_lifecycle` | `covered_full` | CopyOnChange property tree deep copy、child/group copy、internal relink、dependency rewrite、ReferenceShadow / stable subname evidence preserve 与 touched sync 已由 fixture 固定 | `copy_on_change_writeback_contract.status=covered` 仍只是 transport 口径 |

### 仍非 `covered_full`

| 项 | 当前状态 | 为什么不能升级 | 下一步候选 |
| --- | --- | --- | --- |
| `wire_joiner.generated_open_export_bridge` | `covered_main_path` | 已把 `openWireCompound` child-wire 自身的 source lineage、splitter lineage、noOriginal 候选/匹配结果输出到 ledger/history，并让 `getOpenWires()` 与 `topo_shape.cpp` 的 NamedShape / ElementMap producer evidence 优先消费 child-wire source/noOriginal verdict；`splitEdges()` fragment-to-source ledger 已区分 Modified / Generated / input EdgeInfo source sidecar，并透传到 runtime history、`Sketch.InternalShape` history 和 topo evidence；split source lineage 已改为使用原始 `sourceEdgeArray` identity，mutable `sourceEdgeLedgerEdges_` 只作为 vmap vertex ledger，不再冒充 source ownership；本轮拆分 `split_fragment_source_identity_fallback_*` 与 `split_fragment_history_shape_geometry_bridge_*` 诊断，并通过 wire edge -> sourceEdgeArray index sidecar 将 P5 through/branch/cross/segmented/T/arc-lens 的旧 `split_fragment_identity_fallback_edge_info_count` 固定为 0；producer child-wire 输出已优先使用 `sourceEdgeLedgerEdges_` + split fragment producer ledger 的 vmap vertex ledger，`CurrentMemberChildWire` 已改由 root/current-member child ledger 输出并用 `open_wire_compound_root_current_member_producer_output_wire_info_count` 固定，`ResultWireProducerPlan` scoped endpoint debt 只保留为 endpoint materialization evidence；closed-cycle generated open-export 预判已从 bounded-face 结果边采样改为读取 Modified split fragment ledger。已由 child-wire `ExportedWithoutTransitionalSlot` 导出的 entry 不再输出 active `helper_open_export_override`，legacy helper reason string 已从代码路径删除，child-wire 层已无 helper 字段，EdgeInfo 与 child-wire 的 producer identity candidate、source-edge producer 输出与 root/current-member producer lifecycle 均改为 result-wire producer ledger 字段，`ResultWireProducerPlan` scoped endpoint debt 不再参与 producer classification，`resultWireProducerLocatorEdge()` 已删除，分类路径直接使用 `resultWireProducerOpenExportEdge()` 读取 scoped plan ledger / EdgeInfo edge fallback，`resultWireProducerOpenExportEdge()` / `openExportWire()` 不再读取 result-slot endpoint evidence 作为 child-wire 输出，current-member blocker 诊断已从 child-wire ledger 读数，物化侧 result-slot endpoint evidence 仍待删除；producer identity 改为不依赖 helper flag 消费；本轮已删除 `getOpenWires()` 无 child-wire ledger 时从 EdgeInfo 重导出 open wire 的 legacy 分支；open-export history 与 topo evidence 现在直接记录 `openWireCompound` child-wire 输出，child-wire 通过 `producerLedgerWire` 持有已 materialize 的 producer wire，并用 `open_wire_compound_producer_ledger_wire_built_wire_info_count`、`open_wire_compound_source_edge_producer_output_wire_info_count` 与 per-entry producer output 标记固定该消费边界；若 exportable EdgeInfo 缺少 child-wire，只记录 `missing_open_wire_compound_child_wire` / `open_wire_compound_missing_child_wire_history_edge_info_count` 诊断，不再从 `EdgeInfo::openExportWire()` 重导出。本轮新增 `open_wire_compound_producer_ledger_wire_from_source_vmap_wire_info_count`、`open_wire_compound_producer_ledger_wire_from_result_slot_evidence_wire_info_count`，把 child-wire producer 物化区分为 FreeCAD vmap/sourceEdges/split-fragment ledger 路径和仍需 result-slot endpoint evidence 的临时债务；P5 T/cross/segmented 已固定为 source-vmap producer ledger 且 result-slot 计数为 0，three-overlap 这类 closed source split 的旧 current-member closed-source result-slot bridge 诊断面与旧 `TransitionalResultSlotShapeStillUsed` blocker 已删除，剩余的是 `open_wire_compound_producer_ledger_wire_from_result_slot_evidence_wire_info_count=3` 与 3 个 multiplicity-blocked current-member child；本轮新增 `open_wire_compound_current_member_split_ledger_vertex_candidate_wire_info_count=3`，并在 `Sketch.InternalShape.mapper_history` 输出 3 个 `diagnostic_status=wire_joiner_current_member_vertex_multiplicity_blocked` producer event，证明 member/split ledger 已能形成候选，但直接切换会把 three-overlap InternalVertex 从 18 合并为 16，仍需 FaceMaker/MapperHistory vertex identity 正式替代；公开 `result_slot_vertex_evidence_output` / `open_wire_compound_result_slot_vertex_evidence_wire_info_count` 已删除，result-slot 不再有输出形状诊断面。但仍保留 `ResultWireProducerPlanLedger::producerOpenExportEdges`、EdgeInfo `resultWireProducer*` 与 `ResultWireProducerPlan` scoped endpoint debt 这类内部诊断和 producer ledger 解释字段，还不是完整 FreeCAD `WireJoinerP` ledger 生命周期 | 沿 `WireJoinerP::EdgeInfo`、`WireInfo`、`myShapesToReturn`、`MapperHistory`、`ElementMap` 继续补正式账本，直到 EdgeInfo result-wire producer candidate ledger 和 result-slot 字段可删除 |
| `wire_joiner.purge_as_original_bridge` | `covered_main_path` | `purgeAsOriginalOpenEdge` 与公开 `purge_bridge` history/detail 字段已删除；noOriginal purge 候选和 shared-source 匹配收敛到 `OpenWireCompoundWireInfo` child-wire ledger，NamedShape / ElementMap 的 deleted / no_original_purge 也只认 child-wire actual purged verdict，ledgered main path 不再走最终 shared-source 输出 pruning；`getOpenWires()` 已删除无 child-wire ledger 时的最终 shared-source 输出 pruning；`splitFromInputEdge` 已来自 splitter history fragment ledger，不再用端点几何比较判定 split fragment。2026-06-06 试改最终 shared-source-vertex purge 会误删 P5 open-cutter split fragment，因此不能升级为 full | 补完整 vmap replacement、补齐 `openWireCompound` child-wire ownership 和 `WireInfo / EdgeInfo` lifecycle；删除条件是 noOriginal 决策不再依赖 bridge 口径且 P5 open-cutter/cross/T/segmented cutter 全部保持稳定 |
| `assembly.representative_solver_adapter` | `covered_representative` | 只代表 no-grounded 或未链接 Ondsel 时的 stateless fallback DTO；不能替代真实 solver full path | 只有需要扩展 fallback 语义时才拆新项；不要用它证明 Assembly full solver |

最新 WireJoiner gate：three-overlap 的 `open_wire_compound_current_member_split_ledger_vertex_multiplicity_blocked_wire_info_count=3`，每个 blocked child 当前 `candidate_vertex_count=2`、`output_vertex_count=2`、`output_unmatched_vertex_count=2`、`result_slot_only_vertex_count=2`；summary gate 当前固定 `open_wire_compound_current_member_split_ledger_output_unmatched_vertex_count=6`、`open_wire_compound_current_member_split_ledger_result_slot_only_vertex_count=6` 与 `open_wire_compound_current_member_split_ledger_output_candidate_matched_vertex_count=0`。这说明 member/split ledger 已能构造候选边，但 candidate wire 也没有复用当前 output TopoDS_Vertex；result-slot 输出仍携带 member/split/candidate ledger 未覆盖且只由 result-slot evidence 支撑的顶点身份。本轮已把这些 child 的 producer state 收紧为 `ChildWireReady / CurrentMemberVertexMultiplicityBlocked`，并删除旧 `open_wire_compound_current_member_closed_source_result_slot_bridge*` 诊断面。当前 blocker 诊断的 result-slot-only 和 candidate-output 读数已从物化后的 `OpenWireCompoundWireInfo::wire` child ledger 读取，不再回读 EdgeInfo sidecar；删除条件是 unmatched / result-slot-only 归零，同时 candidate-output identity 能覆盖这些顶点，而不是直接切换 candidate wire。

最新 vertex debt ledger：three-overlap 的 current-member blocker 已把 `memberLedgerVertices`、`candidateVertices` 与每个 output vertex 的 `matched_member_split_ledger` / `matched_candidate_ledger` / `result_slot_only_identity` 记录到 `OpenWireCompoundWireInfo` child-wire；`wire_joiner_ledger`、`wire_joiner_history_detail` 与 `Sketch.InternalShape.mapper_history` 暴露 `open_wire_compound_current_member_split_ledger_vertex_debt_recorded`、`member_vertex_count`、`output_vertex_ledger_count`、`output_matched_vertex_count` 与 `output_candidate_matched_vertex_count`。当前 `vertex_debt_wire_info_count=3`、`output_vertex_ledger_count=6`、`output_matched_vertex_count=0`、`output_candidate_matched_vertex_count=0`，仍只是删除 result-slot endpoint evidence 前的正式诊断账本，不是 `covered_full`。

最新边界更新：`EdgeInfo::resultSlotVertexEvidenceEdge` 已删除；`applyResultWireProducerPlan()` 不再保存 result-slot endpoint edge seed，只保留 `ResultWireProducerPlan` scoped endpoint vertex debt；`recordOpenWireCompoundLedger()` 在 openWireCompound child-wire 建账边界把这些 endpoint vertices 复制为 `OpenWireCompoundWireInfo::endpointMaterializationEvidenceVertices`，后续 producer child-wire materialization 读取 child-wire endpoint vertex ledger，不再回读 EdgeInfo sidecar，也不保存或消费 child-wire-local result-slot edge shape。旧 `resultSlotEndpointMaterializationEvidenceVertices` 字段名已删除；公开 result-slot endpoint 诊断仍保留为删除前债务，不能升级到 `covered_full`。

最新内部字段边界：`OpenWireCompoundWireInfo::producerLedgerWireFromResultSlotEvidence` 已改名为 `producerLedgerWireFromEndpointMaterializationEvidence`；公开 `open_wire_compound_producer_ledger_wire_from_result_slot_evidence*` 仍是保留诊断名，只表示 endpoint materialization debt 尚未删除。

最新 source/vmap endpoint ledger：`OpenWireCompoundWireInfo` 增加 child-wire 级 `sourceVmapEndpointLedger*` 读数，按 FreeCAD `WireJoinerP::add()` 的 vmap/sourceEdges 顶点替换语义，统计当前输出 wire 的 endpoint `TopoDS_Vertex` 身份是否已由 request-local source/vmap/split ledger 覆盖。该 ledger 只作为删除 endpoint materialization debt 的 gate，不改变输出 wire：P5 cross / segmented / T 的 source-vmap producer 计数仍为 4 且 result-slot 计数为 0，matched endpoint 分别为 12 / 12 / 11；three-overlap 为 `wire_info_count=15`、`output_vertex_count=30`、`matched_vertex_count=0`，因此仍不能删除 3 个 result-slot endpoint materialization debt。

最新 endpoint provenance ledger：`OpenWireCompoundWireInfo::EndpointProvenance` 已按最终 child-wire 输出顶点记录 source/vmap、current-member candidate、endpoint materialization evidence 三类 `TopoDS_Vertex` identity 命中。该 ledger 由 `recordOpenWireCompoundLedger()` 在 child-wire 边界写入，并通过 `wire_joiner_ledger`、`wire_joiner_history_detail`、`Sketch.InternalShape` history 和 `topo_shape.cpp` evidence 转发；`generated_open_export_bridge.covered` 新增 `child_wire_endpoint_provenance_ledger`。旧 `sourceVmapEndpointLedger*` 保持 producer materialization gate，不被最终 current-member 输出覆盖。当前 three-overlap 固定 `open_wire_compound_endpoint_provenance_wire_info_count=15`、`output_vertex_count=30`、`source_vmap_matched=0`、`candidate_matched=0`、`endpoint_materialization_matched=6`、`unmatched=24`；其中 3 个 blocked child 各自 `output=2 / source_vmap=0 / candidate=0 / endpoint_materialization=2 / unmatched=0`。该字段只是把 endpoint materialization debt 从汇总计数推进到 per-endpoint child-wire 账本，不能把 `generated_open_export_bridge` 升级为 `covered_full`；删除条件仍是 endpoint materialization matched 归零且 source/vmap 或 member/split/candidate identity 覆盖当前输出顶点，同时 P5 open-cutter / cross / T / segmented / three-overlap oracle 不退化。

最新 noOriginal 边界：`sourceEdgeArrayOriginalOpenEdgeCandidate()` 已删除，candidate 由 `OpenWireCompoundWireInfo` child-wire ledger 的 source vertex / `splitFromInputEdge` / producer state 字段计算；`updateOpenWireCompoundNoOriginalPurgeVerdict()` 统一在 child-wire 层计算 shared-source match 与 actual purged verdict，`getOpenWires(noOriginal)` 只通过 `openWireCompoundChildWirePurgedByNoOriginal()` 消费该 verdict。该 verdict 仍是 split/source ledger bridge；删除条件仍是 P5 open-cutter / cross / T / segmented cutter 不因最终 shared-source purge 退化。

最新 noOriginal shared-source edge ledger：`OpenWireCompoundWireInfo` 已按 FreeCAD `WireJoinerP::getOpenWires(noOriginal=true)` 的 `source.findSubShapesWithSharedVertex(TopoShape(edge, -1)).empty()` 谓词记录每条 child-wire 的 edge / matched / unmatched 计数，并公开为 `open_wire_compound_no_original_shared_source_*` summary 与 per-entry history 字段。当前 P5/P6 gate 为 68 条 child-wire ledger、69 条 child edge、39 条 matched、30 条 unmatched；5 条 noOriginal candidate 中 4 条全匹配并 purge，`sketch-internal-face-branch-open-cutter` 固定为 candidate 但 2 条 child edge 只有 1 条 matched，因此不 purge。这只是把 noOriginal verdict 从黑盒布尔推进到 child-wire edge ledger，`purge_as_original_bridge.status` 仍保持 `covered_main_path`。

最新 producer ledger 边界：`result_wire_producer_ledger_entries` 现在由 `OpenWireCompoundWireInfo` child-wire slot 生成，不再在 summary 阶段从 EdgeInfo candidate 重新组装公开 entry；child-wire 层已删除 `resultWireProducerCandidate` 拷贝，改用 `resultWireProducerLedgerEntry` 作为 producer entry / noOriginal candidate gate，且该 gate 现在消费已分类的 `ResultWireProducerIdentity`，不再直接读取 raw EdgeInfo candidate bit；child-wire 层继续删除内部 `sourceEdgeProducerOutput` sidecar，公开 `openWireCompoundSourceEdgeProducerOutput` / JSON `open_wire_compound_source_edge_producer_output` 直接由 child-wire `producerLedgerEdge` 是否 materialized 推导，不再复制 EdgeInfo source-shape sidecar；`ResultWireProducerPlanLedger::producerOpenExportEdges` 仍是临时 bridge，但现在只在 `recordOpenWireCompoundLedger()` 建账边界直接按 scoped plan ledger 复制成 child-wire-local `producerLedgerEdge`，不再通过 `ResultWireProducerIdentity` 的 source-shape-ready 字段 gate；后续 producer wire 物化、open-export history 和 topo evidence 均读取 child-wire ledger，并输出 `open_wire_compound_producer_ledger_edge_materialized` 证明边界，不再回读 EdgeInfo producer shape（该 EdgeInfo shape 字段已删除）。EdgeInfo `resultWireProducerCandidate` 仍是内部临时诊断；删除条件仍是 `WireJoinerP::EdgeInfo / WireInfo / myShapesToReturn / MapperHistory / ElementMap` 生命周期能直接产生 openWireCompound child ownership。

最新 producer state 边界：空 `ResultWireProducerIdentity` 的默认状态已从旧 `TransitionalResultSlotCandidate` 改为 `Unpublished`；`Unpublished` 只允许表示没有 producer identity 的 open-export history / mapper evidence，不发布新的 `result_wire_producer_ledger_entries`。旧 `TransitionalResultSlotCandidate` 已列入 `generated_open_export_bridge.deleted_fields`，不得再作为公开 state 输出。

最新 producer gate：`source_shape_ready_derived_from_plan_ledger_producer_open_export_edge` 已进入 capability，source-shape-ready 分类谓词和同源 sidecar producer helper 现在直接由非空 `ResultWireProducerPlanLedger::producerOpenExportEdges` 推导；`producer_ledger_ready_gate_direct_plan_ledger` 固定内部 gate 已改成 producer-ledger-ready 口径并直接读取 scoped plan ledger，公开 `SourceShapeReady` 只保留为兼容 JSON 状态名；`ResultWireProducerIdentity::sourceShapeReady` 已删除，openWireCompound child-wire materialized 之后的输出 readiness 改由 child-wire `producerLedgerEdge` / `currentMemberProducerOutput` 与 producer state 表达，不再作为 EdgeInfo 分类缓存或 child-wire producer edge copy gate。`child_wire_producer_ledger_edge_copy_gate_from_plan_ledger` / `edge_info_identity_source_edge_output_gate_removed` / `result_wire_producer_identity_source_shape_ready_removed` 固定 child-wire producer edge 复制不再读取 EdgeInfo identity source-shape-ready 状态；`EdgeInfo::resultWireProducerSourceEdgeExportShape` 已删除。该改动把 source-shape-ready 从 EdgeInfo sidecar 收束到 scoped plan ledger，并把 child-wire copy gate 进一步从 EdgeInfo identity 移出，不改变 `generated_open_export_bridge.status=covered_main_path`，删除条件仍是补齐 FreeCAD `WireJoinerP::EdgeInfo / WireInfo / myShapesToReturn / MapperHistory` 生命周期后移除 EdgeInfo result-wire producer 字段。

最新 identity classifier 边界：`ResultWireProducerIdentity` 已删除 `hasSourceLineage`、`hasStrictRemoveSource`、`hasRemovedTarget`、`hasSameSourceRemoveLineage`、`hasFullAHistoryEvidence`、`finalGateEligible` 这类分类 scratch 字段；相同判断只在 `classifyResultWireProducerSlot()` 内作为局部谓词使用。identity 发布面收敛为 kind / state / blocker、producer index、child-wire index 与 `childWireBuilt`。

最新 EdgeInfo 输出 helper 边界：`EdgeInfo::openExportWire()` 已删除，缺 child-wire 的 open-export history 只能报 `missing_open_wire_compound_child_wire` 诊断，不能再从 EdgeInfo wire helper 重导出，也不能复制 EdgeInfo source lineage、split fragment、vertex replacement 或 producer identity payload；`resultWireProducerOpenExportEdge()` 仍是 producer 分类和 child-wire 建账的内部 evidence，不代表 WireJoiner full ledger。

最新 WireJoiner relation 边界：open-export relation 现在先在 `cad-core/src/part/wire_joiner.cpp` 由 `OpenWireCompoundWireInfo` child-wire ledger 分类为 `preserved / split / generated / deleted`，再由 `Sketch.InternalShape` history 和 `topo_shape.cpp` evidence 通过 `wire_joiner_history_relation_from_child_wire_ledger` 消费；`topo_open_export_relation_fallback_removed` 固定 topo 层从 source indices、split flags、producer kind 反推 relation 的 legacy fallback 已删除。该切片删除的是 topo evidence 的 relation 拼装 debt，尚未删除 `ResultWireProducerPlanLedger::producerOpenExportEdges`、EdgeInfo `resultWireProducer*`、`endpointMaterializationEvidenceVertices` 或 noOriginal split/source candidate bridge，因此 `generated_open_export_bridge` 和 `purge_as_original_bridge` 仍保持 `covered_main_path`。

最新 WireJoiner history event 边界：`WireJoinerHistoryRelation` 已进一步写入 request-local `WireJoinerHistoryEvent` 列表，runtime `wire_joiner_history_detail` 与 `Sketch.InternalShape.sketch_internal_history` 输出 `wire_joiner_history_events`、`wire_joiner_history_event_count` 与 `wire_joiner_history_event_from_child_wire_ledger_count`；open-export entry 和 topo evidence 只携带 `wire_joiner_history_event_index` / `wire_joiner_history_event_from_child_wire_ledger`。本轮 `topo_shape.cpp` 的 open-export mapper evidence 已通过 event index 查找 `SketchInternalHistoryContext::wireJoinerHistoryEvents`，并优先消费 event relation/source edge indices，公开 evidence 用 `wire_joiner_history_event_consumed_by_topo=true` 固定该边界。该 event 记录 relation、source lineage、splitter lineage、actual noOriginal purge 和 Modified / Generated fragment 标记，仍只证明 child-wire ledger 到 topo evidence 的可枚举账本边界；删除条件仍是补完 `WireJoinerP::aHistory -> MapperHistory -> ElementMap` 生命周期并移除 `ResultWireProducerPlan` / EdgeInfo result-wire producer debt。

### Diagnostic-only 边界

| 项 | 当前边界 | 诊断 |
| --- | --- | --- |
| Pad / Pocket `UpToShape` multi-face | 仍保留 diagnostic-only unsupported boundary，不和 taper history 混为一个 gap | `unsupported_subshape_kind` |
| Assembly unsupported JointType | RackPinion / Screw / Gears / Belt / Cylindrical 仍只暴露稳定 unsupported diagnostic | unsupported JointType diagnostics |

## 下一阶段候选任务

1. WireJoiner full ledger：只沿 FreeCAD `WireJoinerP::EdgeInfo / WireInfo / myShapesToReturn / MapperHistory / ElementMap` 路径推进，不用输出 pruning、fixture 特判或几何猜测当主路径。
   - 最新推进：`OpenWireCompoundWireInfo` 已承接 child-wire source lineage、splitter lineage、noOriginal purge candidate / match；`splitEdges()` fragment-to-source ledger 已区分 Modified / Generated / input EdgeInfo source sidecar，source lineage 使用原始 `sourceEdgeArray` identity，vmap replacement 留在 `sourceEdgeLedgerEdges_` vertex ledger；本轮新增 wire edge -> sourceEdgeArray index sidecar，P5 through/branch/cross/segmented/T/arc-lens 的旧 `split_fragment_identity_fallback_edge_info_count` 已归零；producer child-wire 输出已优先 `sourceEdgeLedgerEdges_` + split fragment producer ledger 的 vmap vertex ledger，P5 T/cross/segmented 的公开 result-slot 输出诊断已删除，closed source split（three-overlap）的旧 current-member closed-source result-slot bridge 诊断面与旧 `TransitionalResultSlotShapeStillUsed` blocker 已删除，但仍保留 3 个 result-slot endpoint materialization evidence 作为 FaceMaker/MapperHistory vertex identity 未完成前的债务；`CurrentMemberChildWire` 已改由 root/current-member child ledger 输出并用 `open_wire_compound_root_current_member_producer_output_wire_info_count` 固定，非 current-member producer 只在 child-wire endpoint materialization 阶段读取 result-slot evidence；open-export history 与 `topo_shape.cpp` producer evidence 已改为消费 `OpenWireCompoundWireInfo::wire`，`OpenWireCompoundWireInfo::producerLedgerWire` 已承接 child-wire 层 materialized producer wire，公开 `open_wire_compound_source_edge_producer_output` 现在由 `producerLedgerEdge` 直接推导，内部 `sourceEdgeProducerOutput` sidecar 已删除，并输出 `open_wire_compound_producer_ledger_wire_built`、`open_wire_compound_source_edge_producer_output` / `open_wire_compound_current_member_producer_output` 标记；本轮进一步输出 `open_wire_compound_producer_ledger_wire_from_source_vmap`、`open_wire_compound_producer_ledger_wire_from_result_slot_evidence`，并删除旧 `open_wire_compound_current_member_closed_source_result_slot_bridge*` 诊断面，把 result-slot endpoint materialization 固定为可删除债务而非隐式输出路径，并把剩余债务限定到 closed-source current-member child 的 endpoint identity；本轮新增 split-ledger vertex candidate 诊断，并让 topo mapper_history 以 `wire_joiner_current_member_vertex_multiplicity_blocked` 固定 three-overlap 的 3 个 producer event，确认候选存在但直接切换会改变 three-overlap InternalVertex multiplicity（18 -> 16）；本轮进一步记录 multiplicity gate：3 个 candidate 全部 `vertex_multiplicity_blocked`，且每个 child 的 2 个输出顶点都未被 member/split ledger 身份覆盖，同时 2 个都来自 result-slot-only 顶点身份；summary gate 已把该债务固定为 `output_unmatched_vertex_count=6` / `result_slot_only_vertex_count=6`，后续删除 result-slot endpoint evidence 必须让这两个总量同时归零；缺 child-wire 的 open-export history 只输出 `missing_open_wire_compound_child_wire` 诊断且不再携带 EdgeInfo lineage / vertex / producer payload，并由 `open_wire_compound_missing_child_wire_history_edge_info_count=0` 固定当前主路径不再 EdgeInfo 重导出；`splitFromInputEdge` 改为来自 splitter history fragment ledger，并同步到 `wire_joiner_ledger`、`wire_joiner_history_detail`、`Sketch.InternalShape` producer evidence 与 `topo_shape.cpp` 的 NamedShape / ElementMap relation / diagnostic 消费；active `helper_open_export_override` 不再公开输出，legacy helper reason string 已从代码路径删除，child-wire 层已无 helper 字段，EdgeInfo 与 child-wire 的 producer identity candidate、source-edge producer 输出与 root/current-member producer lifecycle 均改为 result-wire producer ledger 字段；本轮删除了 `getOpenWires()` 的 legacy EdgeInfo 重导出分支和最终 shared-source 输出 pruning，但 EdgeInfo result-wire producer candidate ledger 仍是内部临时诊断。这只是 bridge 收敛，不代表 `covered_full`。
2. UpToShape multi-face：若要从 diagnostic-only 进入实现，先拆 Pad / Pocket UpToShape 多面选择的 FreeCAD 源码依据、fixture 和 diagnostics，不复用已收口的 taper history gap 名称。
3. 后续新增缺口必须拆成具体 producer、lifecycle、feature-family、JointType 或 adapter contract 项；不得恢复旧 local-history gap 清单、旧 taper known-gap topo marker 或 broad `known_gaps`。
