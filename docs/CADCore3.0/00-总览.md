# CAD Core 3.0 总览

## 目标

CAD Core 3.0 的目标是建立 FreeCAD 几何库复刻版的长期主线：用本地 FreeCAD 源码定义语义，用 `cad-core` 承载无状态几何计算，用 expected fixture / oracle / diagnostics 固定行为边界。

3.0 仍然不复刻 FreeCAD GUI、Workbench、ViewProvider、TaskPanel 或跨请求后端会话。它复刻的是前端长期编辑所需的几何建模核心语义：DocumentObject graph、属性链接、Sketcher、Part、PartDesign、TopoNaming、Link、Assembly 和 adapter contract。

## 当前基线

CAD Core 2.0 已经完成以下能力：

- `app / base / graph / runtime / sketcher / part / part_design / mesh / assembly / adapters` 分层成立。
- FreeCAD 风格 `Objects[] / Name / ID / TypeId / Properties` 输入成立。
- Sketch + Body + Pad / Pocket / FeatureExtrude / FeatureBase 主链成立。
- ExternalGeometry flags、ReferenceShadow resolver、`elementReferenceUpdates`、`documentObjectUpdates` 基础路径成立。
- FaceMaker / WireJoiner producer evidence 已进入 Sketch InternalShape 主路径。
- Sketcher solver diagnostics 已有 C3-M3 收口切片：同一 target 的 Horizontal / Vertical 冲突、重复 orientation 约束冗余、同一 datum target 不同值冲突、已迁移约束的 malformed index / datum、Block 与同几何一元约束组合的 partial redundancy warning、空约束与已迁移约束的 full-rank request-local DoF 正数欠约束状态，以及整线 Horizontal / Vertical、单端点 `DistanceX/Y` 坐标、Circle `Radius/Diameter`、Line `Distance` 长度、Arc `Distance` 长度、PointOnObject 线端点投影到 Line target、Parallel / Perpendicular 双线关系、Perpendicular Line + Circle/Arc midpoint、Equal line/circle/arc、Tangent Line + Circle/Arc、Symmetric point-pair about Line / center point / Arc endpoint、Symmetric + Tangent coupled relation 的 request-local 几何写回；conflict / redundant / malformed 仍会在 profile 构造前早退，partial redundancy 只发 warning 并继续 profile。
- taper ThruSections、RefineModel 当前覆盖子路径、DressUp SupportTransform AddSubShape slot、Face 选择展开证据、Chamfer 参数变体、Draft 显式 DatumPlane / DatumLine 与自动 neutral-plane guess、PartDesign Thickness no-face copy / FaceN Mode-Join 参数变体 / multi-solid fuse history、失败诊断与链式 Pattern history、transformed / pattern AddSub ownership 子路径已进入统一 MapperHistory / diagnostics。
- PartDesign Body 的外部 `BaseFeature` -> 内部 `FeatureBase` 创建、`Body.Group` 同步、首个 solid feature `BaseFeature` 回写、删除旧 Tip 后 `Body.Tip` / 下一 solid feature `BaseFeature` reroute、显式 `Body.Origin` child link 到 Body placement 的归属、按 `Role` 将外部 Origin datum 链接改写到 Body 自身 Origin datum 的建议，以及 Body Group Add/Sub 按顺序重放并在 Tip 停止的边界已有第一切片，输出到无状态 `documentObjectUpdates`、请求内 placement 账本或 Body replay 元数据。
- PartDesign Hole 的 ThreadType / ThreadSize 表、`Resources/Hole` head cut definition、ModelThread pipe-shell 几何、profile Edge/Vertex -> tool Face mapper history、`protoHole` / `protoThread` compound tool shape 和 subtractive Body cut history 已有第一切片；`cad-core/fixtures/p7/hole-supported-model-thread-counterbore.json` 已正式匹配 FreeCAD threaded + ModelThread + Counterbore native oracle，Body 拓扑固定为 50 faces / 106 edges / 60 vertices。
- Part Workbench `Part::Offset` 面源 offset 已接入 `BRepOffsetAPI_MakeOffsetShape` maker history，`cad-core/fixtures/c3m4/part-offset-face.json` 固定 `Plane.* -> Offset.*` generated history；`Fill=true` 已按 FreeCAD free-bound perimeter + `BRepBuilderAPI_Sewing` 路径补第一片，`cad-core/fixtures/c3m4/part-offset-face-fill.json` 固定 `part_offset_fill:sewing_history`；solid source 不再在 executor 层早退，`makeElementSolid()` shell / compsolid helper 已有 `cad-core/fixtures/c3m1/make-element-solid-from-shell.json` probe；`Part::Offset2D` planar face / no-fill、closed face / fill、Edge/Wire no-fill、Edge/Wire single open-wire fill 均已通过 Part 层 `MakeOffsetFix` 风格路径和 FaceMaker / connector-edge 后处理覆盖，分别固定 `part_offset2d:face_no_fill_makeoffset`、`part_offset2d:face_fill_closed_makeoffset`、`part_offset2d:wire_no_fill_makeoffset`、`part_offset2d:wire_fill_open_makeoffset`；`Part::Compound` Links executor 已按 FreeCAD 去重保序与 `makeElementCompound(shapes)` 补第一片，并在 `NamedShape.child_element_maps` 中记录 FreeCAD `mapSubElement(shapes)` 的 compound source range preserve；嵌套 Compound 已按 `ElementMap::addChildElements()` 的 grand child map resolution 语义组合递归 source range，`cad-core/fixtures/c3m1/element-map-child-map-recursive-compound.json` 固定 `element_map_child_map:recursive_source_ranges`；`Part::Offset2D` compound source 在 `Intersection=false` 时按 child 递归并强制 compound，`cad-core/fixtures/c3m4/part-offset2d-compound-open-wire-fill.json` 固定 `part_compound:make_element_compound`、`element_map_child_map:preserve_source_ranges` 与 `part_offset2d:compound_child_recursive`；`Part::Offset2D` compound source 在 `Intersection=true` 时已按 FreeCAD collective 分支收集非 compound 子 shape 并用同一个 `MakeOffsetFix` maker offset，`cad-core/fixtures/c3m4/part-offset2d-compound-intersection-no-fill.json` 固定 `part_offset2d:compound_collective_makeoffset`；Part Workbench `Part::Thickness` single-solid FaceN 第一片已接入 `BRepOffsetAPI_MakeThickSolid`，`cad-core/fixtures/c3m4/part-thickness-box-face.json` 固定 `part_thickness:make_thick_solid`，`cad-core/fixtures/c3m4/part-thickness-box-face-rectoverso-tangent.json` 固定 `Mode=RectoVerso`、`Join=Tangent` 到 FreeCAD `effective_join=Intersection` 的 mode/join oracle。
- Part Workbench `Part::Section` 已按 `FeaturePartSection.cpp::Section::makeOperation()` / `FCBRepAlgoAPI_Section::setAutoFuzzy()` 补齐第一条稳定 history 验证，`cad-core/fixtures/c3m4/part-section-stable-history.json` 固定 `Plane.Edge* -> Section.Edge*` source-qualified history 与 `Box.Edge*` terminal deleted history。
- Link transaction、Link reference lifecycle、Assembly representative fallback / request-local Ondsel solver adapter、CLI / C ABI / Worker / WASM adapter contract、streaming mesh limit 和 binary mesh payload 已有代表路径；其中 Link CopyOnChange 已推进到完整 deep copy lifecycle，Assembly 已覆盖 grounded Fixed / Revolute / Slider / Ball / Distance / Angle 真实 `runPreDrag()` fixture，并通过双 grounded + Distance 矛盾用例固定 `validateNewPlacements()` 等价拒绝写回。

当前 capability 输出已清空 2.0 遗留的笼统 `known_gaps`；阶段性实现必须继续拆到 producer / lifecycle / adapter / object metadata 级具体项，不能用空 gap 伪装完整迁移。

- `topo_history.remaining_gaps`: 空；ShapeFix deleted-small-edge、ShapeFix Root modified history、ShapeFix generated-empty review、ElementMapPolicy::Drop、ElementMapPolicy::Propagate `makeElementWires` edge identity、ElementMapPolicy::Propagate `makeElementShell` source-to-shell map、ElementMap child-map source range preserve、ElementMap child-map recursive source range、ElementMap child-map postfix source range、ElementMap hashed child-map key evidence、ambiguous split reselect、import shape ElementMap、Part Offset2D collective MakeOffsetFix、Sketch InternalShape bounded-face/open-wire mixed oracle、DressUp multi-selection history / Chamfer 参数变体 / Draft 显式 DatumPlane-Line / 自动 neutral-plane guess / Thickness 参数变体 / multi-solid fuse history / failure diagnostics / chain Pattern history、transformed/pattern full history 与 Hole `findHoles()` profile source history / threaded ModelThread head-cut native oracle 已拆为第一批 capability / diagnostic；`topo_history.producer_matrix` 已把 prism、boolean、section、general_fuse、refine、ShapeFix、import、Link retag、Sketch InternalShape、taper、DressUp、transformed、Hole 拆成 producer 级状态；DressUp / Hole producer remaining 已清空，笼统 `complete_mapper_history` 不再作为 capability gap 保留。
- `link_transaction.remaining_gaps`: 空；ShowElement create / claim / sync / delete / toggle-off、plain group child cache、nested plain group traversal、orphan reclaim、stale child delete、ElementCount owner lists sync、ElementList owner/child sync、CopyOnChange owned child sync、writeback transport、完整 deep copy lifecycle 和 touched 后 sync 已通过 `documentObjectUpdates` 表达，请求图保持 immutable。`copy_on_change_deep_copy_lifecycle.status=covered_full`，copy 后对象可由前端保存进下一次请求 graph 并作为普通对象 recompute。
- `link_reference_lifecycle.remaining_gaps`: 空；source object rename 已有 `ReferenceShadow.targetId` 唯一匹配写回第一切片，direct LinkSub `$OldLabel.` 到当前 target `Label`、nested Link/Group `$OldLabel.` path 写回、cross-document `FullSubList` nested label + `Document` restore 合并写回、duplicate target Label ambiguity 诊断、XLink `Document` name/label restore、stamp/hash mismatch warning、missing external document graph diagnostic、pending/partial reload diagnostic 与 external document unloaded diagnostic 已有第一切片。
- `sketcher.solver.remaining_gaps`: 空；full-rank request-local DoF、dependent parameter group metadata、blocked dependent group metadata，以及 Symmetric + coupled curve relation geometry update 已覆盖。
- `part_design.body_chain.remaining_gaps`: 空；`body_basefeature_featurebase_create`、`body_basefeature_group_sync`、`body_feature_basefeature_sync`、`body_tip_deleted_feature_reroute`、`body_feature_basefeature_delete_reroute`、`Body.Origin` 显式 child link placement、`body_origin_datum_relink` 与 Add/Sub `stop_at_tip` replay 已有第一切片。
- `part_design.hole.remaining_gaps`: 空；thread table、head cut resource definition、ModelThread pipe-shell、point profile + counterbore、`findHoles()` profile source mapper history、compound tool shape、subtractive cut history 和 threaded + ModelThread + Counterbore native oracle 已有第一切片。`hole-supported-model-thread-counterbore` 已匹配 FreeCAD native 拓扑与体积。
- `part_workbench.offset.remaining_gaps`: 空；这里的 `thickness` 指 Part Workbench offset / thickness family，不是本轮已覆盖的 `PartDesign::Thickness` DressUp；面源 `Part::Offset`、`Fill=true`、solid source `makeElementSolid()`、`Part::Offset2D` face/no-fill、closed face/fill、open wire/no-fill、open wire/fill、compound child recursion、compound collective MakeOffsetFix 和 `Part::Thickness` single-solid FaceN / mode-join oracle 已走 topo maker / sewing / solid helper / face rebuild / FaceMaker / wire offset / thick-solid maker，不在 executor 或 adapter 输出端猜 source ownership。
- `assembly.remaining_gaps`: 空；`src/3rdParty/OndselSolver` 已初始化并由 `cad-core` 条件链接，grounded Fixed / Revolute / Slider / Ball / Distance / Angle 用例已走真实 `ASMTAssembly::runPreDrag()`，输出 `real_ondsel_solver` 和 `assembly_set_placement` writeback；`assembly-invalid-grounded-distance-real-solver` 固定 grounded object moved 时 `invalid_assembly_solver_result` 且不写回；`assembly-grounded-distance-joint-real-solver` 与 `assembly-multi-component-placement-writeback` 固定 `documentObjectUpdates` 应用到下一次 request graph 后 no-op 稳定、多 component 写回顺序和 `Placement` 目标字段。无 grounded part / 未链接 Ondsel 的路径仍是 stateless representative fallback，不是 full coverage 依据。
- `adapters.remaining_gaps`: 空；`cad_core_worker_recompute_json`、`cad_core_wasm_recompute_json`、streaming mesh limits、`mesh_limit_exceeded` diagnostic 和 `cad_core_mesh_binary_json` binary payload metadata 已覆盖。
- `object_metadata.remaining_gaps`: 空；`part_design.pad`、`part_design.pocket` 与 `part.extrusion` 的 taper history metadata 已由 `object_metadata.local_history.taper_history.status=covered_full` 固定。Pad / Pocket / Part::Extrusion taper fixture 均输出对象级 `topo_naming_history=maker_history:taper_thru_sections`，不再输出旧 known-gap topo metadata；对应 `NamedShape` 暴露 `element_map_status=history_partial` 与 `mapper_history.maker_stage=maker_history`。
- `known_gaps`: 空。

## 2026-06-06 C3-M8 freeze 口径

- 已降级后推进并完成 Assembly 收口：Assembly full solver 口径不再由 representative 冒充；当前为 `representative_solver_adapter.status=covered_representative` fallback、`ondsel_solver_adapter.status=covered_full`、`placement_writeback.status=covered_full`，`assembly.remaining_gaps=[]`。placement writeback 的 full 依据只覆盖无状态 `documentObjectUpdates` 应用生命周期，不表示 CAD Core 维护跨请求 Assembly session。
- 原整改口径：Link CopyOnChange 曾降级为 `copy_on_change_writeback_contract.status=covered`、`copy_on_change_deep_copy_lifecycle.status=partial`，防止 writeback contract 冒充完整 lifecycle。
- 已收口：Link CopyOnChange 后续真实语义实现已补 `cad_core/app/copy_on_change.h` 与 `cad-core/src/app/copy_on_change.cpp`，Link executor 只调用 App 层 API；`copy_on_change_deep_copy_lifecycle.status=covered_full`，writeback contract 仅保留为 transport 口径。
- 已收口：Pad / Pocket / Part::Extrusion taper local history metadata 已从旧的 local history gap 清单移到 `object_metadata.local_history.taper_history.status=covered_full`；FreeCAD 依据是 `FeatureExtrusion.cpp::Extrusion::extrudeShape()` 与 `FeatureExtrude.cpp` taper 分支调用 `ExtrusionHelper::makeElementDraft()`，再由 `TopoShapeExpansion.cpp::MapperThruSections` 消费 `GeneratedFace(s)` / `FirstShape()` / `LastShape()`。
- 已标注：WireJoiner `generated_open_export_bridge` 与 `purge_as_original_bridge` 仍是 `covered_main_path` bridge，不是 `covered_full`；本轮验证了只把最终 `getOpenWires()` purge 改成 shared-source-vertex 谓词会误删 P5 open-cutter split fragment。当前已补 `sourceEdges_` 原始 sourceEdgeArray ledger、`sourceEdgeLedgerEdges_` mutable internal sourceEdges / vmap vertex ledger、`splitEdges()` fragment-to-source ledger（Modified / Generated / input EdgeInfo source sidecar，并拆分 source identity fallback / history-shape geometry bridge 诊断），让 split source lineage 使用原始 `sourceEdgeArray` identity 且不再把 mutable vmap ledger 当 source ownership；本轮新增 wire edge -> sourceEdgeArray index sidecar 后，P5 through/branch/cross/segmented/T/arc-lens 的 `split_fragment_identity_fallback_edge_info_count=0`。closed-cycle generated open-export 预判从 bounded-face 结果边采样改为读取 Modified split fragment ledger、`OpenWireCompoundWireInfo` child-wire source/noOriginal verdict，并让 `topo_shape.cpp` 的 NamedShape / ElementMap WireJoiner producer evidence 优先消费 child-wire source lineage / purge verdict；producer child-wire 输出已优先使用 `sourceEdgeLedgerEdges_` vmap vertex ledger，`CurrentMemberChildWire` 已改由 root/current-member child ledger 输出并用 `open_wire_compound_root_current_member_producer_output_wire_info_count` 固定，`ResultWireProducerPlan` scoped endpoint debt 只保留为 endpoint materialization evidence。`splitFromInputEdge` 已改为来自 splitter history 账本，不再用端点几何比较判定 split fragment。已由 child-wire `ExportedWithoutTransitionalSlot` 导出的 entry 不再输出 active `helper_open_export_override`，legacy helper reason string 已从代码路径删除，child-wire 层已无 helper 字段，EdgeInfo 与 child-wire 的 producer identity candidate、source-edge producer 输出与 root/current-member producer lifecycle 均改为 result-wire producer ledger 字段；`ResultWireProducerPlan` scoped endpoint debt 不再参与 producer classification，`resultWireProducerLocatorEdge()` 已删除，分类路径直接使用 `resultWireProducerOpenExportEdge()` 读取 scoped plan ledger / EdgeInfo edge fallback，`resultWireProducerOpenExportEdge()` / `openExportWire()` 不再读取 result-slot endpoint evidence 作为 child-wire 输出，current-member blocker 诊断已从 child-wire ledger 读数，物化侧 result-slot endpoint evidence 仍待删除。公开 `purge_bridge` history/detail 字段已删除，producer identity 改为不依赖 helper flag 消费；本轮已删除 `getOpenWires()` 无 child-wire ledger 时从 EdgeInfo 重导出 open wire 和最终 shared-source 输出 pruning 的 legacy 分支，主路径只消费 `OpenWireCompoundWireInfo`。删除条件还必须补完整 vmap replacement、补齐 `openWireCompound` child-wire ownership 与 `WireInfo / EdgeInfo` lifecycle，直到 EdgeInfo result-wire producer candidate ledger / result-slot 诊断字段可删。
- 本轮新增 three-overlap current-member split-ledger vertex candidate 的 topo 诊断：`Sketch.InternalShape.mapper_history` 输出 3 个 `wire_joiner_current_member_vertex_multiplicity_blocked` producer event；candidate 仍不改变 openWireCompound 输出拓扑，不能用于把 WireJoiner bridge 升级为 `covered_full`。
- 最新 gate：`open_wire_compound_current_member_split_ledger_vertex_multiplicity_blocked_wire_info_count=3`，每个 three-overlap blocked child 当前 `candidate_vertex_count=2`、`output_vertex_count=2`、`output_unmatched_vertex_count=2`，且 `result_slot_only_vertex_count=2`；summary gate 已固定 `open_wire_compound_current_member_split_ledger_output_unmatched_vertex_count=6`、`open_wire_compound_current_member_split_ledger_result_slot_only_vertex_count=6` 与 `open_wire_compound_current_member_split_ledger_output_candidate_matched_vertex_count=0`。本轮已把这些 child 的 producer state 收紧为 `ChildWireReady / CurrentMemberVertexMultiplicityBlocked`，旧 `open_wire_compound_current_member_closed_source_result_slot_bridge*` 诊断面已删除，旧 `TransitionalResultSlotShapeStillUsed` blocker 也已删除；未 materialize 的 producer 改由具体 `SourceShapeIdentityNotReady` 或 `CurrentMemberChildWireIdentityNotReady` 表达。当前 blocker 诊断的 result-slot-only 与 candidate-output identity 读数都来自物化后的 `OpenWireCompoundWireInfo::wire` child ledger，不再回读 EdgeInfo sidecar；删除 result-slot endpoint materialization 前必须让这些输出顶点由 member/split 或 candidate ledger 身份覆盖，而不是继续来自 result-slot-only 顶点身份。
- 最新推进：three-overlap blocker 已从 count-only gate 推进为 `OpenWireCompoundWireInfo` child-wire vertex debt ledger，记录 member/split vertices、candidate vertices、每个 output vertex 是否匹配 member/split ledger、是否匹配 candidate wire ledger、以及是否仍是 result-slot-only identity；`wire_joiner_ledger`、`wire_joiner_history_detail` 和 topo evidence 新增 `open_wire_compound_current_member_split_ledger_vertex_debt_*` / matched / candidate-matched / ledger-count 字段。当前 `output_matched_vertex_count=0`、`output_candidate_matched_vertex_count=0`、`output_vertex_ledger_count=6`，说明仍未完成 vertex identity 替代，不能升级 `covered_full`。
- 最新 EdgeInfo sidecar 边界：`EdgeInfo result-slot sidecar` 已删除；`applyResultWireProducerPlan()` 现在只把 endpoint vertices 写入 `ResultWireProducerPlan` scoped endpoint vertex debt，plan 层 result-slot endpoint edge seed 已删除；`recordOpenWireCompoundLedger()` 只在 openWireCompound child-wire 建账边界复制一次为 `OpenWireCompoundWireInfo::endpointMaterializationEvidenceVertices`。后续 producer child-wire materialization 读取 child-wire endpoint vertex ledger，不再保存或消费 child-wire-local result-slot edge shape；旧 `resultSlotEndpointMaterializationEvidenceVertices` 字段名已删除，但 public JSON 的 result-slot endpoint diagnostic 仍保留为剩余 debt 说明，`ResultWireProducerPlan` scoped endpoint vertex seed 仍是待删除 debt，不是 `covered_full`。
- 本轮继续删除 child-wire 内部 result-slot 字段名：`producerLedgerWireFromResultSlotEvidence` 已改为 `producerLedgerWireFromEndpointMaterializationEvidence`；公开 `open_wire_compound_producer_ledger_wire_from_result_slot_evidence*` 诊断仍保留，用来显示当前还有 3 个 three-overlap child 依赖 endpoint materialization debt。
- 最新 source/vmap endpoint ledger：`OpenWireCompoundWireInfo` 现在记录每条 openWireCompound child-wire 的输出端点有多少能直接用 request-local source/vmap/split ledger 的 `TopoDS_Vertex` 身份覆盖，公开为 `open_wire_compound_source_vmap_endpoint_ledger_*`。P5 cross / segmented / T 的 source-vmap producer 仍为 4 且 result-slot 计数为 0，endpoint matched 分别固定为 12 / 12 / 11；three-overlap 固定 `wire_info_count=15`、`output_vertex_count=30`、`matched_vertex_count=0`，说明剩余 3 个 result-slot endpoint debt 不能靠 source/vmap 顶点身份直接删除。
- 最新 endpoint provenance ledger：`OpenWireCompoundWireInfo::EndpointProvenance` 已记录最终 child-wire 输出端点对 source/vmap、current-member candidate 与 endpoint materialization evidence 的 identity 命中，并通过 `wire_joiner_ledger`、`wire_joiner_history_detail`、`Sketch.InternalShape` history 与 topo evidence 输出 `open_wire_compound_endpoint_provenance_*`。three-overlap 固定 `wire_info_count=15`、`output_vertex_count=30`、`source_vmap_matched=0`、`candidate_matched=0`、`endpoint_materialization_matched=6`、`unmatched=24`；3 个 blocked child 各自仍是 `endpoint_materialization=2`。旧 source-vmap endpoint ledger 仍是 producer materialization gate，新 provenance 才表达最终输出端点状态；这仍只是删除 endpoint materialization debt 前的 child-wire 账本证据，不改变 `generated_open_export_bridge.status=covered_main_path`。
- 最新 WireJoiner history event ledger：open-export relation 已从单个 typed entry 字段继续推进到 request-local `WireJoinerHistoryEvent`；`wire_joiner_history_detail` 和 `Sketch.InternalShape.sketch_internal_history` 输出 `wire_joiner_history_events`、`wire_joiner_history_event_count` 与 `wire_joiner_history_event_from_child_wire_ledger_count`，entry / topo evidence 通过 event index 与 child-wire 来源标记绑定。`topo_shape.cpp` 的 open-export mapper evidence 已通过 event lookup 消费 event relation/source lineage，并输出 `wire_joiner_history_event_consumed_by_topo=true`；该 event 记录 relation、source edge indices、splitter lineage、noOriginal actual purge 和 Modified / Generated fragment 标记，但仍未替代 FreeCAD `TopoShape::makeShapeWithElementMap(..., MapperHistory(aHistory), sourceEdges, op)` 的完整消费路径，因此 bridge 仍是 `covered_main_path`。
- 本轮继续收束 noOriginal purge 边界：`sourceEdgeArrayOriginalOpenEdgeCandidate()` 已删除，candidate 由 `OpenWireCompoundWireInfo` child-wire ledger 的 source vertex / `splitFromInputEdge` / producer state 字段计算；`updateOpenWireCompoundNoOriginalPurgeVerdict()` 统一更新 child-wire shared-source match / actual purged verdict，`getOpenWires(noOriginal)` 只消费 `openWireCompoundChildWirePurgedByNoOriginal()`。该 verdict 仍是 split/source ledger bridge 口径，不能升级为 `covered_full`。
- noOriginal shared-source 判断已拆成 child-wire edge ledger：`open_wire_compound_no_original_shared_source_*` 按 FreeCAD `getOpenWires(noOriginal=true)` 对每条 child edge 执行 shared-vertex source 检查，当前 P5/P6 为 68 条 child-wire ledger、69 条 child edge、39 matched、30 unmatched；5 条 candidate 中 4 条 purge，`sketch-internal-face-branch-open-cutter` 固定为 candidate 但有 1 条 unmatched edge 所以不 purge。该字段只证明 verdict 账本化，不代表 `purge_as_original_bridge` 已 full。
- 本轮继续收束 producer ledger 输出边界：`result_wire_producer_ledger_entries` 改为从 `OpenWireCompoundWireInfo` child-wire slot 发布，不再由 summary 根据 EdgeInfo candidate 重新组装公开 entry；child-wire 层已删除 `resultWireProducerCandidate` 拷贝，改用 `resultWireProducerLedgerEntry` 作为 producer entry / noOriginal candidate gate，且该 gate 现在消费已分类的 `ResultWireProducerIdentity`，不再直接读取 raw EdgeInfo candidate bit；child-wire 层继续删除内部 `sourceEdgeProducerOutput` sidecar，公开 `openWireCompoundSourceEdgeProducerOutput` / JSON `open_wire_compound_source_edge_producer_output` 直接由 child-wire `producerLedgerEdge` 是否 materialized 推导，不再复制 EdgeInfo source-shape sidecar；`ResultWireProducerPlanLedger::producerOpenExportEdges` 仍是临时 bridge，但现在只在 `recordOpenWireCompoundLedger()` 建账边界直接按 scoped plan ledger 复制成 child-wire-local `producerLedgerEdge`，不再通过 `ResultWireProducerIdentity` 的 source-shape-ready 字段 gate；后续 producer wire 物化、open-export history 和 topo evidence 均读取 child-wire ledger，并输出 `open_wire_compound_producer_ledger_edge_materialized` 证明边界，不再回读 EdgeInfo producer shape（该 EdgeInfo shape 字段已删除）。EdgeInfo `resultWireProducerCandidate` 仍保留为内部临时诊断，不能升级为 `covered_full`。
- 本轮删除公开 producer state 的旧 result-slot 默认名：空 `ResultWireProducerIdentity` 现在输出 `Unpublished`，只表示没有 child-wire producer identity；旧 `TransitionalResultSlotCandidate` 已加入 `generated_open_export_bridge.deleted_fields`，P5/P6 history 与 mapper evidence 不再输出该旧状态名。公开 producer ledger 当前仍只由 child-wire entry gate 发布，不因 `Unpublished` 新增 entry。
- 最新 identity 收束：`ResultWireProducerIdentity` 已删除 source-shape-ready 之外的内部 classifier booleans；source lineage、strict Remove、removed target、full aHistory 等判断只作为 `classifyResultWireProducerSlot()` 局部谓词存在。identity 只保留 kind / state / blocker、producer index、child-wire index 与 `childWireBuilt` 发布状态，不再缓存分类过程 scratch。
- 本轮继续收束 source-shape-ready 消费边界：source-shape-ready 分类谓词和同源 sidecar producer helper 现在直接由非空 `ResultWireProducerPlanLedger::producerOpenExportEdges` 推导；`producer_ledger_ready_gate_direct_plan_ledger` 固定内部 gate 已改成直接读取 plan ledger，公开 `SourceShapeReady` 只保留为兼容 JSON 状态名；`ResultWireProducerIdentity::sourceShapeReady` 已删除，openWireCompound child-wire materialized 之后的输出 readiness 改由 child-wire `producerLedgerEdge` / `currentMemberProducerOutput` 与 producer state 表达，不再作为 EdgeInfo 分类缓存或 child-wire producer edge copy gate。`EdgeInfo::resultWireProducerSourceEdgeExportShape` 已删除，raw EdgeInfo source-shape 标记和 EdgeInfo identity 都不能再单独触发 child-wire producer 输出。删除条件仍是补完整 FreeCAD `WireJoinerP::EdgeInfo / WireInfo / myShapesToReturn / MapperHistory` 生命周期后，移除 EdgeInfo result-wire producer candidate 字段。
- 本轮继续收束 EdgeInfo 输出 helper 边界：`EdgeInfo::openExportWire()` 已删除，公开 open-export history / topo evidence 没有 EdgeInfo wire 重导出入口；缺 child-wire 的 history 分支现在只保留 `missing_open_wire_compound_child_wire` 诊断，不再复制 EdgeInfo source lineage、split fragment、vertex replacement 或 producer identity payload；`resultWireProducerOpenExportEdge()` 仍保留为内部 producer 分类和 child-wire 建账 evidence，不能据此升级为 `covered_full`。
- 本轮新增 WireJoiner child-wire relation 边界：open-export 的 `preserved / split / generated / deleted` 关系现在由 `WireJoiner` 根据 `OpenWireCompoundWireInfo` child-wire ledger 发布为 `wire_joiner_history_relation`，并通过 `wire_joiner_history_relation_from_child_wire_ledger` 固定 topo evidence 只消费 typed relation；`topo_open_export_relation_fallback_removed` 固定 `topo_shape.cpp` 旧字段推断 fallback 已删除。该改动删除的是 topo relation 拼装债务，不代表 `MapperHistory(aHistory)` / `ElementMap` full。
- 本轮排除：GCS / Sketch solver；`full_solver_dof`、`request_local_solver_dof`、`gcs_solver_dof` 不在本轮处理范围。
- Frozen 旧单 subshape `ReferenceShadow.brep` 快照复用已有第一切片；Frozen / Missing 缺失源对象且无 BREP 时已有 `missing_external_geometry_snapshot` 诊断；Missing 未解析但带单 subshape BREP 时可复用旧快照并保留 `Missing` flag；Detached 已能输出删除 `ExternalGeometry.SubSet` link 的 `documentObjectUpdates`；Detached / Frozen / Missing 的原生旧 `ExternalGeo` 几何池已有无状态请求模型，`ExternalGeo` 条目复用 sketch geometry schema 并携带 `Ref` / `RefIndex` / `ExternalFlags`；Missing 引用可重新解析时的 flags 清理已有第一切片。
- 后续如果发现新的 FreeCAD / OCCT 语义缺口，不能恢复 `known_gaps` 或旧 local-history 清单这类笼统占位；必须拆成具体 producer、lifecycle、feature-family、JointType 或 adapter contract 项，标明 FreeCAD 源文件、CAD Core 落点、fixture / diagnostic 和删除条件。
- C3-M8 后最终状态表和真实剩余项见 `06-C3-M8后续收口清单.md`；该清单只冻结当前 `covered_full`、`covered_main_path`、`covered_representative` 和 diagnostic-only 边界，不把主路径可用写成完全收敛。

## 3.0 分阶段路线

| 阶段 | 主线 | 目标 |
| --- | --- | --- |
| C3-M0 | FreeCAD 语义矩阵 | 按 FreeCAD 模块建立复刻矩阵、gap 分级、oracle 队列和当前 capabilities 对照 |
| C3-M1 | TopoNaming 完全体 | 补 ShapeFix history、import shape ElementMap、full MapperHistory producer 生命周期 |
| C3-M2 | ExternalGeometry native 生命周期 | 旧 `ExternalGeo` 几何持久复用、Missing 可解析清理、Frozen / Missing `ReferenceShadow.brep` 单 subshape 快照复用、Detached link-list + native pool 写回、cross-document nested label writeback 与 external document pending/reload diagnostics 已有第一切片 |
| C3-M3 | Sketcher 复刻 | 补 solver-facing 约束语义、复杂 ExternalGeometry projection/intersection 和 sketch diagnostics |
| C3-M4 | Part Workbench 复刻 | 系统覆盖 Part primitive、Boolean、Extrude/Revolve/Sweep/Loft、Offset/Thickness、import/export history |
| C3-M5 | PartDesign 完整生态 | 补 full transformed / pattern history、DressUp 复杂参数、Body chain writeback、Hole 表驱动、Loft/Pipe/Revolution/Groove 等 family |
| C3-M6 | Link / Assembly 完整生命周期 | 补 `_ChildCache`、copy-on-change deep copy、document hash、Ondsel solver 和 placement write-back |
| C3-M7 | Runtime / adapter 产品化 | 补 Worker / WASM、streaming mesh、binary mesh、资源限制和前端错误码映射 |
| C3-M8 | FreeCAD parity freeze | 收敛 oracle、capabilities、known gaps、文档和回归门禁，形成 3.0 冻结基线 |

## 推荐推进顺序

1. 先做 C3-M0。没有模块矩阵，后续会继续靠局部 fixture 驱动，无法判断“复刻 FreeCAD 几何库”完成度。
2. 再做 C3-M1 / C3-M2。TopoNaming 与 ExternalGeometry 是所有长期引用恢复的底座。
3. 再做 C3-M3 / C3-M4 / C3-M5。Sketcher、Part、PartDesign 的 feature 扩展必须消费统一 history，而不是重新创建局部 mapper。
4. 最后做 C3-M6 / C3-M7。Link、Assembly、Web adapter 依赖前面的引用恢复和 maker history，否则容易把语义泄漏到前端或 adapter。
5. C3-M8 只做冻结，不新增大语义。

## 非目标

- 不实现 FreeCAD GUI / ViewProvider / Workbench / TaskPanel。
- 不把 BREP 作为前端或后端长期状态；唯一例外仍是 `ReferenceShadow.brep` 的旧单 subshape snapshot。
- 不维护跨请求后端 document session；前端的 `DocumentObject graph` 仍是唯一真实持久数据。
- 不用 fixture 名称、输出顺序、bbox、面积、几何类型排序、source edge 猜测来修正引用。
- 不把 Assembly solver、Worker / WASM 或 Web adapter 作为 TopoNaming 主线的前置条件。

## 3.0 完成判定

3.0 完成时应满足：

- FreeCAD 语义矩阵中的 P0 / P1 项均有源码依据、cad-core 落点、fixture / oracle 或明确 `known_gap`。
- ShapeFix、Refine、taper、transformed、DressUp、Part Boolean、import shape、Link retag 后的 source ownership 都进入统一 MapperHistory / ElementMap 生命周期。
- ExternalGeometry、ReferenceShadow、StableSubList、FullSubList、LinkSub、document hash 和 rename recovery 的成功 / 失败路径都有结构化更新或 diagnostics。
- Sketcher / Part / PartDesign 常用建模链可以在前端长期编辑后稳定 recompute。
- Assembly 至少有 stateless representative solver adapter 成功路径和 placement write-back；未支持 JointType 有稳定 diagnostics。
- CLI / C ABI / Worker / WASM / Web adapter 复用同一 core recompute，不承载业务语义。
