# 【已实现】C9-M5 工作步骤总入口

## 目标

把 `SubShapeBinder CopyOnChange request-local DTO` 准入复审拆成 S0-S6。当前收口状态：S0 和 S1 已关闭，S2 到 S6 待执行；本文件只是索引，创建后标 `【已实现】`，避免 goal 队列把索引当成实现步骤。

## 步骤索引

| 步骤 | 文件 | 当前状态 | 解决的问题 |
| --- | --- | --- | --- |
| S0 | `6-28-11-25-【已实现】C9-M5-S0-live基线与known-gap声明口径冻结.md` | 已实现 | 已冻结 live capability、C9-M4 queue-empty 和 known-gap 声明口径。 |
| S1 | `6-28-11-26-【已实现】C9-M5-S1-FreeCAD源码与current覆盖候选矩阵.md` | 已实现 | 已复核 FreeCAD source authority 与 current cad-core coverage。 |
| S2 | `6-28-11-27-C9-M5-S2-范围准入与blocker矩阵.md` | 待执行 | 对 property-state、full cache、DTO、non-goal 和 release gate 做范围准入。 |
| S3 | `6-28-11-28-C9-M5-S3-native-CopyOnChange生命周期probe复审.md` | 待执行 | 复跑并增强 native CopyOnChange lifecycle probe。 |
| S4 | `6-28-11-29-C9-M5-S4-request-local-DTO产品边界复审.md` | 待执行 | 判断是否存在可发布的 request-local DTO。 |
| S5 | `6-28-11-30-C9-M5-S5-cad-core实现闸门与diagnostic发布复审.md` | 待执行 | 决定 S6 是 code gate 还是 no-code retained known gap。 |
| S6 | `6-28-11-31-C9-M5-S6-Oracle实现与发布闸门.md` | 待执行 | 按 S3-S5 evidence 实现或发布 no-code release gate。 |

## 执行顺序

1. S0 已完成，已冻结 live baseline 和 forbidden claims。
2. S1 只读源码和 current cad-core，不采 oracle，不改 C++。
3. S2 完成 scope / blocker / non-goal / backend-gap 初始路由。
4. S3 采集或复核 native evidence。
5. S4 给出 request-local DTO 产品边界裁决。
6. S5 决定是否打开 S6 C++ implementation gate。
7. S6 只消费已证明的 implementation row 或 retained known gap row。

## 当前矩阵闸门

| 矩阵 | 当前用途 | 当前结论 |
| --- | --- | --- |
| `c9m5_copyonchange_source_candidates.tsv` | source authority audit | S1 已复核 FreeCAD full path 与 current cad-core boundary。 |
| `c9m5_copyonchange_scope_review_matrix.tsv` | scope route seed | S0 baseline 已冻结，S1 已审计 source/current；S2-S6 仍待准入。 |
| `c9m5_copyonchange_blocker_queue.tsv` | blocker queue seed | S0 和 S1 已关闭，S2-S6 待执行。 |
| `c9m5_copyonchange_non_goal_registry.tsv` | forbidden claims | GUI/session/cache/guessing/downstream 实现已按 S0 冻结为 non-goal。 |
| `c9m5_copyonchange_backend_gap_classification.tsv` | S6 gate 分类 | 当前只是 candidate，不能直接落代码。 |
| `c9m5_copyonchange_validation_matrix.tsv` | 分层验收 | 每步按 required_at 执行。 |

## 状态纪律

- S0-S6 只有在各自 `验收` 通过后才能重命名为 `【已实现】`。
- `backend_gap_candidate` 不能直接变成 C++ 任务；必须先有 S3 native evidence 和 S4 产品边界。
- `known_gap_retained` 不是失败；它说明当前证据不足以破坏无状态 CAD Core 边界。
- 不允许把 FreeCAD temporary document cache、raw shape、BREP、TopoDS_Shape、NamedShape、ElementMap 或 hidden object cache 写入 request / response 合同。

## 通用验收

```bash
cd /home/user/Chili3DProject/FreeCAD
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore9.0/C9-M5-SubShapeBinderCopyOnChangeRequestLocalDTO准入复审包/工作步骤细分 --format markdown
awk -F '\t' 'FNR==1{n=NF; next} NF!=n{print FILENAME ":" FNR ": expected " n " fields, got " NF; bad=1} END{exit bad}' docs/CADCore9.0/C9-M5-SubShapeBinderCopyOnChangeRequestLocalDTO准入复审包/矩阵/*.tsv
rg -n '[ \t]$' docs/CADCore9.0/C9-M5-SubShapeBinderCopyOnChangeRequestLocalDTO准入复审包 docs/CADCore9.0/README.md
git diff --check
```

## 非目标

- 不重开 C8-M1 ShapeBinder/SubShapeBinder executor 主路径。
- 不重开 C9-M1 到 C9-M4 Assembly 主线。
- 不实现 GUI / ViewProvider / TaskPanel。
- 不修改下游 Rust；若需要同步，另开 `opencascade-rs` 包。
