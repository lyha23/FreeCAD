# 【已实现】C9-M5-S6 Oracle 实现与发布闸门

## 目标

根据 S3-S5 证据完成 C9-M5 release gate。S6 只消费已经明确的 `backend_gap_requires_implementation`、`known_gap_retained`、`needs_more_native_evidence` 或 `release_gate` row，不新增业务范围。

## 执行基线

- `pwd`：`/home/user/Chili3DProject/FreeCAD`
- `git rev-parse --short HEAD`：`c68a9621b5`
- `git log -1 --oneline`：`c68a9621b5 docs: 关闭 C9-M5 S5 diagnostic 发布复审`
- `git -c core.quotepath=false status --short -uall`：无输出，工作区干净。
- 队列首项是本 S6 文件；S0-S5 已全部标为 `【已实现】`。

## 入场条件

- S0-S5 均已按验收通过并重命名为 `【已实现】`。
- `c9m5_copyonchange_scope_review_matrix.tsv` 中每个 scope 都有最终 route。
- `c9m5_copyonchange_backend_gap_classification.tsv` 明确说明是否存在 `backend_gap_requires_implementation`。
- S4 产品裁决和 S5 implementation gate 一致。

## 本轮代码实现目标

只有存在 `backend_gap_requires_implementation` 时，S6 才允许落代码。

| blocker / scope | C++ 落点 | FreeCAD authority | 测试 | 成功标准 |
| --- | --- | --- | --- | --- |
| `C9M5-BLOCKER-601` / `C9M5-SCOPE-201` | `cad-core/src/part_design/feature_shape_binder.cpp` | `SubShapeBinder::setupCopyOnChange()`、`SubShapeBinder::update()` | `cad-core/tests/test_c8_shapebinder.py` 或新增 `tests/test_c9_copyonchange.py` | DTO-backed CopyOnChange 子集 request-local parity，且不保存 hidden cache。 |
| `C9M5-BLOCKER-601` / `C9M5-SCOPE-201` | `cad-core/src/app/copy_on_change.cpp` | `LinkBaseExtension::syncCopyOnChange()`、`makeCopyOnChange()` | focused document update tests | documentObjectUpdates / property delta 与 DTO 一致。 |
| `C9M5-BLOCKER-601` / `C9M5-SCOPE-202` | `cad-core/src/runtime/capability_contract.cpp` | current capability known gap block | `cad-core/tests/test_adapters.py` | 删除或收窄 remaining gap 只发生在 fixture / tests / DTO evidence 同步通过后。 |

禁止路径：

- 不按 fixture 名、bbox、面积、长度、几何类型或输出排序猜 CopyOnChange。
- 不在 adapter/output JSON 末端补业务逻辑。
- 不保存跨请求 FreeCAD document、temporary document、BREP、TopoDS_Shape、NamedShape、ElementMap 或 copied-object cache。
- 不把 `_CopiedObjs`、native object pointer identity 或 hidden document name 作为前后端合同。

## no-code release gate

若 S5 没有 `backend_gap_requires_implementation`，S6 必须关闭为 no-code release gate：

- 保持 `copy_on_change_full_temporary_document_cache` 为 `known_gap_diagnostic` / `oracle_blocked`。
- 更新 README、总入口、方案、步骤索引和矩阵，记录 S3-S5 证据。
- 保持 `delete_condition`：只有 FreeCADCmd / native probe 暴露稳定 request-local CopyOnChange copied-object evidence 且产品批准 DTO 后才替换。
- 保持 `reopen_condition`：只有更强 native oracle 或明确产品决策才能重开。

## S6 发布裁决

本轮裁决：`no_code_retained_known_gap_release_gate`。

S6 消费的证据链如下：

- S3 native evidence：`cad-core/fixtures/c9m5/expected/subshape-binder-copyonchange-lifecycle-probe.freecad.json` 记录 FreeCADCmd `1.2.0 revision 20260519`，可观察 Disabled / Enabled / Mutated / PartialLoad、`_tmp_binder` 和 `_CopiedLink`；但 `_CopiedObjs`、`copyObject()` dependency order 与 `recomputeFeature(true)` internal `ElementMap` lifecycle 仍不可导出为稳定 request-local DTO。
- S4 产品裁决：`dto_rejected_known_gap_retained`。允许字段只限 request graph、request-local `documentObjectUpdates` / diagnostics、source / support / mutation intent 和 deletion / update / reselect 建议。
- S5 implementation gate：`no_code_retained_known_gap_release_gate`。current `cad-core` diagnostic、capability 和 focused tests 已经发布 retained known gap，不存在 `backend_gap_requires_implementation`。

因此 S6 不落 C++ support，不删除 `copy_on_change_full_temporary_document_cache_not_supported` diagnostic，不删除 `remaining_gaps=["copy_on_change_full_temporary_document_cache"]`。`_tmp_binder`、`_CopiedObjs`、native pointer、BREP、`TopoDS_Shape`、`NamedShape`、`ElementMap` 和 copied-object cache 仍不得写入 request / response contract。

## C9M5-BLOCKER-601 关闭

`C9M5-BLOCKER-601` 关闭为 `closed_S6_no_code_retained_known_gap`。

- close condition：C9-M5 队列为空；TSV 字段数检查通过；docs / diff 无空白错误；capability smoke 可生成 `/tmp/c9m5-capabilities.json`；focused tests `tests.test_c8_shapebinder tests.test_diagnostics tests.test_adapters` 通过。
- delete condition：只有 FreeCADCmd / native probe 暴露稳定 request-local CopyOnChange copied-object evidence，且产品批准不含 persistent backend session / temporary document / BREP / `TopoDS_Shape` / `NamedShape` / `ElementMap` / copied-object cache 的 DTO 后，才能替换 retained gap。
- reopen condition：只有更强 native oracle 或明确产品决策批准 request-local CopyOnChange DTO 时，才能重开 implementation gate。

## 验收

Release gate 验收：

```bash
cd /home/user/Chili3DProject/FreeCAD
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore9.0/C9-M5-SubShapeBinderCopyOnChangeRequestLocalDTO准入复审包/工作步骤细分 --format markdown
awk -F '\t' 'FNR==1{n=NF; next} NF!=n{print FILENAME ":" FNR ": expected " n " fields, got " NF; bad=1} END{exit bad}' docs/CADCore9.0/C9-M5-SubShapeBinderCopyOnChangeRequestLocalDTO准入复审包/矩阵/*.tsv
rg -n '[ \t]$' docs/CADCore9.0/C9-M5-SubShapeBinderCopyOnChangeRequestLocalDTO准入复审包 docs/CADCore9.0/README.md
git diff --check
cd /home/user/Chili3DProject/FreeCAD/cad-core
./cad-core capabilities > /tmp/c9m5-capabilities.json
python3 -m unittest tests.test_c8_shapebinder tests.test_diagnostics tests.test_adapters
```

验收标准：

- 若 no-code，capability smoke 仍显示 known gap retained，队列为空，矩阵记录 no-code 裁决。
- 若 code gate，新增或修改的 fixture / tests / capability 与 DTO evidence 同步，且 `remaining_gaps` 只删除已实现子集。
- S6 完成后所有本包链接无 stale basename。

## 验收结果

- `step_goal_queue.py ... --format markdown`：最终只输出 Markdown 表头，C9-M5 queue empty。
- TSV 字段数检查：通过。
- `rg -n '[ \t]$' docs/CADCore9.0/C9-M5-SubShapeBinderCopyOnChangeRequestLocalDTO准入复审包 docs/CADCore9.0/README.md`：无匹配，未发现尾随空白。
- `git diff --check`：通过。
- `./cad-core capabilities > /tmp/c9m5-capabilities.json`：通过；`part_design.sub_shape_binder.status=supported_c8m1_expected_backed_request_local_with_copy_on_change_known_gap`，`remaining_gaps=["copy_on_change_full_temporary_document_cache"]`，known gap 仍为 `known_gap_diagnostic` / `oracle_blocked`，diagnostic 仍为 `copy_on_change_full_temporary_document_cache_not_supported`，`delete_condition` / `reopen_condition` 均存在。
- `python3 -m unittest tests.test_c8_shapebinder tests.test_diagnostics tests.test_adapters`：通过，47 tests OK。

## 非目标

- 不执行全量 FreeCAD CI。
- 不把下游 Rust 同步混进本 repo C++ gate。
- 不采新的 FreeCAD oracle；S6 只消费 S3 已提交 native evidence。
- 不修改 `cad-core/src/part_design/feature_shape_binder.cpp` 或 `cad-core/src/app/copy_on_change.cpp` 业务逻辑。
