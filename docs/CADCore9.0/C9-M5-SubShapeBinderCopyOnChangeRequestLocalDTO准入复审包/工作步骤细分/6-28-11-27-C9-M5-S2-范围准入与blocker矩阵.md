# C9-M5-S2 范围准入与 blocker 矩阵

## 目标

把 S1 的 source/current evidence 分类到明确路线：`native_oracle_required`、`product_decision_required`、`known_gap_retained`、`backend_gap_candidate`、`backend_gap_requires_implementation`、`diagnostic_non_goal` 或 `release_gate`。S2 不采 oracle、不改 C++。

## 分类规则

| 状态 | 使用条件 | 下一步 |
| --- | --- | --- |
| `native_oracle_required` | 需要 FreeCADCmd / native probe 证明行为 | 交 S3。 |
| `product_decision_required` | native 行为可见但 DTO 是否允许需要产品边界 | 交 S4。 |
| `known_gap_retained` | full cache 仍依赖 temporary document 或不可序列化 state | 交 S5/S6 保持 diagnostic。 |
| `backend_gap_candidate` | 可能有 request-local DTO，但证据不足 | S3/S4 前不得落代码。 |
| `backend_gap_requires_implementation` | S3 evidence + S4 产品边界都成立 | S6 代码落点。 |
| `diagnostic_non_goal` | GUI/session/cache/downstream 等明确排除 | 写 non-goal registry。 |
| `release_gate` | capability、diagnostic、test、docs 发布一致性 | 交 S5/S6。 |

## 必须回写的矩阵行

- `C9M5-SCOPE-001` 到 `C9M5-SCOPE-301`
- `C9M5-BLOCKER-201`
- `C9M5-CAT-101` 到 `C9M5-CAT-104`
- `C9M5-NG-001` 到 `C9M5-NG-006`

## 验收

```bash
cd /home/user/Chili3DProject/FreeCAD
rg -n 'C9M5-SCOPE|C9M5-BLOCKER|C9M5-CAT|native_oracle_required|product_decision_required|known_gap_retained|backend_gap_candidate|diagnostic_non_goal|release_gate' docs/CADCore9.0/C9-M5-SubShapeBinderCopyOnChangeRequestLocalDTO准入复审包
awk -F '\t' 'FNR==1{n=NF; next} NF!=n{print FILENAME ":" FNR ": expected " n " fields, got " NF; bad=1} END{exit bad}' docs/CADCore9.0/C9-M5-SubShapeBinderCopyOnChangeRequestLocalDTO准入复审包/矩阵/*.tsv
git diff --check
```

验收标准：

- 每个 scope 都有合法状态、cad-core landing 或 exclusion reason。
- 每个 blocker / nonGoal / backend-gap category 都能追到 scope id。
- 没有无 FreeCAD authority 和 current mismatch evidence 的 `backend_gap_requires_implementation`。
- S2 后 S3/S4/S5/S6 owner step 清楚。

## 非目标

- 不把 `notCollected` 或 `oracle_blocked` 直接改成 C++ 任务。
- 不运行 cad-core build。
