# 【已实现】C9-M2 S0 live 基线与声明口径冻结

## 目标

冻结 C9-M2 live 起点，确认本包是 Assembly request-local solver oracle 批次，不重开 C9-M1 已关闭的 no-code release gate，也不把 oracle-only rows 写成 supported。

## 输入

- `docs/CADCore9.0/README.md`
- `docs/CADCore9.0/C9-M1-AssemblyJointMarkerOffsetPlacementRequestLocal主线/README.md`
- `docs/CADCore9.0/C9-M2-AssemblyRequestLocalSolverOracle批次/README.md`
- `cad-core/src/runtime/capability_contract.cpp`
- `cad-core/tests/test_adapters.py`
- `cad-core/tests/test_p8_features.py`
- `矩阵/*.tsv`

## 必须复核

- `pwd` 是 `/home/user/Chili3DProject/FreeCAD`。
- 当前 `git rev-parse --short=10 HEAD` 与 `git log -1 --oneline` 写入 README 和 blocker 矩阵。
- C9-M1 工作步骤队列为空。
- live capability 中 `assembly.remaining_gaps=[]`、`subshape_marker_placement.remaining_gaps=[]`。
- `non_identity_bundled_offsetPlc` 仍需要 native oracle；`non_assembly_link_subshape_primitive_frame_generalization` 仍是 diagnostic non-goal；zero Angle fallback 仍缺 native expected。

## 必须回写的矩阵行

- `C9M2-SCOPE-001`
- `C9M2-SCOPE-101` 到 `C9M2-SCOPE-304`
- `C9M2-NG-001` 到 `C9M2-NG-006`
- `C9M2-BLOCKER-000`

## S0 关闭结论

- live 基线：`pwd=/home/user/Chili3DProject/FreeCAD`，`HEAD=d52cd67a19`（`d52cd67a19 docs: 关闭 C9-M1 S6 发布闸门`）。S0 起始 `git -c core.quotepath=false status --short -uall` 显示 `docs/CADCore9.0/README.md` 已修改，且 `docs/CADCore9.0/C9-M2-AssemblyRequestLocalSolverOracle批次/**` 为未提交 seed 文档 / 矩阵 / step 文件；未出现 cad-core source、fixtures、expected 或 tests 改动。
- C9-M1 工作步骤队列为空，C9-M1 已关闭且不重开。
- live capability / tests 口径保持：`assembly.ondsel_solver_adapter.status=covered_full`，`subshape_marker_placement.status=covered_representative_subset`，`assembly.remaining_gaps=[]`，`subshape_marker_placement.remaining_gaps=[]`，`placement_writeback.status=covered_full`。
- `non_identity_bundled_offsetPlc` 仍是 native oracle candidate / forbidden guessing；`non_assembly_link_subshape_primitive_frame_generalization` 仍是 diagnostic non-goal；zero Angle fallback 仍缺 native expected。S0 未采 native oracle，未改 cad-core source / fixtures / expected / tests，未跑 build 或重型回归。
- C9-M2 S0 只冻结声明口径和证据边界；后续 S1-S6 仍按队列继续执行。

## 验收

```bash
cd /home/user/Chili3DProject/FreeCAD
git -c core.quotepath=false status --short -uall
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore9.0/C9-M1-AssemblyJointMarkerOffsetPlacementRequestLocal主线/工作步骤细分 --format markdown
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore9.0/C9-M2-AssemblyRequestLocalSolverOracle批次/工作步骤细分 --format markdown
rg -n 'non_identity_bundled_offsetPlc|non_assembly_link_subshape_primitive_frame_generalization|assembly.remaining_gaps|zero Angle|offsetPlc' docs/CADCore9.0 cad-core/src/runtime/capability_contract.cpp cad-core/tests/test_adapters.py
awk -F '\t' 'FNR==1{n=NF; next} NF!=n{print FILENAME ":" FNR ": expected " n " fields, got " NF; bad=1} END{exit bad}' docs/CADCore9.0/C9-M2-AssemblyRequestLocalSolverOracle批次/矩阵/*.tsv
git diff --check
```

S0 关闭时，README 与矩阵必须记录 live HEAD、C9-M1 queue empty、current Assembly capability 口径、C9-M2 批量范围和 forbidden claims。

## 非目标

- 不采 native oracle。
- 不改 `cad-core/src`、fixtures、expected 或 tests。
- 不跑 build 或重型阶段回归。
