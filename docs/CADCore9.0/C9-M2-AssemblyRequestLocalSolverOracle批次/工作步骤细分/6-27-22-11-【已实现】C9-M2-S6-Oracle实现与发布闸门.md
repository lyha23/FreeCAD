# C9-M2 S6 Oracle 实现与发布闸门

## 目标

按 S3-S5 裁决关闭 C9-M2：如果 native oracle 与 current cad-core mismatch 形成 `backend_gap_candidate`，完成受限实现和 focused tests；如果 only expected activation / releaseGate，则补 tests / capability / docs；如果 oracle blocked，则保留 `oracle_candidate` 并写清下一批条件。

## 本轮代码实现目标

S6 只消费 S3-S5 明确标出的 `backend_gap_candidate`、implementable `expected_activation` 或 `releaseGate`。允许落点：

- `cad-core/src/assembly/joint_solver.cpp`
- `cad-core/src/assembly/assembly_utils.cpp`
- `cad-core/src/assembly/assembly_object.cpp`
- `cad-core/src/runtime/capability_contract.cpp`
- `cad-core/tests/test_p8_features.py`
- `cad-core/tests/test_adapters.py`
- `cad-core/fixtures/c3m6/*`

## 本轮关闭落点

| blocker / scope | C++ / test 落点 | FreeCAD authority | 成功标准 |
| --- | --- | --- | --- |
| `C9M2-BLOCKER-301` / `C9M2-SCOPE-101..103` | `joint_solver.cpp`、`assembly_utils.cpp`、`assembly_object.cpp`、`tests/test_p8_features.py`、`fixtures/c3m6` | `getMbDData()`、`handleOneSideOfJoint()`、`setNewPlacements()` | 已关闭：bundled `offsetPlc` object / subshape / writeback expected 与 current parity。 |
| `C9M2-BLOCKER-401` / `C9M2-SCOPE-201` | `tests/test_p8_features.py` | existing custom placement-chain expected | focused tests 直接断言 existing expected，不误宣称 bundled offset。 |
| `C9M2-BLOCKER-501` / `C9M2-SCOPE-301..302` | `joint_solver.cpp`、`tests/test_p8_features.py`、`tests/test_adapters.py` | `makeMbdJointOfType()` zero Angle fallback | 已关闭：zero Angle expected-backed current parity；unsupported diagnostics 继续可见。 |

## 禁止路径

- fixture 名称分支、bbox / area / angle 容差猜测、输出排序修补。
- adapter 层字符串改写来隐藏 Assembly runtime 缺口。
- cross-request solver session、persistent placement cache、full Link ledger。
- GUI / ViewProvider / TaskPanel / Worker / WASM / Web 修改。

## 实现关闭结论

- S6 live 基线：`pwd=/home/user/Chili3DProject/FreeCAD`，`HEAD=7f07180da0`（`7f07180da0 feat(cad-core): 关闭C9-M2 S5 zero angle复审`），起始 `git -c core.quotepath=false status --short -uall` 无输出。
- bundled `offsetPlc` 按 FreeCAD `getMbDData()` / `handleOneSideOfJoint()` / `setNewPlacements()` / `validateNewPlacements()` 关闭：fixed bundle 的 request-local `offsetPlc` 只在本次 solve 内应用，object marker 与 subshape marker 都从 `[0.25,0.5,0.75]` 解为 `[2.25,0.5,0.75]`，`ComponentC` writeback 为 `[6,0,2]`。
- zero Angle fallback 按 FreeCAD `makeMbdJointOfType()` exact-zero native route 关闭：`assembly-angle-zero-and-signed-current-real-solver` 当前解出 `ComponentB=[4,0,4]`，旧 `ondsel_solver_failed` 不再作为该 fixture 的 supported route。
- capability 更新为 bundled offset marker / writeback covered；`non_identity_bundled_offsetPlc` 从 non-goals 移除，`non_assembly_link_subshape_primitive_frame_generalization` 保持 diagnostic non-goal。
- 验证已通过：C9-M2 queue 为空、TSV 字段数一致、尾随空白检查无匹配、`git diff --check` 通过，`cmake --build build` 与 `python3 -m unittest tests.test_p8_features tests.test_adapters` 通过，`./cad-core capabilities` 仍发布 `assembly.remaining_gaps=[]`。
- `C9M2-BLOCKER-601` 已关闭；S6 重命名为 `【已实现】` 后，C9-M2 队列为空。

## 验收

本轮短跑：

```bash
cd /home/user/Chili3DProject/FreeCAD
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore9.0/C9-M2-AssemblyRequestLocalSolverOracle批次/工作步骤细分 --format markdown
awk -F '\t' 'FNR==1{n=NF; next} NF!=n{print FILENAME ":" FNR ": expected " n " fields, got " NF; bad=1} END{exit bad}' docs/CADCore9.0/C9-M2-AssemblyRequestLocalSolverOracle批次/矩阵/*.tsv
rg -n '[ \t]$' docs/CADCore9.0/C9-M2-AssemblyRequestLocalSolverOracle批次 docs/CADCore9.0/README.md
git diff --check
```

若改 C++、fixture、expected 或 capability：

```bash
cd /home/user/Chili3DProject/FreeCAD/cad-core
cmake --build build
python3 -m unittest tests.test_p8_features tests.test_adapters
./cad-core capabilities > /tmp/c9m2-capabilities.json
```

## 关闭条件

- `C9M2-BLOCKER-601` 已关闭。
- S6 文件重命名为 `【已实现】` 后，队列为空。
- README 已记录最终 route、验证命令、remaining / known-gap / non-goal 状态。
- 代码 gate 的 focused tests 和 build 已纳入本轮验证。

## 非目标

- 不把 oracle-only rows 写成 supported。
- 不重开 C8-M2 CopyOnChange 或 C8-M7 ImportShape。
- 不把完整 Assembly solver 产品化放进本轮。
