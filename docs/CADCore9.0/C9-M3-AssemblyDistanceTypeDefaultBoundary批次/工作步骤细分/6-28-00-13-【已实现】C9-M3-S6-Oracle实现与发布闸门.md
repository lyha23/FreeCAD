# 【已实现】C9-M3 S6 Oracle 实现与发布闸门

## 目标

按 S3-S5 裁决关闭 C9-M3：消费 expected-backed `backend_gap_candidate`、implementable unsupported 或 releaseGate。若 oracle blocked 或未采集，保留 `notCollected` / diagnostic route，并写清下一批条件。

## 本轮代码实现目标

S6 只消费 S3-S5 明确标出的 rows。允许落点：

- `cad-core/src/assembly/joint_solver.cpp`
- `cad-core/tools/collect_freecad_expected.py`
- `cad-core/src/runtime/capability_contract.cpp`
- `cad-core/tests/test_p8_features.py`
- `cad-core/tests/test_adapters.py`
- `cad-core/fixtures/c3m6/*`
- `docs/CADCore9.0/C9-M3-AssemblyDistanceTypeDefaultBoundary批次/*`

## 下一轮代码落点

| blocker / scope | C++ / test 落点 | FreeCAD authority | 成功标准 |
| --- | --- | --- | --- |
| `C9M3-BLOCKER-301` / `C9M3-SCOPE-101` | `joint_solver.cpp`、`tests/test_p8_features.py`、`tests/test_adapters.py` | `getDistanceType()` PointCurve + `makeMbdJointDistance()` `ASMTPointInPlaneJoint` | accepted `PointCurve` expected current parity；旧 `point_curve_diagnostic_boundary` 不再是该 fixture 的 supported route。 |
| `C9M3-BLOCKER-401` / `C9M3-SCOPE-201..203` | `joint_solver.cpp`、collector、fixtures、focused tests | `makeMbdJointDistance()` default `ASMTPlanarJoint` + `offset` | accepted default branch fixtures current parity；未采集 rows 保留 `notCollected`。 |
| `C9M3-BLOCKER-501` / `C9M3-SCOPE-301..304` | `capability_contract.cpp`、`tests/test_adapters.py` | capability publication follows source-backed support | supported / default_or_todo / non-goal fields 与最终 route 一致，diagnostics guard 可见。 |

## 禁止路径

- fixture 名称分支、bbox / area / angle 容差猜测、输出排序修补。
- adapter 层字符串改写来隐藏 Assembly runtime 缺口。
- cross-request solver session、persistent placement cache、full Link ledger。
- GUI / ViewProvider / TaskPanel / Worker / WASM / Web 修改。
- 把未采集 default branch case 继承为 supported。

## 验收

本轮短跑：

```bash
cd /home/user/Chili3DProject/FreeCAD
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore9.0/C9-M3-AssemblyDistanceTypeDefaultBoundary批次/工作步骤细分 --format markdown
awk -F '\t' 'FNR==1{n=NF; next} NF!=n{print FILENAME ":" FNR ": expected " n " fields, got " NF; bad=1} END{exit bad}' docs/CADCore9.0/C9-M3-AssemblyDistanceTypeDefaultBoundary批次/矩阵/*.tsv
rg -n '[ \t]$' docs/CADCore9.0/C9-M3-AssemblyDistanceTypeDefaultBoundary批次 docs/CADCore9.0/README.md
git diff --check
```

若改 C++、fixture、expected 或 capability：

```bash
cd /home/user/Chili3DProject/FreeCAD/cad-core
cmake --build build
python3 -m unittest tests.test_p8_features tests.test_adapters
./cad-core capabilities > /tmp/c9m3-capabilities.json
```

## 关闭条件

- `C9M3-BLOCKER-601` 已关闭。
- S6 文件重命名为 `【已实现】` 后，队列为空。
- README 已记录最终 route、验证命令、remaining / known-gap / non-goal 状态。
- 代码 gate 已执行，相关 focused tests 和 build 通过。

## S6 关闭结果

- `PointCurve`：移除 `unsupportedReasonForOndselJoint()` 的 `point_curve_diagnostic_boundary` 拦截，保留 `ASMTPointInPlaneJoint` / `offset=distance` DTO；accepted expected 与 current placement writeback parity。
- accepted default rows：仅 `PlaneCone`、`LineCylinder`、`CurvePlane`、`Other` 映射 `ASMTPlanarJoint` / `offset=distance`；`Other` 的 public DTO 保持 FreeCAD default 语义，request-local Ondsel marker I/J 接线复现 native placement。
- 未采 rows：`CylinderCone`、`ConeCone`、`ConeTorus`、`ConeSphere`、`PointCone`、`PointTorus`、`LineSphere`、`LineCone`、`LineTorus`、`CurveCylinder`、`CurveSphere`、`CurveCone`、`CurveTorus` 不发布 supported，仍在 capability `default_or_todo_boundaries` 中可见。
- accepted expected：5 个 expected 删除 stale `known_gap` / `nonGoal.ids=["DTE-NG-003"]`；四条 default expected 的 solver joint 写入 `ASMTPlanarJoint` / `offset=1.5`。
- collector：未来重采这 5 个 accepted rows 不会重加旧 DTE-NG-003；未 accepted default boundary 仍走 default metadata。
- capability / tests：`native_expected_count=18`，`deferred_diagnostic_cases=[]`；focused tests 覆盖 accepted parity、unsupported JointType、`missing_grounded_part`、`missing_marker_placement`、capability `invalid_assembly_solver_result` guard 和未采 `CurveCylinder` default boundary。

## S6 验证结论

```bash
cd /home/user/Chili3DProject/FreeCAD/cad-core
cmake --build build
python3 -m unittest tests.test_p8_features tests.test_adapters
./cad-core capabilities > /tmp/c9m3-capabilities.json
```

```bash
cd /home/user/Chili3DProject/FreeCAD
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore9.0/C9-M3-AssemblyDistanceTypeDefaultBoundary批次/工作步骤细分 --format markdown
awk -F '\t' 'FNR==1{n=NF; next} NF!=n{print FILENAME ":" FNR ": expected " n " fields, got " NF; bad=1} END{exit bad}' docs/CADCore9.0/C9-M3-AssemblyDistanceTypeDefaultBoundary批次/矩阵/*.tsv
rg -n '[ \t]$' docs/CADCore9.0/C9-M3-AssemblyDistanceTypeDefaultBoundary批次 docs/CADCore9.0/README.md || true
git diff --check
```

## 非目标

- 不把 oracle-only rows 写成 supported。
- 不重开 C9-M1/C9-M2。
- 不把完整 Assembly solver 产品化放进本轮。
