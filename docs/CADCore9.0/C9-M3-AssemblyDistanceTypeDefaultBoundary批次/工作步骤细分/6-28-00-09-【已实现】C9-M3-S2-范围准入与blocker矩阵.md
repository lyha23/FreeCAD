# 【已实现】C9-M3 S2 范围准入与 blocker 矩阵

## 目标

把 S1 的 source candidates 路由成可执行 scope：哪些是已有 expected 可直接复审，哪些需要 native oracle 批量采集，哪些保持 non-goal，哪些只到 S6 release gate。

## S2 live baseline

- 执行目录：`/home/user/Chili3DProject/FreeCAD`。
- 起始 HEAD：`48355eae5d`（`48355eae5d docs: 关闭 C9-M3 S1 源码候选矩阵`）。
- 起始 `git -c core.quotepath=false status --short -uall` 无输出。
- S2 执行前队列首项为本文件；S2 关闭后只应剩余 S3-S6。

## 分类规则

| route | 含义 |
| --- | --- |
| `baseline_closed` | 只证明 live state 和声明口径。 |
| `existing_expected_review` | checked-in expected 已存在，下一步比较 native expected 与 current behavior。 |
| `native_oracle_required` | FreeCAD source 明确但 expected 不完整，下一步必须采集 native expected。 |
| `diagnostics_guard_review` | 现有 unsupported diagnostics 需要在实现后继续可见。 |
| `release_gate` | capability / docs / queue / tests 发布闸门。 |
| `diagnostic_non_goal` | 当前产品边界外，不进入 C9-M3 代码实现。 |
| `notCollected` | 缺 native expected 的 default branch 组合在 S4 未采集前只能显式保留，不能继承其它 expected 结论。 |
| `forbidden_claim` | fixture 名、几何猜测、adapter 文案或单一 expected 继承支持结论均禁止作为实现证据。 |

## 必须回写的矩阵行

- `C9M3-SCOPE-101..404`
- `C9M3-BLOCKER-201`
- `C9M3-BG-101..601`
- `C9M3-NG-001..005`（保留 `C9M3-NG-006` adapter guard）

## S2 路由结果

- `C9M3-SCOPE-101` / `C9M3-BG-101`：`PointCurve` 已有 checked-in expected，交 S3 `existing_expected_review`；S2 不写 supported 或 backendGap。
- `C9M3-SCOPE-201` / `C9M3-BG-201`：`PlaneCone`、`LineCylinder`、`CurvePlane`、`Other` 已有 default expected，交 S4 `existing_expected_review`。
- `C9M3-SCOPE-202` / `C9M3-BG-202`：缺 expected 的 default branch 组合交 S4 `native_oracle_required`，S4 如无法采集必须逐行写 `notCollected`，不能继承 `PlaneCone` 结论。
- `C9M3-SCOPE-203` / `C9M3-BG-203`：default `ASMTPlanarJoint` landing 只作为 expected-backed review / S6 landing，不在 S2 单独形成 backendGap。
- `C9M3-SCOPE-301..304` / `C9M3-BG-301..601`：capability、expected metadata、final validation 保持 `release_gate`；`C9M3-SCOPE-302` / `C9M3-BG-302` 保持 `diagnostics_guard_review`。
- `C9M3-SCOPE-401..404` / `C9M3-NG-001..005`：primitive frame、persistent solver、GUI/session、fixture guessing 保持 `diagnostic_non_goal` 或 `forbidden_claim`；`C9M3-NG-006` 保留 adapter string guard。

## S2 关闭结论

- `C9M3-BLOCKER-201` 已关闭；所有 S2 scope / blocker / backend-gap-classification / non-goal rows 均有 source candidate、owner step、cad-core landing 或 guard、close condition。
- S2 未改 cad-core C++、fixtures、expected 或 tests，未采 oracle，未运行 build，未删除 `default_or_todo_branch_support`。
- S3-S6 保持 pending；下一队列首项应为 `C9-M3 S3 PointCurve 平面化 oracle 复审`。

## 验收

```bash
cd /home/user/Chili3DProject/FreeCAD
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore9.0/C9-M3-AssemblyDistanceTypeDefaultBoundary批次/工作步骤细分 --format markdown
rg -n 'C9M3-SCOPE|C9M3-BLOCKER|C9M3-BG|C9M3-NG|existing_expected_review|native_oracle_required|diagnostic_non_goal|release_gate|notCollected|forbidden_claim' docs/CADCore9.0/C9-M3-AssemblyDistanceTypeDefaultBoundary批次
awk -F '\t' 'FNR==1{n=NF; next} NF!=n{print FILENAME ":" FNR ": expected " n " fields, got " NF; bad=1} END{exit bad}' docs/CADCore9.0/C9-M3-AssemblyDistanceTypeDefaultBoundary批次/矩阵/*.tsv
git diff --check
```

## 验收标准

- 每个 scope 都有 source candidate、owner step、cad-core landing 和 close condition。
- 每个 blocker / backend gap / non-goal row 都能回链到 scope。
- 没有 native expected 或 current mismatch 的行不能被写成 `backendGap`。
- 缺 expected 的 default branch 组合只能进入 `native_oracle_required` 或 `notCollected`，不能继承 `PlaneCone` 的结论。

## 非目标

- 不在 S2 改源码、fixtures、expected 或 tests。
- 不采 oracle，不运行 build。
- 不把 release gate 当作代码实现证据。
- 不把 `default_or_todo_branch_support` 直接删除。
