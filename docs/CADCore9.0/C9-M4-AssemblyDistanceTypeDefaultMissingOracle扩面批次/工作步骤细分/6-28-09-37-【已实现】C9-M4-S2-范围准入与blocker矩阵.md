# 【已实现】C9-M4 S2 范围准入与 blocker 矩阵

## 目标

把 S1 的 source candidates 路由成可执行 scope：哪些是 native oracle batch，哪些需要 release gate，哪些保持 non-goal，哪些只有在 S3-S5 采集 expected 后才能进入 S6 implementation。

## 分类规则

| route | 含义 |
| --- | --- |
| `baseline_closed` | 只证明 live state 和声明口径。 |
| `native_oracle_required` | FreeCAD source 明确但 input / expected 不完整，下一步必须采集 native expected。 |
| `notCollected` | 当前缺 input / expected，不能进入 backendGap 或 supported。 |
| `backend_gap_candidate` | native expected 存在且 current mismatch 已被证明。 |
| `diagnostics_guard_review` | 现有 unsupported diagnostics 需要在实现后继续可见。 |
| `release_gate` | capability / docs / queue / tests 发布闸门。 |
| `diagnostic_non_goal` | 当前产品边界外，不进入 C9-M4 代码实现。 |

## 必须回写的矩阵行

- `C9M4-SCOPE-101..701`
- `C9M4-BLOCKER-201`
- `C9M4-BG-101..701`
- `C9M4-NG-001..006`

## 关闭证据

- live baseline：`pwd=/home/user/Chili3DProject/FreeCAD`，`HEAD=50585ed5ae`，`git log -1 --oneline` 为 `50585ed5ae docs: 关闭 C9-M4 S1 源码覆盖候选`，S2 起始 `git -c core.quotepath=false status --short -uall` 无输出。队列复核显示 S2-S6 pending。
- S2 已把 S0/S1 source evidence 路由为可执行 scope：
  - `C9M4-SCOPE-101` FaceCone rows（`CylinderCone`、`ConeCone`、`ConeTorus`、`ConeSphere`）只进入 S3 `native_oracle_required`，缺 input / expected 时保持 `notCollected`。
  - `C9M4-SCOPE-201..202` Point / Line + Surface rows（`PointCone`、`PointTorus`、`LineSphere`、`LineCone`、`LineTorus`）只进入 S4 `native_oracle_required`，缺 input / expected 时保持 `notCollected`。
  - `C9M4-SCOPE-301` CurveSurface rows（`CurveCylinder`、`CurveSphere`、`CurveCone`、`CurveTorus`）只进入 S5 `native_oracle_required`，并保留 conic publication mirror 复核责任。
  - `C9M4-SCOPE-401` default `ASMTPlanarJoint + offset` 实现入口被 S2 明确为 oracle-gated：只有 S3-S5 采到 native expected 且 current mismatch 后，S6 才能写 `backend_gap_candidate` 或实现。
  - `C9M4-SCOPE-501..601` 仍是 `release_gate` / `diagnostics_guard_review`，不能当作实现证据。
  - `C9M4-SCOPE-701` 保持 `diagnostic_non_goal`：GUI/session、persistent solver state、cross-request placement cache、primitive-frame generalization、inherited C9-M3 support、fixture-name/bbox/geometry-order guessing、adapter string rewrite、output pruning 均不进入 C9-M4 实现。
- 已回写 `矩阵/c9m4_distance_type_default_missing_oracle_scope_review_matrix.tsv` 的 `C9M4-SCOPE-101..701`、`矩阵/c9m4_distance_type_default_missing_oracle_blocker_queue.tsv` 的 `C9M4-BLOCKER-201`、`矩阵/c9m4_distance_type_default_missing_oracle_backend_gap_classification.tsv` 的 `C9M4-BG-101..701` 和 `矩阵/c9m4_distance_type_default_missing_oracle_non_goal_registry.tsv` 的 `C9M4-NG-001..006`。
- S2 未采 native oracle，未修改 `cad-core` C++、fixtures、expected 或 tests，未把缺 oracle rows 写成 supported/backendGap。

## 验收

```bash
cd /home/user/Chili3DProject/FreeCAD
rg -n 'C9M4-SCOPE|C9M4-BLOCKER|C9M4-BG|C9M4-NG|native_oracle_required|notCollected|backend_gap_candidate|diagnostic_non_goal|release_gate' docs/CADCore9.0/C9-M4-AssemblyDistanceTypeDefaultMissingOracle扩面批次
awk -F '\t' 'FNR==1{n=NF; next} NF!=n{print FILENAME ":" FNR ": expected " n " fields, got " NF; bad=1} END{exit bad}' docs/CADCore9.0/C9-M4-AssemblyDistanceTypeDefaultMissingOracle扩面批次/矩阵/*.tsv
git diff --check
```

## 验收标准

- 每个 scope 都有 source candidate、owner step、cad-core landing 和 close condition。
- 每个 blocker / backend gap / non-goal row 都能回链到 scope。
- 没有 native expected 或 current mismatch 的行不能被写成 `backendGap`。
- 缺 expected 的 default branch 组合只能进入 `native_oracle_required` 或 `notCollected`。

## 非目标

- 不在 S2 改源码或 fixture。
- 不把 release gate 当作代码实现证据。
- 不把缺 oracle default rows 继承为 supported。
