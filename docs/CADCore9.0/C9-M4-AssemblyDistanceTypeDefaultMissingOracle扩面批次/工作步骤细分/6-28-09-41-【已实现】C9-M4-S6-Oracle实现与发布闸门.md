# 【已实现】C9-M4 S6 Oracle 实现与发布闸门

## 目标

按 S3-S5 裁决关闭 C9-M4：消费 expected-backed `backend_gap_candidate`、implementable unsupported 或 releaseGate。若 oracle blocked 或未采集，保留 `notCollected` / diagnostic route，并写清下一批条件。

## 关闭证据

- live baseline：`pwd=/home/user/Chili3DProject/FreeCAD`，`HEAD=33b15325a5`，`git log -1 --oneline` 为 `33b15325a5 docs: 关闭 C9-M4 S5 CurveSurface oracle复审`，S6 起始 `git -c core.quotepath=false status --short -uall` 无输出。队列复核显示仅 S6 pending。
- FreeCAD authority：`src/Mod/Assembly/App/AssemblyUtils.cpp::getDistanceType()` 完成 Face/Face、Vertex/Face、Edge/Face 分类和 `swapJCS` ordering；`src/Mod/Assembly/App/AssemblyObject.cpp::makeMbdJointDistance()` default branch 创建 `ASMTPlanarJoint` 并写 `offset=getJointDistance(joint)`。
- S6 已把 C9-M3 四行与 C9-M4 十三行合并为 expected-backed default planar supported set：`CylinderCone`、`ConeCone`、`ConeTorus`、`ConeSphere`、`PointCone`、`PointTorus`、`LineSphere`、`LineCone`、`LineTorus`、`CurveCylinder`、`CurveSphere`、`CurveCone`、`CurveTorus` 均走 `ASMTPlanarJoint + offset=distance/getJointDistance`。
- collector 已刷新 13 个 expected JSON：这些文件不再带 diagnostic-only `known_gap` / `nonGoal`，solver_joint metadata 与 runtime 一致，均为 `distance_type_boundary=expected_backed_default_planar_supported`、`solver_joint_class=ASMTPlanarJoint`、`offset=1.5`。
- capability 已发布最终 route：`distance_type_extended_geometry.native_expected_count=31`，13 个 C9-M4 rows 进入 `supported` 与 `solver_joint_classes`，Assembly `default_or_todo_boundaries=[]`，conic `distance_type_publication.default_or_todo_boundaries=[]`。
- focused tests 保留 diagnostics guard：unsupported JointType、missing marker、missing grounded、invalid solver output 等 guard 不通过 adapter 文案隐藏 runtime 缺口。

## 本轮代码实现目标

S6 只消费 S3-S5 明确标出的 rows。允许落点：

- `cad-core/src/assembly/joint_solver.cpp`
- `cad-core/tools/collect_freecad_expected.py`
- `cad-core/src/runtime/capability_contract.cpp`
- `cad-core/tests/test_p8_features.py`
- `cad-core/tests/test_adapters.py`
- `cad-core/fixtures/c3m6/*`
- `docs/CADCore9.0/C9-M4-AssemblyDistanceTypeDefaultMissingOracle扩面批次/*`

## 代码落点与关闭结果

| blocker / scope | C++ / test 落点 | FreeCAD authority | 成功标准 |
| --- | --- | --- | --- |
| `C9M4-BLOCKER-301` / `C9M4-SCOPE-101` | `joint_solver.cpp`、collector、fixtures、focused tests | `getDistanceType()` Face / Face cone classification + `makeMbdJointDistance()` default `ASMTPlanarJoint` | 已消费 S3 产出的 `CylinderCone`、`ConeCone`、`ConeTorus`、`ConeSphere` backend_gap_candidate，expected-backed current parity。 |
| `C9M4-BLOCKER-401` / `C9M4-SCOPE-201..202` | `joint_solver.cpp`、collector、fixtures、focused tests | Vertex / Face and line Edge / Face classification + default `ASMTPlanarJoint` | accepted Point / Line surface expected current parity；missing marker diagnostics remain visible。 |
| `C9M4-BLOCKER-501` / `C9M4-SCOPE-301` | `joint_solver.cpp`、collector、fixtures、focused tests | non-line Edge / Face classification + default `ASMTPlanarJoint` | accepted Curve surface expected current parity；conic capability mirror default list cleared。 |
| `C9M4-BLOCKER-601` / `C9M4-SCOPE-501..601` | `capability_contract.cpp`、`tests/test_adapters.py` | capability publication follows source-backed support | supported / default_or_todo / non-goal fields 与最终 route 一致，diagnostics guard 可见。 |

## 禁止路径

- fixture 名称分支、bbox / area / angle 容差猜测、输出排序修补。
- adapter 层字符串改写来隐藏 Assembly runtime 缺口。
- cross-request solver session、persistent placement cache、full Link ledger。
- GUI / ViewProvider / TaskPanel / Worker / WASM / Web 修改。
- 把未采集 default branch case 继承为 supported。

## 验收

本轮短跑：

```bash
cd /home/user/Chili3DProject/FreeCAD
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore9.0/C9-M4-AssemblyDistanceTypeDefaultMissingOracle扩面批次/工作步骤细分 --format markdown
awk -F '\t' 'FNR==1{n=NF; next} NF!=n{print FILENAME ":" FNR ": expected " n " fields, got " NF; bad=1} END{exit bad}' docs/CADCore9.0/C9-M4-AssemblyDistanceTypeDefaultMissingOracle扩面批次/矩阵/*.tsv
rg -n '[ \t]$' docs/CADCore9.0/C9-M4-AssemblyDistanceTypeDefaultMissingOracle扩面批次 docs/CADCore9.0/README.md
git diff --check
```

若改 C++、fixture、expected 或 capability：

```bash
cd /home/user/Chili3DProject/FreeCAD/cad-core
cmake --build build
python3 -m unittest tests.test_p8_features tests.test_adapters
./cad-core capabilities > /tmp/c9m4-capabilities.json
```

## 验收标准

- `C9M4-BLOCKER-601` 已关闭，无未采 rows 保留在本包。
- S6 文件重命名为 `【已实现】` 后，队列为空。
- README 记录最终 route、验证命令、remaining / known-gap / non-goal 状态。
- 如果进入代码 gate，相关 focused tests 和 build 通过。

## 非目标

- 不把 oracle-only rows 写成 supported。
- 不重开 C9-M1/C9-M2/C9-M3。
- 不把完整 Assembly solver 产品化放进本轮。
