# 【已实现】C9-M4 工作步骤总入口

## 目标

把 C9-M4 Assembly DistanceType default missing oracle 扩面批次拆成 S0-S6 可执行步骤。当前收口状态：S0-S3 已关闭，S4 到 S6 均为待执行。本文是索引文件，创建后标 `【已实现】`，避免 goal 队列把索引当作独立实现步骤。

## 步骤索引

| 步骤 | 文件 | 当前状态 | 解决的问题 |
| --- | --- | --- | --- |
| S0 | `6-28-09-35-【已实现】C9-M4-S0-live基线与缺oracle声明口径冻结.md` | 已实现 | live baseline、C9-M3 closure、缺 oracle claim boundary。 |
| S1 | `6-28-09-36-【已实现】C9-M4-S1-FreeCAD源码与current覆盖候选.md` | 已实现 | FreeCAD source authority、current cad-core landing、missing inventory。 |
| S2 | `6-28-09-37-【已实现】C9-M4-S2-范围准入与blocker矩阵.md` | 已实现 | scope / blocker / non-goal / backendGap 初始路由。 |
| S3 | `6-28-09-38-【已实现】C9-M4-S3-FaceCone族native-oracle复审.md` | 已实现 | Face / Face cone family native oracle；四行交 S6 `backend_gap_candidate`。 |
| S4 | `6-28-09-39-C9-M4-S4-PointLineSurface族native-oracle复审.md` | 待执行 | Point / Line + Surface family native oracle。 |
| S5 | `6-28-09-40-C9-M4-S5-CurveSurface族native-oracle复审.md` | 待执行 | Curve + Surface family native oracle。 |
| S6 | `6-28-09-41-C9-M4-S6-Oracle实现与发布闸门.md` | 待执行 | oracle compare 后的 C++ 实现或 release gate。 |

## 执行顺序

1. S0 冻结 C9-M3 关闭状态和缺 oracle forbidden claims。
2. S1 复核 FreeCAD / cad-core / collector / tests / capability source authority。
3. S2 把 13 个 rows 路由到 S3-S5 oracle 或 S6 release gate。
4. S3 处理 `CylinderCone`、`ConeCone`、`ConeTorus`、`ConeSphere`。
5. S4 处理 `PointCone`、`PointTorus`、`LineSphere`、`LineCone`、`LineTorus`。
6. S5 处理 `CurveCylinder`、`CurveSphere`、`CurveCone`、`CurveTorus`。
7. S6 只消费 native expected backed rows；missing oracle rows 保持 visible default boundary。

## 当前矩阵闸门

| 矩阵 | 当前用途 | 当前结论 |
| --- | --- | --- |
| source_candidates | FreeCAD / cad-core authority evidence | S1 已复核 FreeCAD source、cad-core current、collector、capability/tests 和 fixture inventory。 |
| scope_review_matrix | scope seed | S3 已把 FaceCone rows 从 native oracle route 推进到 S6 `backend_gap_candidate`；其余缺 oracle rows 只进 S4-S5 native oracle 或 notCollected。 |
| blocker_queue | S0-S6 blocker seed | `C9M4-BLOCKER-000` 已由 S0 关闭，`C9M4-BLOCKER-101` 已由 S1 关闭，`C9M4-BLOCKER-201` 已由 S2 关闭，`C9M4-BLOCKER-301` 已由 S3 关闭，S4-S6 待执行。 |
| non_goal_registry | forbidden claims seed | GUI/session、persistent solver、cross-request cache、primitive frame、inherited support、guessing、adapter string rewrite、output pruning 已由 S2 保持 non-goal / guard。 |
| backend_gap_classification | oracle / backendGap / releaseGate seed | S3 已把 FaceCone expected-backed current unsupported rows 写为 `backend_gap_candidate`；其余 rows 等 S4-S5 oracle。 |
| validation_matrix | 分层验收 seed | S6 按最终 route 收口。 |

## 状态纪律

- 除索引文件和已验收的 S0 外，S1-S6 不得在创建包时标 `【已实现】`。
- 每一步只有在本步验收通过并记录证据后，才把对应文件名改为 `【已实现】`。
- 不从 C9-M3 accepted rows 推断缺 oracle rows supported。
- 缺 native oracle 的 rows 保持 `notCollected` 或 `native_oracle_required`，不得写 backendGap。
- 不重开 C9-M1/C9-M2/C9-M3 已关闭能力。

## 通用验收

```bash
cd /home/user/Chili3DProject/FreeCAD
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore9.0/C9-M4-AssemblyDistanceTypeDefaultMissingOracle扩面批次/工作步骤细分 --format markdown
awk -F '\t' 'FNR==1{n=NF; next} NF!=n{print FILENAME ":" FNR ": expected " n " fields, got " NF; bad=1} END{exit bad}' docs/CADCore9.0/C9-M4-AssemblyDistanceTypeDefaultMissingOracle扩面批次/矩阵/*.tsv
rg -n '[ \t]$' docs/CADCore9.0/C9-M4-AssemblyDistanceTypeDefaultMissingOracle扩面批次 docs/CADCore9.0/README.md
git diff --check
```

## 非目标

- 不实现 GUI / ViewProvider / TaskPanel / Worker / WASM / Web。
- 不引入 cross-request solver session 或 persistent placement cache。
- 不实现 non-AssemblyLink primitive frame generalization。
- 不靠 fixture 名称、bbox、几何排序或输出修剪支持 default branch。
