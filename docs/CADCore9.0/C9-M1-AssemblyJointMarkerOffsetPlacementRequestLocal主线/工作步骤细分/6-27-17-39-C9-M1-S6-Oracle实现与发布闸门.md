# C9-M1 S6 Oracle 实现与发布闸门

## 目标

按 S5 裁决关闭 C9-M1：如果存在 source-backed `backend_gap_candidate`，完成受限实现和 focused tests；如果只剩 oracle / non-goal / publication rows，则执行 no-code 或 capability release gate。最终必须清空 C9-M1 队列。

## 本轮代码实现目标

S6 只消费 S3-S5 明确标出的 `backend_gap_candidate` 或 `release_gate` 行。允许落点：

- `cad-core/src/assembly/joint_solver.cpp`
- `cad-core/src/assembly/assembly_utils.cpp`
- `cad-core/src/assembly/assembly_object.cpp`
- `cad-core/src/runtime/capability_contract.cpp`
- `cad-core/tests/test_p8_features.py`
- `cad-core/tests/test_adapters.py`
- `cad-core/fixtures/c3m6/*`

如果 S5 未打开 backend gap，S6 不得主动修改 runtime。若需要采 native oracle，必须记录 FreeCADCmd / LibPack / OCCT 基线，并且不得用 current cad-core output 刷 expected。

## 禁止路径

- fixture 名称分支、几何类型猜测、bbox / angle 容差猜测、输出排序修补。
- adapter 层字符串改写来隐藏 Assembly runtime 缺口。
- cross-request solver session、persistent placement cache、full BREP / TopoDS_Shape / NamedShape / ElementMap 持久状态。
- GUI / ViewProvider / TaskPanel / Worker / WASM / Web 修改。

## 发布闸门

本轮短跑：

```bash
cd /home/user/Chili3DProject/FreeCAD
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore9.0/C9-M1-AssemblyJointMarkerOffsetPlacementRequestLocal主线/工作步骤细分 --format markdown
awk -F '\t' 'FNR==1{n=NF; next} NF!=n{print FILENAME ":" FNR ": expected " n " fields, got " NF; bad=1} END{exit bad}' docs/CADCore9.0/C9-M1-AssemblyJointMarkerOffsetPlacementRequestLocal主线/矩阵/*.tsv
rg -n '[ \t]$' docs/CADCore9.0
git diff --check
```

若改 C++、fixture、expected 或 capability：

```bash
cd /home/user/Chili3DProject/FreeCAD/cad-core
cmake --build build
python3 -m unittest tests.test_p8_features tests.test_adapters
./cad-core capabilities > /tmp/c9m1-capabilities.json
```

## 关闭条件

- `C9M1-BLOCKER-601` 关闭。
- C9-M1 S6 文件重命名为 `【已实现】` 后，队列为空。
- README 记录最终 route、验证命令、remaining / known-gap / non-goal 状态。
- 如果进入代码 gate，相关 focused tests 和 build 通过。

## 非目标

- 不把 oracle-only rows 写成 supported。
- 不重开 C8-M2 CopyOnChange 或 C8-M7 ImportShape。
- 不把完整 Assembly solver 产品化放进本轮。
