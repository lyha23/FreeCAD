# 【已实现】C9-M1 S0 live 基线与声明口径冻结

## 目标

冻结 C9-M1 live 起点，确认本包只处理 Assembly Joint marker / `offsetPlc` request-local 扩面，不重开 C8 已关闭主线或 C8-M2 CopyOnChange oracle-blocked known gap。

## 输入

- `docs/CADCore9.0/README.md`
- `docs/CADCore9.0/C9-M1-AssemblyJointMarkerOffsetPlacementRequestLocal主线/README.md`
- `docs/CADCore8.0/README.md`
- `docs/CADCore方案/细化方案/11-P8-Part导入导出与Assembly后续.md`
- `cad-core/src/runtime/capability_contract.cpp`
- `cad-core/tests/test_adapters.py`
- `cad-core/tests/test_p8_features.py`
- `../矩阵/*.tsv`

## 必须复核

- `pwd` 是 `/home/user/Chili3DProject/FreeCAD`。
- `git rev-parse --short=10 HEAD` 与 `git log -1 --oneline` 写回 README 和 blocker 矩阵。
- C8-M1 到 C8-M7 工作步骤队列为空。
- current capability 中 `assembly.ondsel_solver_adapter.available=true`，`subshape_marker_placement.status=covered_representative_subset`，non-goals 仍包含 `non_identity_bundled_offsetPlc` 与 `non_assembly_link_subshape_primitive_frame_generalization`。
- `part_design.sub_shape_binder.copy_on_change_full_temporary_document_cache` 保持 C8 known gap / oracle-blocked，不进入 C9-M1 实现范围。

## 必须回写

- `C9M1-SCOPE-001`
- `C9M1-SCOPE-101`
- `C9M1-NG-001` 到 `C9M1-NG-004`
- `C9M1-BLOCKER-000`
- `C9M1-VAL-001` 到 `C9M1-VAL-004`

## 冻结结果

- live baseline：`pwd=/home/user/Chili3DProject/FreeCAD`，`HEAD=ebd6fd1243`，`git log -1 --oneline` 为 `ebd6fd1243 docs: 新增 C9-M1 Assembly Joint marker 方案包`。
- 起点状态：`git -c core.quotepath=false status --short -uall` 无输出。
- C8 队列证据：C8-M1 到 C8-M7 的 `工作步骤细分` 目录逐一运行 `step_goal_queue.py --format markdown`，输出均只有表头，无 pending 行。
- C9 队列证据：S0 关闭前队列从本文件开始；S0 重命名后队列应从 S1 开始。
- capability 口径：`assembly.ondsel_solver_adapter.available=true`，`subshape_marker_placement.status=covered_representative_subset`，non-goals 仍包含 `non_identity_bundled_offsetPlc` 与 `non_assembly_link_subshape_primitive_frame_generalization`。
- CopyOnChange 排除：`part_design.sub_shape_binder.copy_on_change_full_temporary_document_cache` 保持 C8 known gap / `oracle_blocked`，不进入 C9-M1 实现范围。

## 验收标准

```bash
cd /home/user/Chili3DProject/FreeCAD
git -c core.quotepath=false status --short -uall
for d in docs/CADCore8.0/*/工作步骤细分; do python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py "$d" --format markdown; done
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore9.0/C9-M1-AssemblyJointMarkerOffsetPlacementRequestLocal主线/工作步骤细分 --format markdown
awk -F '\t' 'FNR==1{n=NF; next} NF!=n{print FILENAME ":" FNR ": expected " n " fields, got " NF; bad=1} END{exit bad}' docs/CADCore9.0/C9-M1-AssemblyJointMarkerOffsetPlacementRequestLocal主线/矩阵/*.tsv
rg -n '[ \t]$' docs/CADCore9.0
git diff --check
rg -n 'subshape_marker_placement|non_identity_bundled_offsetPlc|non_assembly_link_subshape_primitive_frame_generalization|copy_on_change_full_temporary_document_cache' cad-core/src/runtime/capability_contract.cpp docs/CADCore9.0 docs/CADCore8.0/README.md
```

S0 关闭时，C9-M1 README 与矩阵必须记录 live HEAD、C8 queue empty、current Assembly capability 口径、CopyOnChange known gap 排除理由。

## 非目标

- 不采 FreeCAD oracle。
- 不改 `cad-core/src`、fixtures、expected 或 tests。
- 不跑重型阶段回归。
