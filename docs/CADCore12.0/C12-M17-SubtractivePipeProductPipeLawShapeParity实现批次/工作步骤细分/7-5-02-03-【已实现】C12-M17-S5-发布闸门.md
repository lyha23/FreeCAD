# 【已实现】C12-M17 S5 发布闸门

## 目标

汇总 S0-S4 证据，发布 C12-M17 的最终实现状态，并把结果写回 package README、方案、总入口、矩阵和 root README。

## 必读文件

- `../README.md`
- `../7-5-01-56-C12-M17-SubtractivePipeProductPipeLawShapeParity实现批次方案.md`
- `../7-5-01-56-C12-M17-SubtractivePipeProductPipeLawShapeParity实现批次总入口.md`
- `../矩阵/*.tsv`
- `docs/CADCore12.0/README.md`

## 操作

1. 确认 S0-S4 均已 `【已实现】`，或保留明确 blocker。
2. 发布 final status：`implemented_freecad_main_shape_parity_product_law_retained`、`blocked_missing_expected_or_test_surface` 或 `blocked_contract_conflict`。
3. 更新 README / 方案 / 总入口 / root README 当前状态。
4. 更新 blocker queue 和 validation matrix。
5. 运行队列、TSV、whitespace 和 `git diff --check`。
6. 将本步骤重命名为 `【已实现】`。

## 执行记录

- S5 发布 final status：`implemented_freecad_main_shape_parity_product_law_retained`。
- 证据汇总：S3 已让 main product `SubtractivePipe` response、mesh、subshapes、bbox、volume 和 namedShape 跟随 post-cut `featureShape` / `featureNamedShape`；`context.shapes[pipe]` 与主响应一致；`AddSubShape` subtractive cache 仍保存 pre-boolean `toolShape` / `toolNamedShape`；PipeLaw `LawSamples` / `Linear` / `S-shape` 仍是 CAD Core product extensions；几何共线 BSpline / 非 Line PartDesign axis 仍保留为 product extension / non-native boundary。
- S4 已同步公开口径：capability contract、adapter assertion、expected wording 和 capability 边界文档均区分 PipeLaw product extension 与 `SubtractivePipe` 主 `Shape` FreeCAD-compatible lifecycle。
- 本步只更新 release docs、root README、矩阵和本 step 记录；未新增生产 C++ 功能、fixtures、expected、tests、capability source、frontend work 或新验证 surface。
- S4 已通过 `cmake --build build`、`python3 -m unittest tests.test_adapters.CadCoreAdapterTest.test_c_api_capabilities_exposes_web_contract_facts` 和 `python3 -m unittest tests.test_p7_features.CadCoreP7FeatureTest.test_c6m3_partdesign_pipe_interpolation_law_products_publish_contract_metadata`。S5 未触碰代码 / test / expected / capability source，未重跑 focused tests。
- S5 轻量验证通过：队列脚本只输出 markdown 表头；C12-M17 TSV 字段数检查无输出；尾随空白检查无输出（`rg` exit 1 为 no-match 预期）；`git diff --check` 无输出。
- `C12M17-BLOCKER-601` 已关闭；blocker queue 无 open row。重开条件：未来 focused regression、expected/current mismatch 或明确产品契约冲突。

## 关闭条件

- final status 与 S0-S4 evidence 一致。
- root README 已写入 C12-M17 状态。
- 队列关闭后只输出表头。
- blocker queue 无悬空 open row，或每个 open row 有明确 reopen condition。

## 非目标

- 不在发布闸门临时补新功能。
- 不把 axis product extension 改成 strict FreeCAD parity。
- 不做全量 FreeCAD build，除非 S4 明确要求。

## 验收

```bash
cd /Users/li/Chili3DProject/FreeCAD
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore12.0/C12-M17-SubtractivePipeProductPipeLawShapeParity实现批次/工作步骤细分 --format markdown
awk -F '\t' 'FNR==1{n=NF; next} NF!=n{print FILENAME ":" FNR ": expected " n " fields, got " NF; bad=1} END{exit bad}' docs/CADCore12.0/C12-M17-SubtractivePipeProductPipeLawShapeParity实现批次/矩阵/*.tsv
rg -n '[ \t]$' docs/CADCore12.0/C12-M17-SubtractivePipeProductPipeLawShapeParity实现批次 docs/CADCore12.0/README.md
git diff --check
```
