# C12-M6 S0 live 基线与继承口径冻结【已实现】

## 目标

冻结 C12-M6 起点，确认 C12-M5 队列已关闭、旧 PARTSURF wire/wire blocker 的继承口径，以及 current capability 是否已经声称 wire/wire supported。

## 必读来源

- `docs/CADCore12.0/README.md`
- `docs/CADCore12.0/C12-M5-SubShapeBinderCopyOnChangeRequestLocalDTO准入复审批次/`
- `docs/FreeCAD几何生态迁移工程-细分/C3M4-PartWorkbenchSurface-RuledProjection收口主线/矩阵/part_surface_blocker_queue.tsv`
- `cad-core/src/runtime/capability_contract.cpp`
- `cad-core/tests/test_adapters.py`

## 操作

1. 执行 live baseline：`pwd`、`git rev-parse --short HEAD`、`git log -1 --oneline`、`git -c core.quotepath=false status --short -uall`。
2. 检查 C12-M1..M5 队列为空，并记录 C12-M6 当前队列。
3. 运行 `cad-core/build/cad-core capabilities`，摘出 `part_workbench.ruled_surface` 的 status、fixtures、remaining_gaps、covered 和 request_local_boundaries。
4. 读取旧 `PARTSURF-BLOCK-005`、`PARTSURF-SCOPE-007`、`PARTSURF-FIX-005`，确认旧 deferred 条件是 collector、input schema、shell/topo provenance。
5. 更新本包 scope / blocker / validation 矩阵，只关闭 S0 行。

## S0 live 冻结结果

- `pwd=/Users/li/Chili3DProject/FreeCAD`。
- `git rev-parse --short HEAD=71dba06fb6`。
- `git log -1 --oneline=71dba06fb6 docs: 完成 C12-M5 S5 发布闸门`。
- `git -c core.quotepath=false status --short -uall` 显示 `docs/CADCore12.0/README.md` 已修改，C12-M6 包为未跟踪新增；未发现 `cad-core/src`、fixtures、expected、tests、adapters 或 capability source 改动。
- C12-M1 / C12-M2 / C12-M3 / C12-M4 / C12-M5 `工作步骤细分` 队列均只输出表头。
- C12-M6 S0 开始前队列为索引、S0、S1、S2、S3、S4、S5；索引只承担导航职责，已随 S0 标记为 `【已实现】`。
- `cad-core/build/cad-core capabilities` 已运行，`part_workbench.ruled_surface.status=supported_wire_wire_expected_backed`。
- `part_workbench.ruled_surface.fixtures=["p8/part-ruled-surface-line-line","p8/part-ruled-surface-conic-line","p8/part-ruled-surface-orientation-reversed","p8/part-ruled-surface-invalid-input","c4m1/part-ruled-surface-wire-wire"]`。
- `part_workbench.ruled_surface.remaining_gaps=[]`。
- `part_workbench.ruled_surface.covered` 包含 `source_backed_document_object_executor`、`curve1_curve2_property_link_sub`、`orientation_automatic_forward_reversed`、`edge_edge_brepfill_face`、`wire_wire_brepfill_shell`、`source_edge_provenance`、`wire_edge_provenance`、`conic_edge_consumer`、`expected_backed_fixtures`。
- `part_workbench.ruled_surface.request_local_boundaries` 包含 `source_shape_recomputed_from_document_graph`、`edge_edge_brepfill_face`、`wire_wire_brepfill_shell`、`conic_line_expected_uses_make_ruled_surface_after_link_resolve`。

## S0 继承口径

- 旧 `PARTSURF-BLOCK-005` 为 `deferred-S2`，原因是当时 collector path、cad-core input schema、shell/topo provenance checks 未 ready。
- 旧 `PARTSURF-SCOPE-007` 冻结 wire/shell branch：只有 collector、input schema、provenance validation 全部设计并通过后，才能从 deferred 晋升。
- 旧 `PARTSURF-FIX-005` 冻结 `part-ruled-surface-wire-wire`：FreeCAD 调用 `BRepFill::Shell(Wire, Wire)`，但旧 S2 没有 collector path、input schema proof 或 shell/topo provenance assertion。
- S0 只冻结 live 起点和继承条件，不把 old deferred 行直接判为 current backend gap，也不发布 `wire_wire_admitted_current_supported`。

## 下一步

- 下一步是 S1 source/current evidence 复核：读取 FreeCAD `Part::RuledSurface` / `TopoShapeExpansion` 调用链，并对照 current cad-core executor、fixture、expected、tests 和 capability 口径。

## 输出

- live baseline 写入 `c12m6_ruled_surface_wire_wire_scope_review_matrix.tsv`。
- `C12M6-BLOCKER-000` 关闭为 S0 完成。
- 记录下一步为 S1 source/current evidence 复核。

## 验收

```bash
cd /Users/li/Chili3DProject/FreeCAD
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore12.0/C12-M6-RuledSurfaceWireWire准入验证批次/工作步骤细分 --format markdown
git diff --check
```
