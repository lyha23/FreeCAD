# C12-M6 工作步骤总入口【已实现】

## 目标

按顺序执行 C12-M6 S0-S5，验证 `Part::RuledSurface` wire/wire current support 是否能正式准入，并裁决旧 `PARTSURF-BLOCK-005`。

## 执行规则

1. 每步开始前执行 live baseline：`pwd`、`git rev-parse --short HEAD`、`git log -1 --oneline`、`git -c core.quotepath=false status --short -uall`。
2. S0-S4 不改 `cad-core/src`、fixtures、expected、tests、adapters 或 capability source；只读 live evidence 并更新本包矩阵。
3. S5 只在三闸门通过后允许文档发布修复；只有 expected/current mismatch 明确定位到 C++ 才输出 implementation candidate。
4. 每步完成后重命名为 `【已实现】` 并更新 README / 总入口 / 矩阵中对应状态。

## S0 索引复核结果

- S0 执行已确认本文件只承担 C12-M6 S0-S5 队列索引职责，顺序和验收命令成立。
- 本索引不承载独立实现任务；为避免队列卡在索引，随 S0 一并标记为 `【已实现】`。

## 顺序

- S0：live 基线与继承口径冻结（已完成，下一步为 S1 source/current evidence 复核）。
- S1：FreeCAD 源码与旧 PARTSURF 证据复核（已完成，下一步为 S2 collector / expected gate）。
- S2：collector / expected 准入验证（已完成）。
- S3：request-local input schema 准入验证（已完成，下一步为 S4 shell/topo provenance gate）。
- S4：shell/topo provenance 准入验证。
- S5：implementation gate 与发布闸门。

## 验收

```bash
cd /Users/li/Chili3DProject/FreeCAD
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore12.0/C12-M6-RuledSurfaceWireWire准入验证批次/工作步骤细分 --format markdown
awk -F '\t' 'FNR==1{n=NF; next} NF!=n{print FILENAME ":" FNR ": expected " n " fields, got " NF; bad=1} END{exit bad}' docs/CADCore12.0/C12-M6-RuledSurfaceWireWire准入验证批次/矩阵/*.tsv
git diff --check
```
