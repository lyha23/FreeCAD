# 【已实现】C12-M1 S2 范围准入与 blocker 矩阵

## 目标

把 S1 source candidates 路由成 scope review、blocker queue、non-goal registry 和 backend gap classification。S2 只做准入分类，不采 oracle，不改 C++。

## 分类规则

| 分类 | 准入条件 | S6 走向 |
| --- | --- | --- |
| `active_remaining_gap_oracle_blocked` | live capability 有非空 `remaining_gaps`，但 known gap route 是 oracle/product blocked。 | S3 复审；默认 retained。 |
| `representative_or_product_scope_candidate` | capability 是 representative subset、fallback metadata 或产品边界候选。 | S4 复审；需要产品授权和 expected 才能开代码。 |
| `historical_narrowed_or_non_parity` | `remaining_gaps=[]`，但存在 narrowed/historical native evidence。 | S5 复审；需要 stable oracle/current mismatch 才能重开。 |
| `non_goal` | GUI、session、persistent state、backend cache、adapter repair 等。 | 保留 forbidden claim。 |
| `implementation_candidate` | 有 source authority、stable expected/current mismatch、request-local 边界和 focused test route。 | 只能作为 S6 条件占位；S2 不授权代码。 |

## 必须回写的矩阵行

- `C12M1-SCOPE-001..401`
- `C12M1-BLOCKER-201`
- `C12M1-NG-001..008`
- `C12M1-CAT-001..005`

## 验收

```bash
cd /home/user/Chili3DProject/FreeCAD
rg -n 'implementation_candidate|active_remaining_gap_oracle_blocked|representative_or_product_scope_candidate|historical_narrowed_or_non_parity|non_goal' docs/CADCore12.0/C12-M1-CADCoreCapabilityImplementationCandidate盘点批次/矩阵
awk -F '\t' 'FNR==1{n=NF; next} NF!=n{print FILENAME ":" FNR ": expected " n " fields, got " NF; bad=1} END{exit bad}' docs/CADCore12.0/C12-M1-CADCoreCapabilityImplementationCandidate盘点批次/矩阵/*.tsv
git diff --check
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore12.0/C12-M1-CADCoreCapabilityImplementationCandidate盘点批次/工作步骤细分 --format markdown
```

通过条件：

- 每个 scope 行都有 owner step、current status 和 close condition。
- `backend_gap_classification.tsv` 中没有无证据的 `implementation_candidate`。
- non-goal registry 覆盖 session/cache/GUI/adapter repair/historical evidence overclaim。
- 验证后本文件已重命名为 `6-29-16-30-【已实现】C12-M1-S2-范围准入与blocker矩阵.md`，并更新工作步骤索引。

## S2 结论

- `C12M1-SCOPE-001..401` 已补齐 `owner_step`、`current_status`、`next_step` 和 `close_condition`：CopyOnChange 交 S3，Assembly representative / marker 交 S4，Sweep / Filling / GeomPlate / Loft / ProjectOnSurface 交 S5，S6 只保留发布闸门。
- `C12M1-BLOCKER-201` 已关闭为 `closed_s2`。
- `C12M1-NG-001..008` 已覆盖 session、cross-request cache、GUI / TaskPanel、adapter repair、App::Link overclaim、historical notCollected 和 C11 closed-line overclaim。
- `C12M1-CAT-001..005` 已完成分类：`implementation_candidate` 只是 `none_s2_placeholder`，没有 scope 被 S2 授权进入代码。

## 非目标

- S2 不运行 FreeCADCmd。
- S2 不创建 expected 或 fixtures。
- S2 不把 `notCollected`、`narrowed_gaps` 或 representative subset 直接写成 backend gap。
