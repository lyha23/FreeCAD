# 【已实现】C12-M7 S5 发布闸门与后续分流

## 目标

消费 S0-S4，发布 C12-M7 最终出口并关闭队列。

## 本轮基线

- `pwd=/Users/li/Chili3DProject/FreeCAD`。
- `git rev-parse --short HEAD=735f6ed5f7`。
- `git log -1 --oneline=735f6ed5f7 docs: 关闭 C12-M7 S4 验证闸门`。
- 起点 `git -c core.quotepath=false status --short -uall` 无输出。
- S5 执行前队列首项仍是本文件。

## 最终出口

最终出口发布为 `product_diagnostic_contract_published`。

- Native failure 仍成立：S1 继续采用 `FreeCADCmd 1.2.0 revision 20260519` 下两个 Groove UpTo fixtures 报 `Groove: Revolution: Up to face: Could not revolve the sketch!` 的证据。
- S2 已批准 CAD Core request-local product diagnostic contract。
- S3 已完成两个 expected、expected-backed focused test、capability wording、adapter assertion 和 C12-M7 文档 / 矩阵迁移。
- S4 focused Groove test、C API capability smoke 和 `/tmp/c12m7-s4-capabilities.json` review 均通过，`part_design.revolution_groove` 仍发布 `product_diagnostic_contract_non_parity`，且 `freecad_native_parity=false`。

## 后续分流

本包不创建 geometry implementation package，不改 `cad-core/src`、`cad-core/include`、fixtures、expected、tests 或 adapters。

只有同一 FreeCAD / LibPack / OCCT oracle baseline 证明 `Groove Type=UpToFirst` 与 `Groove Type=UpToFace` native success，且 current CAD Core 与 stable expected 存在 mismatch 时，才另开 geometry implementation candidate。该后续包必须同时覆盖 UpToFirst 与 UpToFace，不得只迁移单个 fixture，也不得删除 C12-M7 native failure note。

## 输出

- C12-M7 README、总入口、方案、矩阵和 `docs/CADCore12.0/README.md` 已记录最终出口、release gate 和重开条件。
- `C12M7-BLOCKER-501` 关闭为 `closed_s5_product_diagnostic_contract_published`。
- `C12M7-SCOPE-007` 关闭为 `closed_s5_release_gate_published`。
- 最终 validation `whitespace_check` / `diff_check` 标记为 `passed_s5`。

## 验收

```bash
cd /Users/li/Chili3DProject/FreeCAD
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore12.0/C12-M7-PartDesignGrooveUpTo产品契约准入批次/工作步骤细分 --format markdown
awk -F '\t' 'FNR==1{n=NF; next} NF!=n{print FILENAME ":" FNR ": expected " n " fields, got " NF; bad=1} END{exit bad}' docs/CADCore12.0/C12-M7-PartDesignGrooveUpTo产品契约准入批次/矩阵/*.tsv
rg -n '[ \t]$' docs/CADCore12.0/C12-M7-PartDesignGrooveUpTo产品契约准入批次 docs/CADCore12.0/README.md
git diff --check
```
