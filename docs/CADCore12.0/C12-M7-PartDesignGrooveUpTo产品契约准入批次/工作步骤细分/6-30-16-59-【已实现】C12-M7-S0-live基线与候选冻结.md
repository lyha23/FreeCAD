# C12-M7 S0 live 基线与候选冻结【已实现】

## 目标

冻结 C12-M7 起点，确认 C12-M1..M6 队列为空，并记录为何下一步选择 Groove UpTo exact narrowed gap，而不是 CopyOnChange 或 RuledSurface wire/wire。

## 必读来源

- `docs/CADCore12.0/README.md`
- `cad-core/src/runtime/capability_contract.cpp`
- `cad-core/tests/test_adapters.py`
- `docs/CADCore12.0/C12-M5-SubShapeBinderCopyOnChangeRequestLocalDTO准入复审批次/`
- `docs/CADCore12.0/C12-M6-RuledSurfaceWireWire准入验证批次/`

## 操作

1. 执行 live baseline。
2. 检查 C12-M1..M6 队列均为空。
3. 运行 `cad-core/build/cad-core capabilities`，摘录 `part_design.revolution_groove`、`part_design.sub_shape_binder` 和 `part_workbench.ruled_surface`。
4. 更新 scope / source / blocker / validation 中 S0 行，只关闭 S0 blocker。

## S0 live 基线

- `pwd=/Users/li/Chili3DProject/FreeCAD`。
- `git rev-parse --short HEAD=bb69e61a0f`。
- `git log -1 --oneline=bb69e61a0f docs: 关闭 C12-M7 工作步骤总入口`。
- `git -c core.quotepath=false status --short -uall` 无输出，S0 起点 worktree clean。
- S0 开始前 C12-M7 队列首项为本文件；C12-M1..M6 队列均只输出表头。

## capability 摘录

- `part_design.revolution_groove`：`status=supported_c51s1_advanced_with_historical_groove_upto_native_failure`，`remaining_gaps=[]`，`narrowed_gaps.partdesign_groove_upto_brepfeat_cut_native_failure.status=published_c6m9_historical_native_failure`，`route=historical_native_failure`，fixtures 为 `c51m1/partdesign-groove-uptofirst-body` 与 `c51m1/partdesign-groove-uptoface-body`；CAD Core diagnostic 为 `BRepFeat_MakeRevol could not revolve profile up to face`。
- `part_design.sub_shape_binder`：`status=supported_c8m1_expected_backed_request_local_with_copy_on_change_known_gap`，`remaining_gaps=["copy_on_change_full_temporary_document_cache"]`，该 known gap 仍为 `status=known_gap_diagnostic`、`route=oracle_blocked`、diagnostic=`copy_on_change_full_temporary_document_cache_not_supported`。
- `part_workbench.ruled_surface`：`status=supported_wire_wire_expected_backed`，fixtures 包含 `c4m1/part-ruled-surface-wire-wire`，`remaining_gaps=[]`，covered / request-local boundaries 含 `wire_wire_brepfill_shell`。

## 冻结结论

- C12-M1..M6 队列均为空，C12-M7 S0 起点成立。
- 不选择 CopyOnChange：C12-M5 最终出口是 `no_code_retained_diagnostic`，native copied-object graph、产品 DTO 和 current mismatch 没有同时成立。
- 不选择 RuledSurface wire/wire：C12-M6 最终出口是 `wire_wire_admitted_current_supported`，live capability 已无 remaining gap。
- 选择 Groove UpTo exact narrowed gap：`part_design.revolution_groove` 没有 active remaining gap，但 exact narrowed gap 已有两条 fixtures、FreeCAD source authority、current CAD Core diagnostic 和 product-contract reopen condition；下一步应进入 S1 native failure 与 current diagnostic 复核。

## 输出

- `C12M7-BLOCKER-000` 关闭为 `closed_s0_live_candidate_frozen`。
- `C12M7-SCOPE-001` 关闭为 `closed_s0_live_candidate_frozen`。
- `C12M7-SRC-001` 关闭为 `closed_s0`。
- `C12M7-VAL-001..003` 标记为 `passed`。
- 本步未修改 `cad-core/src`、`cad-core/include`、fixtures、expected、tests、adapters 或 capability source，未运行 FreeCADCmd，未采 oracle，未处理 S1-S5，也未批准 product diagnostic contract。

## 验收

```bash
cd /Users/li/Chili3DProject/FreeCAD
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore12.0/C12-M7-PartDesignGrooveUpTo产品契约准入批次/工作步骤细分 --format markdown
awk -F '\t' 'FNR==1{n=NF; next} NF!=n{print FILENAME ":" FNR ": expected " n " fields, got " NF; bad=1} END{exit bad}' docs/CADCore12.0/C12-M7-PartDesignGrooveUpTo产品契约准入批次/矩阵/*.tsv
git diff --check
```
