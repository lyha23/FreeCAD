# C12-M5 S0 live 基线与继承口径冻结【已实现】

## 目标

冻结 C12-M5 起点：当前 live capability、C12-M1..M4 队列状态、C9-M5 / C10-M4 CopyOnChange retained diagnostic 结论和本包 forbidden claims。

## 必读文件

- `../README.md`
- `../6-30-01-03-C12-M5-SubShapeBinderCopyOnChangeRequestLocalDTO准入复审批次总入口.md`
- `../矩阵/c12m5_copy_on_change_scope_review_matrix.tsv`
- `../矩阵/c12m5_copy_on_change_blocker_queue.tsv`
- `docs/CADCore9.0/C9-M5-SubShapeBinderCopyOnChangeRequestLocalDTO准入复审包/README.md`
- `docs/CADCore10.0/C10-M4-SubShapeBinderCopyOnChangeDTO准入批次/README.md`

## 操作

1. 记录 `pwd`、HEAD、最新 commit、dirty boundary。
2. 运行 C12-M1..M4 `step_goal_queue.py`，确认旧队列为空。
3. 运行 `cad-core/build/cad-core capabilities`，记录 SubShapeBinder remaining gap、known gap status、route 和 diagnostic。
4. 更新 S0 owner 的 scope / blocker / validation 行。
5. 不运行 FreeCADCmd，不采 oracle，不改 C++。

## S0 live 冻结结果

- `pwd=/Users/li/Chili3DProject/FreeCAD`。
- `git rev-parse --short HEAD=84179ae66d`。
- `git log -1 --oneline=84179ae66d docs: 新增 C12-M5 CopyOnChange 准入方案`。
- `git -c core.quotepath=false status --short -uall` 无输出，dirty boundary 为 `<clean>`；未发现非本任务 dirty work。
- C12-M1 / C12-M2 / C12-M3 / C12-M4 `工作步骤细分` 队列均只输出表头。
- `part_design.sub_shape_binder.remaining_gaps=["copy_on_change_full_temporary_document_cache"]`。
- `part_design.sub_shape_binder.known_gaps.copy_on_change_full_temporary_document_cache.status=known_gap_diagnostic`。
- `part_design.sub_shape_binder.known_gaps.copy_on_change_full_temporary_document_cache.route=oracle_blocked`。
- diagnostic 仍为 `copy_on_change_full_temporary_document_cache_not_supported`，delete condition 仍要求 FreeCADCmd 暴露不依赖 persistent backend session 的 stable request-local copied-object evidence。

## S0 继承口径

- C9-M5 S6 已关闭为 `no_code_retained_known_gap_release_gate`，CopyOnChange full temporary-document copied-object lifecycle 仍只保留 diagnostic。
- C10-M4 S6 已关闭为 no-code retained diagnostic / release gate，App::Link `documentObjectUpdates` 仍只是 DTO 参考路径，不等同于 SubShapeBinder CopyOnChange supported。
- 本轮不打开 implementation candidate；S1-S5 继续按 source authority、native evidence、DTO 产品边界、current mismatch 和发布闸门顺序执行。

## 非目标

- 不打开 implementation candidate。
- 不改 `cad-core/src`、fixtures、expected、tests、adapter 或 capability source。
- 不把 C9-M5 / C10-M4 旧结论删掉。

## 验收

```bash
cd /Users/li/Chili3DProject/FreeCAD
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore12.0/C12-M5-SubShapeBinderCopyOnChangeRequestLocalDTO准入复审批次/工作步骤细分 --format markdown
awk -F '\t' 'FNR==1{n=NF; next} NF!=n{print FILENAME ":" FNR ": expected " n " fields, got " NF; bad=1} END{exit bad}' docs/CADCore12.0/C12-M5-SubShapeBinderCopyOnChangeRequestLocalDTO准入复审批次/矩阵/*.tsv
git diff --check
```
