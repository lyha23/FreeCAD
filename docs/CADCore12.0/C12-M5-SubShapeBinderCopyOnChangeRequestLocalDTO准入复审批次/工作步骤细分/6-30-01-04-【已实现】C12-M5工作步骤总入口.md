# C12-M5 工作步骤总入口【已实现】

## 目标

按顺序执行 C12-M5 S0-S5，判断 SubShapeBinder CopyOnChange 是否能从 retained known gap 升级为 request-local DTO implementation candidate。

## 执行规则

1. 每步开始前执行 live baseline：`pwd`、`git rev-parse --short HEAD`、`git log -1 --oneline`、`git -c core.quotepath=false status --short -uall`。
2. 每步只处理本步骤 owner 的矩阵行和 blocker，不提前关闭后续步骤。
3. 若发现 unrelated dirty work，不回退、不覆盖；只读取或暂停确认。
4. 不把 CopyOnChange full temporary document cache 写成 supported，除非 S4 明确发布 implementation candidate。
5. 每步完成后重命名为 `【已实现】` 并更新 README / 总入口 / 矩阵中对应状态。

## S0 索引复核结果

- S0 执行已确认本文件只承担 C12-M5 S0-S5 队列索引职责，顺序和验收命令成立。
- 本索引不承载独立实现任务；为避免队列卡在索引，随 S0 一并标记为 `【已实现】`。

## 顺序

- S0：live 基线与继承口径冻结。
- S1：FreeCAD 源码与 current 覆盖矩阵。
- S2：native evidence 刷新与 probe 准入（已完成，关闭为 `native_evidence_retained_blocker`）。
- S3：request-local DTO 产品边界冻结。
- S4：current mismatch 与实现候选闸门。
- S5：发布闸门与后续分流。

## 验收

```bash
cd /Users/li/Chili3DProject/FreeCAD
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore12.0/C12-M5-SubShapeBinderCopyOnChangeRequestLocalDTO准入复审批次/工作步骤细分 --format markdown
awk -F '\t' 'FNR==1{n=NF; next} NF!=n{print FILENAME ":" FNR ": expected " n " fields, got " NF; bad=1} END{exit bad}' docs/CADCore12.0/C12-M5-SubShapeBinderCopyOnChangeRequestLocalDTO准入复审批次/矩阵/*.tsv
git diff --check
```
