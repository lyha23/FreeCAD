# C12-M5 S5 发布闸门与后续分流【已实现】

## 目标

消费 S0-S4 结论，发布 C12-M5 最终出口：implementation package、oracle refresh、DTO rejected 或 no-code retained diagnostic。

## 必读文件

- `../README.md`
- `../6-30-01-03-C12-M5-SubShapeBinderCopyOnChangeRequestLocalDTO准入复审批次总入口.md`
- `../矩阵/*.tsv`
- `docs/CADCore12.0/README.md`

## 操作

1. 逐行复核 source、scope、DTO、backend classification、blocker、non-goal 和 validation 矩阵。
2. 如果 S4 发布 implementation candidate，写清后续 implementation package 的最小文件面和 focused tests。
3. 如果没有 implementation candidate，发布 `no_code_retained_diagnostic`，保留 capability `remaining_gaps` 和 diagnostic。
4. 更新 root `docs/CADCore12.0/README.md` 当前状态。
5. 重命名 S5 文件为 `【已实现】`，确认队列只输出表头。

## S5 live 基线

- `pwd=/Users/li/Chili3DProject/FreeCAD`。
- `git rev-parse --short HEAD=c1f02e3d7e`。
- `git log -1 --oneline=c1f02e3d7e docs: 完成 C12-M5 S4 实现候选闸门`。
- `git -c core.quotepath=false status --short -uall` 无输出，dirty boundary 为 `<clean>`；未发现非本任务 dirty work。
- S5 开始前队列首项为本文件。

## S5 复核结论

- source/current：FreeCAD full CopyOnChange 仍依赖 `_tmp_binder`、`_CopiedObjs`、`copyObject()`、`recomputeFeature(true)` 和 `_CopiedLink`；current `cad-core` 仍只保留 retained diagnostic。
- scope/evidence：S2=`native_evidence_retained_blocker`，没有 stable copied-object graph、dependency order、copied support rewrite 或 ElementMap lifecycle DTO 证据。
- DTO/product：S3=`dto_rejected_known_gap_retained`，temporary document、native pointer、full object BREP / TopoDS、post-request `NamedShape` / `ElementMap` cache、`_CopiedObjs` 和 `_tmp_binder` session state 仍禁止进入 request-local DTO。
- current gate：S4=`no_current_mismatch_retained_diagnostic`，不存在 approved DTO 与 current diagnostic 的冲突。
- 最终出口：`no_code_retained_diagnostic`。C12-M5 不创建 implementation package，不刷新 oracle，不改 `cad-core/src`、tests、fixtures、expected 或 adapter。
- capability 继续保留 `remaining_gaps=["copy_on_change_full_temporary_document_cache"]`、`known_gap_diagnostic` / `oracle_blocked` 和 diagnostic `copy_on_change_full_temporary_document_cache_not_supported`。
- 后续重开条件仅为 stable native copied-object graph evidence + 产品批准 request-local DTO + current `cad-core` mismatch；该 future condition 不代表 current supported。

## 非目标

- 不在发布闸门临时新增 C++ 改动。
- 不删除 retained diagnostic。
- 不把 future reopen condition 写成 current supported。

## 验收

```bash
cd /Users/li/Chili3DProject/FreeCAD
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore12.0/C12-M5-SubShapeBinderCopyOnChangeRequestLocalDTO准入复审批次/工作步骤细分 --format markdown
awk -F '\t' 'FNR==1{n=NF; next} NF!=n{print FILENAME ":" FNR ": expected " n " fields, got " NF; bad=1} END{exit bad}' docs/CADCore12.0/C12-M5-SubShapeBinderCopyOnChangeRequestLocalDTO准入复审批次/矩阵/*.tsv
rg -n '[ \t]$' docs/CADCore12.0/C12-M5-SubShapeBinderCopyOnChangeRequestLocalDTO准入复审批次 docs/CADCore12.0/README.md
git diff --check
```
