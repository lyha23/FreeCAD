# 【已实现】C12-M13 S0 live 基线与 C12-M12 继承冻结

## 目标

冻结 C12-M13 实现前的真实仓库状态，并确认 C12-M12 的哪些结论可以继承、哪些剩余项必须继续实现。

## 必读文件

- `../README.md`
- `../7-4-10-35-C12-M13-FreeCADSweepPipe剩余语义迁移批次方案.md`
- `../../C12-M12-FreeCADSweepPipeParity迁移批次/README.md`
- `../矩阵/c12m13_sweep_remainder_scope_matrix.tsv`
- `../矩阵/c12m13_sweep_remainder_blocker_queue.tsv`
- `../矩阵/c12m13_sweep_remainder_validation_matrix.tsv`

## 操作

1. 记录 `pwd`、`git rev-parse --short HEAD`、`git log -1 --oneline`。
2. 记录 `git -c core.quotepath=false status --short -uall`，按 docs、`cad-core/src`、tests、fixtures、other 分组。
3. 运行 `step_goal_queue.py` 确认 C12-M12 队列为空、C12-M13 队列从 S0 开始。
4. 盘点现有 Pipe/Sweep focused tests 与 fixtures，特别是 `c5m3`、`c51m4`、`c12m12`、P7/P8。
5. 回写 blocker / validation matrix，并将本步骤重命名为 `【已实现】`。

## 关闭记录

- live 基线：`pwd=/Users/li/Chili3DProject/FreeCAD`；`HEAD=592ee9f5b2`；`git log -1 --oneline=592ee9f5b2 feat: 支持 PartDesign Pipe 多线截面缝合`。
- dirty boundary：docs=`M docs/CADCore12.0/README.md` 与未跟踪的 C12-M13 docs/package；`cad-core/src`=`<none>`；tests=`<none>`；fixtures=`<none>`；other=`<none>`。未发现非本包 dirty。
- 队列：C12-M12 队列脚本只输出表头；C12-M13 关闭前队列从 S0-S6 pending 开始，S0 关闭后下一步应为 S1。
- 可继承：C12-M12 final status `partial_implementation_multiwire_pipe_sewing`、multi-wire shell lane + shared cap/sewing、fixed/round selected-spine current-supported、Part Sweep wrapper/response regression current-supported。
- 不可继承为完成：完整 Sweep / Pipe 迁移、profile vertex / last-section vertex / inner wire count diagnostic、`AddSubShape` / `rawShape` / Boolean / Body Tip 生命周期、Part Sweep mutable helper lifecycle、ORACLE-001 用户失败复现。
- 现有 surface：P7 Pipe fixtures/tests 覆盖 `c4m2`、`c5m3`、`c51m4`、`c12m12`、`c6m3`；P8 Sweep fixtures/tests 覆盖 `c3m4`、`c5m10`、`c5m12`、`c6m4`。

## 关闭条件

- dirty boundary 已记录，非本包改动不会被覆盖。
- C12-M12 继承口径写入 scope matrix。
- S1 需要复核的 FreeCAD source 和 cad-core landing 已确认。

## 非目标

- 不运行 FreeCADCmd。
- 不修改 `cad-core/src`。
- 不新增 expected。
