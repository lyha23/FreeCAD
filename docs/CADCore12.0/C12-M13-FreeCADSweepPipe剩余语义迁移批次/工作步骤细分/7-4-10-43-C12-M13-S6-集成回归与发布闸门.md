# C12-M13 S6 集成回归与发布闸门

## 目标

汇总 S0-S5 证据，发布 C12-M13 的最终状态，并把剩余 blocker 或下一步写回 root README。

## 必读文件

- `../README.md`
- `../7-4-10-35-C12-M13-FreeCADSweepPipe剩余语义迁移批次方案.md`
- `../矩阵/c12m13_sweep_remainder_scope_matrix.tsv`
- `../矩阵/c12m13_sweep_remainder_oracle_matrix.tsv`
- `../矩阵/c12m13_sweep_remainder_blocker_queue.tsv`
- `../矩阵/c12m13_sweep_remainder_validation_matrix.tsv`
- `docs/CADCore12.0/README.md`

## 操作

1. 确认 S0-S5 均已 `【已实现】`，或保留明确 blocker。
2. 运行 focused P7/P8 tests、TSV 字段数检查、whitespace scan、`git diff --check`。
3. 汇总 final status：`implementation_complete_for_remainder_batch`、`partial_implementation_with_named_followups` 或 `blocked_by_oracle_or_helper_visibility`。
4. 更新 package README、方案、总入口、root README、capability / adapter wording。
5. 将本步骤重命名为 `【已实现】`。

## 关闭条件

- final status 与 evidence 一致。
- validation matrix 记录所有关键命令。
- blocker queue 无悬空 `open` 行，或每个 open row 有 reopen condition。
- root README 已写入 C12-M13 最终状态。

## 非目标

- 不在发布闸门临时补新功能。
- 不做全量 FreeCAD build。
- 不提交 git commit，除非用户另行要求。
