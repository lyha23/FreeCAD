# C12-M8 S0 live 基线与继承口径冻结

## 目标

冻结 C12-M8 起点：当前 HEAD、dirty boundary、C12-M1..M7 队列、live SubShapeBinder capability、C12-M5 retained diagnostic 和 C12-M7 后续分流口径。

## 必读文件

- `../README.md`
- `../6-30-21-24-C12-M8-SubShapeBinderCopyOnChangeNativeCopiedGraphDTO准入解锁批次总入口.md`
- `../矩阵/c12m8_copy_on_change_scope_review_matrix.tsv`
- `../矩阵/c12m8_copy_on_change_blocker_queue.tsv`
- `docs/CADCore12.0/README.md`
- `docs/CADCore12.0/C12-M5-SubShapeBinderCopyOnChangeRequestLocalDTO准入复审批次/README.md`
- `docs/CADCore12.0/C12-M7-PartDesignGrooveUpTo产品契约准入批次/README.md`

## 操作

1. 记录 `pwd`、HEAD、latest commit 和 dirty boundary。
2. 运行 C12-M1..M7 `step_goal_queue.py`，确认旧队列为空。
3. 运行 `cad-core/build/cad-core capabilities`，记录 `part_design.sub_shape_binder` 的 status、remaining gap、known gap、route、diagnostic、delete condition 和 reopen condition。
4. 更新 S0 owner 的 scope / blocker / validation 行。
5. 随 S0 复核并标记工作步骤总入口。

## 关闭条件

- `C12M8-BLOCKER-001` 关闭：live baseline 已冻结。
- `C12M8-BLOCKER-002` 关闭：C12-M1..M7 队列为空。
- `C12M8-SCOPE-001` 记录为 `active_unlock_batch`。
- `C12M8-VAL-001..003` 记录实际结果。

## 非目标

- 不运行 FreeCADCmd。
- 不采 oracle。
- 不改 `cad-core/src`、fixtures、expected、tests、adapters 或 capability source。

## 验收

```bash
cd /Users/li/Chili3DProject/FreeCAD
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore12.0/C12-M8-SubShapeBinderCopyOnChangeNativeCopiedGraphDTO准入解锁批次/工作步骤细分 --format markdown
git diff --check
```
