# C12-M8 S0 live 基线与继承口径冻结【已实现】

## 目标

冻结 C12-M8 起点：当前 HEAD、dirty boundary、C12-M1..M7 队列、live SubShapeBinder capability、C12-M5 retained diagnostic 和 C12-M7 后续分流口径。

## 必读文件

- `../README.md`
- `../6-30-21-24-C12-M8-SubShapeBinderCopyOnChangeNativeCopiedGraphDTO准入解锁批次总入口.md`
- `../矩阵/c12m8_copy_on_change_scope_review_matrix.tsv`
- `../矩阵/c12m8_copy_on_change_blocker_queue.tsv`
- `docs/CADCore12.0/README.md`
- `docs/CADCore12.0/C12-M5-SubShapeBinderCopyOnChangeRequestLocalDTO准入复审批次/README.md`
- `docs/CADCore12.0/C12-M7-PartDesignGrooveUpTo产品契约准入批次/README.md`

## 操作

1. 记录 `pwd`、HEAD、latest commit 和 dirty boundary。
2. 运行 C12-M1..M7 `step_goal_queue.py`，确认旧队列为空。
3. 运行 `cad-core/build/cad-core capabilities`，记录 `part_design.sub_shape_binder` 的 status、remaining gap、known gap、route、diagnostic、delete condition 和 reopen condition。
4. 更新 S0 owner 的 scope / blocker / validation 行。
5. 随 S0 复核并标记工作步骤总入口。

## 关闭条件

- `C12M8-BLOCKER-001` 关闭：live baseline 已冻结。
- `C12M8-BLOCKER-002` 关闭：C12-M1..M7 队列为空。
- `C12M8-SCOPE-001` 记录为 `active_unlock_batch`。
- `C12M8-VAL-001..003` 记录实际结果。

## S0 live 冻结结果

- `pwd=/Users/li/Chili3DProject/FreeCAD`。
- `git rev-parse --short HEAD=fd9810dc23`。
- `git log -1 --oneline=fd9810dc23 文档：新增 C12-M8 CopyOnChange 解锁方案`。
- `git -c core.quotepath=false status --short -uall` 无输出，dirty boundary 为 `<clean>`；未发现非本任务 dirty work。
- S0 前 C12-M8 队列显示工作步骤总入口、S0-S6 均为 pending；S0 完成后队列应从 S1 开始。
- C12-M1 / C12-M2 / C12-M3 / C12-M4 / C12-M5 / C12-M6 / C12-M7 `工作步骤细分` 队列均只输出表头。
- `part_design.sub_shape_binder.status=supported_c8m1_expected_backed_request_local_with_copy_on_change_known_gap`。
- `part_design.sub_shape_binder.remaining_gaps=["copy_on_change_full_temporary_document_cache"]`。
- `part_design.sub_shape_binder.known_gaps.copy_on_change_full_temporary_document_cache.status=known_gap_diagnostic`。
- `part_design.sub_shape_binder.known_gaps.copy_on_change_full_temporary_document_cache.route=oracle_blocked`。
- diagnostic 仍为 `copy_on_change_full_temporary_document_cache_not_supported`。
- delete condition 仍是：FreeCADCmd 暴露不依赖 persistent backend session 的 stable request-local CopyOnChange copied-object evidence。
- reopen condition 仍是：产品批准由更强 native oracle 支撑的 request-local CopyOnChange DTO。

## S0 继承口径

- C12-M5 S5 已发布 `no_code_retained_diagnostic`，S2=`native_evidence_retained_blocker`、S3=`dto_rejected_known_gap_retained`、S4=`no_current_mismatch_retained_diagnostic` 继续作为本包约束。
- App::Link `documentObjectUpdates` 仍只是 reference-only transport 词汇，不等同于 SubShapeBinder `_tmp_binder` / `_CopiedObjs` / `copyObject()` lifecycle 已支持。
- C12-M7 S5 已发布 `product_diagnostic_contract_published`；Groove UpTo 的后续重开条件仍是同一 FreeCAD / LibPack / OCCT oracle baseline 证明 native success 且 current CAD Core 与 stable expected mismatch。
- RuledSurface wire/wire 仍继承 C12-M6 `wire_wire_admitted_current_supported`，不在 C12-M8 重开。
- 本轮不打开 implementation candidate；S1 只进入 FreeCAD source 与 current coverage 复核。

## 非目标

- 不运行 FreeCADCmd。
- 不采 oracle。
- 不改 `cad-core/src`、fixtures、expected、tests、adapters 或 capability source。

## 验收

```bash
cd /Users/li/Chili3DProject/FreeCAD
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore12.0/C12-M8-SubShapeBinderCopyOnChangeNativeCopiedGraphDTO准入解锁批次/工作步骤细分 --format markdown
git diff --check
```
