# C12-M8 S6 发布闸门与后续分流【已实现】

## 目标

发布 C12-M8 最终状态，更新 CADCore12.0 总 README，并确认队列、矩阵和验证命令闭环。

## 必读文件

- `../README.md`
- `../6-30-21-24-C12-M8-SubShapeBinderCopyOnChangeNativeCopiedGraphDTO准入解锁批次方案.md`
- `../6-30-21-24-C12-M8-SubShapeBinderCopyOnChangeNativeCopiedGraphDTO准入解锁批次总入口.md`
- `docs/CADCore12.0/README.md`
- `../矩阵/c12m8_copy_on_change_backend_gap_classification.tsv`
- `../矩阵/c12m8_copy_on_change_validation_matrix.tsv`

## 操作

1. 更新本包 README / 方案 / 总入口，记录 S0-S5 最终结果。
2. 更新 `docs/CADCore12.0/README.md` 的当前基线、入口、重开条件、队列检查和文档验收。
3. 若 S5 为 `implementation_package_authorized`，只创建或指向后续 implementation package，不在 C12-M8 混入代码实现。
4. 若 S5 为 retained diagnostic，明确保留 `part_design.sub_shape_binder.remaining_gaps=["copy_on_change_full_temporary_document_cache"]`。
5. 运行本包队列、TSV 字段数、尾随空白和 `git diff --check`。

## 关闭条件

- C12-M8 队列只输出表头。
- TSV 字段数检查通过。
- 尾随空白检查无输出。
- `git diff --check` 通过。
- `C12M8-VAL-601..604` 全部记录。

## S6 结果

- 本轮 baseline：`pwd=/Users/li/Chili3DProject/FreeCAD`，`git rev-parse --short HEAD=7e5befaa50`，`git log -1 --oneline=7e5befaa50 docs: 关闭 C12-M8 S5 no-code 裁决`，起点 `git -c core.quotepath=false status --short -uall` 无输出。
- S6 发布结果：C12-M8 最终状态为 `no_code_retained_diagnostic` published；队列关闭后只输出表头。
- 最终事实：S2=`native_evidence_retained_blocker`，S3=`dto_not_reviewed_due_to_native_blocker`，S4=`no_current_mismatch_retained_diagnostic`，S5=`no_code_retained_diagnostic`。
- 本轮不创建 C12-M9 package，不授权 C++ work，不改 `cad-core/src`、`include`、fixtures、expected、tests、adapters 或 capability source。
- 保留 `part_design.sub_shape_binder.remaining_gaps=["copy_on_change_full_temporary_document_cache"]`，known gap 继续是 `known_gap_diagnostic` / `oracle_blocked`，diagnostic 继续是 `copy_on_change_full_temporary_document_cache_not_supported`。
- final delete condition：只有 FreeCADCmd 稳定暴露 `_CopiedObjs` identity、copyObject dependency order、support rewrite map、`recomputeFeature(true)` lifecycle、ElementMap / NamedShape lifecycle，且这些证据能转成前端持久化 graph / `documentObjectUpdates` 后，才可替换 diagnostic。
- final reopen condition：更强 native copied graph artifact 必须先重开并通过 S2，再重新执行 S3 DTO approval 与 S4 current mismatch gate。
- `C12M8-BLOCKER-601` 与 `C12M8-VAL-601..604` 已按 S6 发布闸门收口。

## 非目标

- 不扩大为 broad SubShapeBinder / ShapeBinder 家族实现。
- 不重开 Groove UpTo 或 RuledSurface wire/wire。
- 不在发布闸门补 C++。

## 验收

```bash
cd /Users/li/Chili3DProject/FreeCAD
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore12.0/C12-M8-SubShapeBinderCopyOnChangeNativeCopiedGraphDTO准入解锁批次/工作步骤细分 --format markdown
awk -F '\t' 'FNR==1{n=NF; next} NF!=n{print FILENAME ":" FNR ": expected " n " fields, got " NF; bad=1} END{exit bad}' docs/CADCore12.0/C12-M8-SubShapeBinderCopyOnChangeNativeCopiedGraphDTO准入解锁批次/矩阵/*.tsv
rg -n '[ \t]$' docs/CADCore12.0/C12-M8-SubShapeBinderCopyOnChangeNativeCopiedGraphDTO准入解锁批次 docs/CADCore12.0/README.md
git diff --check
```
