# C12-M8 工作步骤总入口【已实现】

## 目标

按顺序执行 C12-M8 S0-S6，裁决 SubShapeBinder CopyOnChange full temporary-document gap 是否具备后续 implementation package 的证据链。

## 执行规则

1. 每步开始前刷新 live repo 状态和 C12-M8 队列。
2. 本索引只承担导航职责；S0 完成本索引复核后，可随 S0 一并标记为 `【已实现】`。
3. S0-S4 不改 `cad-core/src`、fixtures、expected、tests、adapters 或 capability source。
4. S5 只有在 native copied graph evidence、DTO approval 和 current mismatch 同时成立时，才输出 implementation package。

## 顺序

- S0：live 基线与继承口径冻结（已完成）。
- S1：FreeCAD source 与 current coverage 复核（已完成）。
- S2：native copied graph probe schema 与 evidence gate（已完成，`native_evidence_retained_blocker`）。
- S3：request-local DTO 产品边界裁决（已完成，`dto_not_reviewed_due_to_native_blocker`）。
- S4：current mismatch 与 implementation candidate gate。
- S5：implementation package authorization / no-code retained decision。
- S6：发布闸门与后续分流。

## S0 索引复核结果

- 本文件只承担 C12-M8 S0-S6 队列索引职责；S0 已复核执行规则、顺序和验收命令与 README / 包级总入口 / 矩阵一致。
- 为避免队列卡在索引，已按执行规则随 S0 一并标记为 `【已实现】`。

## S1 索引复核结果

- S1 已关闭 FreeCAD source authority 与 App::Link reference-only transport blocker。
- S1 关闭后进入 native copied graph probe schema 与 evidence gate；不得从 App::Link transport 或 current retained diagnostic 直接跳到实现。

## S2 索引复核结果

- S2 已关闭为 `native_evidence_retained_blocker`：本机 FreeCADCmd 可运行，但 raw C9M5 artifact 仍不能导出 stable copied-object graph evidence。
- S2 之后队列应从 S3 开始；S3/S4 必须继承 retained blocker，不能把 property/session 状态、`_tmp_binder` document name、label、bbox、shape count 或 `_CopiedLink` target 当作 implementation approval 证据。

## S3 索引复核结果

- S3 已关闭为 `dto_not_reviewed_due_to_native_blocker`：`C12M8-DTO-001..004` 与 `C12M8-DTO-006` 因 S2 native blocker deferred，`C12M8-DTO-005` 只批准 input-only，`C12M8-DTO-007..012` rejected。
- S3 之后队列应从 S4 开始；S4/S5 不得进入 implementation approval，除非更强 native copied graph artifact 先重开并通过 S2/S3。

## 验收

```bash
cd /Users/li/Chili3DProject/FreeCAD
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore12.0/C12-M8-SubShapeBinderCopyOnChangeNativeCopiedGraphDTO准入解锁批次/工作步骤细分 --format markdown
awk -F '\t' 'FNR==1{n=NF; next} NF!=n{print FILENAME ":" FNR ": expected " n " fields, got " NF; bad=1} END{exit bad}' docs/CADCore12.0/C12-M8-SubShapeBinderCopyOnChangeNativeCopiedGraphDTO准入解锁批次/矩阵/*.tsv
git diff --check
```
