# C12-M4 S2 产品契约边界与 non-goal 冻结【已实现】

## 目标

发布 ProjectOnSurface request-local ledger 的产品契约边界，明确它是 CAD Core contract，不是 FreeCAD native `ElementMap` parity。

## 必读文件

- C12-M4 方案与总入口。
- `矩阵/c12m4_project_on_surface_request_local_ledger_contract_fields.tsv`
- `矩阵/c12m4_project_on_surface_request_local_ledger_scope_review_matrix.tsv`
- `矩阵/c12m4_project_on_surface_request_local_ledger_non_goal_registry.tsv`
- `docs/temp/6-29-23-05-c12m3-s4-project-on-surface-native-provenance-probe-output.json`

## 操作

1. 冻结 contract 字段的生产者、消费者、语义和验收规则。
2. 保留 `native_oracle_unavailable` 注记，不把 C12-M3 negative probe 改写成 parity success。
3. 拒绝 output guessing、persistent native state、GUI/Workbench、full BREP transport。
4. 更新 blocker / validation matrix，关闭 contract-fields blocker 或写清缺口。

## 非目标

- 不改 fixtures expected。
- 不改 capabilities。
- 不开 C++ implementation。

## 验收

```bash
cd /home/user/Chili3DProject/FreeCAD
awk -F '\t' 'FNR==1{n=NF; next} NF!=n{print FILENAME ":" FNR ": expected " n " fields, got " NF; bad=1} END{exit bad}' docs/CADCore12.0/C12-M4-ProjectOnSurfaceRequestLocalLedger产品契约批次/矩阵/*.tsv
rg -n '[ \t]$' docs/CADCore12.0/C12-M4-ProjectOnSurfaceRequestLocalLedger产品契约批次 docs/CADCore12.0/README.md
git diff --check
```

## 完成记录

- S2 live 起点：`pwd=/Users/li/Chili3DProject/FreeCAD`，`HEAD=49cdd9d086`，`git log -1 --oneline=49cdd9d086 docs: 完成 C12-M4 S1 ledger 证据矩阵`，起点 status 为 `<clean>`。
- 队列确认从 S2 开始，S2 文件执行前未标 `【已实现】`。
- `projection_item_ledger`、`MapperHistoryEvent`、edge/wire、face rebuild、height/compound/offset、`element_history_status` / recovery hook 已冻结为 CAD Core request-local product contract；invalid projection diagnostic 已冻结为 product diagnostic contract。
- `native_oracle_unavailable` 已保留：C12-M3 仍是 `native_hidden_retained`、`s5_input=null`、`native_provenance_expected_ready_count=0`，未发布 FreeCAD native parity success。
- Non-goal 已冻结：FreeCAD native ElementMap parity、output guessing、GUI/Workbench、persistent native Document / TopoDS / NamedShape / ElementMap cache、full BREP transport，以及本 decision package 内直接改 C++、include、fixture expected、tests、adapters 或 capability wording。
- `C12M4-BLOCKER-002` 已关闭；S3 expected/capability 迁移与 S4 发布闸门仍未关闭。
