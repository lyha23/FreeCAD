# C12-M4 S4 产品契约发布闸门【已实现】

## 目标

发布 C12-M4 最终状态：contract published、contract migration ready、implementation deferred 或 blocker retained。

## 必读文件

- C12-M4 README、总入口、方案。
- C12-M4 全部矩阵。
- 已实现 S0-S3 step。
- C12-M3 S4 artifact 与 S5 no-comparison evidence。

## 操作

1. 逐行确认 contract 字段、产品边界、expected migration 和 blocker 状态。
2. 若 contract 已足够清楚，发布 `contract_published`。
3. 若 expected/capability wording 迁移范围也明确，发布 `contract_migration_ready` 并建议另开 implementation package。
4. 若 blocker 仍存在，写清 retained blocker 和删除条件。
5. 更新 README、总入口、root README、validation matrix，并把 S4 step 重命名为 `【已实现】`。

## 非目标

- 不直接改 C++。
- 不直接改 checked-in expected。
- 不把 C12-M4 变成 native oracle package。

## 验收

```bash
cd /home/user/Chili3DProject/FreeCAD
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore12.0/C12-M4-ProjectOnSurfaceRequestLocalLedger产品契约批次/工作步骤细分 --format markdown
awk -F '\t' 'FNR==1{n=NF; next} NF!=n{print FILENAME ":" FNR ": expected " n " fields, got " NF; bad=1} END{exit bad}' docs/CADCore12.0/C12-M4-ProjectOnSurfaceRequestLocalLedger产品契约批次/矩阵/*.tsv
rg -n '[ \t]$' docs/CADCore12.0/C12-M4-ProjectOnSurfaceRequestLocalLedger产品契约批次 docs/CADCore12.0/README.md
git diff --check
```

## 完成记录

- S4 live 起点：`pwd=/Users/li/Chili3DProject/FreeCAD`，`HEAD=af1cc283b1`，`git log -1 --oneline=af1cc283b1 docs: 完成 C12-M4 S3 迁移设计`，起点 status 为 `<clean>`。
- 队列确认只剩 S4，S4 文件执行前未标 `【已实现】`。
- 逐行复核矩阵后，无新增 retained blocker；`C12M4-BLOCKER-004` 和 `C12M4-BLOCKER-005` 均关闭为 `closed_s4`。
- 最终发布 `contract_published`、`contract_migration_ready`、`implementation_deferred` 和 `rejected_native_parity_dependency`。建议另开 follow-up implementation package 迁移 C5-M9 expected wording、capability source、adapter assertion 和接口文档 wording。
- 保留 `native_oracle_unavailable`：C12-M3 仍是 `native_hidden_retained`、`s5_input=null`、`native_provenance_expected_ready_count=0`，不是 FreeCAD native parity success。
- 本步未改 C++、include、fixtures expected、tests、adapters、capability source、接口文档正文或 C5-M9 expected JSON；未运行 FreeCADCmd、current comparison 或 full build。
