# C12-M4 工作步骤总入口

## 队列目标

按顺序执行 S0-S4，把 ProjectOnSurface request-local ledger 从 C5-M9 source-backed known-gap context 升级为 CAD Core 产品契约，并生成后续 fixture/capability wording 迁移包的明确边界。

## 执行顺序

1. S0：live 基线与 C12-M3 继承口径冻结。
2. S1：当前 request-local ledger 字段与 focused tests 证据矩阵。
3. S2：产品契约边界与 non-goal 冻结。
4. S3：expected / tests / capability wording 迁移设计。
5. S4：产品契约发布闸门。

## 通用纪律

- 每个 step 完成后把对应 Markdown 文件重命名为带 `【已实现】`。
- C12-M4 默认只改 `docs/CADCore12.0/C12-M4-ProjectOnSurfaceRequestLocalLedger产品契约批次/` 与 root `docs/CADCore12.0/README.md`。
- 不在本包直接改 C++、fixture expected、tests、adapters 或 capability wording。
- 不把 C12-M3 native-hidden blocker 改写成 FreeCAD native expected。
