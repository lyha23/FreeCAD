# C12-M4 S0 live 基线与 C12-M3 继承口径冻结【已实现】

## 目标

冻结 C12-M4 live 起点，确认 C12-M3 的 `native_hidden_retained` / `no_code_retained_gate` 是本包前提，并明确 C12-M4 是产品契约决策包，不是 C++ implementation 包。

## 必读文件

- `docs/CADCore12.0/README.md`
- `docs/CADCore12.0/C12-M3-PartWorkbenchProjectOnSurfaceMapperProvenanceNativeProbe批次/README.md`
- `docs/CADCore12.0/C12-M3-PartWorkbenchProjectOnSurfaceMapperProvenanceNativeProbe批次/6-29-21-29-C12-M3-PartWorkbenchProjectOnSurfaceMapperProvenanceNativeProbe批次总入口.md`
- `docs/temp/6-29-23-05-c12m3-s4-project-on-surface-native-provenance-probe-output.json`
- `docs/temp/6-29-22-40-c12m3-s5-project-on-surface-no-comparison-evidence.json`
- `docs/CADCore12.0/C12-M4-ProjectOnSurfaceRequestLocalLedger产品契约批次/README.md`
- `docs/CADCore12.0/C12-M4-ProjectOnSurfaceRequestLocalLedger产品契约批次/矩阵/c12m4_project_on_surface_request_local_ledger_blocker_queue.tsv`

## 操作

1. 记录 `pwd`、`git rev-parse --short HEAD`、`git log -1 --oneline` 和 `git -c core.quotepath=false status --short -uall`。
2. 检查 C12-M3 与 C12-M4 队列；C12-M3 应为空，C12-M4 应从 S0 开始。
3. 回写 C12-M4 README / 总入口 / validation matrix 的 baseline、dirty boundary、队列状态和 C12-M3 继承口径。
4. 明确 C12-M4 不授权 `cad-core/src`、`cad-core/include`、fixtures expected、tests、adapters、capability wording 或 full build。

## 非目标

- 不修改 C5-M9 expected wording。
- 不运行 current comparison。
- 不改 C++、tests 或 capability wording。

## 验收

```bash
cd /home/user/Chili3DProject/FreeCAD
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore12.0/C12-M4-ProjectOnSurfaceRequestLocalLedger产品契约批次/工作步骤细分 --format markdown
awk -F '\t' 'FNR==1{n=NF; next} NF!=n{print FILENAME ":" FNR ": expected " n " fields, got " NF; bad=1} END{exit bad}' docs/CADCore12.0/C12-M4-ProjectOnSurfaceRequestLocalLedger产品契约批次/矩阵/*.tsv
rg -n '[ \t]$' docs/CADCore12.0/C12-M4-ProjectOnSurfaceRequestLocalLedger产品契约批次 docs/CADCore12.0/README.md
git diff --check
```
