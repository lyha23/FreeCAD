# C12-M10 S0 live 基线与继承口径冻结（已实现）

## 目标

冻结 C12-M10 起点：当前 HEAD、dirty boundary、C12-M1..M9 队列、live capability snapshot，以及 C12-M8 / C12-M9 retained diagnostic / no-code backlog 继承口径。

## 必读文件

- `../README.md`
- `../7-1-02-48-C12-M10-SubShapeBinderCopyOnChangeCopiedGraphOracle产品契约解锁批次总入口.md`
- `docs/CADCore12.0/README.md`
- `docs/CADCore12.0/C12-M8-SubShapeBinderCopyOnChangeNativeCopiedGraphDTO准入解锁批次/README.md`
- `docs/CADCore12.0/C12-M9-CADCoreImplementationCandidate再盘点批次/README.md`
- `../矩阵/c12m10_copy_on_change_scope_review_matrix.tsv`
- `../矩阵/c12m10_copy_on_change_blocker_queue.tsv`
- `../矩阵/c12m10_copy_on_change_validation_matrix.tsv`

## 操作

1. 记录 `pwd`、HEAD、latest commit 和 dirty boundary。
2. 运行 C12-M1..M9 `step_goal_queue.py`，确认旧队列为空。
3. 运行 `cad-core/build/cad-core capabilities`，记录 SubShapeBinder live status、唯一 remaining gap、known gap、route、diagnostic、delete condition 和 reopen condition。
4. 更新 S0 owner 的 scope / blocker / validation 行。
5. 将本 S0 step 文件重命名为带 `【已实现】` 的同名文件。

## 关闭条件

- `C12M10-BLOCKER-001` 关闭：live baseline 已冻结。
- `C12M10-BLOCKER-002` 关闭：C12-M1..M9 队列为空。
- `C12M10-VAL-001..003` 记录实际结果。

## 非目标

- 不运行 FreeCADCmd oracle。
- 不做 DTO approval 或 current mismatch 判断。
- 不改 production code。

## 验收

```bash
cd /Users/li/Chili3DProject/FreeCAD
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore12.0/C12-M10-SubShapeBinderCopyOnChangeCopiedGraphOracle产品契约解锁批次/工作步骤细分 --format markdown
git diff --check
```
