# C12-M9 S4 最高优先候选 source 与验证范围复核【已实现】

## 目标

若 S3 产生 candidate，复核其 FreeCAD source authority、cad-core 分层落点、fixtures / focused tests 和 non-goal 边界；若 S3 没有 candidate，则记录 no-comparison / no-code 证据。

## 必读文件

- `../矩阵/c12m9_candidate_source_candidates.tsv`
- `../矩阵/c12m9_candidate_backend_gap_classification.tsv`
- `../矩阵/c12m9_candidate_non_goal_registry.tsv`
- S3 指定的 FreeCAD source 文件。
- S3 指定的 cad-core source / tests / fixtures。

## 操作

1. 对 S3 candidate 写出 FreeCAD 调用链、关键字段和 cad-core 落点。
2. 写出最小完整语义批次，避免单 fixture 或单属性碎片化推进。
3. 写出 focused validation：本轮短跑、阶段回归、重型收口分层。
4. 若无 candidate，记录 S4=`no_candidate_after_s3_gate`，并保留 S5 no-code gate 输入。

## 执行结果

- 执行基线：`pwd=/Users/li/Chili3DProject/FreeCAD`，`HEAD=50c3691d8c`（`50c3691d8c docs: 关闭 C12-M9 S3 mismatch 准入`），起点 `git -c core.quotepath=false status --short -uall` 无输出，即 worktree clean。
- 执行前 C12-M9 队列第一项为本 S4，后续为 S5-S6；本步关闭后队列应从 S5 继续。
- 已复核 S3 结果和 backend/scope/non-goal 矩阵：`C12M9-CAT-001..005` 均为 `not_s4_candidate`，`C12M9-CAT-006` 为 `no_s4_candidate_yet`；没有 admitted `mismatch-confirmed` 行。
- S4 结果为 `no_candidate_after_s3_gate`。没有可写 FreeCAD source authority、cad-core landing、fixtures / focused tests 或 implementation surface；不得发明 C++ 范围。
- `C12M9-BLOCKER-401` 已关闭为 no-candidate evidence。`C12M9-SCOPE-501` / `C12M9-CAT-006` 作为 S5 输入只能走 `no_code_backlog_gate`，或在后续新证据出现时另开 oracle / product-contract package。
- 本步未运行 FreeCADCmd，未新增或修改 fixture expected，未修改 `cad-core/src`、`include`、tests、adapters 或 capability source，未创建后续 implementation 包。

## 关闭条件

- 若有 candidate：source authority、implementation surface、fixtures/tests、non-goal 均清楚。
- 若无 candidate：`C12M9-BLOCKER-401` 以 no-code comparison path 关闭。

## 非目标

- 不在 S4 实现代码。
- 不创建后续包，除非 S5 授权。
- 不扩大成全 CAD Core sweep。

## 验收

```bash
cd /Users/li/Chili3DProject/FreeCAD
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore12.0/C12-M9-CADCoreImplementationCandidate再盘点批次/工作步骤细分 --format markdown
awk -F '\t' 'FNR==1{n=NF; next} NF!=n{print FILENAME ":" FNR ": expected " n " fields, got " NF; bad=1} END{exit bad}' docs/CADCore12.0/C12-M9-CADCoreImplementationCandidate再盘点批次/矩阵/*.tsv
git diff --check
```
