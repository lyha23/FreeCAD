# C12-M9 S0 live 基线与继承口径冻结【已实现】

## 目标

冻结 C12-M9 起点：当前 HEAD、dirty boundary、C12-M1..M8 队列、live capability snapshot，以及 C12-M8 retained diagnostic / no-code 裁决继承口径。

## 必读文件

- `../README.md`
- `../7-1-01-23-C12-M9-CADCoreImplementationCandidate再盘点批次总入口.md`
- `docs/CADCore12.0/README.md`
- `docs/CADCore12.0/C12-M8-SubShapeBinderCopyOnChangeNativeCopiedGraphDTO准入解锁批次/README.md`
- `../矩阵/c12m9_candidate_scope_review_matrix.tsv`
- `../矩阵/c12m9_candidate_blocker_queue.tsv`
- `../矩阵/c12m9_candidate_validation_matrix.tsv`

## 操作

1. 记录 `pwd`、HEAD、latest commit 和 dirty boundary。
2. 运行 C12-M1..M8 `step_goal_queue.py`，确认旧队列为空。
3. 运行 `cad-core/build/cad-core capabilities`，记录唯一 live remaining gap、known gap、route、diagnostic 和 `narrowed_gaps` presence。
4. 更新 S0 owner 的 scope / blocker / validation 行。
5. 将本 S0 step 文件重命名为带 `【已实现】` 的同名文件。

## 执行结果

- 执行基线：`pwd=/Users/li/Chili3DProject/FreeCAD`，`HEAD=d7602e1bd2`（`d7602e1bd2 文档：关闭 C12-M9 工作步骤总入口`），起点 worktree clean。
- C12-M9 执行前队列第一项为本 S0，后续为 S1-S6；本步关闭后队列应从 S1 继续。
- C12-M1..M8 队列均只输出 markdown 表头，旧批次 release gate 关闭状态成立。
- `cad-core/build/cad-core capabilities` 快照保存到 `/tmp/c12m9-s0-capabilities.json`。唯一非空 `remaining_gaps` 是 `part_design.sub_shape_binder.remaining_gaps=["copy_on_change_full_temporary_document_cache"]`。
- CopyOnChange known gap 继续是 `status=known_gap_diagnostic`、`route=oracle_blocked`、`diagnostic=copy_on_change_full_temporary_document_cache_not_supported`。
- `narrowed_gaps` presence 仅在 `part_design.revolution_groove`、`part_workbench.filling`、`part_workbench.geomplate`、`part_workbench.loft`、`part_workbench.project_on_surface`、`part_workbench.sweep` 路径上冻结；S0 不展开 S1-S6 的候选归类。
- C12-M8 retained diagnostic 继承口径继续有效：S2=`native_evidence_retained_blocker`，S3=`dto_not_reviewed_due_to_native_blocker`，S4=`no_current_mismatch_retained_diagnostic`，S5=`no_code_retained_diagnostic`。
- 本步未运行 FreeCADCmd oracle，未做 current mismatch 判断，未修改 production code、fixtures、expected、tests、adapters 或 capability source。

## 关闭条件

- `C12M9-BLOCKER-001` 已关闭：live baseline 已冻结。
- `C12M9-BLOCKER-002` 已关闭：C12-M1..M8 队列为空。
- `C12M9-VAL-001..003` 已记录实际结果。

## 非目标

- 不运行 FreeCADCmd oracle。
- 不做 current mismatch 判断。
- 不改 production code。

## 验收

```bash
cd /Users/li/Chili3DProject/FreeCAD
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore12.0/C12-M9-CADCoreImplementationCandidate再盘点批次/工作步骤细分 --format markdown
git diff --check
```
