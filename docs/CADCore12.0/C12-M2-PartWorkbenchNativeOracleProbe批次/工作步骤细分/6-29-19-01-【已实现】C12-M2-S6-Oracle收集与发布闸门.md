# 【已实现】C12-M2 S6 Oracle 收集与发布闸门

## 目标

消费 S0-S5 的所有矩阵和 probe artifact，发布 C12-M2 结论。S6 决定是否存在后续 implementation package，但不在本包写代码。

## 发布规则

每个 row 只能发布为以下之一：

- `oracle_expected_ready`：source authority、stable expected、request-local/product boundary、current mismatch 同时成立。
- `current_covered`：stable expected 存在，但 current cad-core 已覆盖。
- `native_probe_blocked`：FreeCADCmd、helper、wrapper、native-hidden 或 collector 阻塞仍未解除。
- `product_boundary_rejected`：行为不适合 CAD Core request-local 产品边界。
- `retained_no_expected`：仍只有 crash、timeout、TypeError、notCollected、native-hidden 或 probe-only 历史证据。

## 执行步骤

1. 逐行复核 backend gap classification，不允许 S6 之前出现代码实现候选泄漏。
2. 对 `oracle_expected_ready` 行写清后续 implementation 包建议：family、source authority、expected artifact、current mismatch、C++ landing boundary、focused tests。
3. 对未 ready 行写清 blocker、close condition 和是否需要更窄 native probe 包。
4. 更新 C12-M2 README、总入口、方案和 `docs/CADCore12.0/README.md` 的最终结论。
5. 如果没有任何 `oracle_expected_ready`，发布 `oracle_probe_retained_gate` 或 `no_code_oracle_blocked_gate`，并明确不授权 C++。
6. 将本文件重命名为 `【已实现】` 后，确保 C12-M2 队列只剩表头。

## 更新目标

- `README.md`
- `6-29-18-53-C12-M2-PartWorkbenchNativeOracleProbe批次总入口.md`
- `6-29-18-53-C12-M2-PartWorkbenchNativeOracleProbe批次方案.md`
- `docs/CADCore12.0/README.md`
- 全部 C12-M2 矩阵

## 验收命令

```bash
cd /home/user/Chili3DProject/FreeCAD
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore12.0/C12-M2-PartWorkbenchNativeOracleProbe批次/工作步骤细分 --format markdown
awk -F '\t' 'FNR==1{n=NF; next} NF!=n{print FILENAME ":" FNR ": expected " n " fields, got " NF; bad=1} END{exit bad}' docs/CADCore12.0/C12-M2-PartWorkbenchNativeOracleProbe批次/矩阵/*.tsv
rg -n '[ \t]$' docs/CADCore12.0/C12-M2-PartWorkbenchNativeOracleProbe批次 docs/CADCore12.0/README.md
git diff --check
```

## 完成条件

C12-M2 必须给出清晰发布结论：哪些 row 拿到了可实现 oracle，哪些仍被 native/helper/product boundary 阻塞。没有 `oracle_expected_ready` 就不能开 C++。

## S6 执行结论

本步 live baseline：

```text
pwd=/Users/li/Chili3DProject/FreeCAD
HEAD=1ff82629d7
git log -1 --oneline=1ff82629d7 docs: 完成 C12-M2 S5 helper mapper probe 复审
git -c core.quotepath=false status --short -uall=<clean>
```

最终发布 `no_code_oracle_blocked_gate`：没有任何 row 同时满足 source authority、stable native expected、request-local/product boundary 和 current mismatch；C12-M2 不授权 C++、fixtures expected、tests、adapters 或 capability wording 改动。

逐行分类：

- Sweep Location overload：`native_probe_blocked`；no-location auxiliary / binormal / tolerance controls 只作为 `current_covered` context。
- Filling helper：`helper_blocked`。
- Loft selected subelement：`native_hidden`。
- ProjectOnSurface provenance：`native_hidden`。
- GeomPlate no-initial-surface curve2d 与 G1 curve-on-surface：`native_hidden` / retained no expected，不比较 current。
- GeomPlate projected curve2d + initial surface：`current_covered`。

唯一 S6 comparison candidate 的证据：

- S5 artifact `docs/temp/6-29-20-40-c12m2-s5-geomplate-native-probe-output.json` 证明 projected curve2d + initial surface native wrapper path 可收集 Face summary，topology 为 1 face / 4 edges / 4 vertices，volume `5.966720601`；G1 curve-on-surface 仍是 `NotImplementedError`，no-initial-surface curve2d `is_done=false`。
- Existing expected `cad-core/fixtures/c5m13/expected/part-geomplate-projected-curve2d-initial-surface.freecad.json` 是 checked-in expected-backed path，记录 FreeCAD `1.2.0 revision 20260519`、`Part.GeomPlate.CurveConstraint.setProjectedCurve`、1 face / 4 edges / 4 vertices、volume `5.966720600637519`。
- Current comparison artifact `docs/temp/6-29-20-58-c12m2-s6-geomplate-current-comparison.json` 由 `CAD_CORE_TEST_LEGACY_OUTPUT=1` recompute 产生，记录 diagnostics empty、GeomPlate status ok、helper `Part.GeomPlate.BuildPlateSurface`、DTO `PartGeomPlateSurfaceDTO`、topology 1 face / 4 edges / 4 vertices、volume `5.9667206006373235`。
- Focused unittest `python3 -m unittest tests.test_p8_features.CadCoreP8FeatureTest.test_c5m13_part_geomplate_projected_curve2d_initial_surface_is_expected_backed` 通过，证明 current cad-core 已匹配 checked-in expected。

S5 artifact 的 bbox 来自 probe 脚本对 `surface.makeApprox()` 的 no-arg summary；checked-in expected 和 current comparison 使用 `collect_freecad_expected.py` 的 explicit approximation 参数和 `bbox_delta` 口径。S6 不把该 bbox envelope 差异升级为 current mismatch。

`C12M2-BLOCKER-004` 已关闭为 `closed_s6_current_covered_geomplate_only`。后续若要继续，只能另开更窄 native probe 包解除 Location/helper/native-hidden blocker；没有 implementation package 授权。
