# C12-M2 工作步骤总入口

## 队列目标

按顺序执行 S0-S6，把 C12-M1 S5/S6 留下的 Part Workbench retained evidence 独立复开为 oracle collection / native probe 结果。该队列只负责 native expected 准入与发布闸门，不负责 C++ implementation。

## 执行顺序

1. S0：live 基线与 oracle 声明口径冻结。
2. S1：FreeCAD 源码与 probe 候选矩阵。
3. S2：范围准入与 blocker 矩阵。
4. S3：通用 NativeProbe harness 与 FreeCADCmd 基线。
5. S4：Sweep / Loft 原生 DocumentObject probe 复审。
6. S5：Filling / GeomPlate / ProjectOnSurface helper-mapper probe 复审。
7. S6：Oracle 收集与发布闸门。

## 队列检查

```bash
cd /home/user/Chili3DProject/FreeCAD
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore12.0/C12-M2-PartWorkbenchNativeOracleProbe批次/工作步骤细分 --format markdown
```

## 通用纪律

- 每个 step 完成后把对应 Markdown 文件重命名为带 `【已实现】`。
- 每个 step 只改 C12-M2 文档和矩阵，除非 step 明确授权创建 probe artifact；不得修改 `cad-core/src`、fixtures expected、tests 或 adapters。
- native probe 失败必须分类为 blocker / native-hidden / helper lifecycle / sandbox limitation / product boundary，不得直接写成 cad-core mismatch。
- 只有 S6 可发布后续 implementation package 建议。
