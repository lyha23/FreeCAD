# 【已实现】C12-M14 S0 live 基线与 C12-M13 继承冻结

## 目标

冻结 C12-M14 实施前的真实仓库状态，并确认 C12-M13 的哪些结论可以继承、哪些 helper follow-up 必须继续处理。

## live 基线

- `pwd`：`/Users/li/Chili3DProject/FreeCAD`。
- `git rev-parse --short HEAD`：`09e2f66c73`。
- `git log -1 --oneline`：`09e2f66c73 文档：新增 C12-M14 helper 生命周期证据方案`。
- `git -c core.quotepath=false status --short -uall`：无输出。

## dirty boundary

| 分组 | 起点状态 |
| --- | --- |
| docs | `<none>` |
| `cad-core/src` | `<none>` |
| tests | `<none>` |
| fixtures | `<none>` |
| other | `<none>` |

非本包 dirty 未发现；后续步骤仍按 dirty worktree 保护执行，不覆盖、回退或清理非本包改动。

## 队列冻结

- C12-M14 `工作步骤细分` 队列在 S0 开始前输出 S0-S5 pending，第一项是本步骤。
- C12-M13 `工作步骤细分` 队列只输出表头，确认已空。
- S0 关闭后，C12-M14 下一步应从 S1 `source 与 current helper landing 复核` 继续。

## 继承结论

- C12-M13 final status 继承为 `partial_implementation_with_named_followups`。
- `C12M13-ORACLE-301` 的 collected subset `add/isReady/getStatus/build/shape/makeSolid` 继承为 current-supported，只作为 C12-M14 baseline regression guard。
- `remove/firstShape/lastShape/generated/simulate` 仍缺 checked-in native expected 或 approved product-contract artifact，必须由 C12-M14 S2/S3 继续采证和准入裁决。
- `C12M13-ORACLE-302` / Part Sweep wrapper no-mix regression 作为已关闭面继承；C12-M14 不重写 plain `Part::Sweep` wrapper。
- S1 需要复核的 FreeCAD source authority 与 `cad-core` landing 保持在 `c12m14_helper_lifecycle_source_matrix.tsv` 和 `c12m14_helper_lifecycle_scope_matrix.tsv`，S0 不做 source 复核实现。

## 本步改动边界

- 更新 S0 文档、package README、总入口当前状态，以及 scope / blocker / validation 矩阵中的 S0 相关记录。
- 未运行 FreeCADCmd。
- 未修改 `cad-core/src`、fixtures、expected 或 tests。

## 验收

```bash
cd /Users/li/Chili3DProject/FreeCAD
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore12.0/C12-M14-PartSweepHelperMutableLifecycle证据解锁批次/工作步骤细分 --format markdown
awk -F '\t' 'FNR==1{n=NF; next} NF!=n{print FILENAME ":" FNR ": expected " n " fields, got " NF; bad=1} END{exit bad}' docs/CADCore12.0/C12-M14-PartSweepHelperMutableLifecycle证据解锁批次/矩阵/*.tsv
rg -n '[ \t]$' docs/CADCore12.0/C12-M14-PartSweepHelperMutableLifecycle证据解锁批次 docs/CADCore12.0/README.md
git diff --check
```
