# C12-M14 S0 live 基线与 C12-M13 继承冻结

## 目标

冻结 C12-M14 实施前的真实仓库状态，并确认 C12-M13 的哪些结论可以继承、哪些 helper follow-up 必须继续处理。

## 必读文件

- `../README.md`
- `../7-4-11-54-C12-M14-PartSweepHelperMutableLifecycle证据解锁批次方案.md`
- `../../C12-M13-FreeCADSweepPipe剩余语义迁移批次/README.md`
- `../矩阵/c12m14_helper_lifecycle_scope_matrix.tsv`
- `../矩阵/c12m14_helper_lifecycle_blocker_queue.tsv`
- `../矩阵/c12m14_helper_lifecycle_validation_matrix.tsv`

## 操作

1. 记录 `pwd`、`git rev-parse --short HEAD`、`git log -1 --oneline`。
2. 记录 `git -c core.quotepath=false status --short -uall`，按 docs、`cad-core/src`、tests、fixtures、other 分组。
3. 运行 `step_goal_queue.py` 确认 C12-M13 队列为空、C12-M14 队列从 S0 开始。
4. 盘点 C12-M13 `ORACLE-301` 和 `ORACLE-302` 的继承口径。
5. 回写 blocker / validation matrix，并将本步骤重命名为 `【已实现】`。

## 关闭条件

- dirty boundary 已记录，非本包改动不会被覆盖。
- C12-M13 helper collected subset / uncollected methods 继承口径写入 scope matrix。
- S1 需要复核的 FreeCAD source 和 cad-core landing 已确认。

## 非目标

- 不运行 FreeCADCmd。
- 不修改 `cad-core/src`。
- 不新增 expected。
