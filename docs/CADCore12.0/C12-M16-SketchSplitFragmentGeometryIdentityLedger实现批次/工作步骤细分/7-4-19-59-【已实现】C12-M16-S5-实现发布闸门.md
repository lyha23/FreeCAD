# 【已实现】C12-M16 S5 实现发布闸门

## 目标

汇总 S0-S4 证据，发布 C12-M16 的最终实现状态，并把结果写回 package README、方案、总入口、矩阵和 root README。

## live 基线

- `pwd`：`/Users/li/Chili3DProject/FreeCAD`。
- `git rev-parse --short HEAD`：`cf4849ce9a`。
- `git log -1 --oneline`：`cf4849ce9a 验证 C12-M16 S4 adapter 接入`。
- `git -c core.quotepath=false status --short -uall`：无输出。

## 必读文件

- `../README.md`
- `../7-4-19-52-C12-M16-SketchSplitFragmentGeometryIdentityLedger实现批次方案.md`
- `../7-4-19-52-C12-M16-SketchSplitFragmentGeometryIdentityLedger实现批次总入口.md`
- `../矩阵/*.tsv`
- `docs/CADCore12.0/README.md`

## 操作

1. 确认 S0-S4 均已 `【已实现】`，或保留明确 blocker。
2. 发布 final status：`implemented_current_supported`、`partial_implementation_with_followups` 或 `blocked_with_red_tests`。
3. 更新 README / 方案 / 总入口 / root README 当前状态。
4. 更新 blocker queue 和 validation matrix。
5. 运行队列、TSV、whitespace 和 `git diff --check`。
6. 将本步骤重命名为 `【已实现】`。

## 发布结论

- S0-S4 均已 `【已实现】`；S3 实现 `g<ID>:splitN` request-local fragment ledger，S4 验证 response / reference resolution / adapter / capability public wording 共享同一 fragment token。
- Final status：`implemented_current_supported`。
- `C12M16-BLOCKER-601` 已关闭；blocker queue 无悬空 open row。
- C12-M16 保留边界：不重开普通 `g<ID>` raw edge identity，不处理 my-chili3d frontend sync，不处理 C12-M11 open wire mesh 产品契约，不引入 persistent backend sketch session/cache。
- 重开条件：未来 focused regression、checked-in expected/current mismatch、response/reference/adapter/capability token drift、split fragment diagnostic 丢失，或用户明确要求 persistent FreeCAD session parity。

## 关闭条件

- final status 与 S0-S4 evidence 一致。
- root README 已写入 C12-M16 状态。
- 队列关闭后只输出表头。
- blocker queue 无悬空 open row，或每个 open row 有明确 reopen condition。

## 非目标

- 不在发布闸门临时补新功能。
- 不做全量 FreeCAD build，除非 S4 明确要求。

## 验收

```bash
cd /Users/li/Chili3DProject/FreeCAD
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore12.0/C12-M16-SketchSplitFragmentGeometryIdentityLedger实现批次/工作步骤细分 --format markdown
awk -F '\t' 'FNR==1{n=NF; next} NF!=n{print FILENAME ":" FNR ": expected " n " fields, got " NF; bad=1} END{exit bad}' docs/CADCore12.0/C12-M16-SketchSplitFragmentGeometryIdentityLedger实现批次/矩阵/*.tsv
rg -n '[ \t]$' docs/CADCore12.0/C12-M16-SketchSplitFragmentGeometryIdentityLedger实现批次 docs/CADCore12.0/README.md
git diff --check
```

结果：

- C12-M16 step queue：S5 rename 后只输出 markdown 表头。
- C12-M16 矩阵 TSV 字段数：无 field-count errors。
- docs trailing whitespace：无输出；`rg` exit 1 为预期通过。
- `git diff --check`：无 whitespace errors。
