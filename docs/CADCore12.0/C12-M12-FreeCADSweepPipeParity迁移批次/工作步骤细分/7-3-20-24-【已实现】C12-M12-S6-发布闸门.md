# 【已实现】C12-M12 S6 发布闸门

## 目标

汇总 S0-S5 证据，发布 FreeCAD Sweep / Pipe 迁移结果，或者明确保留 blocker。

## 必读文件

- `../README.md`
- `../7-3-20-16-C12-M12-FreeCADSweepPipeParity迁移批次方案.md`
- `../矩阵/c12m12_sweep_source_matrix.tsv`
- `../矩阵/c12m12_sweep_drift_audit.tsv`
- `../矩阵/c12m12_sweep_oracle_matrix.tsv`
- `../矩阵/c12m12_sweep_blocker_queue.tsv`
- `../矩阵/c12m12_sweep_validation_matrix.tsv`
- `../矩阵/c12m12_sweep_non_goal_registry.tsv`
- `docs/CADCore12.0/README.md`

## 操作

1. 确认队列入口到 S5 均已 `【已实现】`，或明确未关闭 blocker。
2. 汇总 final status：`implementation_complete`、`current_supported_with_regression_added`、`blocked_by_missing_native_oracle` 或 `split_frontend_consumer_followup`。
3. 更新 package README、总入口、方案、root README。
4. 运行 focused tests、TSV 字段数检查、whitespace scan、`git diff --check`。
5. 将本步骤重命名为 `【已实现】`。

## 关闭条件

- final status 与 evidence 一致。
- validation matrix 记录所有执行命令与结果。
- blocker queue 无悬空 `open` 行，或 open 行有明确 reopen condition。
- root README 已写入 C12-M12 最终状态。

## 关闭记录

- final status：`partial_implementation_multiwire_pipe_sewing`。
- S0-S5 与工作步骤总入口均已 `【已实现】`；队列在本步骤重命名后只输出表头。
- S1 source matrix 中 C12M12-SRC-001..010 均为 `reviewed`；S2 drift rows 均有 owner step、required evidence 和 close condition。
- S3 保留 `C12M12-ORACLE-001=blocked_missing_user_input` / `waiting_user_repro`；该行不是当前 backend 发布 blocker，只有用户提供 failing request/result 或明确 fixture 时才重开。
- S4 证明代表性 PartDesign Pipe 红灯来自本地 `cad-core/build` CMake cache 错绑；刷新当前源码 build 后 `c51m4/partdesign-pipe-fixed-round-body` current Body volume `0.7199999999999999`、edges `20`、vertices `12` 与 FreeCAD expected 一致。
- S5/S6 focused regression 已通过：S6 复跑 PartDesign fixed/round selected-spine 与 8 个 Part Sweep controls，`Ran 9 tests in 1.177s OK`。
- 用户反馈后已追加真实代码迁移子批次：`cad-core/src/part/topo_shape_expansion.cpp` 支持 FreeCAD `Pipe::execute()` multi-wire shell lane + shared front/back cap sewing；新增 `cad-core/fixtures/c12m12/partdesign-pipe-multiwire-sewing.json`、expected 和 focused test。
- `C12M12-BLOCKER-701` 已关闭为 `closed_partial_implementation_multiwire_pipe_sewing`；non-goal 中 profile relocation 与 invalid planar rebuild 保留为 `retained_non_goal`。
- 本步骤仍不能声称 FreeCAD Sweep / Pipe 已完整迁移；剩余 vertex / rawShape / AddSubShape lifecycle、Part Sweep mutable helper lifecycle 与 ORACLE-001 用户失败样例需后续批次继续。

## 已更新文件

- `../README.md`
- `../7-3-20-16-C12-M12-FreeCADSweepPipeParity迁移批次总入口.md`
- `../7-3-20-16-C12-M12-FreeCADSweepPipeParity迁移批次方案.md`
- `../矩阵/c12m12_sweep_blocker_queue.tsv`
- `../矩阵/c12m12_sweep_validation_matrix.tsv`
- `../矩阵/c12m12_sweep_non_goal_registry.tsv`
- `../../README.md`
- `7-3-20-24-【已实现】C12-M12-S6-发布闸门.md`

## 非目标

- 不在发布闸门临时补代码。
- 不扩大到其他 Part surface family。
- 不提交 git commit，除非用户另行要求。

## 验收

```bash
cd /Users/li/Chili3DProject/FreeCAD
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore12.0/C12-M12-FreeCADSweepPipeParity迁移批次/工作步骤细分 --format markdown
awk -F '\t' 'FNR==1{n=NF; next} NF!=n{print FILENAME ":" FNR ": expected " n " fields, got " NF; bad=1} END{exit bad}' docs/CADCore12.0/C12-M12-FreeCADSweepPipeParity迁移批次/矩阵/*.tsv
rg -n '[ \t]$' docs/CADCore12.0/C12-M12-FreeCADSweepPipeParity迁移批次 docs/CADCore12.0/README.md
git diff --check
```
