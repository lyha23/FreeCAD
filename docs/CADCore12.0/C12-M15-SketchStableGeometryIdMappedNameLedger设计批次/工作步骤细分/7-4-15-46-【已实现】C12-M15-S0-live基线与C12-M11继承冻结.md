# 【已实现】C12-M15 S0 live 基线与 C12-M11 继承冻结

## 目标

冻结 C12-M15 设计前的真实仓库状态，并确认 C12-M11 stable-id follow-up 与 C12-M14 helper closeout 的继承边界。

## live 基线

- `pwd`：`/Users/li/Chili3DProject/FreeCAD`。
- `git rev-parse --short HEAD`：`b3d2df945a`。
- `git log -1 --oneline`：`b3d2df945a docs: 关闭 C12-M15 工作步骤总入口`。
- `git -c core.quotepath=false status --short -uall`：无输出。

## dirty boundary

| 分组 | 起点状态 |
| --- | --- |
| docs | `<none>` |
| `cad-core/src` | `<none>` |
| tests | `<none>` |
| fixtures / expected | `<none>` |
| adapters | `<none>` |
| other | `<none>` |

非本包 dirty 未发现；后续步骤仍按 dirty worktree 保护执行，不覆盖、回退或清理非本包改动。

## 队列冻结

- C12-M11 `工作步骤细分` 队列只输出表头，确认已空。
- C12-M14 `工作步骤细分` 队列只输出表头，确认 helper lifecycle closeout 已空。
- C12-M15 `工作步骤细分` 队列在 S0 开始前输出 S0-S4 pending，第一项是本步骤。
- S0 关闭后，C12-M15 下一步应从 S1 `FreeCAD source 与 current identity 管线复核` 继续。

## capability gap 递归检查

- 验证命令：`cd cad-core && ./build/cad-core capabilities | jq -r 'paths as $p | select($p[-1] == "remaining_gaps" or $p[-1] == "known_gaps") | [($p|map(tostring)|join(".")), (getpath($p)|@json)] | @tsv'`。
- live 输出中每个 `remaining_gaps` 路径均为 `[]`。
- live 输出中 root `known_gaps` 为 `[]`，`part_design.sub_shape_binder.known_gaps` 为 `{}`。
- 因此 C12-M15 不是 capability remaining gap 或 known gap 入口；它是 C12-M11 明确拆出的 stable geometry id / mapped-name ledger 设计 follow-up。

## 继承结论

- C12-M11 S4/S5 已确认 closed internal profile backend response contract 为 `current_supported`：`InternalEdgeN` edgeSegments/subshapes 与 request-local `stableSubname=EdgeN` 当前支持。
- C12-M11 gap matrix 的 `C12M11-CAT-006` 明确把 FreeCAD-grade geometry id stability 分流为 `C12-M11-StableGeometryIdMappedNameLedger设计批次`；C12-M15 承接该 follow-up。
- C12-M15 不重开 C12-M11 closed profile backend C++ implementation，不把 `EdgeN` 顺序升级为 FreeCAD-grade stable id。
- C12-M14 helper lifecycle 队列已空，但它处理的是 Part Sweep helper lifecycle，与 sketch geometry id ledger 没有继承关系；C12-M15 不是 C12-M14 helper lifecycle 后续。
- S1 需要继续复核 FreeCAD `updateGeoHistory()` / `generateId()` / `convertSubName()` / `getEdge()` 和 current cad-core identity 管线；S0 不裁决 ledger interface。

## 本步改动边界

- 更新 S0 文档、package README、总入口当前状态、root README 当前基线，以及 scope / blocker / validation 矩阵中的 S0 相关记录。
- 未运行 FreeCADCmd。
- 未修改 C++、fixtures、expected、tests、adapters 或 capability source。

## 非目标

- 不运行 FreeCADCmd。
- 不修改 C++、fixtures、expected、tests 或 adapters。
- 不裁决 ledger interface。

## 验收

```bash
cd /Users/li/Chili3DProject/FreeCAD
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore12.0/C12-M11-SketchInternalEdgeSubshapeMeshContract批次/工作步骤细分 --format markdown
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore12.0/C12-M14-PartSweepHelperMutableLifecycle证据解锁批次/工作步骤细分 --format markdown
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore12.0/C12-M15-SketchStableGeometryIdMappedNameLedger设计批次/工作步骤细分 --format markdown
cd cad-core
./build/cad-core capabilities | jq -r 'paths as $p | select($p[-1] == "remaining_gaps" or $p[-1] == "known_gaps") | [($p|map(tostring)|join(".")), (getpath($p)|@json)] | @tsv'
cd ..
awk -F '\t' 'FNR==1{n=NF; next} NF!=n{print FILENAME ":" FNR ": expected " n " fields, got " NF; bad=1} END{exit bad}' docs/CADCore12.0/C12-M15-SketchStableGeometryIdMappedNameLedger设计批次/矩阵/*.tsv
git diff --check
```
