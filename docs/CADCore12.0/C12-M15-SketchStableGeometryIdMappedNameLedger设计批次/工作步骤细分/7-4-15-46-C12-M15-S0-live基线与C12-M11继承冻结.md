# C12-M15 S0 live 基线与 C12-M11 继承冻结

## 目标

冻结 C12-M15 设计前的真实仓库状态，并确认 C12-M11 stable-id follow-up 与 C12-M14 helper closeout 的继承边界。

## 必读文件

- `../README.md`
- `../7-4-15-44-C12-M15-SketchStableGeometryIdMappedNameLedger设计批次方案.md`
- `docs/CADCore12.0/README.md`
- `docs/CADCore12.0/C12-M11-SketchInternalEdgeSubshapeMeshContract批次/工作步骤细分/7-1-03-03-【已实现】C12-M11-S4-implementation最小语义批次.md`
- `docs/CADCore12.0/C12-M11-SketchInternalEdgeSubshapeMeshContract批次/矩阵/c12m11_sketch_edge_gap_classification.tsv`
- `../矩阵/c12m15_sketch_geometry_id_scope_matrix.tsv`
- `../矩阵/c12m15_sketch_geometry_id_blocker_queue.tsv`
- `../矩阵/c12m15_sketch_geometry_id_validation_matrix.tsv`

## 操作

1. 记录 `pwd`、`git rev-parse --short HEAD`、`git log -1 --oneline` 和 `git -c core.quotepath=false status --short -uall`。
2. 确认 C12-M11 / C12-M14 队列均为空。
3. 递归检查 live capability 中 `remaining_gaps` / `known_gaps`，确认 C12-M15 是 C12-M11 follow-up，不是 capability remaining gap。
4. 更新 S0 scope / blocker / validation 行。
5. 将本步骤重命名为 `【已实现】`。

## 关闭条件

- live 基线和 dirty boundary 已记录。
- C12-M11 stable-id follow-up 已继承。
- C12-M14 helper lifecycle 不作为本包来源。
- 下一步队列进入 S1。

## 非目标

- 不运行 FreeCADCmd。
- 不修改 C++、fixtures、expected、tests 或 adapters。
- 不裁决 ledger interface。

## 验收

```bash
cd /Users/li/Chili3DProject/FreeCAD
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore12.0/C12-M11-SketchInternalEdgeSubshapeMeshContract批次/工作步骤细分 --format markdown
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore12.0/C12-M14-PartSweepHelperMutableLifecycle证据解锁批次/工作步骤细分 --format markdown
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore12.0/C12-M15-SketchStableGeometryIdMappedNameLedger设计批次/工作步骤细分 --format markdown
cd cad-core
./build/cad-core capabilities | jq -r 'paths as $p | select($p[-1] == "remaining_gaps" or $p[-1] == "known_gaps") | [($p|map(tostring)|join(".")), (getpath($p)|@json)] | @tsv'
cd ..
awk -F '\t' 'FNR==1{n=NF; next} NF!=n{print FILENAME ":" FNR ": expected " n " fields, got " NF; bad=1} END{exit bad}' docs/CADCore12.0/C12-M15-SketchStableGeometryIdMappedNameLedger设计批次/矩阵/*.tsv
git diff --check
```
