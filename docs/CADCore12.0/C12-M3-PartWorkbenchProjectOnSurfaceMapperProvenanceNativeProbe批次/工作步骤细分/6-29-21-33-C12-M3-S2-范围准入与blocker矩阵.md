# C12-M3 S2 范围准入与 blocker 矩阵

## 目标

判断每个 ProjectOnSurface provenance axis 是否属于 CAD Core request-local 产品边界，并把 native-hidden、collector、current comparison 和 product-boundary blocker 固化到矩阵。

## 必读文件

- `docs/CADCore12.0/C12-M3-PartWorkbenchProjectOnSurfaceMapperProvenanceNativeProbe批次/6-29-21-29-C12-M3-PartWorkbenchProjectOnSurfaceMapperProvenanceNativeProbe批次方案.md`
- `docs/CADCore12.0/C12-M3-PartWorkbenchProjectOnSurfaceMapperProvenanceNativeProbe批次/矩阵/c12m3_project_on_surface_mapper_native_probe_source_candidates.tsv`
- `docs/CADCore12.0/C12-M3-PartWorkbenchProjectOnSurfaceMapperProvenanceNativeProbe批次/矩阵/c12m3_project_on_surface_mapper_native_probe_scope_review_matrix.tsv`
- `docs/CADCore12.0/C12-M3-PartWorkbenchProjectOnSurfaceMapperProvenanceNativeProbe批次/矩阵/c12m3_project_on_surface_mapper_native_probe_blocker_queue.tsv`
- `docs/CADCore12.0/C12-M3-PartWorkbenchProjectOnSurfaceMapperProvenanceNativeProbe批次/矩阵/c12m3_project_on_surface_mapper_native_probe_non_goal_registry.tsv`

## 操作

1. 对 `edge_wire_provenance`、`face_rebuild_provenance`、`all_compound_height_offset`、`invalid_projection_diagnostic` 和 `api_observability` 做范围准入。
2. 对依赖 GUI session、Workbench 状态、跨请求 native document、持久 TopoDS / NamedShape / ElementMap cache、完整 BREP 传输或 output guessing 的行，写成 `product_boundary_rejected` 或 blocker。
3. 对可继续 probe 的行写出 required evidence 和 S4 close condition。
4. 更新 backend classification：所有 row 在 S2 只能是 probe candidate 或 rejected，不能是 implementation candidate。

## 非目标

- 不采集 FreeCAD expected。
- 不运行 current comparison。
- 不把 C5-M9 current source-backed expected 当作 native expected。

## 验收

```bash
cd /home/user/Chili3DProject/FreeCAD
awk -F '\t' 'FNR==1{n=NF; next} NF!=n{print FILENAME ":" FNR ": expected " n " fields, got " NF; bad=1} END{exit bad}' docs/CADCore12.0/C12-M3-PartWorkbenchProjectOnSurfaceMapperProvenanceNativeProbe批次/矩阵/*.tsv
rg -n '[ \t]$' docs/CADCore12.0/C12-M3-PartWorkbenchProjectOnSurfaceMapperProvenanceNativeProbe批次 docs/CADCore12.0/README.md
git diff --check
```
