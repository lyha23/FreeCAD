# C12-M3 S6 发布闸门

## 目标

发布 C12-M3 的最终分类：no-code retained、current-covered，或建议另开 ProjectOnSurface mapper/history implementation 包。

## 必读文件

- `docs/CADCore12.0/README.md`
- `docs/CADCore12.0/C12-M3-PartWorkbenchProjectOnSurfaceMapperProvenanceNativeProbe批次/6-29-21-29-C12-M3-PartWorkbenchProjectOnSurfaceMapperProvenanceNativeProbe批次总入口.md`
- `docs/CADCore12.0/C12-M3-PartWorkbenchProjectOnSurfaceMapperProvenanceNativeProbe批次/矩阵/c12m3_project_on_surface_mapper_native_probe_source_candidates.tsv`
- `docs/CADCore12.0/C12-M3-PartWorkbenchProjectOnSurfaceMapperProvenanceNativeProbe批次/矩阵/c12m3_project_on_surface_mapper_native_probe_scope_review_matrix.tsv`
- `docs/CADCore12.0/C12-M3-PartWorkbenchProjectOnSurfaceMapperProvenanceNativeProbe批次/矩阵/c12m3_project_on_surface_mapper_native_probe_blocker_queue.tsv`
- `docs/CADCore12.0/C12-M3-PartWorkbenchProjectOnSurfaceMapperProvenanceNativeProbe批次/矩阵/c12m3_project_on_surface_mapper_native_probe_backend_gap_classification.tsv`
- `docs/CADCore12.0/C12-M3-PartWorkbenchProjectOnSurfaceMapperProvenanceNativeProbe批次/矩阵/c12m3_project_on_surface_mapper_native_probe_validation_matrix.tsv`
- S4/S5 产生的 `docs/temp/*c12m3*` artifacts。

## 操作

1. 逐行确认 source authority、expected status、current status、product boundary 和 code gate。
2. 若没有 row 同时满足 stable native provenance expected、request-local boundary 和 current mismatch，发布 no-code retained。
3. 若 row 为 current-covered，记录已覆盖依据，不建议 implementation 包。
4. 若 row 满足 backend gap candidate，只建议另开 implementation package；写清 C++ 落点、非目标、fixture/test 入口和验收边界。
5. 更新 C12-M3 总入口、README、root `docs/CADCore12.0/README.md` 和 validation matrix；把 S6 step 重命名为 `【已实现】`。

## 非目标

- 不在 S6 直接实现 C++。
- 不提交 unverified expected。
- 不把 blocker 留白成“待实现”。

## 验收

```bash
cd /home/user/Chili3DProject/FreeCAD
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore12.0/C12-M3-PartWorkbenchProjectOnSurfaceMapperProvenanceNativeProbe批次/工作步骤细分 --format markdown
awk -F '\t' 'FNR==1{n=NF; next} NF!=n{print FILENAME ":" FNR ": expected " n " fields, got " NF; bad=1} END{exit bad}' docs/CADCore12.0/C12-M3-PartWorkbenchProjectOnSurfaceMapperProvenanceNativeProbe批次/矩阵/*.tsv
rg -n '[ \t]$' docs/CADCore12.0/C12-M3-PartWorkbenchProjectOnSurfaceMapperProvenanceNativeProbe批次 docs/CADCore12.0/README.md
git diff --check
```
