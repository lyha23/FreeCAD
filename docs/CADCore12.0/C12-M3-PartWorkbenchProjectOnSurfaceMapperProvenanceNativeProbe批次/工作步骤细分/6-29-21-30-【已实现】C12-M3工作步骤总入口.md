# C12-M3 工作步骤总入口

## 队列目标

按顺序执行 S0-S6，把 C12-M2 留下的 ProjectOnSurface `native_hidden` mapper/provenance blocker 窄化为原生可观测性结论。该队列只负责 native provenance expected 准入与发布闸门，不负责 C++ implementation。

## 执行顺序

1. S0：live 基线与 C12-M2 继承口径冻结。
2. S1：FreeCAD 源码与现有 provenance 证据矩阵。
3. S2：范围准入与 blocker 矩阵。
4. S3：NativeProvenanceProbe harness 与 artifact schema。
5. S4：ProjectOnSurface 原生 provenance 可观测性 probe。
6. S5：Current comparison 与 implementation gate 审计。
7. S6：发布闸门。

## 队列检查

```bash
cd /home/user/Chili3DProject/FreeCAD
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore12.0/C12-M3-PartWorkbenchProjectOnSurfaceMapperProvenanceNativeProbe批次/工作步骤细分 --format markdown
```

## 通用纪律

- 每个 step 完成后把对应 Markdown 文件重命名为带 `【已实现】`。
- 每个 step 只改 C12-M3 文档和矩阵，除非 step 明确授权创建 `docs/temp` probe artifact。
- S4 之前不得采信 current cad-core mismatch；S5 只能比较 expected-ready row。
- 不得修改 `cad-core/src`、`cad-core/include`、fixtures expected、tests、adapters 或 capability wording。
- native probe 必须区分 expected-ready、native-hidden、collector bug、sandbox limitation 和 product boundary；不得从 output order / bbox / EdgeN 猜 provenance。
- 只有 S6 可发布后续 implementation package 建议。
