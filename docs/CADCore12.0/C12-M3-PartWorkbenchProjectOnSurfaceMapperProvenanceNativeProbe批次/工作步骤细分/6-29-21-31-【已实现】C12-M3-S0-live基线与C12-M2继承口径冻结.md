# C12-M3 S0 live 基线与 C12-M2 继承口径冻结【已实现】

## 目标

冻结 C12-M3 的 live 起点，确认 C12-M2 S6 结论仍然是 no-code gate，并把本包限定为 ProjectOnSurface mapper/provenance native probe。

## 必读文件

- `docs/CADCore12.0/README.md`
- `docs/CADCore12.0/C12-M2-PartWorkbenchNativeOracleProbe批次/6-29-18-53-C12-M2-PartWorkbenchNativeOracleProbe批次总入口.md`
- `docs/CADCore12.0/C12-M2-PartWorkbenchNativeOracleProbe批次/矩阵/c12m2_partworkbench_native_oracle_backend_gap_classification.tsv`
- `docs/CADCore12.0/C12-M3-PartWorkbenchProjectOnSurfaceMapperProvenanceNativeProbe批次/README.md`
- `docs/CADCore12.0/C12-M3-PartWorkbenchProjectOnSurfaceMapperProvenanceNativeProbe批次/6-29-21-29-C12-M3-PartWorkbenchProjectOnSurfaceMapperProvenanceNativeProbe批次总入口.md`

## 操作

1. 记录 `pwd`、`git rev-parse --short HEAD`、`git log -1 --oneline` 和 `git -c core.quotepath=false status --short -uall`。
2. 用 `step_goal_queue.py` 检查 C12-M1、C12-M2 和 C12-M3 队列；C12-M3 应从 S0 开始。
3. 回写 C12-M3 README / 总入口 / validation matrix 的 S0 基线、dirty boundary、队列状态和 C12-M2 继承口径。
4. 明确 C12-M3 不授权 `cad-core/src`、`cad-core/include`、fixtures expected、tests、adapters、capability wording 或 full build。

## 非目标

- 不运行 FreeCADCmd probe。
- 不做 current cad-core comparison。
- 不新增 implementation row。

## 验收

```bash
cd /home/user/Chili3DProject/FreeCAD
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore12.0/C12-M3-PartWorkbenchProjectOnSurfaceMapperProvenanceNativeProbe批次/工作步骤细分 --format markdown
awk -F '\t' 'FNR==1{n=NF; next} NF!=n{print FILENAME ":" FNR ": expected " n " fields, got " NF; bad=1} END{exit bad}' docs/CADCore12.0/C12-M3-PartWorkbenchProjectOnSurfaceMapperProvenanceNativeProbe批次/矩阵/*.tsv
rg -n '[ \t]$' docs/CADCore12.0/C12-M3-PartWorkbenchProjectOnSurfaceMapperProvenanceNativeProbe批次 docs/CADCore12.0/README.md
git diff --check
```
