# 【已实现】C11-M1 S0 live 基线与声明口径冻结

## 目标

冻结 C11-M1 起点：确认 C10-M1 到 C10-M4 队列关闭、CopyOnChange 仍是 retained gap、`part_workbench.sweep` 当前是 C6-M4 product contract non-parity 且 `remaining_gaps=[]`。S0 不采 oracle、不改 C++。

## 输入

- `docs/CADCore10.0/README.md`
- `docs/CADCore6.0/C6-M4-PartWorkbenchSweepLocatedProfileCombinedPipeShellProductContract主线/README.md`
- `cad-core/src/runtime/capability_contract.cpp`
- `cad-core/tests/test_adapters.py`
- 当前 `git status --short -uall`

## live baseline

本轮 S0 执行基线：

```text
pwd=/home/user/Chili3DProject/FreeCAD
HEAD=a7df521d6a
git log -1 --oneline=a7df521d6a test: 同步 c3m6 Assembly expected 状态
git -c core.quotepath=false status --short -uall=<clean>
```

起点工作区干净；S0 不需要记录或隔离非本包 dirty 文件。本轮只允许产生
`docs/CADCore11.0/C11-M1-PartSweepLocationOverloadNativeParity复开批次` 内的文档、
索引和矩阵状态变更。

## 声明口径

| 项 | C11-M1 口径 |
| --- | --- |
| 主题 | Part Sweep Location overload native parity 复开。 |
| 当前 supported | live capability / adapter test 均显示 C6-M4 CAD Core product contract non-parity 已发布。 |
| 当前 gap | `part_workbench.sweep.remaining_gaps=[]`；两个 wrapper blocker 只保留为 narrowed historical wrapper evidence。 |
| C++ 闸门 | 只有 S3 native oracle stable 且 S4 证明 current mismatch 时才打开。 |
| CopyOnChange | C10-M4 已关闭为 retained diagnostic，不进入 C11-M1。 |
| dirty boundary | S0 起点 clean；本包只碰 `docs/CADCore11.0/C11-M1-PartSweepLocationOverloadNativeParity复开批次`。 |

## 禁止声明

- 禁止声明 `Part::Sweep` native DocumentObject 已支持 `SectionOptions`、`AuxiliarySpine` 或 `Tolerance` direct properties。
- 禁止声明 c6m4 product contract 已经是 FreeCAD parity。
- 禁止删除 c5m10 historical known-gap guard。
- 禁止把 `notCollected` 当作 C++ gap。
- 禁止把 GUI、PartDesign Pipe/Hole、persistent wrapper lifecycle 混入本批次。

## 必须回写的矩阵行

- `C11M1-SCOPE-001`
- `C11M1-BLOCKER-000`
- `C11M1-CAT-000`
- `C11M1-VAL-000..004`

## S0 结论

- C10-M1、C10-M2、C10-M3、C10-M4 的 `step_goal_queue.py` 输出均只有表头，确认队列已关闭。
- `docs/CADCore10.0/README.md` 继续把 CopyOnChange 记录为 C10-M4 docs-only no-code retained diagnostic / release gate；`cad-core/src/runtime/capability_contract.cpp` 中 SubShapeBinder 仍发布 `copy_on_change_full_temporary_document_cache` 为 `known_gap_diagnostic` / `oracle_blocked` / `remaining_gaps`，不进入 C11-M1。
- `docs/CADCore6.0/C6-M4-PartWorkbenchSweepLocatedProfileCombinedPipeShellProductContract主线/README.md` 与 live capability 一致：C6-M4 已关闭，located profile 与 advanced combined 是 CAD Core product contract non-parity，不是 FreeCAD parity。
- live capability / `cad-core/tests/test_adapters.py` 仍能追溯 `part_sweep_located_profile_freecadcmd_wrapper_build_blocker`、`part_sweep_advanced_combined_freecadcmd_wrapper_build_blocker`、`freecadcmd_location_overload_status=notCollected` 和 `part_workbench.sweep.remaining_gaps=[]`。
- S0 未运行 FreeCADCmd，未采 oracle，未改 C++、tests、fixtures、expected 或 collector，未新增 backend gap。

## 验收标准

```bash
cd /home/user/Chili3DProject/FreeCAD
git -c core.quotepath=false status --short -uall
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore10.0/C10-M1-SketchOpenWireInternalFaceStableSelector批次/工作步骤细分 --format markdown
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore10.0/C10-M2-PartDesignDressUpHoleTopoHistory批次/工作步骤细分 --format markdown
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore10.0/C10-M3-ReferenceShadowShadowSubNativeRecovery准入批次/工作步骤细分 --format markdown
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore10.0/C10-M4-SubShapeBinderCopyOnChangeDTO准入批次/工作步骤细分 --format markdown
rg -n 'part_workbench\\.sweep|part_sweep_located_profile_freecadcmd_wrapper_build_blocker|part_sweep_advanced_combined_freecadcmd_wrapper_build_blocker|freecadcmd_location_overload_status|remaining_gaps' cad-core/src/runtime/capability_contract.cpp cad-core/tests/test_adapters.py
awk -F '\t' 'FNR==1{n=NF; next} NF!=n{print FILENAME ":" FNR ": expected " n " fields, got " NF; bad=1} END{exit bad}' docs/CADCore11.0/C11-M1-PartSweepLocationOverloadNativeParity复开批次/矩阵/*.tsv
git diff --check
```

通过条件：

- C10-M1 到 C10-M4 队列均无待执行 step。S0 已确认。
- live capability 命中 C6-M4 non-parity status、两个 narrowed blocker、`freecadcmd_location_overload_status=notCollected` 和 sweep `remaining_gaps=[]`。S0 已确认。
- 矩阵字段一致；`git diff --check` 通过。S0 已复跑通过。
- 未修改 `cad-core/src`、tests、fixtures、expected 或 collector。S0 已遵守。

## 非目标

- S0 不运行 FreeCADCmd。
- S0 不判断 oracle 是否可采。
- S0 不创建 backend gap。
