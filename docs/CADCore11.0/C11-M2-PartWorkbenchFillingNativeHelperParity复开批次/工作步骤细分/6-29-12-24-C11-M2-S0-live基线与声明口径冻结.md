# C11-M2 S0 live 基线与声明口径冻结

## 目标

冻结 C11-M2 起点：确认 C11-M1 已关闭、C6-M5 Filling product contract 已发布、`part_workbench.filling.remaining_gaps=[]`，且 six native helper evidence 仍只是 historical narrowed evidence。S0 不采 oracle、不改 C++。

## 输入

- `docs/CADCore11.0/README.md`
- `docs/CADCore11.0/C11-M1-PartSweepLocationOverloadNativeParity复开批次/`
- `docs/CADCore6.0/C6-M5-PartWorkbenchFillingSurfaceSupportOrderParamProductContract主线/README.md`
- `cad-core/src/runtime/capability_contract.cpp`
- `cad-core/tests/test_adapters.py`
- 当前 `git status --short -uall`

## 声明口径

| 项 | C11-M2 口径 |
| --- | --- |
| 主题 | `Part.makeFilledFace` native helper parity 复开。 |
| 当前 supported | C6-M5 CAD Core request-local product contract non-parity 已发布。 |
| 当前 gap | `part_workbench.filling.remaining_gaps=[]`；six native helper evidence 只保留为 narrowed historical evidence。 |
| C++ 闸门 | 只有 S3 native helper stable 且 S4 证明 current mismatch 时才打开。 |
| C11-M1 | Sweep Location overload 已关闭，不进入 C11-M2。 |
| dirty boundary | 本批次只允许产生 `docs/CADCore11.0/C11-M2-PartWorkbenchFillingNativeHelperParity复开批次` 与 `docs/CADCore11.0/README.md` 的文档变更。 |

## 禁止声明

- 禁止声明 native upstream `Part::FilledFace` DocumentObject parity。
- 禁止声明 C6-M5 product contract 已经是 FreeCAD parity。
- 禁止删除 C6-M5 retained native helper evidence。
- 禁止把 `notCollected`、timeout、crash 或 ConstructionError 直接升级成 C++ backend gap。
- 禁止把 direct mutable wrapper lifecycle、Surface Workbench GUI、GeomPlate、Loft、Sweep 或 CopyOnChange 混入本批次。

## 必须回写的矩阵行

- `C11M2-SCOPE-001`
- `C11M2-BLOCKER-000`
- `C11M2-CAT-000`
- `C11M2-VAL-000..006`

## 验收标准

```bash
cd /home/user/Chili3DProject/FreeCAD
git -c core.quotepath=false status --short -uall
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore11.0/C11-M1-PartSweepLocationOverloadNativeParity复开批次/工作步骤细分 --format markdown
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore6.0/C6-M5-PartWorkbenchFillingSurfaceSupportOrderParamProductContract主线/工作步骤细分 --format markdown
rg -n 'part_workbench\\.filling|supported_expected_backed_plus_c6m5_product_contract_non_parity|filling_surface_native_helper_blocker|filling_support_order_g1_native_helper_blocker|filling_support_order_g2_native_helper_blocker|filling_params_pts_anisotropy_tol_g1_g2_max_segments_blocker|filling_params_all_native_helper_blocker|filling_non_boundary_support_order_native_helper_blocker|remaining_gaps' cad-core/src/runtime/capability_contract.cpp cad-core/tests/test_adapters.py
awk -F '\t' 'FNR==1{n=NF; next} NF!=n{print FILENAME ":" FNR ": expected " n " fields, got " NF; bad=1} END{exit bad}' docs/CADCore11.0/C11-M2-PartWorkbenchFillingNativeHelperParity复开批次/矩阵/*.tsv
git diff --check
```

通过条件：

- C11-M1 和 C6-M5 队列均无待执行 step。
- live capability 命中 Filling C6-M5 non-parity status、six retained evidence、historical native helper evidence 和 empty `remaining_gaps`。
- 矩阵字段一致；`git diff --check` 通过。
- 未修改 `cad-core/src`、tests、fixtures、expected 或 collector。

## 非目标

- S0 不运行 FreeCADCmd。
- S0 不判断 oracle 是否可采。
- S0 不创建 backend gap。
