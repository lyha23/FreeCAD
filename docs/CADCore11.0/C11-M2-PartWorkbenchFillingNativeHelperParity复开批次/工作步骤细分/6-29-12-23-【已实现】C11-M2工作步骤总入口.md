# 【已实现】C11-M2 工作步骤总入口

## 目标

建立 C11-M2 `Part.makeFilledFace` native helper parity 复开批次的 goal 队列索引。本文只是索引文件；创建后可标 `【已实现】`，避免 goal 队列把索引当作独立实现步骤。

当前收口状态：S0 已完成 live 基线与声明口径冻结；S1 已完成 FreeCAD 源码与 helper 候选矩阵复核；S2 已完成范围准入与 blocker 路由；S3 已完成 FreeCADCmd 原生 helper 复采集并确认 101/102/201/202 均为 `notCollected_s3_reconfirmed`，203 为 direct wrapper diagnostic `dependency_retained`。S4 到 S6 仍为待执行；不得把整个主线写成“已实现”。

## 步骤索引

| 步骤 | 文件 | 当前状态 | 解决的问题 |
| --- | --- | --- | --- |
| S0 | [6-29-12-24-【已实现】C11-M2-S0-live基线与声明口径冻结.md](6-29-12-24-【已实现】C11-M2-S0-live基线与声明口径冻结.md) | 已实现 | 冻结 live capability、dirty boundary、C11-M1/C6-M5 继承口径和 forbidden claims。 |
| S1 | [6-29-12-25-【已实现】C11-M2-S1-FreeCAD源码与helper候选矩阵.md](6-29-12-25-【已实现】C11-M2-S1-FreeCAD源码与helper候选矩阵.md) | 已实现 | 复核 FreeCAD helper source、direct wrapper controls 和 current cad-core landings，关闭 `C11M2-BLOCKER-101`。 |
| S2 | [6-29-12-26-【已实现】C11-M2-S2-范围准入与blocker矩阵.md](6-29-12-26-【已实现】C11-M2-S2-范围准入与blocker矩阵.md) | 已实现 | 已把 source candidates 路由到 scope、blocker、oracle fixture、nonGoal 和 backend gap 分类。 |
| S3 | [6-29-12-27-【已实现】C11-M2-S3-FreeCADCmd原生FillingHelper复采集.md](6-29-12-27-【已实现】C11-M2-S3-FreeCADCmd原生FillingHelper复采集.md) | 已实现 | 已复采集 Surface、Supports/Orders、ExplicitParams、non-boundary support/order native helper oracle，均无 stable helper expected。 |
| S4 | [6-29-12-28-C11-M2-S4-ProductContract到Parity升级审计.md](6-29-12-28-C11-M2-S4-ProductContract到Parity升级审计.md) | 待执行 | 在 stable native oracle 存在时比较 C6-M5 product contract 与 native expected。 |
| S5 | [6-29-12-29-C11-M2-S5-协议边界与non-goal复审.md](6-29-12-29-C11-M2-S5-协议边界与non-goal复审.md) | 待执行 | 关闭 native DocumentObject、GUI、persistent wrapper lifecycle、adapter fixup 等非目标。 |
| S6 | [6-29-12-30-C11-M2-S6-Oracle实现与发布闸门.md](6-29-12-30-C11-M2-S6-Oracle实现与发布闸门.md) | 待执行 | 消费 S3-S5 结论；有 backend gap 才落 C++ / fixtures / tests / capability，否则 no-code release gate。 |

## 执行顺序

1. S0 已完成：确认工作区边界、C11-M1 已关闭、C6-M5 product contract 当前状态和 Filling narrowed evidence。
2. S1 已完成：只复核源码与 current code，不运行 FreeCADCmd，不升级 support。
3. S2 已完成：所有 scope / blocker / oracle / nonGoal / backend classification 已有合法 owner，`C11M2-BLOCKER-201` 已关闭。
4. S3 已完成：运行 FreeCADCmd probe，输出写入 `docs/temp`；101/102/201/202 为 `notCollected_s3_reconfirmed`，203 为 diagnostic `dependency_retained`。
5. 再做 S4：只消费 S3 stable oracle；本轮没有 stable native expected，因此不得比较 parity 或创建 backend gap。
6. 再做 S5：关闭协议和非目标边界，保证 S6 不被 wrapper lifecycle / GUI / adapter fixup 污染。
7. 最后做 S6：根据 S3-S5 结果选择 no-code gate 或代码实现 gate。

## 当前矩阵闸门

| 矩阵 | 当前用途 | 当前结论 |
| --- | --- | --- |
| `c11m2_part_workbench_filling_native_helper_source_candidates.tsv` | FreeCAD / cad-core authority evidence | S1 已复核 `C11M2-SRC-001..010`，含 helper forwarding caveat、builder、wrapper、current cad-core、capability/tests 和 C6-M5 retained evidence。 |
| `c11m2_part_workbench_filling_native_helper_scope_review_matrix.tsv` | scope 和 owner step | S3 已把 native helper 101/102/201/202 复确认为 `notCollected_s3_reconfirmed`，203 为 direct wrapper diagnostic dependency；S4 只能记录 no comparison。 |
| `c11m2_part_workbench_filling_native_helper_blocker_queue.tsv` | blocker 与 close condition | S0 baseline blocker、`C11M2-BLOCKER-101`、`C11M2-BLOCKER-201` 与 `C11M2-BLOCKER-301..304` 已关闭；S4-S6 blocker 仍待执行。 |
| `c11m2_part_workbench_filling_native_helper_oracle_fixture_matrix.tsv` | retained evidence 与 S3 probe route | C6-M5 retained helper evidence 是输入，不是 C11-M2 stable native expected；S3 已写入 `C11M2-ORC-101..202` 的 notCollected 证据。 |
| `c11m2_part_workbench_filling_native_helper_non_goal_registry.tsv` | 非目标和 reopen condition | S2 已写入 S5 owner 口径，待 S5 关闭。 |
| `c11m2_part_workbench_filling_native_helper_backend_gap_classification.tsv` | implementation gate 分类 | S2 未创建 backend gap；S4 以后才允许在 stable oracle 加 current mismatch 的证据下产生 candidate。 |
| `c11m2_part_workbench_filling_native_helper_validation_matrix.tsv` | 验收命令索引 | 当前定义 docs / TSV / source / S2 route / oracle / release gate。 |

## 状态纪律

- S3-S6 不得预先标 `【已实现】`；S0、S1、S2 已验证并按文件名标记完成。
- `notCollected` 不能自动变成 `backendGap`。
- `backendGap` 必须同时有 FreeCAD authority、stable native expected 和 current cad-core request-local mismatch。
- C6-M5 product contract 可作为 current implementation evidence，不可直接改写成 FreeCAD parity。
- 如果 native helper oracle 仍失败，S6 只能发布 no-code retained non-parity gate。

## 通用验收

```bash
cd /home/user/Chili3DProject/FreeCAD
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore11.0/C11-M2-PartWorkbenchFillingNativeHelperParity复开批次/工作步骤细分 --format markdown
awk -F '\t' 'FNR==1{n=NF; next} NF!=n{print FILENAME ":" FNR ": expected " n " fields, got " NF; bad=1} END{exit bad}' docs/CADCore11.0/C11-M2-PartWorkbenchFillingNativeHelperParity复开批次/矩阵/*.tsv
rg -n '[ \t]$' docs/CADCore11.0/C11-M2-PartWorkbenchFillingNativeHelperParity复开批次 docs/CADCore11.0/README.md
git diff --check
```

## 非目标

- 不继续实现 CopyOnChange / SubShapeBinder temporary document cache。
- 不重开 C11-M1 Sweep Location overload。
- 不声明 native `Part::FilledFace` DocumentObject parity。
- 不实现 Surface Workbench GUI / native feature。
- 不保存跨请求 mutable `BRepOffsetAPI_MakeFilling` wrapper、TopoDS shape 或 BREP 状态。
- 不用 fixture 名称、bbox、面积、输出排序或 adapter 层补丁修复 Filling parity。
