# C11-M2 S5 协议边界与 non-goal 复审

## 目标

在 S6 前关闭协议边界，确保 C11-M2 只处理 request-local `Part.makeFilledFace` helper native oracle 与 product-contract comparison，不被 native DocumentObject、GUI、mutable wrapper lifecycle 或 adapter shortcut 污染。

## 需要关闭的边界

| non-goal | 边界 |
| --- | --- |
| `C11M2-NG-001` | Native upstream `Part::FilledFace` DocumentObject parity。 |
| `C11M2-NG-002` | Surface Workbench GUI / native feature。 |
| `C11M2-NG-003` | Cross-request mutable `BRepOffsetAPI_MakeFilling` wrapper lifecycle。 |
| `C11M2-NG-004` | Direct wrapper UV point-on-support branch as helper DTO。 |
| `C11M2-NG-005` | Full Part surface family、GeomPlate、Loft、Sweep。 |
| `C11M2-NG-006` | Adapter trimming / output repair / fixture-name branch。 |
| `C11M2-NG-007` | Product contract as parity without stable native oracle。 |
| `C11M2-NG-008` | Cross-request BREP / TopoDS / NamedShape state。 |

## 必须回写的矩阵行

- `C11M2-SCOPE-401`
- `C11M2-BLOCKER-501`
- `C11M2-NG-001..008`
- `C11M2-CAT-401`
- `C11M2-VAL-501..503`

## 验收标准

```bash
cd /home/user/Chili3DProject/FreeCAD
rg -n 'native Part::FilledFace|Surface Workbench|BRepOffsetAPI_MakeFilling wrapper|UV point-on-support|adapter|product contract as parity|cross-request' docs/CADCore11.0/C11-M2-PartWorkbenchFillingNativeHelperParity复开批次/矩阵/c11m2_part_workbench_filling_native_helper_non_goal_registry.tsv
rg -n 'freecad_native_document_object|source_backed_helper|no_cross_request_mutable_makefilling_wrapper_state|wrapper_uv_point_on_support_not_helper_dto|full_part_surface_family_not_claimed' cad-core/src/runtime/capability_contract.cpp cad-core/tests/test_adapters.py
awk -F '\t' 'FNR==1{n=NF; next} NF!=n{print FILENAME ":" FNR ": expected " n " fields, got " NF; bad=1} END{exit bad}' docs/CADCore11.0/C11-M2-PartWorkbenchFillingNativeHelperParity复开批次/矩阵/*.tsv
git diff --check
```

通过条件：

- 每个 non-goal row 都有 scope reason、user/protocol behavior 和 reopen condition。
- S5 不新增 backend gap。
- S5 明确 S6 只能消费 S3 stable / notCollected、S4 comparison conclusion 和 S5 non-goal boundary。
- Adapter 仍是协议转换层，不承接 Filling 业务语义。

## 非目标

- S5 不修改 capability 状态。
- S5 不运行 heavy tests。
- S5 不删除 retained helper evidence。
