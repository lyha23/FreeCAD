# 【已实现】C11-M2 S2 范围准入与 blocker 矩阵

## 目标

把 S1 source candidates 路由到 scope、blocker、oracle fixture、non-goal 和 backend gap 分类。S2 的核心闸门是：没有 stable native oracle 之前，不允许出现 implementation-ready backend gap。

## live 基线

| 项 | 结果 |
| --- | --- |
| `pwd` | `/home/user/Chili3DProject/FreeCAD` |
| `git rev-parse --short HEAD` | `f08b6658d1` |
| `git log -1 --oneline` | `f08b6658d1 docs: 完成 C11-M2 S1 源码候选矩阵复核` |
| `git -c core.quotepath=false status --short -uall` | 无输出，S2 起点工作区干净。 |

## S2 完成结论

- `C11M2-SCOPE-101`、`C11M2-SCOPE-102`、`C11M2-SCOPE-201`、`C11M2-SCOPE-202` 已统一路由为 `native_oracle_pending_s3`；S3 复采集 stable native oracle 前，不允许进入 parity comparison 或 backend gap。
- `C11M2-SCOPE-301` 已路由为 `current_product_comparison_pending_s4`；只有 S3 stable native expected 加 S4 current request-local mismatch 才能产生 `backend_gap_candidate`。
- `C11M2-SCOPE-203`、`C11M2-SCOPE-401` 与 `C11M2-NG-001..008` 已路由为 `nonGoal_pending_s5`，覆盖 native DocumentObject、Surface Workbench GUI、mutable wrapper lifecycle、UV point-on-support、full Part surface family、adapter/output repair、premature parity claim 和 cross-request shape state。
- `C11M2-SCOPE-501` 与 `C11M2-ORC-401` 已路由为 `release_gate_pending_s6`；S6 只能消费 S3-S5 的证据后选择 no-code retained gate 或 proven backend gap implementation gate。
- `C11M2-BLOCKER-201` 已关闭为 `closed_s2`；`C11M2-BLOCKER-301..304`、`401`、`501`、`601` 仍分别由 S3-S6 消费。
- `C11M2-ORC-001..004` 保留 C6-M5 retained evidence 或 expected-backed subset 口径，但状态进入 S3 native oracle 复采集；没有删除 C6-M5 retained evidence。
- `C11M2-CAT-101..501` 明确了 S3/S4/S5/S6 owner route，backend gap classification 仍不包含未经 S3/S4 证明的 implementation-ready gap。
- S2 未运行 FreeCADCmd，未创建 expected/fixtures，未修改 C++、tests 或 collectors。

## 分类规则

| 状态 | 含义 | owner |
| --- | --- | --- |
| `baseline_frozen_pending_s0` | live 状态需要 S0 确认。 | S0 |
| `source_authority_pending_s1` | 只证明源码路径，不证明可采 oracle 或 support。 | S1 |
| `native_oracle_pending_s3` | 需要 FreeCADCmd 复采集才能决定 stable / notCollected。 | S3 |
| `current_product_comparison_pending_s4` | C6-M5 current product contract 可作为 comparison target，但依赖 S3 stable oracle。 | S4 |
| `nonGoal_pending_s5` | 协议外、UI、wrapper lifecycle 或 shortcut 行。 | S5 |
| `release_gate_pending_s6` | S3-S5 以后才决定 no-code gate 或 code gate。 | S6 |

## 范围内项目

- Surface initial face link helper oracle。
- Supports/Orders C0/G1/G2 helper oracle。
- Explicit params：PtsOnCurve、Anisotropy、TolG1、TolG2、MaxSegments、all params helper oracle，同时保留 C5-M13 expected-backed subset。
- Non-boundary edge/wire/face/vertex support/order helper oracle，同时保留 C5-M12 no-support/order expected-backed subset。
- C6-M5 current product contract comparison target。

## 范围外项目

- Native upstream `Part::FilledFace` DocumentObject claim。
- Surface Workbench GUI / native feature。
- Cross-request mutable `BRepOffsetAPI_MakeFilling` wrapper lifecycle。
- Direct wrapper UV point-on-support branch as helper DTO。
- GeomPlate、Loft、Sweep、CopyOnChange、full Part surface family。
- Adapter trimming、fixture-name branch、bbox/area/output-order repair。

## 必须回写的矩阵行

- `C11M2-SCOPE-001..501`
- `C11M2-BLOCKER-201`
- `C11M2-ORC-001..401`
- `C11M2-CAT-101..501`
- `C11M2-NG-001..008`
- `C11M2-VAL-201..203`

## 验收标准

```bash
cd /home/user/Chili3DProject/FreeCAD
awk -F '\t' 'FNR==1{n=NF; next} NF!=n{print FILENAME ":" FNR ": expected " n " fields, got " NF; bad=1} END{exit bad}' docs/CADCore11.0/C11-M2-PartWorkbenchFillingNativeHelperParity复开批次/矩阵/*.tsv
rg -n 'C11M2-SCOPE-101|C11M2-SCOPE-102|C11M2-SCOPE-201|C11M2-SCOPE-202|C11M2-SCOPE-301|C11M2-BLOCKER-301|C11M2-BLOCKER-401|C11M2-CAT-301|C11M2-NG-001' docs/CADCore11.0/C11-M2-PartWorkbenchFillingNativeHelperParity复开批次/矩阵
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore11.0/C11-M2-PartWorkbenchFillingNativeHelperParity复开批次/工作步骤细分 --format markdown
rg -n '[ \t]$' docs/CADCore11.0/C11-M2-PartWorkbenchFillingNativeHelperParity复开批次 docs/CADCore11.0/README.md
git diff --check
```

通过条件：

- 每个 scope 都有合法 status、source candidate、landing、test/probe route 和 next step。
- 每个 blocker 都有 required evidence 和 close condition。
- 每个 non-goal 都有 user/protocol behavior 和 reopen condition。
- backend gap classification 不包含未经 S3/S4 证明的 backend gap candidate。
- S2 重命名后，队列只把 S3-S6 作为待执行步骤，索引文件不进入待执行队列。
- S2 文件已在验证后重命名为 `6-29-12-26-【已实现】C11-M2-S2-范围准入与blocker矩阵.md`，并同步更新 C11-M2 链接和状态行。

## 非目标

- S2 不运行 FreeCADCmd。
- S2 不创建 expected。
- S2 不删除 C6-M5 retained evidence。
