# 04 验收矩阵与回归命令

## 1. 总验收指标

最终完成必须满足：

```text
generated_open_export_edge_info_count == 0
open_wire_compound_generated_wire_info_count == 0
open_wire_compound_helper_open_export_override_helper_shape_wire_info_count == 0
open_wire_compound_legacy_helper_shape_wire_info_count == 0
result_wire_producer_unknown_invariant_count == 0
result_wire_producer_exported_without_helper_wire_info_count == migrated_legacy_helper_slot_count
all result_wire_producer_ledger_entries have state == ExportedWithoutHelper and blocker == None
non-source-edge migrated slots must be SuperEdgeRoot or CurrentMemberChildWire producer entries with root producer evidence
source_lineage_missing_open_export_edge_info_count == 0
open_export_helper_override_missing_source_lineage_edge_count == 0
repeated_split_exhaust_generated_identity_blocked_edge_info_count == 0
```

## 2. 阶段验收矩阵

| 阶段 | 必须为 0 | 允许非 0 | 说明 |
|---|---|---|---|
| P0 | `UnknownInvariant` | `LegacyHelperShapeStillUsed` | 只冻结诊断扩散，不切输出 |
| P1 | `ProducerKind::None` with no blocker | `SourceShapeIdentityNotReady` | producer contract ready |
| M2S | `source_shape_identity_unknown_count` | branch-specific blocker | source-shape gate ready |
| P3 | unowned 子集的 `LegacyHelperShapeStillUsed` | primary/secondary blocker | 只切 unowned-removal ready 子集 |
| P4 | primary/secondary 子集 unknown | 尚未处理 reason blocker | 分 branch 迁移 |
| P5 | reason-specific legacy helper output | 无 | reason 全闭环 |
| P6 | 所有 generated/helper shape output | 无 | helper shape 删除 |
| P7 | history / ElementMap identity mismatch | 无 | 下游消费同一 identity |

## 3. 当前阶段验收快照（2026-06-03）

- P0 / P1：通过。`result_wire_producer_ledger_entry_count == helper_open_export_override_edge_info_count`，`result_wire_producer_unknown_invariant_count == 0`，`result_wire_producer_none_without_blocker_count == 0`。
- M2S：通过。`source_shape_identity_unknown_count == 0`，`result_wire_producer_source_shape_ready_count + result_wire_producer_source_shape_not_ready_count == result_wire_producer_child_wire_ready_count`；source-shape gate 覆盖 FreeCAD edge-level shared-vertex purge、child-wire identity 与 root-open member input 顶点 coverage。
- P3 / P4：通过。unowned、primary、secondary branch 均走 current-member ready 子集；`unowned_removal_ready_legacy_helper_shape_output_count == 0`，`unowned_removal_current_member_producer_output_count == unowned_removal_ready_slot_count`，`multi_member_root_direct_output_count == 0`。
- P5：通过当前重点 fixture final gate。foreign producer、同源 sidecar、live-reset open edge、root-open current-member 的 helper slot 均已切到 producer identity 输出，不再使用 helper shape。
- 当前重点 fixture 输出分布：`cross-cutters` 为 12/12 exported，`segmented-cross-cutter` 为 10/10，`t-cutter` 为 8/8，`three-overlap-circles` 为 15/15，`arc-lens` 为 1/1。
- 全量 P5 fixture gate：遍历 `cad-core/fixtures/p5/*.json` 时，所有 WireJoiner ledger 的 `generated_open_export_edge_info_count`、`open_wire_compound_generated_wire_info_count`、`open_wire_compound_helper_open_export_override_helper_shape_wire_info_count`、`open_wire_compound_legacy_helper_shape_wire_info_count`、`result_wire_producer_blocker_legacy_helper_shape_still_used_count` 与 `result_wire_producer_unknown_invariant_count` 均必须为 0，且 `result_wire_producer_exported_without_helper_wire_info_count == migrated_legacy_helper_slot_count`。每个 `result_wire_producer_ledger_entries` 条目都必须是 `state == ExportedWithoutHelper`、`blocker == None`，并与 runtime open-export history 的 identity 字段一致；没有 `helper_open_export_override_source_edge_export_shape` 的条目必须是 `SuperEdgeRoot` 或 `CurrentMemberChildWire`，并带 root producer evidence，防止旧 finder/helper fallback 被重新扩成泛用输出路径。
- 当前重点 fixture 必须为 0 的 P5 blocker：`ForeignAHistorySourceGeometryMismatch`、`SameSourceSidecarGeometryMismatch`、`LiveResetSourceShapeWouldPurgeOriginal`、`CurrentMemberSourceShapeWouldPurgeOriginal`、`SourceShapeIdentityNotReady`、`SourceShapeMemberVertexIdentityNotReady`、`MultiMemberRootPendingSuppression`、`CurrentMemberChildWireIdentityNotReady`、`CurrentMemberMissingSidecarEvidence`、`CurrentMemberRootOpenProducerNotReady`、`CurrentMemberSidecarGeometryMismatch`、`MissingFullAHistoryProducerEvidence`、`MissingAHistoryRemoveSource`、`MissingRemovedTargetEvidence`、`ForeignAHistorySourceLineage`、`ForeignAHistorySourceShapeIdentityNotReady`、`ForeignAHistorySourceShapeReadyLineageMismatch` 与 `SameSourceSidecarSourceShapeIdentityNotReady`。
- P6：旧 finder 已删除。全量 P5 fixture 已满足 generated/helper output 全 0、exported/migrated slot 等式，以及 producer ledger 逐条 `ExportedWithoutHelper / None`；`generatedOpenExportShapeForSketchInternals()` 与中间态 `legacyResultSlotLocatorEdgesForDiagnosticsOnly()` 均已从代码中移除。`partial_shared_closed_wire`、`consumed_open_cutter_graph`、`partial_junction_open_cutter` 与 `closed_wire_cycle` 均改由 final `EdgeInfo` / source lineage / closed-wire boundary / bounded-result evidence 识别，旧 helper / legacy 兼容 counter 已由 `LegacyHelperShapeStillUsed` blocker 派生。
- P7：history / ElementMap identity gate 已落地。`test_p7_sketch_internal_history_consumes_result_wire_producer_identity` 要求重点 P5 fixture 中 producer ledger、runtime open-export history 与 `Sketch.InternalShape.sketch_internal_history` 的 result-wire producer kind/state/blocker/index 字段完全一致，并要求 `named_shape_history_missing_result_wire_identity_count == 0` 与 `element_map_result_wire_identity_mismatch_count == 0`。
- 当前允许非 0：`RootRemovedBy*Branch` 等 history/lifecycle 账本可以作为 producer 归因保留，但不得导致 helper output、`LegacyHelperShapeStillUsed` 或 `UnknownInvariant` 非 0。

## 4. 重点 fixture

必须覆盖：

```text
sketch-internal-face-cross-cutters
sketch-internal-face-segmented-cross-cutter
sketch-internal-face-t-cutter
sketch-internal-face-three-overlap-circles
sketch-internal-face-arc-lens
sketch-internal-face-bullseye
```

## 5. 建议测试命令

```bash
pytest cad-core/tests/test_p5_sketch.py   -k "sketch-internal-face-cross-cutters or sketch-internal-face-segmented-cross-cutter or sketch-internal-face-t-cutter or sketch-internal-face-three-overlap-circles or sketch-internal-face-arc-lens or sketch-internal-face-bullseye"
```

若项目中 fixture 名不是 pytest `-k` 可直接匹配的测试名，则改用现有测试过滤参数，但必须保持上述六类 fixture 全覆盖。

当前 P7 history identity gate 可直接运行：

```bash
python3 -m unittest cad-core.tests.test_p5_sketch.CadCoreP5SketchTest.test_p7_sketch_internal_history_consumes_result_wire_producer_identity
```

## 6. 禁止的验收方式

- 禁止把 `helper_open_export_override_edge_info_count == 0` 作为 P0/P1 验收；它只是 legacy slot 数。
- 禁止把 source-edge export shape 自然满足当成 final lifecycle 完成。
- 禁止用输出数量反推 `wireInfo` ownership。
- 禁止因为某个 fixture expected 数量变了就调整 oracle。
- 禁止在 `getOpenWires()` 输出端临时裁剪 root `superEdge` 的 sibling member。

## 7. 通过/失败判定

### 通过

满足阶段矩阵中当前阶段的“必须为 0”指标，并且所有非 0 项都属于明确允许的下一阶段 blocker。

### 失败

任一情况出现即失败：

- `UnknownInvariant > 0`
- helper shape 在 P6 后仍被输出
- generated shape 在 P6 后仍被输出
- multi-member root 直接输出
- missing branch 自动输出
- source lineage 人工补到 helper copy edge
- 普通 removed target 被当成 strict Remove source
