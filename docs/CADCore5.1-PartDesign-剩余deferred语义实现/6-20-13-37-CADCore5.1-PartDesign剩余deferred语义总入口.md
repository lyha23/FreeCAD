# CADCore5.1 PartDesign 剩余 deferred 语义总入口

## 目标

C5.1 不是重新做完整 PartDesign Workbench，而是把 C5 freeze 明确留下、且现在被产品范围重开的剩余语义做成可实施队列。唯一继续排除的是 GUI Attachment editor / ViewProvider / TaskPanel；其余 C5 deferred / non-goal 项都要走 oracle-first、cad-core 分层实现、focused tests 和 freeze 收口。

## 必读文件

- `docs/CADCore5.1-PartDesign-剩余deferred语义实现/6-20-13-37-CADCore5.1-PartDesign剩余deferred语义一揽子方案.md`
- `docs/CADCore5.1-PartDesign-剩余deferred语义实现/矩阵/cadcore51_scope_review_matrix.tsv`
- `docs/CADCore5.1-PartDesign-剩余deferred语义实现/矩阵/cadcore51_blocker_queue.tsv`
- `docs/CADCore5.1-PartDesign-剩余deferred语义实现/矩阵/cadcore51_oracle_fixture_matrix.tsv`
- `docs/CADCore5.1-PartDesign-剩余deferred语义实现/矩阵/cadcore51_input_contract_matrix.tsv`
- `docs/CADCore5.1-PartDesign-剩余deferred语义实现/矩阵/cadcore51_non_goal_registry.tsv`
- `docs/CADCore5.1-PartDesign-剩余deferred语义实现/矩阵/cadcore51_validation_matrix.tsv`

## 队列

```bash
cd /Users/li/Chili3DProject/FreeCAD
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore5.1-PartDesign-剩余deferred语义实现/工作步骤细分 --format markdown
```

## 总体顺序

1. C51-S0：范围重开、FreeCAD 调用链和 oracle 基线。
2. C51-S1：Revolution / Groove 的 UpTo、Profile subshape、FuseOrder、Datum/App line axis。
3. C51-S2：Boolean Compound / Section 从 C5-NG-005 转为实现目标。
4. C51-S3：Loft Closed / multi-wire / full MapperSewing。
5. C51-S4：Pipe Auxiliary / Binormal / Fixed / Round / scaling / SpineTangent / full sewing。
6. C51-S5：Datum AttachEngine selected map modes 和 input-driven AttachmentSupport shadow-sub writeback。
7. C51-S6：freeze 收口，更新 docs、capability、fixtures、tests 和 remaining boundary。
