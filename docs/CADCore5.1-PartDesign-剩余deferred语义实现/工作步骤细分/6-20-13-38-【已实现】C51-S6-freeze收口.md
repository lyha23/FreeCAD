# 【已实现】C51-S6 freeze 收口

## 目标

同步 C51 docs、matrices、capabilities、fixtures、expected、focused tests 和 remaining boundary，形成 CADCore5.1 PartDesign 剩余 deferred 语义 freeze。

## 必读

- `docs/CADCore5.1-PartDesign-剩余deferred语义实现/6-20-13-37-【已实现】CADCore5.1-PartDesign剩余deferred语义一揽子方案.md`
- `docs/CADCore5.1-PartDesign-剩余deferred语义实现/矩阵/cadcore51_blocker_queue.tsv`
- `docs/CADCore5.1-PartDesign-剩余deferred语义实现/矩阵/cadcore51_validation_matrix.tsv`
- C51-S0 到 C51-S5 已实现 step 文件

## 工作内容

- 确认 C51-S0 到 C51-S5 队列为空或剩余项都是 exact blocker。
- 逐项复核 `cadcore51_blocker_queue.tsv` child blockers：父 blocker 只有在其 child rows closed 或 exact blocker 化后才能关闭。
- 同步 capability metadata，不能留下旧 C5 broad deferred 文案。
- 写 freeze summary，记录最终验证命令、supported rows、remaining exact blockers 和 GUI-only non-goal。
- 重型回归通过后，将本 step 和方案类文档按仓库规则标记为 `【已实现】`。

## 验收

```bash
cd /Users/li/Chili3DProject/FreeCAD
git diff --check -- docs/CADCore5.1-PartDesign-剩余deferred语义实现 cad-core
cd /Users/li/Chili3DProject/FreeCAD/cad-core
cmake --build build
python3 -m unittest tests.test_p7_features tests.test_expected_fixtures tests.test_adapters
python3 -m unittest tests.test_mvp tests.test_diagnostics tests.test_feature_flows tests.test_p5_sketch tests.test_p6_topology tests.test_p7_features tests.test_p8_features tests.test_expected_fixtures tests.test_adapters
```

## 完成条件

- C51 blocker queue 全部关闭或具体化。
- GUI Attachment editor / ViewProvider / TaskPanel / visual resize 是唯一保留的 Attachment non-goal。
- 工作区提交前不混入非本包 dirty 文件。

## 实施记录

### parent / child 复核

- `C51-BLK-101` 已在 `C51-BLK-111` exact Groove UpTo native BRepFeat blocker、`112` Profile.SubList/InternalFace supported、`113` FeatureFirst supported、`114` ReferenceAxis supported 后关闭。
- `C51-BLK-201` 已在 `211` Compound product contract、`212` Section standalone / Body Tip diagnostic、`213` Body ownership diagnostics supported 后关闭。
- `C51-BLK-301` 已在 `311` section ledger、`312` Closed/multi-section active parity、`313` MapperThruSections / MapperSewing ElementMap supported 后关闭。
- `C51-BLK-401` 已在 `411` selected spine supported with tangent exact blocker、`412` Fixed/Auxiliary/Binormal supported、`413` Round supported with scaling exact blocker、`414` front/back sewing supported 后关闭。
- `C51-BLK-501` 已在 `511` 3D/Plane selected modes、`512` Line/Point selected modes、`513` offset/reverse/path parameter、`514` request-local writeback supported 后关闭；remaining modes 只作为 exact blocker。

### final boundary

Supported rows are `C51-BLK-112`、`113`、`114`、`211`、`212`、`213`、`311`、`312`、`313`、`412`、`414`、`511`、`512`、`513`、`514` plus supported portions of `111`、`411`、`413`、`501`.

Remaining exact blockers：

- `partdesign_groove_upto_brepfeat_cut_native_failure`
- `partdesign_pipe_transformation_laws_source_commented`
- `partdesign_pipe_spine_tangent_source_commented`
- `datum_attach_engine_remaining_modes`

Non-goals：

- Attachment non-goal is only GUI Attachment editor / ViewProvider / TaskPanel / visual resize.
- Cross-request backend attachment session remains non-goal; AttachmentSupport writeback returns request-local `documentObjectUpdates` / `elementReferenceUpdates` only.

### capability / fixture / test sync

- `cad-core/src/adapters/c_api/c_api.cpp` reports S1-S5 as supported or supported-with-exact-blocker; Datum Attachment deferred text is GUI-only and `non_goals` records cross-request backend attachment session.
- `cad-core/tests/test_adapters.py` asserts C51 capability statuses, fixtures, exact blockers, empty broad deferred lists, GUI-only Attachment deferred text and backend-session non-goal.
- C51 fixture families are checked in under `cad-core/fixtures/c51m1`..`c51m5`; active expected files are present for Revolution, Boolean product contracts, Loft, Pipe and Datum selected modes.

### 验证结果

- `git diff --check -- docs/CADCore5.1-PartDesign-剩余deferred语义实现 cad-core`：通过。
- TSV 列数一致性检查：通过。
- `cmake --build build`：通过。
- `python3 -m unittest tests.test_p7_features tests.test_expected_fixtures tests.test_adapters`：144 tests OK, skipped=14。
- `python3 -m unittest tests.test_mvp tests.test_diagnostics tests.test_feature_flows tests.test_p5_sketch tests.test_p6_topology tests.test_p7_features tests.test_p8_features tests.test_expected_fixtures tests.test_adapters`：537 tests OK, skipped=14。
