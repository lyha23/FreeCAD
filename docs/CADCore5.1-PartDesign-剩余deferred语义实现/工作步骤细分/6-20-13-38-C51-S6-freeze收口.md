# C51-S6 freeze 收口

## 目标

同步 C51 docs、matrices、capabilities、fixtures、expected、focused tests 和 remaining boundary，形成 CADCore5.1 PartDesign 剩余 deferred 语义 freeze。

## 必读

- `docs/CADCore5.1-PartDesign-剩余deferred语义实现/6-20-13-37-CADCore5.1-PartDesign剩余deferred语义一揽子方案.md`
- `docs/CADCore5.1-PartDesign-剩余deferred语义实现/矩阵/cadcore51_blocker_queue.tsv`
- `docs/CADCore5.1-PartDesign-剩余deferred语义实现/矩阵/cadcore51_validation_matrix.tsv`
- C51-S0 到 C51-S5 已实现 step 文件

## 工作内容

- 确认 C51-S0 到 C51-S5 队列为空或剩余项都是 exact blocker。
- 同步 capability metadata，不能留下旧 C5 broad deferred 文案。
- 写 freeze summary，记录最终验证命令、supported rows、remaining exact blockers 和 GUI-only non-goal。
- 重型回归通过后，将本 step 和方案类文档按仓库规则标记为 `【已实现】`。

## 验收

```bash
cd /Users/li/Chili3DProject/FreeCAD
git diff --check -- docs/CADCore5.1-PartDesign-剩余deferred语义实现 cad-core
cd /Users/li/Chili3DProject/FreeCAD/cad-core
cmake --build build
python3 -m unittest tests.test_p7_features tests.test_expected_fixtures tests.test_adapters
python3 -m unittest tests.test_mvp tests.test_diagnostics tests.test_feature_flows tests.test_p5_sketch tests.test_p6_topology tests.test_p7_features tests.test_p8_features tests.test_expected_fixtures tests.test_adapters
```

## 完成条件

- C51 blocker queue 全部关闭或具体化。
- GUI Attachment editor 是唯一保留的 Attachment non-goal。
- 工作区提交前不混入非本包 dirty 文件。
