# C51X-S2 Pipe 产品扩展契约裁决

## 目标

裁决 `Transformation=Linear/S-shape/Interpolation` 与 `SpineTangent/AuxiliarySpineTangent` 是否作为 CAD Core product extension 实现。当前 FreeCAD 源码中这些分支是 source-commented exact blockers，不能声称已对齐 FreeCAD parity。

## 必读

- `src/Mod/PartDesign/App/FeaturePipe.cpp`
- `src/Mod/Part/App/TopoShapeExpansion.cpp`
- `cad-core/src/part_design/feature_pipe.cpp`
- `cad-core/include/cad_core/part/topo_shape_expansion.h`
- `cad-core/src/part/topo_shape_expansion.cpp`
- `cad-core/fixtures/c51m4/partdesign-pipe-source-backed-blockers.json`

## 工作内容

- 先保持 exact blocker 并记录产品是否需要这两类扩展。
- 若产品需要，实现前必须写 request DTO：law 类型、law 参数、section/profile 对应规则、spine tangent expansion 的连续边选择策略。
- PipeShell setupAlgorithm 参数只能落在 `PipeShellOptions` / topo helper，不写进 adapter。
- Tangent expansion 必须有连续边账本和 source edge provenance，不能只把相邻 edge 拼成 compound。
- Capability 要标为 CAD Core product extension，并保留 FreeCAD source-commented 说明。

## 完成记录

- `cad-core/fixtures/c51m4/partdesign-pipe-source-backed-blockers.json` 当前仍覆盖 Linear / S-shape / Interpolation 和 SpineTangent / AuxiliarySpineTangent diagnostics。
- capability 继续声明 `partdesign_pipe_transformation_laws_source_commented` 与 `partdesign_pipe_spine_tangent_source_commented` exact blockers。
- 本包未批准 CAD Core product extension DTO，也未实现 continuous-edge ledger。
- blocker 状态：两项均为 `still_exact_blocker_source_commented`。

## 验收

```bash
cd /Users/li/Chili3DProject/FreeCAD
git diff --check -- docs/CADCore5.1-PartDesign-剩余deferred语义实现 cad-core
cd /Users/li/Chili3DProject/FreeCAD/cad-core
cmake --build build
python3 -m unittest tests.test_p7_features tests.test_expected_fixtures tests.test_adapters
```

## 完成条件

- 每个 Pipe blocker 被归类为 `still_exact_blocker` 或 `cad_core_product_extension`。
- 如果实现扩展，必须有 fixtures、focused tests、capability product-contract 文案和 source-commented FreeCAD 说明。
