# C51X-S0 live 基线与路由冻结

## 目标

复核 C51 freeze 后的 live repo 状态，确认后续只围绕 exact blockers 展开，不重开旧 C5 broad deferred。

## 必读

- `docs/CADCore5.1-PartDesign-剩余deferred语义实现/6-20-13-38-【已实现】C51-S6-freeze收口总结.md`
- `docs/CADCore5.1-PartDesign-剩余deferred语义实现/6-20-17-35-【已实现】CADCore5.1-exact-blocker后续实现方案.md`
- `cad-core/src/adapters/c_api/c_api.cpp`
- `cad-core/tests/test_adapters.py`

## 工作内容

- 跑队列检查，确认旧 C51-S0..S6 都是 `【已实现】`，新队列从 C51X-S0 开始。
- 读取 `cad_core_capabilities_json()` 当前 `part_design.revolution_groove`、`part_design.pipe`、`part_design.datum_attachment` 状态。
- 在 `cadcore51_followup_*` 矩阵中确认 Part Workbench surface rows 只作为路由边界，不进入本包 implementation。
- 如发现 capability 与 C51 freeze 总结不一致，先修正文档/矩阵，不直接改 executor。

## 完成记录

- 已确认 C51-S0..S6 旧队列关闭，新队列按 C51X-S0..S4 执行。
- Follow-up 矩阵只保留 C51 freeze 后 exact blockers 与 Part Workbench surface route。
- live capability 中 Revolution/Groove、Pipe、Datum Attachment 均已清空 broad deferred；C51X 后续只围绕 exact blocker 或 bounded Datum mode family 推进。

## 验收

```bash
cd /Users/li/Chili3DProject/FreeCAD
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore5.1-PartDesign-剩余deferred语义实现/工作步骤细分 --format markdown
git diff --check -- docs/CADCore5.1-PartDesign-剩余deferred语义实现
```

## 完成条件

- 后续包边界冻结。
- 每个 follow-up blocker 都有 owner、默认动作和 validation id。
