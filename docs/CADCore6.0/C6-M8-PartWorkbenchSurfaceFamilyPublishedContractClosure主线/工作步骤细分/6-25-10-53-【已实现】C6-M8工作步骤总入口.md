# 【已实现】C6-M8 工作步骤总入口

本文是 C6-M8 的步骤索引，创建时标 `【已实现】`，避免 `goal-step-runner` 把索引当作独立实现步骤。

## 队列

| step | file | status |
| --- | --- | --- |
| S0 | `6-25-10-54-C6-M8-S0-live基线与surface-family状态冻结.md` | pending |
| S1 | `6-25-10-55-C6-M8-S1-FreeCAD源码与capability批量矩阵.md` | pending |
| S2 | `6-25-10-56-C6-M8-S2-准入路由与ProjectOnSurface裁决.md` | pending |
| S3 | `6-25-10-57-C6-M8-S3-批量实现或发布口径收口.md` | pending |
| S4 | `6-25-10-58-C6-M8-S4-fixtures-tests-capability-docs发布.md` | pending |
| S5 | `6-25-10-59-C6-M8-S5-阶段回归与release-gate.md` | pending |

## 顺序规则

1. 每次只执行当前 pending step。
2. 执行前刷新 `step_goal_queue.py`，不要一次生成所有 prompt 后盲跑。
3. S2 之前不得改 C++ 或 fixtures；S2 必须明确每个 candidate 的 route。
4. 若 S2 判定有实现型 backend gap，S3 必须按同一 DTO/API 边界批量补 code、fixtures、focused tests 和 capability。
5. 若 S2 判定为 non-goal / historical narrowed boundary，S3/S4 仍必须同步 adapter assertion、矩阵和 docs，不能只删除 `remaining_gaps` 字符串。
6. 每步完成后更新矩阵、README / 总入口状态，并把步骤文件重命名为 `【已实现】`。

