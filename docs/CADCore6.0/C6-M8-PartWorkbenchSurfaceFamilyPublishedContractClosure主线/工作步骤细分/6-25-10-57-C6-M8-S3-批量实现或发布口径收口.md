# C6-M8 S3 批量实现或发布口径收口

## 目标

消费 S2 route。若存在实现型 gap，S3 必须按同一 DTO/API 边界批量补 cad-core C++、fixtures、expected/product metadata、focused tests、capability/docs；若 S2 只剩发布口径冲突，S3 必须通过 capability + adapter assertion + docs 硬化收口。

## 实现规则

- 不允许只为一个 fixture 加特判。
- 不允许把 GUI / session / persistent wrapper 写成 CAD Core stateless product contract。
- 不允许从 cad-core output 伪造 FreeCAD expected。
- 若改 `ProjectOnSurface`，必须覆盖同一 API 边界的代表模式，不得只处理一个最容易过的 Projection item。
- 若只移动 `remaining_gaps`，必须同步 `test_adapters.py` 断言并保留 historical / non-goal evidence。

## 可能落点

| route | 落点 |
| --- | --- |
| `backend_gap_requires_implementation` | `cad-core/src/part/*.cpp`、`cad-core/include/cad_core/part/*.h`、fixtures、expected/product metadata、focused tests |
| `cad_core_product_contract_non_parity` | product fixtures、object_fields metadata、capability product_contract evidence、adapter assertion |
| `historical_narrowed_gap` | `narrowed_gaps`、delete condition、historical fixture evidence、adapter assertion |
| `non_goal_frozen` | `non_goals`、request_local_boundaries、README/矩阵、adapter assertion |

## 验收

```bash
cd /Users/li/Chili3DProject/FreeCAD/cad-core
cmake --build build
python3 -m unittest tests.test_adapters.CadCoreAdapterTest.test_c_api_capabilities_exposes_web_contract_facts
```

若 S3 改了具体 owner 的实现或 fixtures，补跑：

```bash
cd /Users/li/Chili3DProject/FreeCAD/cad-core
python3 -m unittest tests.test_p8_features tests.test_expected_fixtures
```

文档校验：

```bash
cd /Users/li/Chili3DProject/FreeCAD
awk -F '\t' 'FNR==1{n=NF; next} NF!=n{print FILENAME ":" FNR ": expected " n " fields, got " NF; bad=1} END{exit bad}' docs/CADCore6.0/C6-M8-PartWorkbenchSurfaceFamilyPublishedContractClosure主线/矩阵/*.tsv
git diff --check -- cad-core docs/CADCore6.0/C6-M8-PartWorkbenchSurfaceFamilyPublishedContractClosure主线 docs/CADCore6.0/README.md
```

## 通过条件

- S2 选择的每个 route 都有对应代码/fixture/test/capability/docs 或明确非目标证据。
- adapter capability focused test 断言最新发布口径。
- S3 文件名和标题标记为 `【已实现】` 后，队列推进到 S4。

