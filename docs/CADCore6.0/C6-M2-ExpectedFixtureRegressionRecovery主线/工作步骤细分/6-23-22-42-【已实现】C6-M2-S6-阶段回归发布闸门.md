# 【已实现】C6-M2 S6 阶段回归发布闸门

## 目标

运行 C6-M2 阶段回归，发布 expected fixture recovery 状态。S6 是发布闸门：只有阶段回归通过，或剩余失败都被写成明确 known gap，才能关闭 C6-M2。

## 本轮基线

- `pwd`：`/Users/li/Chili3DProject/FreeCAD`
- `git rev-parse --short HEAD`：`76acc921aa`
- `git log -1 --oneline`：`76acc921aa 完成 C6-M2 S5 Pocket 无 base 修复`
- `git -c core.quotepath=false status --short -uall`：无输出，S6 开始时工作区干净。

## 验证结果

Focused expected fixture gate：

```bash
cd /Users/li/Chili3DProject/FreeCAD/cad-core
python3 -m unittest tests.test_expected_fixtures.CadCoreExpectedFixtureTest.test_expected_fixtures_match_recompute_results
```

结果：`Ran 1 test in 49.586s`，`OK (skipped=29)`。

阶段回归发布闸门：

```bash
cd /Users/li/Chili3DProject/FreeCAD/cad-core
python3 -m unittest tests.test_p7_features tests.test_expected_fixtures tests.test_adapters
```

结果：`Ran 180 tests in 80.086s`，`OK (skipped=29)`。

重型收口决策：S3-S5 触及 assembly solver adapter schema、object bbox/export、PartDesign Body 与 expected fixture 合同，命中 validation matrix 的 heavy 条件，因此补跑重型命令。

```bash
cd /Users/li/Chili3DProject/FreeCAD/cad-core
python3 -m unittest tests.test_p6_topology tests.test_p7_features tests.test_expected_fixtures tests.test_adapters
```

结果：`Ran 216 tests in 88.451s`，`OK (skipped=29)`。

文档 / 矩阵收口验证：

```bash
cd /Users/li/Chili3DProject/FreeCAD
git diff --check -- docs/CADCore6.0 cad-core
for f in docs/CADCore6.0/C6-M2-ExpectedFixtureRegressionRecovery主线/矩阵/*.tsv; do awk -F '\t' 'NR==1{n=NF} NF!=n{print FILENAME ":" NR ": expected " n " fields, got " NF}' "$f"; done
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore6.0/C6-M2-ExpectedFixtureRegressionRecovery主线/工作步骤细分 --format markdown
```

结果：`git diff --check` 无输出，TSV field-count check 无输出，队列脚本无 pending row。

## 发布结论

C6-M2 关闭。S0 冻结的 15 条 expected fixture mismatch 已全部有 authority 结论并完成收口：8 条 expected refresh、3 条 implementation fix、3 条 object bbox implementation fix、1 条 ORC-013 local OCCT imported LinkGroup bbox known environment gap。expected fixture gate 与阶段回归均已恢复通过。

`ORC-013` 不是 full FreeCAD parity：`p8/app-link-imported-element-map-chain` 保留 checked-in FreeCAD 1.2 expected bbox，并通过该 expected 文件中的 `bbox_delta=0.028` 容纳本地 OCCT 7.9.3 imported LinkGroup bbox 差异。删除条件：当 CAD Core 运行基线与 expected 采集基线一致，或实现能在当前基线上复现 FreeCAD 1.2 imported LinkGroup bbox 时，删除 `bbox_delta` / `known_gap` 记录并用 focused expected fixture 与阶段回归重新验证。

非目标保持：未加入 Pipe `Transformation=Interpolation` / `LawSamples` 产品合同，未运行全量上游 FreeCAD build，未把 known gap 写成 supported parity。
