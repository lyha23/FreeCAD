# C6-M2 S0 live 回归基线冻结

## 目标

复跑并冻结 C6-M1 S6 遗留的阶段回归失败清单，形成后续 S1-S6 的唯一输入。S0 只记录 live baseline 和 failure list，不修改 expected、C++、fixture 或测试断言。

## 必读输入

- `docs/CADCore6.0/README.md`
- `docs/CADCore6.0/C6-M2-ExpectedFixtureRegressionRecovery主线/6-23-22-34-C6-M2-ExpectedFixtureRegressionRecovery主线总入口.md`
- `docs/CADCore6.0/C6-M1-PartDesignPipeProductExtension-TransformationLawAndTangentExpansion主线/工作步骤细分/6-23-19-50-【已实现】C6-M1-S6-Oracle实现与发布闸门.md`
- `cad-core/tests/test_expected_fixtures.py`
- `cad-core/fixtures`

## 实施内容

1. 记录 `pwd`、`git rev-parse --short HEAD`、`git log -1 --oneline`、`git -c core.quotepath=false status --short -uall`。
2. 运行阶段回归命令并保存失败摘要：

   ```bash
   cd /Users/li/Chili3DProject/FreeCAD/cad-core
   python3 -m unittest tests.test_p7_features tests.test_expected_fixtures tests.test_adapters
   ```

3. 提取每个 `CadCoreExpectedFixtureTest.test_expected_fixtures_match_recompute_results` failure 的 fixture 名、字段路径、expected/current 摘要和初始 owner 猜测。
4. 更新 `矩阵/c6m2_expected_fixture_regression_fixture_oracle_matrix.tsv`、`scope_review_matrix.tsv`、`blocker_queue.tsv` 的 S0 baseline 行。
5. 将本文件改名为 `6-23-22-36-【已实现】C6-M2-S0-live回归基线冻结.md`，只在完成上述记录后执行。

## 非目标

- 不更新 expected JSON。
- 不修改 `cad-core` C++、adapter 或 tests。
- 不采集 native FreeCAD expected。
- 不解释每个 failure 的最终归属；S0 只冻结清单，S1 才分类。

## 验收

```bash
cd /Users/li/Chili3DProject/FreeCAD
git diff --check -- docs/CADCore6.0
for f in docs/CADCore6.0/C6-M2-ExpectedFixtureRegressionRecovery主线/矩阵/*.tsv; do awk -F '\t' 'NR==1{n=NF} NF!=n{print FILENAME ":" NR ": expected " n " fields, got " NF}' "$f"; done
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore6.0/C6-M2-ExpectedFixtureRegressionRecovery主线/工作步骤细分 --format markdown
```
