# 【已实现】C6-M3 S6 阶段回归发布闸门

## 完成结论

- S6 已关闭 C6-M3 发布闸门。C6-M3 `Transformation=Interpolation` + request-local `LawSamples` 作为 CAD Core product contract 发布，不声明 FreeCAD parity 或 full PartDesign Pipe coverage。
- 发布前 live baseline：`pwd=/home/user/Chili3DProject/FreeCAD`，`git rev-parse --short HEAD=9689663a33`，`git log -1 --oneline=9689663a33 发布 C6-M3 S5 Pipe 插值 LawSamples 合同`，开始时 `git -c core.quotepath=false status --short -uall` 无输出。
- 队列刷新确认 S6 是唯一 pending step；本步完成后文件已重命名为 `6-24-00-24-【已实现】C6-M3-S6-阶段回归发布闸门.md`。
- S5 触达 PipeShell law/history、Body replay fixtures、expected gate 和 capability schema，因此 S6 必跑 `C6M3-VAL-901` heavy command，不能用阶段回归替代。
- build、阶段回归和 heavy 均通过，无 residual failure；因此 `C6M3-BLK-501/CAT-501/SCOPE-501/ORC-501` 关闭，不新增 blocker、known gap、owner 或删除条件。

## 验证结果

```bash
cd /home/user/Chili3DProject/FreeCAD/cad-core
cmake --build build
```

- 通过；`OndselSolver`、`cad-core-lib`、`cad_core_ffi`、`cad-core`、`cad-core-c3m1-topology-probe` 和 `cad-core-c6m3-pipe-interpolation-law-probe` 均已构建。

```bash
cd /home/user/Chili3DProject/FreeCAD/cad-core
python3 -m unittest tests.test_p7_features tests.test_expected_fixtures tests.test_adapters
```

- 通过：`Ran 182 tests in 65.996s`，`OK (skipped=29)`。

```bash
cd /home/user/Chili3DProject/FreeCAD/cad-core
python3 -m unittest tests.test_p6_topology tests.test_p7_features tests.test_expected_fixtures tests.test_adapters
```

- 通过：`Ran 218 tests in 74.459s`，`OK (skipped=29)`。

```bash
cd /home/user/Chili3DProject/FreeCAD
git diff --check -- docs/CADCore6.0 cad-core
for f in docs/CADCore6.0/C6-M3-PartDesignPipeInterpolationLawSamplesProductContract主线/矩阵/*.tsv; do awk -F '\t' 'NR==1{n=NF} NF!=n{print FILENAME ":" NR ": expected " n " fields, got " NF}' "$f"; done
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore6.0/C6-M3-PartDesignPipeInterpolationLawSamplesProductContract主线/工作步骤细分 --format markdown
```

- `git diff --check -- docs/CADCore6.0 cad-core`：通过，无输出。
- TSV field-count check：通过，无输出。
- C6-M3 队列刷新：只输出 markdown 表头，已无 pending row；该空队列是 S6 closure authority。

## 发布状态更新

- `docs/CADCore6.0/README.md`、C6-M3 README、主线总入口和工作步骤索引已记录 C6-M3 发布状态。
- `c6m3_pipe_interpolation_law_validation_matrix.tsv` 已记录 S6 stage / heavy 结果。
- `c6m3_pipe_interpolation_law_blocker_queue.tsv`、`c6m3_pipe_interpolation_law_backend_gap_classification.tsv`、`c6m3_pipe_interpolation_law_scope_review_matrix.tsv`、`c6m3_pipe_interpolation_law_oracle_fixture_matrix.tsv` 已关闭 release gate 行。
- 本步未改 C++、fixtures、capability 或 adapter；未新增 C6-M4 范围。

## 非目标保持

- 不把 Interpolation 写成 FreeCAD parity。
- 不把 known gap 写成 supported。
- 不声明 full PartDesign Pipe coverage。
- 不为了发布隐藏 failure；本轮没有 residual failure 需要登记。
