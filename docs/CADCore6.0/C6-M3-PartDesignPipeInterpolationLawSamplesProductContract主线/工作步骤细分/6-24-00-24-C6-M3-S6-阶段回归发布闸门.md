# C6-M3 S6 阶段回归发布闸门

## 目标

运行 C6-M3 发布闸门，确认 Interpolation LawSamples product contract 已通过 focused、expected fixture、adapter 和阶段回归。只有通过，或剩余失败全部有明确 known gap / blocker，才能关闭 C6-M3。

## 必读输入

- S0-S5 已实现文档
- `矩阵/c6m3_pipe_interpolation_law_validation_matrix.tsv`
- `矩阵/c6m3_pipe_interpolation_law_blocker_queue.tsv`
- `docs/CADCore6.0/README.md`
- C6-M3 README 和主线总入口

## 实施内容

1. 运行 focused C6-M3 tests。
2. 运行 expected fixture gate。
3. 运行阶段回归。
4. 若 touched PipeShell history、topo naming、Body replay 或 shared schema，补跑 heavy command。
5. 更新 README、主线总入口、validation matrix、blocker queue 和 capability 状态。
6. 完成后将本文件改名为 `6-24-00-24-【已实现】C6-M3-S6-阶段回归发布闸门.md`。

## 非目标

- 不为了发布隐藏 failure。
- 不把 known gap 写成 supported。
- 不新增 C6-M4 范围。

## 验收

```bash
cd /Users/li/Chili3DProject/FreeCAD/cad-core
python3 -m unittest tests.test_p7_features tests.test_expected_fixtures tests.test_adapters
cd /Users/li/Chili3DProject/FreeCAD
git diff --check -- docs/CADCore6.0 cad-core
for f in docs/CADCore6.0/C6-M3-PartDesignPipeInterpolationLawSamplesProductContract主线/矩阵/*.tsv; do awk -F '\t' 'NR==1{n=NF} NF!=n{print FILENAME ":" NR ": expected " n " fields, got " NF}' "$f"; done
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore6.0/C6-M3-PartDesignPipeInterpolationLawSamplesProductContract主线/工作步骤细分 --format markdown
```
