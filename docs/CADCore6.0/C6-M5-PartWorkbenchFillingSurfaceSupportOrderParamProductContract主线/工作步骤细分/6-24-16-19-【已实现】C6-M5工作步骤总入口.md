# 【已实现】C6-M5 工作步骤总入口

## 目标

为 C6-M5 建立可由 `$goal-step-runner` 顺序执行的 S0-S6 队列，把 `part_workbench.filling` 的 Surface、Supports/Orders、显式参数和 non-boundary support/order 缺口从文档状态推进到 CAD Core product contract 实现闭环。

当前收口状态：S0 已完成 live 基线与边界复核；S1 已完成 FreeCAD 源码与 helper-oracle 候选矩阵复核；S2 已完成 implementation-ready 合同与 fixture 路由；S3 已完成 Surface / Supports / Orders 产品合同实现；S4 已完成 ExplicitParams 与 non-boundary support/order 产品合同实现；S5 已完成 fixtures / tests / capability / docs 发布；S6 仍为待执行。矩阵已写入 S0/S1/S2/S3/S4/S5 证据，`part_workbench.filling.remaining_gaps=[]`，但主线 release gate 尚未完成，不得把 S6 或整个主线写成“已实现”。本文是索引文件，创建后标 `【已实现】`，避免 goal 队列把索引当作独立实现步骤。

## 步骤索引

| 步骤 | 文件 | 当前状态 | 解决的问题 |
| --- | --- | --- | --- |
| S0 | `6-24-16-20-【已实现】C6-M5-S0-live基线与Filling剩余gap边界复核.md` | 已实现 | 冻结 live baseline、状态字典、non-goal 和禁止声明。 |
| S1 | `6-24-16-21-【已实现】C6-M5-S1-FreeCAD源码与helper-oracle候选矩阵.md` | 已实现 | 复核 FreeCAD source、wrapper control 和 cad-core 落点。 |
| S2 | `6-24-16-22-【已实现】C6-M5-S2-Filling合同与oracle复采集.md` | 已实现 | 将 helper oracle blocker 拆为 notCollected 与可实现 product contract。 |
| S3 | `6-24-16-23-【已实现】C6-M5-S3-SurfaceAndSupportOrder产品合同实现.md` | 已实现 | 实现 Surface、Supports/Orders G1/G2 request-local 合同。 |
| S4 | `6-24-16-24-【已实现】C6-M5-S4-ExplicitParams与nonBoundarySupportOrder实现.md` | 已实现 | 实现 explicit params 与 non-boundary support/order 合同。 |
| S5 | `6-24-16-25-【已实现】C6-M5-S5-fixtures-tests-capability-docs发布.md` | 已实现 | 已发布 fixtures、tests、capability 和文档状态，删除已覆盖的 exact remaining gaps。 |
| S6 | `6-24-16-26-C6-M5-S6-阶段回归与release-gate.md` | 待执行 | 阶段回归、heavy 收口和 remaining_gaps 删除审计。 |

## 执行顺序

1. 先做 S0，确认 live capability 与当前测试事实。
2. S1/S2 只建立证据和准入，不直接改业务代码。
3. S3/S4 是主要代码落点；每步都必须带 fixture 或 focused unittest。
4. S5 负责发布状态，不得提前移除未实现的 `remaining_gaps`。
5. S6 只在阶段 / heavy 证据通过后关闭主线。

## 当前矩阵闸门

| 矩阵 | 当前用途 | 当前结论 |
| --- | --- | --- |
| source candidates | 源码候选 seed | S1 已复核 FreeCAD helper、builder、wrapper control 与 cad-core 落点。 |
| scope review | 范围准入 seed | S3 已覆盖 Surface、Supports/Orders；S4 已覆盖显式参数和 non-boundary support/order。 |
| input contract | DTO 合同 seed | S3 已写入 Surface/SupportOrder object_fields、diagnostics、shape_summary 合同；S4 已写入 params_source_status、explicit_param_fields、non_boundary_support_order_status 和 diagnostics。 |
| oracle fixtures | oracle / fixture 路由 seed | S3 已新增 c6m5 Surface/SupportOrder product fixtures；S4 已新增 explicit params 与 non-boundary support/order product/invalid fixtures；S5 已发布这些 product fixtures，并保留 crash/timeout/notCollected helper evidence。 |
| blocker queue | blocker seed | BLK-101/102 implementation blocker 已由 S3 关闭；BLK-201/202 implementation blocker 已由 S4 关闭；BLK-301 已由 S5 发布关闭，remaining_gaps 已清空。 |
| backend gap | backendGap 分类 seed | GAP-101/102 已由 S3 覆盖并由 S5 发布；GAP-201/202 已由 S4 覆盖并由 S5 发布；S6 只做阶段 / heavy release gate。 |
| non-goals | 不做项 seed | S0 已确认。 |
| validation | 验收命令 seed | S0/S1 验收已记录；待后续步骤追加结果。 |

## 状态纪律

- S0-S5 文件已按证据改名为 `【已实现】`；S6 在 release gate 通过前不得改名为 `【已实现】`。
- `remaining_gaps` 只能在代码、fixture、focused tests 和 capability 同步通过后删除。
- FreeCADCmd native helper crash / timeout 只能作为 blocker 证据，不是 CAD Core product contract 的失败证明。
- 本主线不声明 FreeCAD parity。

## 通用验收标准

```bash
cd /home/user/Chili3DProject/FreeCAD
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore6.0/C6-M5-PartWorkbenchFillingSurfaceSupportOrderParamProductContract主线/工作步骤细分 --format markdown
git diff --check -- docs/CADCore6.0/C6-M5-PartWorkbenchFillingSurfaceSupportOrderParamProductContract主线 docs/CADCore6.0/README.md
```

## 非目标

- 不做 Surface Workbench GUI。
- 不做跨请求 wrapper lifecycle。
- 不做 full Part surface family。
- 不把 C6-M5 与 C6-M4 Sweep、GeomPlate、Loft、Groove 混成一个批次。
