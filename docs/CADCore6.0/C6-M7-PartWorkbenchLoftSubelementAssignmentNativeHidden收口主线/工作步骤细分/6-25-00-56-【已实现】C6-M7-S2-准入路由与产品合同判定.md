# 【已实现】C6-M7-S2 准入路由与产品合同判定

## 目标

基于 S0/S1 evidence 判定 `part_loft_subelement_assignment_native_hidden` 的处理路线：继续 nativeHidden / diagnosticOnly，或批准 request-local CAD Core product contract non-parity。

## 执行基线

- `pwd=/Users/li/Chili3DProject/FreeCAD`。
- `git rev-parse --short HEAD=5587487eaa`。
- `git log -1 --oneline=5587487eaa 完成 C6-M7 S1 Loft Sections 源码复核`。
- S2 开始时工作区干净。
- S2 开始时 C6-M7 队列从 `6-25-00-56-C6-M7-S2-准入路由与产品合同判定.md` 继续。

## route decision

- S2 批准 request-local CAD Core selected subelement product contract，route decision 为 `cad_core_product_contract_non_parity`。
- FreeCAD `Part::Loft.Sections` 仍是 object-level `App::PropertyLinkList`，不能保存 subname；不得生成 FreeCAD native expected，也不得声明 parity。
- C5-M12 `part-loft-subelement-assignment-diagnostic.freecad.json` 继续作为 `part_loft_subelement_assignment_native_hidden` 的 `nativeHidden` / `diagnosticOnly` evidence 保留。
- Product Contract 必须与 FreeCAD expected 分开：S3 如新增 C6-M7 fixture，应记录 request-local product DTO、`contract_provenance=cad_core_product_contract_non_parity` 或等价 capability 字段，并保留 FreeCAD diagnostic known_gap 的来源。
- delete condition 不在 S2 删除：只有 upstream `Part::Loft.Sections` 出现 native subelement storage，或后续 S3/S4/S5 完整发布 request-local product contract non-parity 并由 capability/tests 证明后，才能处理 active remaining gap。

## 产物

- 已更新 README / 主线总入口 / 方案 / 步骤总入口。
- 已更新 scope、backend gap、input contract、oracle fixture、non-goal、blocker、validation 和 source candidates 矩阵中的 S2 actual evidence。
- S3 next step 明确为实现 product contract，不走纯 diagnostic / narrowed evidence 路线。

## 验收

```bash
cd /Users/li/Chili3DProject/FreeCAD
rg -n 'part_loft_subelement_assignment_native_hidden|known_gap|delete_condition|PropertyLinkList|SubList|cad_core_product_contract_non_parity|product_contract_non_parity|nativeHidden|diagnosticOnly' cad-core/fixtures/c5m12/expected/part-loft-subelement-assignment-diagnostic.freecad.json cad-core/tests/test_p8_features.py cad-core/tests/test_expected_fixtures.py cad-core/src/runtime/capability_contract.cpp cad-core/tests/test_adapters.py docs/CADCore6.0/C6-M7-PartWorkbenchLoftSubelementAssignmentNativeHidden收口主线
awk -F '\t' 'FNR==1{n=NF; next} NF!=n{print FILENAME ":" FNR ": expected " n " fields, got " NF; bad=1} END{exit bad}' docs/CADCore6.0/C6-M7-PartWorkbenchLoftSubelementAssignmentNativeHidden收口主线/矩阵/*.tsv
git diff --check -- docs/CADCore6.0/C6-M7-PartWorkbenchLoftSubelementAssignmentNativeHidden收口主线
```

## 非目标

- 不在 S2 写 C++。
- 不改 collector/runtime/capability。
- 不创建 fixture。
- 不删除 remaining gap。
- 不把 Product Contract 与 FreeCAD expected 混为一谈。
