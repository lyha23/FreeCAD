# C6-M7-S2 准入路由与产品合同判定

## 目标

基于 S0/S1 evidence 判定 `part_loft_subelement_assignment_native_hidden` 的处理路线：继续 nativeHidden / diagnosticOnly，或批准 request-local CAD Core product contract non-parity。

## 路由规则

- 如果 FreeCAD `PropertyLinkList` 仍无法保存 subname，则不得生成 FreeCAD native expected。
- 如果 cad-core 已能表达 request-local subelement DTO，则必须明确标记 `cad_core_product_contract_non_parity`。
- 如果只保留 blocker，需要写清 delete condition：上游 `Part::Loft.Sections` 支持 native subelement storage，或 C6-M7 明确批准 request-local DTO。

## 产物

- 更新 scope、backend gap、input contract、oracle fixture、non-goal 矩阵。
- 决定 S3 是实现 product contract 还是强化 diagnostic / narrowed evidence。

## 验收

```bash
cd /Users/li/Chili3DProject/FreeCAD
rg -n 'part_loft_subelement_assignment_native_hidden|known_gap|delete_condition|PropertyLinkList|SubList|cad_core_product_contract_non_parity' cad-core/fixtures/c5m12/expected/part-loft-subelement-assignment-diagnostic.freecad.json cad-core/tests/test_p8_features.py cad-core/tests/test_expected_fixtures.py cad-core/src/runtime/capability_contract.cpp cad-core/tests/test_adapters.py docs/CADCore6.0/C6-M7-PartWorkbenchLoftSubelementAssignmentNativeHidden收口主线
git diff --check -- docs/CADCore6.0/C6-M7-PartWorkbenchLoftSubelementAssignmentNativeHidden收口主线
```

## 非目标

- 不在 S2 写 C++。
- 不把 Product Contract 与 FreeCAD expected 混为一谈。
