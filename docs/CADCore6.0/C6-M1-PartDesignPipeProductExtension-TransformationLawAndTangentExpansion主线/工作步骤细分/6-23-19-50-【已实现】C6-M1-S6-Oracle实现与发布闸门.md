# 【已实现】C6-M1 S6 Oracle 实现与发布闸门

## 完成结论

- S6 已落地 C6-M1 最小完整语义批次：`Linear` / `S-shape` 通过 request-local `PipeLaw` DTO 和 PipeShell `SetLaw` 发布为 CAD Core product extension；`Interpolation` 保持 `product_contract_required` 诊断边界，不实现 `LawSamples` 几何合同。
- `SpineTangent` / `AuxiliarySpineTangent` 已实现 selected `EdgeN` request-local `continuous_edge_ledger`，输出 `source_object`、`requested_subnames`、`expanded_subnames`、`continuity_rule`、`adjacency_evidence`、`rejection_reason`；`non_edge_subname`、`disconnected_path`、`ambiguous_branch_junction`、`closed_loop_ambiguity` 均有 locatable diagnostic。
- S6 新增 `cad-core/fixtures/c6m1`，覆盖 law、tangent 和 diagnostic；focused tests 已覆盖 C6 product fixtures、C51 Pipe regression、capability publication。
- Capability 已发布为 `supported_c6m1_pipe_cad_core_product_extension_with_contract_boundaries`，只写 CAD Core product extension，不写 FreeCAD parity 或 full PartDesign Pipe coverage。
- `C6M1-BLK-201/202/301/401/501` 已关闭；`C6M1-BLK-203` 以稳定 `product_contract_required` 诊断边界关闭，`partdesign_pipe_interpolation_law_product_contract_required` 作为 precise remaining gap 保留。

## 目标

消费 S2-S5 留下的 product extension / backendGap / releaseGate，落地 C++、fixtures、focused tests、capability 和 docs。S6 不扩大范围。

## 实施顺序

1. 已保留现有 C51 Pipe regression，并新增 `cad-core/fixtures/c6m1`。
2. 已在 `feature_pipe.cpp` 中把 source-blocked diagnostics 改成合同驱动：未提供 C6 DTO 时仍保留 exact diagnostic，提供 DTO 且合法时进入 product extension。
3. 已在 `part/topo_shape_expansion` 中封装 `PipeScalingLaw` 与 `ContinuousEdgeLedger`，避免 executor 散落 OCCT `Law_*` 细节。
4. 已为 Linear / S-shape 增加最小完整语义批次 fixture；Interpolation 已按 S3 裁决为 `product_contract_required` 诊断边界。
5. 已为 SpineTangent / AuxiliarySpineTangent 增加连续边 ledger fixture、branch / disconnected / non-edge / closed-loop diagnostic 和 selected EdgeN regression。
6. 已更新 capability product-contract，并用 focused tests 保护 capability、fixtures 和 C51 regression。

## 下一轮代码落点

| blocker / scope | C++ 落点 | FreeCAD authority | tests / fixtures | 成功标准 |
| --- | --- | --- | --- | --- |
| `C6M1-BLK-201` / `C6M1-SCOPE-201` | `cad-core/src/part_design/feature_pipe.cpp`、`cad-core/include/cad_core/part/topo_shape_expansion.h` | `FeaturePipe.cpp::Pipe::execute()` commented `Law_Linear` | `cad-core/fixtures/c6m1/partdesign-pipe-linear-law-product.json`、`tests/test_p7_features.py` | 已关闭：Linear law request 通过，metadata 标 `cad_core_product_contract`。 |
| `C6M1-BLK-202` / `C6M1-SCOPE-202` | 同上 | commented `Law_S` | `partdesign-pipe-s-shape-law-product.json`、`partdesign-pipe-law-tangent-diagnostics.json` | 已关闭：S-shape law request 通过，非法 ScalingData 有 locatable diagnostic。 |
| `C6M1-BLK-203` / `C6M1-SCOPE-203` | `feature_pipe.cpp` | `TransformEnums` 只有 enum | `partdesign-pipe-interpolation-law-boundary.json` | 已关闭为 diagnostic boundary：保持 `product_contract_required`，不实现 LawSamples 或 fallback 几何。 |
| `C6M1-BLK-301` / `C6M1-SCOPE-301` | `feature_pipe.cpp`、`part/topo_shape_expansion.cpp` | `buildPipePath()` commented `getContinuousEdges()` | `partdesign-pipe-spine-tangent-ledger-product.json`、`partdesign-pipe-auxiliary-tangent-ledger-product.json`、`partdesign-pipe-law-tangent-diagnostics.json` | 已关闭：selected EdgeN 扩张输出 ordered ledger；branch / disconnected / non-edge / closed-loop 诊断稳定。 |
| `C6M1-BLK-401` / `C6M1-SCOPE-401` | `topo_shape_expansion.cpp`、`topo` | `makeElementShape(mkPS, ..., PipeShell)` / `MapperSewing` | C51 Pipe fixtures + C6 law/tangent fixtures | 已关闭：PipeShell maker history 和 front/back sewing 回归通过。 |
| `C6M1-BLK-501` / `C6M1-SCOPE-501` | `cad-core/src/runtime/capability_contract.cpp`、`tests/test_adapters.py` | C51X capability exact blockers | adapter capability test | 已关闭：capability 明确 product extension，非 full parity。 |

## 禁止路径

- 不按 fixture 名、bbox、volume、edge 数量或输出顺序推断 law / tangent 语义。
- 不在 adapter 层实现 Pipe law 或 continuous-edge expansion。
- 不保存跨请求 PipeShell、shape、BREP、NamedShape 或 ElementMap 状态。
- 不把 `Interpolation` 临时映射成 Linear / S-shape。
- 不删除 exact blocker，除非对应 C6 product fixture 和 capability assertion 同时落地。

## 验收分层

本轮短跑：

```bash
cd /Users/li/Chili3DProject/FreeCAD
git diff --check -- docs/CADCore6.0 cad-core
for f in docs/CADCore6.0/C6-M1-PartDesignPipeProductExtension-TransformationLawAndTangentExpansion主线/矩阵/*.tsv; do awk -F '\t' 'NR==1{n=NF} NF!=n{print FILENAME ":" NR ": expected " n " fields, got " NF}' "$f"; done
cd cad-core
cmake --build build
python3 -m unittest tests.test_p7_features.CadCoreP7FeatureTest tests.test_adapters.CadCoreAdapterTest.test_c_api_capabilities_exposes_web_contract_facts
```

阶段回归：

```bash
cd /Users/li/Chili3DProject/FreeCAD/cad-core
python3 -m unittest tests.test_p7_features tests.test_expected_fixtures tests.test_adapters
```

重型收口条件：修改 `topo_shape_expansion`、PipeShell history 或 ElementMap 时，补跑 `tests.test_p6_topology tests.test_p7_features tests.test_expected_fixtures tests.test_adapters`。

## 验收标准

- `C6M1-BLK-201/202/301/401/501` 关闭或带明确未关闭原因。
- `C6M1-BLK-203` 若没有产品合同，不得实现，只能保持 diagnostic。
- `cad-core/fixtures/c6m1` 存在并覆盖 law、tangent、diagnostic 和 C51 regression。
- capability 不再把已实现 C6 项列为 source-commented exact blocker，同时保留未实现项的 precise blocker。
- queue 输出只剩未执行步骤；实现完成后按步骤证据逐个改名为 `【已实现】`。

## 验收结果

- 短跑通过：`git diff --check -- docs/CADCore6.0 cad-core` 无输出；矩阵 TSV 字段数检查无输出；队列脚本输出为空；`cmake --build build` 通过。
- Focused 通过：`python3 -m unittest tests.test_p7_features.CadCoreP7FeatureTest tests.test_adapters.CadCoreAdapterTest.test_c_api_capabilities_exposes_web_contract_facts`，144 tests OK。
- 阶段回归已执行但未全绿：`python3 -m unittest tests.test_p7_features tests.test_expected_fixtures tests.test_adapters`，178 tests，15 failures，28 skipped。失败均在 `tests.test_expected_fixtures.CadCoreExpectedFixtureTest.test_expected_fixtures_match_recompute_results`，涉及既有 expected fixture 的 bbox、diagnostic_codes、external_geometry_count、link 类型和 assembly solver_adapter 字段漂移；本轮 C6M1 focused、P7 和 adapter capability 未出现 failure。

## 非目标

- 不要求 FreeCADCmd native expected。
- 不修改上游 FreeCAD。
- 不声明 GUI / full PartDesign Pipe / Part Workbench Sweep support。
