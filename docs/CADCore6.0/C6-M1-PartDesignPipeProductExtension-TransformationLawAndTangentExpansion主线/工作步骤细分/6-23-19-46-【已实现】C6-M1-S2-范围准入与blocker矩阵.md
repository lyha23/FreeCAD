# 【已实现】C6-M1 S2 范围准入与 blocker 矩阵

## 完成结论

- S2 已在 2026-06-23 live 复核完成。基线：`pwd=/Users/li/Chili3DProject/FreeCAD`，`HEAD=7a671628ee`，`git log -1 --oneline` 为 `7a671628ee 文档：完成 C6-M1 S1 源码候选矩阵`，S2 开始前 `git -c core.quotepath=false status --short -uall` 无输出。
- 已消费 S0/S1 证据和 C5.1 C51X blocker：`C51X-BLK-211`、`C51X-BLK-212` 仍是 `still_exact_blocker_source_commented`，未定义产品 DTO / continuous-edge ledger 前不实现 CAD Core extension。
- 已回写 `C6M1-SCOPE-201` 到 `C6M1-SCOPE-501`、`C6M1-BLK-201` 到 `C6M1-BLK-501`、`C6M1-CAT-201` 到 `C6M1-CAT-501`。
- S2 路由结果：`C6M1-SCOPE-101` 是 `productExtensionCandidate`；`C6M1-SCOPE-201/202/203/301/302` 是 `productContractRequired`；`C6M1-SCOPE-401/402/501` 是 `releaseGate`；`C6M1-SCOPE-900` 是 `nonGoal`；live exact blocker 基线 `C6M1-SCOPE-000` 是 `blocked-by-contract`。
- `C6M1-NG-001` 到 `C6M1-NG-009` 均有排除原因、用户/协议行为和 reopen condition；每个 blocker 均有 `close_condition`。
- 本步骤没有把 Linear / S-shape 提前升为 backendGap，没有把 Interpolation 偷换为 Linear / S-shape，没有把 continuous-edge expansion 简化成 compound 全边收集，也没有修改 C++、capability、fixture 或 source-backed diagnostics。

## 目标

把 S1 候选路由到可执行范围：product extension、productContractRequired、backendGap、releaseGate、nonGoal 或 blocked-by-contract。S2 不能凭源码存在就创建 backendGap。

## 分类规则

| 状态 | 准入条件 | 下一步 |
| --- | --- | --- |
| `baselineSupported` | C51 已支持且 C6 只回归保护 | S5/S6 regression gate |
| `productExtensionCandidate` | FreeCAD 属性 / 注释可支撑产品扩展，但无 parity claim | S3/S4 定义合同 |
| `productContractRequired` | 没有可执行 FreeCAD path 或字段含义不足 | S3/S4 先定产品合同 |
| `backendGap` | 产品合同清楚且 cad-core 当前未实现 | S6 C++ |
| `releaseGate` | 代码可能已落或待落，但发布需要 capability/docs/tests | S5/S6 |
| `nonGoal` | 本包排除 | non-goal registry |
| `blocked-by-contract` | 当前 source-backed diagnostic 必须保留，直到产品合同和发布闸门关闭 | S3-S6 |

## blocker 路由

| blocker | 路由 | 原因 |
| --- | --- | --- |
| Law DTO | productContractRequired -> backendGap | 需要 S3 定义 request 字段、默认值、诊断和 fixture 后才能实现。 |
| Linear / S-shape SetLaw | productContractRequired | FreeCAD 注释块只给出低层方向；DTO/合同未定前仍由 S3 决定，S2 不创建 backendGap。 |
| Interpolation | productContractRequired | 只有 enum，没有 FreeCAD law 分支；只能由 S3 定产品合同或保持 `product_contract_required` 诊断。 |
| continuous-edge ledger | productContractRequired | S4 必须定义 adjacency / continuity / ordering / source identity 账本，不能用 compound 全边收集。 |
| PipeShell history / capability | releaseGate | S5/S6 需保护已有 C51 PipeShell maker history、MapperSewing 和 capability 文案。 |

## 已回写矩阵行

- `c6m1_pipe_product_extension_scope_review_matrix.tsv`：`C6M1-SCOPE-201` 到 `C6M1-SCOPE-501` 已按合法状态路由。
- `c6m1_pipe_product_extension_blocker_queue.tsv`：`C6M1-BLK-201` 到 `C6M1-BLK-501` 均有 `close_condition`。
- `c6m1_pipe_product_extension_backend_gap_classification.tsv`：`C6M1-CAT-201` 到 `C6M1-CAT-501` 已补 `routing_status` 和分类原因。
- `c6m1_pipe_product_extension_non_goal_registry.tsv`：`C6M1-NG-001` 到 `C6M1-NG-009` 均有排除原因、用户/协议行为、reopen condition。

## 验收标准

```bash
cd /Users/li/Chili3DProject/FreeCAD
rg -n 'C6M1-SCOPE-20[1-3]|C6M1-BLK-20[1-3]|productContractRequired|backendGap|releaseGate' docs/CADCore6.0/C6-M1-PartDesignPipeProductExtension-TransformationLawAndTangentExpansion主线/矩阵
for f in docs/CADCore6.0/C6-M1-PartDesignPipeProductExtension-TransformationLawAndTangentExpansion主线/矩阵/*.tsv; do awk -F '\t' 'NR==1{n=NF} NF!=n{print FILENAME ":" NR ": expected " n " fields, got " NF}' "$f"; done
git diff --check -- docs/CADCore6.0
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore6.0/C6-M1-PartDesignPipeProductExtension-TransformationLawAndTangentExpansion主线/工作步骤细分 --format markdown
```

通过条件：每个 scope 都有合法状态；每个 blocker 都有 close condition；没有没有证据的 `backendGap`；S2 文件名和标题标记为 `【已实现】`，队列显示 S0-S2 已完成、S3-S6 待执行。

## 非目标

- 不把 `Interpolation` 偷换为 Linear 或 S-shape。
- 不把 continuous-edge expansion 简化成 compound 全边收集。
- 不修改 source-backed diagnostics。
- 不改 C++ / capability / fixture。
- 不执行 S3-S6。
