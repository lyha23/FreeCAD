# 【已实现】C6-M1 S0 声明口径与 live 基线复核

## 完成结论

- S0 已在 2026-06-23 live 复核完成。基线：`pwd=/Users/li/Chili3DProject/FreeCAD`，`HEAD=b5768acf40`，`git log -1 --oneline` 为 `b5768acf40 docs: 补充 C6-M1 Pipe 扩展方案包`，S0 开始前 `git -c core.quotepath=false status --short -uall` 无输出。
- C6-M1 口径冻结为 CAD Core product extension，不是 FreeCAD parity 修复；本包只处理 PartDesign Pipe `Transformation=Linear/S-shape/Interpolation` 与 `SpineTangent/AuxiliarySpineTangent` 的产品扩展边界。
- live exact blocker 仍来自 `cad-core/src/runtime/capability_contract.cpp`：`part_design.pipe` 状态是 `supported_c51s4_pipe_advanced_with_exact_source_blockers`，并列出 `partdesign_pipe_transformation_laws_source_commented` 与 `partdesign_pipe_spine_tangent_source_commented`。
- live executor 证据仍来自 `cad-core/src/part_design/feature_pipe.cpp::rejectSourceBlockedPipeBranches()`：`Transformation>=2`、`SpineTangent`、`AuxiliarySpineTangent` 当前返回 `unsupported_property` source-blocked diagnostics。
- FreeCAD source 只提供属性、enum 和注释代码依据：`FeaturePipe.cpp::Pipe::execute()` 中 `Law_Linear` / `Law_S` scaling law 块被注释，`Transformation=Interpolation` 没有可执行 law 分支；`buildPipePath()` 中 `getContinuousEdges(shape, subedge)` 调用被注释。
- 已回写矩阵行：`C6M1-SCOPE-000`、`C6M1-SCOPE-101`、`C6M1-BLK-000`、`C6M1-NG-001` 到 `C6M1-NG-005`。这些行只锁定产品扩展口径和禁止声明，不关闭 S1-S6 的实现 blocker。

## 目标

冻结 C6-M1 的声明口径：本包是 CAD Core product extension，不是 FreeCAD parity 修复；只处理 PartDesign Pipe `Transformation=Linear/S-shape/Interpolation` 与 `SpineTangent/AuxiliarySpineTangent`。

## 输入

- C51X follow-up：`docs/CADCore5.1-PartDesign-剩余deferred语义实现/README.md`
- C51X blocker：`docs/CADCore5.1-PartDesign-剩余deferred语义实现/矩阵/cadcore51_followup_blocker_queue.tsv`
- live capability：`cad-core/src/runtime/capability_contract.cpp`
- current executor：`cad-core/src/part_design/feature_pipe.cpp`
- FreeCAD source：`src/Mod/PartDesign/App/FeaturePipe.cpp`、`FeaturePipe.h`

## 声明口径

| 类型 | 口径 |
| --- | --- |
| 必须声明 | C6-M1 是产品扩展；FreeCAD source-commented 分支仍作为依据和边界。 |
| 必须声明 | 当前 cad-core source-backed diagnostics 是基线，不是实现完成证据。 |
| 禁止声明 | FreeCAD 支持 Linear / S-shape / Interpolation law parity。 |
| 禁止声明 | `SpineTangent` 能按 FreeCAD native path 自动扩张 tangent edges。 |
| 禁止声明 | 本包覆盖 GUI Pipe、Part Workbench Sweep wrapper、cross-request PipeShell lifecycle。 |

## 范围

| 项 | 纳入 | 初始状态 |
| --- | --- | --- |
| `Transformation=Linear` | 是 | productExtensionCandidate |
| `Transformation=S-shape` | 是 | productExtensionCandidate |
| `Transformation=Interpolation` | 是 | productContractRequired |
| `SpineTangent` | 是 | continuousEdgeLedgerRequired |
| `AuxiliarySpineTangent` | 是 | continuousEdgeLedgerRequired |
| PipeShell front/back sewing history | 是 | releaseGate |
| GUI task panel / persistent wrapper | 否 | nonGoal |

## 状态词典

| 状态 | 含义 |
| --- | --- |
| `baselineSupported` | C51 已支持，C6 只做回归保护。 |
| `productExtensionCandidate` | FreeCAD 有属性 / 注释依据，但实现只能按 CAD Core 产品合同推进。 |
| `productContractRequired` | 没有可执行 FreeCAD path，必须先定义产品 DTO 和验收语义。 |
| `backendGap` | 产品合同已明确，cad-core 当前代码未实现。 |
| `releaseGate` | 需要 capability、docs、fixtures、tests 同步后才可发布。 |
| `nonGoal` | 本包明确排除，必须写用户行为和 reopen condition。 |

## 必须回写的矩阵行

- `C6M1-SCOPE-000`：live baseline 已记录为 S0 完成。
- `C6M1-SCOPE-101`：product-extension claim 已锁定，仍保留 `productExtensionCandidate` 语义。
- `C6M1-BLK-000`：baseline gate 已关闭到文档证据，不改变 C++ blocker。
- `C6M1-NG-001` 到 `C6M1-NG-005`：禁止声明已锁定。

## 验收标准

```bash
cd /home/user/Chili3DProject/FreeCAD
rg -n 'partdesign_pipe_transformation_laws_source_commented|partdesign_pipe_spine_tangent_source_commented|supported_c51s4_pipe_advanced_with_exact_source_blockers' cad-core/src/runtime/capability_contract.cpp
rg -n 'Transformation=Linear|SpineTangent|productExtensionCandidate|productContractRequired|C6M1-SCOPE-101' docs/CADCore6.0/C6-M1-PartDesignPipeProductExtension-TransformationLawAndTangentExpansion主线
for f in docs/CADCore6.0/C6-M1-PartDesignPipeProductExtension-TransformationLawAndTangentExpansion主线/矩阵/*.tsv; do awk -F '\t' 'NR==1{n=NF} NF!=n{print FILENAME ":" NR ": expected " n " fields, got " NF}' "$f"; done
```

通过条件：声明口径、范围表、状态词典和 non-goal 行存在；无任何文件把本包写成 FreeCAD parity 或把 law / tangent blocker 写成已实现。

## 非目标

- 不改 C++。
- 不新增 fixture。
- 不删除 capability exact blocker。
