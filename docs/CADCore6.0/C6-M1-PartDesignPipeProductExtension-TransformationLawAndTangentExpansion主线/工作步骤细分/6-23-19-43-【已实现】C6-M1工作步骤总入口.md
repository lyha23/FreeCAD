# 【已实现】C6-M1 工作步骤总入口

## 目标

为 PartDesign Pipe product extension 建立可执行 S0-S6 队列。当前收口状态：S0 已完成 live 基线复核，S1 已完成 FreeCAD / cad-core source candidate matrix 复核，S2 已完成范围准入与 blocker 矩阵路由，S3 已完成 TransformationLawDTO 专项复审，S4 已完成 ContinuousEdgeLedger 专项复审，S5 已完成 PipeShellHistoryCapability 专项复审，S6 已完成 Oracle 实现与发布闸门。C6-M1 已发布为 CAD Core product extension，不声明 FreeCAD parity 或 full PartDesign Pipe coverage。

## 步骤索引

| 步骤 | 文件 | 当前状态 | 解决的问题 |
| --- | --- | --- | --- |
| S0 | `6-23-19-44-【已实现】C6-M1-S0-声明口径与live基线复核.md` | 已实现 | 冻结 C6 口径、live blocker、禁止声明。 |
| S1 | `6-23-19-45-【已实现】C6-M1-S1-FreeCAD源码候选矩阵.md` | 已实现 | 建 source authority，不把候选误升为支持。 |
| S2 | `6-23-19-46-【已实现】C6-M1-S2-范围准入与blocker矩阵.md` | 已实现 | 路由 productExtension / backendGap / nonGoal / releaseGate。 |
| S3 | `6-23-19-47-【已实现】C6-M1-S3-TransformationLawDTO专项复审.md` | 已实现 | 定义 Linear / S-shape / Interpolation law DTO 和实现边界。 |
| S4 | `6-23-19-48-【已实现】C6-M1-S4-ContinuousEdgeLedger专项复审.md` | 已实现 | 定义 continuous-edge expansion 账本，禁止 compound guessing。 |
| S5 | `6-23-19-49-【已实现】C6-M1-S5-PipeShellHistoryCapability专项复审.md` | 已实现 | 锁定 PipeShell history、front/back sewing 和 capability 文案。 |
| S6 | `6-23-19-50-【已实现】C6-M1-S6-Oracle实现与发布闸门.md` | 已实现 | 执行 C++、fixture、focused tests 和发布闸门。 |

## 执行顺序

1. S0 先确认这是 CAD Core product extension，不是 FreeCAD parity。
2. S1 只建源码候选和证据，不改 capability 或 C++。
3. S2 将候选转成可实施 blocker，并明确 non-goal / reopen condition。
4. S3-S5 分别审 DTO、连续边账本、PipeShell history / capability。
5. S6 只消费 S2-S5 留下的 productExtension / backendGap / releaseGate，不扩大范围。

## 当前矩阵闸门

| 矩阵 | 当前用途 | 当前结论 |
| --- | --- | --- |
| `c6m1_pipe_product_extension_source_candidates.tsv` | FreeCAD / cad-core 源码入口 | S1 已按 live 源码复核 `C6M1-SRC-001` 到 `C6M1-SRC-008`；候选仍不代表支持或发布能力。 |
| `c6m1_pipe_product_extension_scope_review_matrix.tsv` | 范围和状态词典 | S3 已将 `C6M1-SCOPE-201/202` 路由为 backendGap，`C6M1-SCOPE-203` 保持 `product_contract_required` 诊断边界；S4 已将 `C6M1-SCOPE-301/302` 路由为 continuous-edge ledger backendGap，history/capability 是 releaseGate。 |
| `c6m1_pipe_product_extension_blocker_queue.tsv` | 待执行队列 | S6 已关闭 `C6M1-BLK-201/202/301/401/501`；`C6M1-BLK-203` 以 `product_contract_required` 诊断边界关闭，Interpolation 几何仍保留 precise remaining gap。 |
| `c6m1_pipe_product_extension_non_goal_registry.tsv` | 禁止声明 | S2 已锁定 `C6M1-NG-001` 到 `C6M1-NG-009` 的排除原因、用户/协议行为和 reopen condition。 |
| `c6m1_pipe_product_extension_backend_gap_classification.tsv` | 产品扩展分类 | S3 已补 law routing：Linear/S-shape 是 S6 backendGap，Interpolation 是 `product_contract_required` 诊断边界；S4 已补 tangent ledger backendGap，S5 已锁定 PipeShell history/capability releaseGate。 |
| `c6m1_pipe_product_extension_oracle_fixture_matrix.tsv` | fixture / test oracle | S6 已新增 `cad-core/fixtures/c6m1`，覆盖 Linear/S-shape law、Interpolation boundary、spine/auxiliary tangent ledger 与 diagnostics。 |
| `c6m1_pipe_product_extension_input_contract_matrix.tsv` | request / response 合同 | S3 已定稿 law DTO 行，S4 已定稿 tangent ledger 行。 |

## 状态纪律

- 不把 FreeCAD 注释代码声明为 FreeCAD supported。
- 不从 fixture bbox、volume、shape count 或输出顺序倒推 Pipe law / tangent 业务逻辑。
- `Transformation=Interpolation` 没有 FreeCAD 可执行分支；若实施，必须有产品合同。
- `SpineTangent` / `AuxiliarySpineTangent` 必须先有 continuous-edge ledger，不能在输出端补边。
- 所有实现必须保持 request-local，无 backend PipeShell session。

## 通用验收

```bash
cd /Users/li/Chili3DProject/FreeCAD
git diff --check -- docs/CADCore6.0
for f in docs/CADCore6.0/C6-M1-PartDesignPipeProductExtension-TransformationLawAndTangentExpansion主线/矩阵/*.tsv; do awk -F '\t' 'NR==1{n=NF} NF!=n{print FILENAME ":" NR ": expected " n " fields, got " NF}' "$f"; done
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore6.0/C6-M1-PartDesignPipeProductExtension-TransformationLawAndTangentExpansion主线/工作步骤细分 --format markdown
```

## 非目标

- GUI Pipe task panel、ViewProvider、交互拖拽和动画。
- 上游 FreeCAD 源码修复或让 cad-core 冒充 FreeCAD parity。
- persistent `BRepOffsetAPI_MakePipeShell` wrapper / backend solver session。
- Part Workbench `Part::Sweep` wrapper located profile blocker。
