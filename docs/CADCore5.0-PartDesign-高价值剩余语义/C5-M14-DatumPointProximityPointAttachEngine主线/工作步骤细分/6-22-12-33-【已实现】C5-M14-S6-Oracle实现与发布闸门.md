# 【已实现】C5-M14-S6 Oracle 实现与发布闸门

状态：`done_C5-M14_S6_release_gate`

## 目标

消费 C5-M14 的 `pending_backendGap` 和 `pending_oracle` 行，实际落 C++、fixtures、FreeCAD expected、focused tests、capability/docs，并在验收通过后关闭 `ProximityPoint1/2` exact blocker。本步骤已完成：`C5M14-BLK-201/202/203/301/501` 已由 S6 实现、oracle 和发布闸门关闭。

## 本轮代码实现目标

| ID | 类型 | 文件 | 成功条件 |
| --- | --- | --- | --- |
| `C5M14-BLK-201` | C++ | `cad-core/src/part_design/datum_attachment.h` | edge-face intersection 代表 fixture 与 FreeCAD expected 一致 |
| `C5M14-BLK-202` | C++ | `cad-core/src/part_design/datum_attachment.h` | fallback p1/p2 与 FreeCAD expected 一致 |
| `C5M14-BLK-203` | tests / diagnostics | `cad-core/tests/test_p7_features.py`、fixtures diagnostics | 少 support / bad subname / distance failure 有稳定 diagnostics |
| `C5M14-BLK-301` | request-local boundary | `datum_attachment.h`、tests | 多 support 解析不引入 session，writeback 只在 response 中出现 |
| `C5M14-BLK-501` | capability/docs | `cad-core/src/adapters/c_api/c_api.cpp`、C5/C51 docs | blocker 移除 `ProximityPoint1/2`，其它 remaining modes 保持 |

## 代码落点顺序

1. 在 `datum_attachment.h` 加 OCCT includes 和 proximity helper。
2. 抽出 `proximityPointPlacement()`，输入 `std::vector<SupportResolution>`、mode、context/object。
3. 扩展 support family 判定，让 `ProximityPoint1/2` 消费两个 support links。
4. 在 `selectedDatumPlacement()` 中只对 `DatumAttachmentEngine::Point` 支持两个 mode。
5. 新增 fixtures：
   - `cad-core/fixtures/c51m5/partdesign-datum-point-proximity-modes.json`
   - `cad-core/fixtures/c51m5/partdesign-datum-point-proximity-diagnostics.json`
   - `cad-core/fixtures/c51m5/expected/partdesign-datum-point-proximity-modes.freecad.json`
   - `cad-core/fixtures/c51m5/expected/partdesign-datum-point-proximity-diagnostics.freecad.json`
6. 新增 focused tests：
   - `test_c51x_datum_point_proximity_modes_match_expected`
   - `test_c51x_datum_point_proximity_invalid_diagnostics`
   - capability blocker removal assertions。
7. 同步 C5-M14 / C51X docs 和 matrix 状态。

## 禁止捷径

- 不按 fixture 名称、对象名、几何类型组合写特判。
- 不通过输出端排序、bbox 修剪、坐标四舍五入来凑 expected。
- 不在 adapter 层实现 proximity point 业务逻辑。
- 不用 cad-core result 生成 expected。
- 不修改 C5-M13 surface blocker 或 Part Workbench helper。

## 验收标准

本轮短跑：

```bash
cd /home/user/Chili3DProject/FreeCAD
git diff --check -- docs/CADCore5.0-PartDesign-高价值剩余语义/C5-M14-DatumPointProximityPointAttachEngine主线 docs/CADCore5.1-PartDesign-剩余deferred语义实现 cad-core/src/part_design/datum_attachment.h cad-core/tests cad-core/fixtures/c51m5 cad-core/src/adapters/c_api/c_api.cpp
```

focused 实现验证：

```bash
cd /home/user/Chili3DProject/FreeCAD/cad-core
cmake --build build
FREECADCMD=/home/user/.local/bin/freecadcmd python3 tools/collect_freecad_expected.py fixtures/c51m5/partdesign-datum-point-proximity-modes.json --check
python3 -m unittest tests.test_p7_features.CadCoreP7FeatureTest.test_c51x_datum_point_proximity_modes_match_expected tests.test_p7_features.CadCoreP7FeatureTest.test_c51x_datum_point_proximity_invalid_diagnostics
python3 -m unittest tests.test_adapters.CadCoreAdapterTest.test_c_api_capabilities_exposes_web_contract_facts
```

阶段回归条件：

```bash
cd /home/user/Chili3DProject/FreeCAD/cad-core
python3 -m unittest tests.test_p7_features tests.test_expected_fixtures tests.test_adapters
```

只有在 S6 修改 shared helper 或 capability broader contract 时才需要阶段回归；docs-only S0-S2 不跑 cad-core build。

## 发布条件

- `ProximityPoint1` / `ProximityPoint2` 从 `datum_attach_engine_remaining_modes` 移除。
- `IntersectionPoint`、Focus、curve frame、three-point/folding 等 mode 仍保留 exact blocker 或 non-goal。
- fixtures expected 来自 FreeCADCmd。
- C5-M14 矩阵状态从 pending 更新为 done 或保留精确 blocker，并记录 delete condition。
- queue runner 不再显示 S6 之前的已完成步骤为 pending。

## 非目标

- 不把 C5-M14 写成完整 Datum AttachEngine freeze。
- 不引入 GUI / session / persistent shape lifecycle。
- 不重开 C5-M13 Part Workbench surface narrowed blockers。
