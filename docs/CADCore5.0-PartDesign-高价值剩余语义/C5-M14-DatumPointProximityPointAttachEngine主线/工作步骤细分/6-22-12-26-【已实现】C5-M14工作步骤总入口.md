# 【已实现】C5-M14 工作步骤总入口

## 目标

为 C5-M14 DatumPoint `ProximityPoint1/2` 建立可由 goal runner 顺序执行的 S0-S6 队列。本文只是索引文件，创建后标记 `【已实现】`；当前 S0-S4 已完成复审/准备项，S5-S6 仍待执行，不代表 oracle、代码或发布已经完成。

## 步骤索引

| 步骤 | 文件 | 当前状态 | 解决的问题 |
| --- | --- | --- | --- |
| S0 | `6-22-12-27-【已实现】C5-M14-S0-liveAttachEngineBlocker冻结.md` | 已实现 | 冻结 live blocker、禁止声明和状态字典 |
| S1 | `6-22-12-28-【已实现】C5-M14-S1-FreeCADProximityPoint源码候选矩阵.md` | 已实现 | 记录 FreeCAD proximity source authority |
| S2 | `6-22-12-29-【已实现】C5-M14-S2-scope准入与待实现矩阵.md` | 已实现 | 路由 scope、backendGap、blocker、nonGoal |
| S3 | `6-22-12-30-【已实现】C5-M14-S3-edgeFaceIntersection专项复审.md` | 已实现 | 复审 edge-face intersection 优先路径，S6 才落代码 |
| S4 | `6-22-12-31-【已实现】C5-M14-S4-distanceFallbackAndDiagnostics专项复审.md` | 已实现 | 复审 distance fallback、p1/p2 分流与 diagnostics 边界，S6 才落代码 |
| S5 | `6-22-12-32-C5-M14-S5-requestLocalWriteback专项复审.md` | 待执行 | 复核 request-local support/writeback/capability 边界 |
| S6 | `6-22-12-33-C5-M14-S6-Oracle实现与发布闸门.md` | 待执行 | 落代码、fixtures、tests、capability/docs |

## 执行顺序

1. S0 必须先跑，确认当前 capability 和 docs 没有漂移。
2. S1 只读 FreeCAD source，不改代码。
3. S2 只改矩阵状态，不把未采 oracle 的内容标 supported。
4. S3 已明确 edge-face 交点优先路径；S4 已明确 no-hit 后进入同一 `BRepExtrema_DistShapeShape` fallback、`PointOnShape1/2` 分流和 diagnostics；S6 才写 C++。
5. S5 检查无状态 CAD Core 边界。
6. S6 才允许落 C++、fixtures、expected、tests 和 capability 发布。

## 当前矩阵闸门

| 矩阵 | 当前用途 | 当前结论 |
| --- | --- | --- |
| `c5m14_datum_point_proximity_source_candidates.tsv` | FreeCAD source 候选 | S1 已复核，S2 已补 scope 路由 |
| `c5m14_datum_point_proximity_scope_review_matrix.tsv` | scope / status | S4 已复审 `C5M14-SCOPE-202/203`；S5-S6 待执行 |
| `c5m14_datum_point_proximity_blocker_queue.tsv` | 待实现队列 | S4 已复审 `BLK-202/203`，仍待 S6 实现关闭；`BLK-501` 待发布步骤 |
| `c5m14_datum_point_proximity_backend_gap_classification.tsv` | backendGap 分类 | S2 已拆为 `CAT-001..005` 和 excluded `CAT-900` |
| `c5m14_datum_point_proximity_fixture_oracle_matrix.tsv` | oracle / fixture | seed，native expected 未采集前不得标 done |
| `c5m14_datum_point_proximity_non_goal_registry.tsv` | 非目标 | seed，S0/S2 复核 |
| `c5m14_datum_point_proximity_validation_matrix.tsv` | 验收命令 | seed，S6 发布前分层执行 |

## 状态纪律

- S0-S6 默认 `待执行`，不得因为方案文件已创建而标 `【已实现】`。
- `ProximityPoint1/2` 只有在 FreeCAD expected、cad-core implementation、focused tests 和 capability blocker 移除同时完成后才可标 supported。
- `IntersectionPoint` 只能保持 source-unclear non-goal，不能在 C5-M14 中顺带实现。
- `backendGap` 必须同时有 FreeCAD authority 和当前 cad-core mismatch 证据。
- expected 必须来自 FreeCADCmd / FreeCAD source，不能来自 cad-core 输出。

## 通用验收

```bash
cd /home/user/Chili3DProject/FreeCAD
git diff --check -- docs/CADCore5.0-PartDesign-高价值剩余语义/C5-M14-DatumPointProximityPointAttachEngine主线 docs/CADCore5.0-PartDesign-高价值剩余语义/README.md
for f in docs/CADCore5.0-PartDesign-高价值剩余语义/C5-M14-DatumPointProximityPointAttachEngine主线/矩阵/*.tsv; do awk -F '\t' 'NR==1{n=NF} NF!=n{print FILENAME ":" NR ": expected " n " fields, got " NF}' "$f"; done
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore5.0-PartDesign-高价值剩余语义/C5-M14-DatumPointProximityPointAttachEngine主线/工作步骤细分 --format markdown
```

## 非目标

- 不修改 C5-M13 Part Workbench surface package。
- 不跑全量 FreeCAD build。
- 不把 GUI / ViewProvider / TaskPanel 纳入 cad-core。
- 不实现 C5-M14 之外的 AttachEngine remaining modes。
