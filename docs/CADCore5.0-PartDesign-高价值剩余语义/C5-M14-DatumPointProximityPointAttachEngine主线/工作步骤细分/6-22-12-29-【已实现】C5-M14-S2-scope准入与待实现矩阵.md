# 【已实现】C5-M14-S2 scope 准入与待实现矩阵

状态：`s2_scope_backend_gap_routed`

## 目标

把 S1 的 source candidates 路由到 scope、blocker、backendGap、nonGoal 和 oracle fixture 行。S2 的结果必须让 S6 可以直接落代码，而不是停留在 broad AttachEngine 描述。

## 分类规则

| 分类 | 准入条件 | C5-M14 处理 |
| --- | --- | --- |
| `pending_backendGap` | FreeCAD 有明确行为，cad-core 当前未实现 | `ProximityPoint1/2` success 与 diagnostics |
| `pending_oracle` | 需要 FreeCADCmd expected 才能发布 | success fixtures 与 diagnostics fixtures |
| `nonGoal_source_unclear` | FreeCAD route 未确认或不属于本批 | `IntersectionPoint` |
| `out_of_scope` | 属于其它 package | C5-M13 surface、GUI、Focus/Frenet/three-point 等 |

## 待实现矩阵准入

`blocker_queue.tsv` 中至少保留以下待实现项：

- `C5M14-BLK-201`：edge-face intersection 优先路径。
- `C5M14-BLK-202`：distance fallback 与 `ProximityPoint1/2` 分别返回 p1/p2。
- `C5M14-BLK-203`：少 support / unresolved support diagnostics。
- `C5M14-BLK-501`：capability/docs closeout。

## 必须回写的矩阵行

- `C5M14-SCOPE-201`、`C5M14-SCOPE-202`、`C5M14-SCOPE-203`、`C5M14-SCOPE-501`
- `C5M14-BLK-201`、`C5M14-BLK-202`、`C5M14-BLK-203`、`C5M14-BLK-501`
- `C5M14-CAT-001` 到 `C5M14-CAT-005`

## S2 结果

- `C5M14-SCOPE-201` / `C5M14-BLK-201`：已路由为 edge-face intersection priority，进入 S3/S6。
- `C5M14-SCOPE-202` / `C5M14-BLK-202`：已路由为 distance fallback 与 `ProximityPoint1/2` p1/p2 split，进入 S4/S6。
- `C5M14-SCOPE-203` / `C5M14-BLK-203`：已路由为 missing / unresolved / null support diagnostics，进入 S4/S6。
- `C5M14-SCOPE-501` / `C5M14-BLK-501`：已路由为 capability/docs release gate，进入 S6。
- `C5M14-BLK-101` 已标为 `source_authority_done`，只代表 S1 source authority 完成，不代表 supported。
- `C5M14-CAT-001..005` 已拆分为 edge-face、distance fallback、diagnostics、request-local boundary 和 release gate；`C5M14-CAT-900` 继续保留 excluded remaining AttachEngine families，`product_decision_needed=yes`。
- oracle 行仍保持 `pending_native_oracle` / `pending_diagnostic`；本步骤未采集 FreeCADCmd expected。
- `IntersectionPoint` 只保留在 non-goal / excluded scope，不升级为 backendGap 或 supported。

## 验收标准

```bash
cd /home/user/Chili3DProject/FreeCAD
for f in docs/CADCore5.0-PartDesign-高价值剩余语义/C5-M14-DatumPointProximityPointAttachEngine主线/矩阵/*.tsv; do awk -F '\t' 'NR==1{n=NF} NF!=n{print FILENAME ":" NR ": expected " n " fields, got " NF}' "$f"; done
rg -n 'C5M14-BLK-201|C5M14-BLK-202|C5M14-BLK-203|pending_backendGap|nonGoal_source_unclear' docs/CADCore5.0-PartDesign-高价值剩余语义/C5-M14-DatumPointProximityPointAttachEngine主线/矩阵
```

通过条件：

- 每个 scope row 都能路由到 blocker、oracle、non-goal 或 closeout。
- 每个 backendGap 都有 FreeCAD authority 和当前 cad-core blocker evidence。
- `IntersectionPoint` 只出现在 non-goal / excluded scope 中。

## 非目标

- 不把 notCollected 直接转实现。
- 不把 unsupported 长期留在文档里；若 FreeCAD authority 足够，S6 必须生成代码落点。
