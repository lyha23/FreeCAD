# 【已实现】C5-M20-S4 Params 专项复审

状态：`done_params_reprobe`

## 目标

复跑 C5-M13 后仍 blocked 的 constructor params：`PtsOnCurve`、`Anisotropy`、`TolG1+TolG2`、`MaxSegments` 和 all explicit params。

## 结论

| case | 结论 |
| --- | --- |
| `pts_on_curve_only` | SIGSEGV，栈含 `PyObject_IsTrue` / `Part.so` |
| `anisotropy_only` | timeout 60s |
| `tol_g1_g2_only` | SIGSEGV，栈含 `GeomPlate_MakeApprox` / `BRepFill_Filling::Build` |
| `max_segments_only` | SIGSEGV，栈在 `Wrapped_ParseTupleAndKeywords` / `Part.so` |
| `all_params` | SIGSEGV，栈在 `Wrapped_ParseTupleAndKeywords` / `Part.so` |

## 必须回写的矩阵行

- `C5M20-BLK-103`
- `C5M20-BLK-104`
- `C5M20-ORC-301`

## 验收

- c5m13 已 supported 的四个单字段 expected 不被降级。
- 剩余 params 不新增 expected，不新增 collector supported path。
- `delete_condition` 要求每个 crash/timeout 子字段稳定后才删除 all-params blocker。

## 非目标

- 不放宽 expected 断言。
- 不用 cad-core 当前输出替代 FreeCADCmd expected。
