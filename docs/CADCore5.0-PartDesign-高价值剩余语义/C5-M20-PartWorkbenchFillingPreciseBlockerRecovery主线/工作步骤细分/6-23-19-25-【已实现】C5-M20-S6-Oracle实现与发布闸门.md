# 【已实现】C5-M20-S6 Oracle 实现与发布闸门

状态：`done_no_expected_release_gate`

## 发布结果

- 无新增 `cad-core/fixtures/c5m20`。
- 无新增 FreeCAD expected。
- 无 `cad-core/src/part/part_filling.cpp` 代码变更。
- 无 collector supported route 变更。
- C5-M20 只发布更精确的 source-audited blocker evidence 和 delete condition。

## 发布闸门

| gate | 标准 | 结论 |
| --- | --- | --- |
| helper oracle | `Part.makeFilledFace(...)` representative 稳定返回 geometry expected | 未通过；保持 blocker |
| direct wrapper | low-level builder control 可解释但不替代 helper | 通过 |
| no false support | 不新增 supported fixture / expected | 通过 |
| request-local | 不引入 persistent wrapper lifecycle | 通过 |
| docs/root matrix | package 和 C5 root matrices 同步 | 通过 |

## 下一轮代码落点

当前没有可落 C++ 的 helper-backed backendGap。只有在以下任一条件满足后，才允许打开下一轮实现：

- `surface=`、support/order G1/G2 或 non-boundary support/order 在 FreeCADCmd `Part.makeFilledFace(...)` 下稳定返回 shape summary。
- 剩余 constructor params 子集不再 SIGSEGV / timeout，并且 all-params 可被稳定采集。
- 上游 FreeCAD 修复 helper kwargs parse/runtime 后，可以用相同 collector 生成 expected。

禁止路径：按 fixture 名分支、cad-core 输出倒推 expected、bbox/edge 数匹配伪造 oracle、adapter 层业务逻辑、跨请求保存 wrapper / BREP 状态。

## 必跑验收

```bash
cd /home/user/Chili3DProject/FreeCAD
git diff --check -- docs/CADCore5.0-PartDesign-高价值剩余语义/C5-M20-PartWorkbenchFillingPreciseBlockerRecovery主线 docs/CADCore5.0-PartDesign-高价值剩余语义/README.md docs/CADCore5.0-PartDesign-高价值剩余语义/矩阵
for f in docs/CADCore5.0-PartDesign-高价值剩余语义/C5-M20-PartWorkbenchFillingPreciseBlockerRecovery主线/矩阵/*.tsv docs/CADCore5.0-PartDesign-高价值剩余语义/矩阵/*.tsv; do awk -F '\t' 'NR==1{n=NF} NF!=n{print FILENAME ":" NR ": expected " n " fields, got " NF}' "$f"; done
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore5.0-PartDesign-高价值剩余语义/C5-M20-PartWorkbenchFillingPreciseBlockerRecovery主线 --format markdown
cd /home/user/Chili3DProject/FreeCAD/cad-core
python3 -m unittest tests.test_p8_features.CadCoreP8FeatureTest.test_c5m13_part_filling_param_subsets_are_expected_backed tests.test_p8_features.CadCoreP8FeatureTest.test_c5m8_part_filling_non_default_params_are_constructor_batch tests.test_p8_features.CadCoreP8FeatureTest.test_c5m8_part_filling_support_order_sources_are_source_backed_known_gap
```

## 非目标

- 不跑全量 FreeCAD CI。
- 不提交 Part surface 新代码。
- 不把 direct wrapper control 当成 request-local helper support。
