# 【已实现】C5-M20-S1 FreeCAD 源码候选矩阵

状态：`done_source_candidates`

## 目标

把 C5-M20 的 FreeCAD 依据限定在 `Part.makeFilledFace()`、`TopoShape::makeElementFilledFace()` 和 direct `BRepOffsetAPI_MakeFilling` wrapper；不引入 Surface Workbench GUI/native feature 或完整 Part surface family。

## FreeCAD 依据

| 语义 | 源码 | 结论 |
| --- | --- | --- |
| helper kwargs | `/home/user/Chili3DProject/FreeCAD/src/Mod/Part/App/AppPartPy.cpp::makeFilledFace()` | request-local helper 入口，C5-M20 only supported oracle 来源 |
| builder 主路径 | `/home/user/Chili3DProject/FreeCAD/src/Mod/Part/App/TopoShapeExpansion.cpp::TopoShape::makeElementFilledFace()` | `BRepOffsetAPI_MakeFilling` 低层语义依据 |
| direct wrapper | `/home/user/Chili3DProject/FreeCAD/src/Mod/Part/App/BRepOffsetAPI_MakeFillingPyImp.cpp` | native control / lifecycle boundary；不发布 persistent wrapper |

## 必须回写的矩阵行

- `C5M20-SRC-001~004`
- `C5M20-SCOPE-001~004`
- `C5M20-NG-001~003`

## 验收

- `c5m20_filling_precise_blocker_source_candidates.tsv` 包含 helper、topo expansion、direct wrapper、cad-core current landing 四类 source。
- source rows 不把 C5-M14~M19、GUI、full surface family 作为候选 owner。

## 非目标

- 不用 fixture 输出倒推 FreeCAD 业务逻辑。
- 不把 direct wrapper lifecycle 作为 cad-core API。
