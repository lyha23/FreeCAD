# 【已实现】C5-M19-S6 发布收口

状态：`done_source_audited_capability_closed`

## 发布结果

- `TangentU`、`TangentV`、`IntersectionPoint` 均经 FreeCAD `Attacher.cpp` / `Attacher.h` 源码审计确认为不可选模式。
- `IntersectionLine` 已支持且仍在 capability supported 列表中。
- capability exact blocker 已删除 `datum_attach_engine_remaining_modes`。
- non-goals 增加 source-audited enum-only 三项。
- C5-M19 不新增 DTO、fixtures、expected、focused placement tests 或 cad-core placement helper。

## 发布闸门

| gate | 标准 | 结论 |
| --- | --- | --- |
| source-backed | enum/name 与 `modeRefTypes` / switch 证据齐全 | 通过 |
| no false support | 不把三项放入 supported | 通过 |
| no false blocker | 不把不可选模式继续放在 exact blocker | 通过 |
| IntersectionLine guard | supported claim 保留 | 通过 |
| request-local | 不新增 session / full BREP | 通过 |
| docs/root matrix | C5-M19 包和根矩阵已同步 | 通过 |

## 必跑验收

```bash
cd /home/user/Chili3DProject/FreeCAD/cad-core
python3 -m unittest tests.test_adapters.CadCoreAdapterTest.test_c_api_capabilities_exposes_web_contract_facts
cd /home/user/Chili3DProject/FreeCAD
git diff --check -- cad-core docs/CADCore5.0-PartDesign-高价值剩余语义/C5-M19-DatumAttachEngineSourceAuditCloseout主线 docs/CADCore5.0-PartDesign-高价值剩余语义/README.md docs/CADCore5.0-PartDesign-高价值剩余语义/矩阵
for f in docs/CADCore5.0-PartDesign-高价值剩余语义/C5-M19-DatumAttachEngineSourceAuditCloseout主线/矩阵/*.tsv docs/CADCore5.0-PartDesign-高价值剩余语义/矩阵/*.tsv; do awk -F '\t' 'NR==1{n=NF} NF!=n{print FILENAME ":" NR ": expected " n " fields, got " NF}' "$f"; done
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore5.0-PartDesign-高价值剩余语义/C5-M19-DatumAttachEngineSourceAuditCloseout主线 --format markdown
```

## 后续路线

C5 Datum AttachEngine 线已收口。后续优先转向 Part surface precise blockers，例如 Filling / GeomPlate / Sweep 的 FreeCADCmd/native blocker。

## 非目标

- 不提交 Part surface 代码。
- 不重跑全量 FreeCAD CI。
