# C5-M17-S6 Oracle 实现与发布闸门

## 目标

消费 C5-M17 的 conic landmark `pending_native_oracle`、`pending_backendGap` 和 `releaseGate` 行，完成 FreeCADCmd expected、cad-core C++、fixtures、focused tests、capability/docs 和验收记录闭环。

## 队列消费规则

- 只消费 conic landmark blockers：`C5M17-BLK-101`、`C5M17-BLK-102`、`C5M17-BLK-501`。
- `Folding`、`IntersectionPoint`、`TangentU/V` 不进入本轮实现。
- 若某个 conic mode 无法稳定采集 oracle，必须拆 precise blocker，不得把整个 conic family 写成 supported。
- 不创建 cad-core-output-derived expected。

## 本轮代码实现目标

| blocker | C++ / fixture 落点 | FreeCAD authority | focused tests | 成功标准 |
| --- | --- | --- | --- | --- |
| `C5M17-BLK-101` | `datum_attachment.h`、`datum_line.cpp`、conic modes fixture | `Attacher.cpp:2413-2419,2613-2702` | conic modes parity | Directrix1/2、Asymptote1/2 placement 与 FreeCAD expected 一致 |
| `C5M17-BLK-102` | `datum_attachment.h`、`datum_point.cpp`、conic modes fixture | `Attacher.cpp:2842-2845,2937-2990` | conic modes parity | Focus1/2 placement 与 FreeCAD expected 一致 |
| `C5M17-BLK-501` | `c_api.cpp`、C5/C51 docs、root/package matrices | capability exact blocker source | adapter capability test | exact blocker 只移除本包 proven conic modes |

## Oracle batch

| fixture | 场景 | expected 字段 |
| --- | --- | --- |
| `fixtures/c51m5/partdesign-datum-conic-landmark-modes.json` | ellipse/hyperbola/parabola directrix/focus；hyperbola asymptote | placement base/rotation, line direction, point, map_mode |
| `fixtures/c51m5/partdesign-datum-conic-landmark-diagnostics.json` | missing support, wrong shape, non-conic edge, parabola second focus/directrix, unsupported asymptote support | diagnostic codes, object/property/subname context |

## 验收分层

本轮短跑：

```bash
cd /Users/li/Chili3DProject/FreeCAD
git diff --check -- docs/CADCore5.0-PartDesign-高价值剩余语义/C5-M17-DatumRemainingAttachEngine主线 docs/CADCore5.0-PartDesign-高价值剩余语义/README.md docs/CADCore5.0-PartDesign-高价值剩余语义/矩阵 cad-core
```

Focused oracle / tests：

```bash
cd /Users/li/Chili3DProject/FreeCAD/cad-core
FREECADCMD=/Users/li/.cargo/bin/freecadcmd python3 tools/collect_freecad_expected.py fixtures/c51m5/partdesign-datum-conic-landmark-modes.json --check
FREECADCMD=/Users/li/.cargo/bin/freecadcmd python3 tools/collect_freecad_expected.py fixtures/c51m5/partdesign-datum-conic-landmark-diagnostics.json --check
python3 -m unittest tests.test_p7_features.CadCoreP7FeatureTest.test_c51x_datum_conic_landmark_modes_match_expected tests.test_p7_features.CadCoreP7FeatureTest.test_c51x_datum_conic_landmark_invalid_diagnostics
python3 -m unittest tests.test_expected_fixtures tests.test_adapters.CadCoreAdapterTest.test_c_api_capabilities_exposes_web_contract_facts
```

阶段收口：

```bash
cd /Users/li/Chili3DProject/FreeCAD/cad-core
cmake --build build
python3 -m unittest tests.test_p7_features tests.test_expected_fixtures tests.test_adapters
```

## 发布闸门

- `c_api.cpp` exact blocker 只能删除 FreeCADCmd expected-backed 且 focused tests / adapter capability test 已通过的 proven conic modes：`Directrix1/2`、`Asymptote1/2`、`Focus1/2`。
- `Folding`、`IntersectionPoint`、`TangentU/V` 仍在 exact blocker 或 non-goal。
- `fixtures/c51m5/expected` 中存在 FreeCADCmd-backed expected，不是 cad-core output。
- focused tests 和 adapter capability test 通过。
- C5 README、global matrices、package matrices、C51X docs 的状态一致。
- 后端仍只消费本次 request graph 的 support shape/subname/MapMode/AttachmentOffset/MapReversed；response 只返回 placement、diagnostics、`documentObjectUpdates` / `elementReferenceUpdates` suggestions；不引入 backend attachment session 或 complete BREP 状态，`ReferenceShadow.brep` 只保留单 referenced subshape snapshot 例外。
- goal queue 不再显示 C5-M17 待执行步骤后，才允许把 S6 和主线入口改名为 `【已实现】`。

## 非目标

- 不实现 `Folding`、`IntersectionPoint`、`TangentU/V`。
- 不引入跨请求 backend session 或完整 BREP 状态。
