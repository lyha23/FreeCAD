# 【已实现】C5-M17 工作步骤总入口

## 目标

建立 C5-M17 DatumRemainingAttachEngine 的 S0-S6 执行队列。本文是索引文件，创建后标 `【已实现】`，避免 goal runner 把索引当作独立实现步骤。

## 步骤索引

| 步骤 | 文件 | 当前状态 | 解决的问题 |
| --- | --- | --- | --- |
| S0 | [6-23-03-28-【已实现】C5-M17-S0-liveRemainingAttachEngineBlocker冻结.md](6-23-03-28-【已实现】C5-M17-S0-liveRemainingAttachEngineBlocker冻结.md) | 已实现 | 冻结 current exact blocker 与禁止声明 |
| S1 | [6-23-03-29-【已实现】C5-M17-S1-FreeCADRemainingAttachEngine源码候选矩阵.md](6-23-03-29-【已实现】C5-M17-S1-FreeCADRemainingAttachEngine源码候选矩阵.md) | 已实现 | FreeCAD source authority 与 source candidates |
| S2 | [6-23-03-30-【已实现】C5-M17-S2-scope准入与待实现矩阵.md](6-23-03-30-【已实现】C5-M17-S2-scope准入与待实现矩阵.md) | 已实现 | scope / backendGap / nonGoal / later package 路由 |
| S3 | [6-23-03-31-【已实现】C5-M17-S3-ConicLandmark合同复审.md](6-23-03-31-【已实现】C5-M17-S3-ConicLandmark合同复审.md) | 已实现 | conic landmark placement 和 diagnostics 合同已冻结 |
| S4 | [6-23-03-32-【已实现】C5-M17-S4-ExcludedFamilies拆包复审.md](6-23-03-32-【已实现】C5-M17-S4-ExcludedFamilies拆包复审.md) | 已实现 | Folding / IntersectionPoint / TangentU/V 拆包证据已冻结 |
| S5 | [6-23-03-33-【已实现】C5-M17-S5-requestLocalPlacementAndCapability专项复审.md](6-23-03-33-【已实现】C5-M17-S5-requestLocalPlacementAndCapability专项复审.md) | 已实现 | request-local placement、writeback、capability release gate 已冻结 |
| S6 | [6-23-03-34-C5-M17-S6-Oracle实现与发布闸门.md](6-23-03-34-C5-M17-S6-Oracle实现与发布闸门.md) | 待执行 | oracle、C++、fixtures、focused tests、release gate |

## 执行顺序

1. S0 先确认 `datum_attach_engine_remaining_modes` 的 live list，没有 live 证据不得继续。
2. S1 审计 FreeCAD source，不改 code，不晋级 support 状态。
3. S2 把 conic landmarks、Folding、IntersectionPoint、TangentU/V 分别路由。
4. S3 只冻结 conic landmark 可实现合同。
5. S4 已冻结 excluded families 的后续包边界。
6. S5 已冻结 request-local response 与 capability 发布口径：后端只消费本次 request graph，response 只返回 placement、diagnostics 和 update suggestions。
7. S6 批量采集 conic oracle、落 C++、fixtures、focused tests 和 docs/capability closeout。

## 通用验收

```bash
cd /Users/li/Chili3DProject/FreeCAD
git diff --check -- docs/CADCore5.0-PartDesign-高价值剩余语义/C5-M17-DatumRemainingAttachEngine主线 docs/CADCore5.0-PartDesign-高价值剩余语义/README.md docs/CADCore5.0-PartDesign-高价值剩余语义/矩阵
for f in docs/CADCore5.0-PartDesign-高价值剩余语义/C5-M17-DatumRemainingAttachEngine主线/矩阵/*.tsv docs/CADCore5.0-PartDesign-高价值剩余语义/矩阵/*.tsv; do awk -F '\t' 'NR==1{n=NF} NF!=n{print FILENAME ":" NR ": expected " n " fields, got " NF}' "$f"; done
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore5.0-PartDesign-高价值剩余语义/C5-M17-DatumRemainingAttachEngine主线 --format markdown
```

## 当前队列头

S5 完成后，goal runner 的队列头应推进到 S6；S6 仍待执行，不得提前标 `【已实现】`。

## 非目标

- 不重新打开 C5-M14/C5-M15/C5-M16 已关闭 modes。
- 不在 conic S6 中实现 Folding、IntersectionPoint、TangentU/V。
- 不要求 docs-only 阶段运行 cad-core build。
