# 【已实现】C5-M18 工作步骤总入口

## 目标

建立 C5-M18 DatumFoldingAttachEngine 的 S0-S6 执行队列。本文是索引文件，创建后标 `【已实现】`，避免 goal runner 把索引当作独立实现步骤。

## 步骤索引

| 步骤 | 文件 | 当前状态 | 解决的问题 |
| --- | --- | --- | --- |
| S0 | [6-23-18-18-【已实现】C5-M18-S0-liveFoldingBlocker冻结.md](6-23-18-18-【已实现】C5-M18-S0-liveFoldingBlocker冻结.md) | 已实现 | 冻结 live blocker 与发布边界 |
| S1 | [6-23-18-19-【已实现】C5-M18-S1-FreeCADFolding源码候选矩阵.md](6-23-18-19-【已实现】C5-M18-S1-FreeCADFolding源码候选矩阵.md) | 已实现 | FreeCAD source authority 与 source candidates |
| S2 | [6-23-18-20-【已实现】C5-M18-S2-scope准入与待实现矩阵.md](6-23-18-20-【已实现】C5-M18-S2-scope准入与待实现矩阵.md) | 已实现 | scope / backendGap / nonGoal routing |
| S3 | [6-23-18-21-【已实现】C5-M18-S3-Folding合同复审.md](6-23-18-21-【已实现】C5-M18-S3-Folding合同复审.md) | 已实现 | four-line support、fold-angle、placement contract |
| S4 | [6-23-18-22-【已实现】C5-M18-S4-requestLocalBoundary复审.md](6-23-18-22-【已实现】C5-M18-S4-requestLocalBoundary复审.md) | 已实现 | request-local support/writeback 和 no-session 边界 |
| S5 | [6-23-18-23-【已实现】C5-M18-S5-OracleFixtures与Expected.md](6-23-18-23-【已实现】C5-M18-S5-OracleFixtures与Expected.md) | 已实现 | FreeCADCmd success / diagnostics expected |
| S6 | [6-23-18-24-【已实现】C5-M18-S6-实现与发布闸门.md](6-23-18-24-【已实现】C5-M18-S6-实现与发布闸门.md) | 已实现 | C++、fixtures、focused tests、capability、docs closeout |

## 执行顺序

1. S0 先确认 `Folding` 是 live exact blocker，且 C5-M18 只能删除 expected-backed `Folding`。
2. S1 审计 FreeCAD `mmFolding` 和 `calculateFoldAngle()`。
3. S2 把 `Folding` 归入本包 backendGap，把 `TangentU/V`、`IntersectionPoint` 和 GUI/session/full BREP 保留为 non-goal 或 later package。
4. S3 冻结 four-line support contract、diagnostics 和 placement axes。
5. S4 冻结 request-local graph 输入、writeback suggestion 和 no-session 边界。
6. S5 采集 FreeCADCmd success expected 和 diagnostics evidence。
7. S6 落 C++、fixtures、focused tests、capability/docs 和 root matrices closeout。

## 通用验收

```bash
cd /home/user/Chili3DProject/FreeCAD
git diff --check -- cad-core docs/CADCore5.0-PartDesign-高价值剩余语义/C5-M18-DatumFoldingAttachEngine主线 docs/CADCore5.0-PartDesign-高价值剩余语义/README.md docs/CADCore5.0-PartDesign-高价值剩余语义/矩阵
for f in docs/CADCore5.0-PartDesign-高价值剩余语义/C5-M18-DatumFoldingAttachEngine主线/矩阵/*.tsv docs/CADCore5.0-PartDesign-高价值剩余语义/矩阵/*.tsv; do awk -F '\t' 'NR==1{n=NF} NF!=n{print FILENAME ":" NR ": expected " n " fields, got " NF}' "$f"; done
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore5.0-PartDesign-高价值剩余语义/C5-M18-DatumFoldingAttachEngine主线 --format markdown
```

## 当前队列状态

C5-M18 package-local 队列已清空；后续只通过 dedicated `IntersectionPoint` 或 surface tangent package 继续剩余 exact blocker。

## 非目标

- 不重新打开 C5-M14 至 C5-M17 已关闭 modes。
- 不实现 `TangentU/V`、`IntersectionPoint`。
- 不要求 docs-only 阶段运行全量 FreeCAD build。
