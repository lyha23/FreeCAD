# 【已实现】C5-M15-S2 scope 准入与待实现矩阵

状态：`s2_scope_routing_verified`

## 目标

把 S1 的 source candidates 路由到 scope、blocker、backendGap、nonGoal 和 oracle fixture 行。S2 的结果必须让 S6 可以直接落代码，而不是停留在 broad AttachEngine 描述。

## 分类规则

| 分类 | 准入条件 | C5-M15 处理 |
| --- | --- | --- |
| `pending_backendGap` | FreeCAD 行为明确，cad-core 当前未实现 | `Translate`、`ThreePointsPlane`、`ThreePointsNormal`、`TangentPlane` |
| `pending_native_oracle` | 需要 FreeCADCmd expected 后才能晋级 | success + diagnostics fixtures |
| `releaseGate` | 需要 capability/docs/test 发布收口 | exact blocker 移除和 C5/C51 docs 同步 |
| `nonGoal` | 属于其它 family 或 source route 不清 | Folding、curve frame/curvature、conic landmarks、IntersectionPoint |

## scope 行

| scope | 状态 | S6 blocker |
| --- | --- | --- |
| `C5M15-SCOPE-101` Translate | `pending_backendGap` | `C5M15-BLK-101` |
| `C5M15-SCOPE-201` ThreePointsPlane | `pending_backendGap` | `C5M15-BLK-201` / `202` |
| `C5M15-SCOPE-202` ThreePointsNormal | `pending_backendGap` | `C5M15-BLK-201` / `202` |
| `C5M15-SCOPE-301` TangentPlane | `pending_backendGap` | `C5M15-BLK-301` / `302` |
| `C5M15-SCOPE-302` TangentPlane diagnostics | `pending_native_oracle` | `C5M15-BLK-302` |
| `C5M15-SCOPE-501` capability/docs closeout | `releaseGate` | `C5M15-BLK-501` |
| `C5M15-SCOPE-900` excluded family | `nonGoal` | `C5M15-NG-001..005` |

## 必须回写的矩阵行

- `c5m15_datum_3d_plane_scope_review_matrix.tsv`：所有 `C5M15-SCOPE-*`。
- `c5m15_datum_3d_plane_blocker_queue.tsv`：所有 `C5M15-BLK-*`。
- `c5m15_datum_3d_plane_backend_gap_classification.tsv`：`C5M15-CAT-*`。
- `c5m15_datum_3d_plane_non_goal_registry.tsv`：excluded family。

## 本轮回写结果

- `scope_review_matrix.tsv` 已确认 `C5M15-SCOPE-101/201/202/301` 为 `pending_backendGap`，`C5M15-SCOPE-302` 为 `pending_native_oracle` diagnostic route，`C5M15-SCOPE-501` 为 `releaseGate`，`C5M15-SCOPE-900` 为 `nonGoal`。
- `blocker_queue.tsv` 已让 `C5M15-BLK-101/201/202/301/302/501` 都带 required evidence、cad-core landing、fixture/test route、next step 和 close condition；S6 可直接按这些 blocker 落代码。
- `backend_gap_classification.tsv` 已为每个 `C5M15-CAT-*` 写明 classification、scope、blocker 和 recommended next step，不留下未解释 backendGap。
- `fixture_oracle_matrix.tsv` 已把 success fixture、diagnostics fixture 和 release gate 对齐到对应 scope/blocker；状态仍是 `pending_native_oracle` 或 `releaseGate`，本轮不采集 expected。
- `non_goal_registry.tsv` 已记录 Folding、curve frame / curvature、conic landmarks、IntersectionPoint 和 GUI/session 的用户或协议行为与 reopen condition；这些 family 不进入 S6。

## 验收

```bash
cd /home/user/Chili3DProject/FreeCAD
for f in docs/CADCore5.0-PartDesign-高价值剩余语义/C5-M15-Datum3DPlaneAttachEngine主线/矩阵/*.tsv; do awk -F '\t' 'NR==1{n=NF} NF!=n{print FILENAME ":" NR ": expected " n " fields, got " NF}' "$f"; done
rg -n 'pending_backendGap|pending_native_oracle|releaseGate|nonGoal' docs/CADCore5.0-PartDesign-高价值剩余语义/C5-M15-Datum3DPlaneAttachEngine主线/矩阵
rg -n 'C5M15-BLK-101|C5M15-BLK-201|C5M15-BLK-301|C5M15-BLK-501|C5M15-CAT-900' docs/CADCore5.0-PartDesign-高价值剩余语义/C5-M15-Datum3DPlaneAttachEngine主线/矩阵
```

验收标准：

- 每个 in-scope item 都有 blocker、fixture/test route 和 close condition。
- 每个 nonGoal 有用户/协议行为和 reopen condition。
- 不存在未解释的 `backendGap`。
- S2 不修改 C++，不采集 expected。

## 非目标

- 不把 `Folding` 或 curve frame 拆到 C5-M15 S6。
- 不把 `TangentPlane` 的风险降级为 docs-only。
