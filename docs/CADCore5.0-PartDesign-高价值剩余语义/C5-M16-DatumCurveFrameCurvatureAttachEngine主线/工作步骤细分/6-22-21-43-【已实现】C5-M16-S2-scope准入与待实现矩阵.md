# 【已实现】C5-M16-S2 scope 准入与待实现矩阵

## 目标

把 S1 source candidates 路由到 scope、blocker、backendGap、fixture/oracle 和 non-goal，形成 S3-S6 可执行合同。

## 工作内容

1. 对 `NormalToPath` / `FrenetNB/TN/TB` 标记 `pending_backendGap`，进入 S3。
2. 对 `Concentric` / `SectionOfRevolution` 标记 `pending_backendGap`，进入 S4。
3. 对 `AxisOfCurvature` / `Normal` / `Binormal` / `CenterOfCurvature` 标记 alias batch，进入 S4。
4. 对 invalid diagnostics 标记 `pending_native_oracle`，进入 S6。
5. 对 `Folding`、conic landmarks、`IntersectionPoint`、`TangentU/V` 标记 `nonGoal` 或 later package。

## 产物

- `矩阵/c5m16_datum_curve_frame_scope_review_matrix.tsv`
- `矩阵/c5m16_datum_curve_frame_blocker_queue.tsv`
- `矩阵/c5m16_datum_curve_frame_backend_gap_classification.tsv`
- `矩阵/c5m16_datum_curve_frame_fixture_oracle_matrix.tsv`
- `矩阵/c5m16_datum_curve_frame_non_goal_registry.tsv`
- 根 `README.md` 与根矩阵 C5-M16 状态行

## S2 冻结结论

- `NormalToPath`、curve support、`attachParameter`、optional vertex projection 与 `FrenetNB/TN/TB` 已路由为 `pending_backendGap`，下一步进入 S3。`NormalToPath` 只作为 shared projection/helper 合同，不作为本包 release mode。
- `Concentric`、`SectionOfRevolution` 已路由为 `pending_backendGap`，进入 S4 curvature-center 合同。
- `AxisOfCurvature`、`Normal`、`Binormal`、`CenterOfCurvature` 已路由为 alias batch，进入 S4，并要求复用 3D curve-frame helper。
- invalid diagnostics 已路由为 `pending_native_oracle`，进入 S6；但 release/capability 仍 `blocked_by_c5m15_s6`，不得提前移除 exact blocker。
- `Folding`、conic landmarks、`IntersectionPoint`、`TangentU/V`、GUI/session 已冻结为 non-goal / later package，并在 registry 中保留 reopen condition。
- 每个 package-local blocker 已补 source authority、cad-core landing、fixture/test route、close condition；根矩阵状态已前进到 S3 pending。

## 完成条件

- 没有未解释的 backendGap。
- 每个 blocker 都有 source authority、cad-core landing、fixture/test route 和 close condition。
- non-goal 有 reopen condition。

## 验收

```bash
cd /Users/li/Chili3DProject/FreeCAD
for f in docs/CADCore5.0-PartDesign-高价值剩余语义/C5-M16-DatumCurveFrameCurvatureAttachEngine主线/矩阵/*.tsv; do awk -F '\t' 'NR==1{n=NF} NF!=n{print FILENAME ":" NR ": expected " n " fields, got " NF}' "$f"; done
```
