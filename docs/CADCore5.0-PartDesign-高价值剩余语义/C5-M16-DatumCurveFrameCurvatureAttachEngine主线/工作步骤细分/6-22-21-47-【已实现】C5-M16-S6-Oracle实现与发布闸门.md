# 【已实现】C5-M16-S6 Oracle 实现与发布闸门

## 目标

消费 C5-M16 的 `pending_native_oracle`、`pending_backendGap` 和 `releaseGate` 行，完成 FreeCADCmd expected、cad-core C++、fixtures、focused tests、capability/docs 和验收记录闭环。

状态：`done_expected_backed_release_gate_closed`

## 队列消费规则

- 必须在 C5-M15 S6 已关闭后执行。
- 只消费 `C5M16-BLK-101`、`201`、`301`、`401`、`501`。
- `C5M16-NG-*` 不进入实现队列。
- 若某一 curve mode 无法稳定采集 oracle，必须拆 precise blocker，不得把整个 package 写成 supported。
- 不创建 cad-core-output-derived expected。

## 本轮代码实现目标

| blocker | C++ / fixture 落点 | FreeCAD authority | focused tests | 成功标准 |
| --- | --- | --- | --- | --- |
| `C5M16-BLK-101` | `cad-core/src/part_design/datum_attachment.h`、`fixtures/c51m5/partdesign-datum-curve-frame-modes.json` | `Attacher.cpp:1674-1755` | `test_c51x_datum_curve_frame_modes_match_expected`;`test_c51x_datum_curve_frame_invalid_diagnostics` | edge parameter、vertex projection、D1 failure 与 FreeCAD expected 一致；`projection_failed` 为 source/code-path-backed diagnostic |
| `C5M16-BLK-201` | `datum_attachment.h`、success + diagnostics fixture | `Attacher.cpp:1766-1831` | modes + diagnostics | FrenetNB/TN/TB frame 和 undefined normal diagnostics 一致 |
| `C5M16-BLK-301` | `datum_attachment.h`、success + diagnostics fixture | `Attacher.cpp:1832-1847` | modes + diagnostics | Concentric / SectionOfRevolution 曲率中心和 infinite radius diagnostics 一致 |
| `C5M16-BLK-401` | `datum_attachment.h`、`datum_line.cpp`、`datum_point.cpp` | `Attacher.cpp:2400-2483,2833-2889` | modes parity | AxisOfCurvature / Normal / Binormal / CenterOfCurvature aliases 走 3D helper |
| `C5M16-BLK-501` | `cad-core/src/adapters/c_api/c_api.cpp`、C5/C51 docs、root/package matrices | capability exact blocker source | adapter capability test | exact blocker 只移除本包 proven modes |

## Oracle batch

| fixture | 场景 | expected 字段 |
| --- | --- | --- |
| `fixtures/c51m5/partdesign-datum-curve-frame-modes.json` | `FrenetNB/TN/TB` edge parameter；vertex projection；circle/arc curvature；`Concentric`；`SectionOfRevolution`；DatumLine alias；DatumPoint alias | placement base/rotation, object_fields.map_mode, alias source mode, documentObjectUpdates if applicable |
| `fixtures/c51m5/partdesign-datum-curve-frame-diagnostics.json` | missing support, wrong shape, zero derivative, undefined Frenet normal, infinite curvature radius | diagnostic codes, object/property/subname context；`projection_failed` 暂无稳定 primitive fixture |

## 验收分层

本轮短跑：

```bash
cd /Users/li/Chili3DProject/FreeCAD
git diff --check -- docs/CADCore5.0-PartDesign-高价值剩余语义/C5-M16-DatumCurveFrameCurvatureAttachEngine主线 docs/CADCore5.0-PartDesign-高价值剩余语义/README.md docs/CADCore5.0-PartDesign-高价值剩余语义/矩阵 cad-core
```

Focused oracle / tests：

```bash
cd /Users/li/Chili3DProject/FreeCAD/cad-core
FREECADCMD=/Users/li/.cargo/bin/freecadcmd python3 tools/collect_freecad_expected.py fixtures/c51m5/partdesign-datum-curve-frame-modes.json --check
FREECADCMD=/Users/li/.cargo/bin/freecadcmd python3 tools/collect_freecad_expected.py fixtures/c51m5/partdesign-datum-curve-frame-diagnostics.json --check
python3 -m unittest tests.test_p7_features.CadCoreP7FeatureTest.test_c51x_datum_curve_frame_modes_match_expected tests.test_p7_features.CadCoreP7FeatureTest.test_c51x_datum_curve_frame_invalid_diagnostics
python3 -m unittest tests.test_expected_fixtures tests.test_adapters.CadCoreAdapterTest.test_c_api_capabilities_exposes_web_contract_facts
```

阶段收口：

```bash
cd /Users/li/Chili3DProject/FreeCAD/cad-core
cmake --build build
python3 -m unittest tests.test_p7_features tests.test_expected_fixtures tests.test_adapters
```

## 发布闸门

- C5-M15 S6 已关闭；否则不得修改 `datum_attach_engine_remaining_modes`。
- `c_api.cpp` exact blocker 不再列出本包 expected-backed proven modes：`FrenetNB`、`FrenetTN`、`FrenetTB`、`Concentric`、`SectionOfRevolution`、`AxisOfCurvature`、`Normal`、`Binormal`、`CenterOfCurvature`。如果某个 mode 只能 precise blocker split，不能删除。
- `Folding`、`Focus1/2`、`Directrix1/2`、`Asymptote1/2`、`IntersectionPoint`、`TangentU/V` 和 GUI/session 仍在 exact blocker 或 non-goal。
- `fixtures/c51m5/expected` 中存在 FreeCADCmd-backed expected，不是 cad-core output。
- focused tests 和 adapter capability test 通过。
- C5 README、global matrices、package matrices、C51X docs 的状态一致。
- goal queue 不再显示 C5-M16 待执行步骤后，把 S6 和主线入口改名为 `【已实现】`。

## 非目标

- 不实现 `Folding`、conic landmarks、`IntersectionPoint`、`TangentU/V`。
- 不把 straight-line / undefined normal 的失败用默认 frame 掩盖。
- 不引入跨请求 backend session 或完整 BREP 状态。
