# C5-M6-S3 剩余复杂 Profile 与高级契约分流

## 目标

把 C5-M6 不应继续扩大的剩余边界拆清 owner：Loft `complex_profile_family` 与 Sweep advanced PipeShell contracts 进入后续独立包，不在本轮靠旧 gap 或 wrapper 名称推进实现。

## 必读

- `cad-core/src/adapters/c_api/c_api.cpp` 中 `part_workbench.loft` / `part_workbench.sweep`
- `cad-core/tests/test_adapters.py` 对 Loft / Sweep remaining gaps 的断言
- `docs/CADCore3.0/capabilities-gap对照表.md`
- `docs/CADCore3.0/oracle-fixture队列.md`
- 本包 non-goal registry。

## 工作内容

1. 确认 Loft 只剩 `complex_profile_family`，且仍为 explicit remaining gap / non-goal。
2. 确认 Sweep advanced contracts 包括 auxiliary spine、support mode、binormal、location mode、tolerance 等，不混成 supported。
3. 写清未来包建议：
   - `future_loft_complex_profile_family`
   - `future_sweep_advanced_contract`
4. 更新本包矩阵和 CADCore3.0 文档，不新增 C++。

## 验收

```bash
cd /Users/li/Chili3DProject/FreeCAD
git diff --check -- docs/CADCore5.0-PartDesign-高价值剩余语义/C5-M6-PartWorkbenchSurface-ProfilePostProcess第二批主线 docs/CADCore3.0
```

完成后重命名为 `6-20-22-07-【已实现】C5-M6-S3-剩余复杂Profile与高级契约分流.md`。
