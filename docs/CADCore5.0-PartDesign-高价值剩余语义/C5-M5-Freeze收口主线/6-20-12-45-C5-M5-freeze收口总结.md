# C5-M5 freeze 收口总结

## 当前基线

- 仓库：`/Users/li/Chili3DProject/FreeCAD`
- commit：`be97abba70 docs: 新增 CADCore5.0 PartDesign 高价值剩余语义方案`
- 收口边界：只同步 C5-M1 到 C5-M4 已实现或 deferred diagnostic 状态，不新增 PartDesign 大语义，不修改 expected。

## executed mainlines

- C5-M0 队列为空；范围审计和 C5 matrix baseline 已关闭。
- C5-M1 队列为空；Revolution / Groove `TwoAngles`、Groove `ThroughAll`、Part EdgeN axis 已 supported，UpTo / Profile subshape / `FuseOrder=FeatureFirst` 保持 locatable diagnostics。
- C5-M2 队列为空；Boolean `AllowCompound=true` multi-solid、multi-tool ownership 和 missing/non-solid diagnostics 已同步；LinkStage3-only `Compound` / `Section` 保持 `C5-NG-005` non-goal / unsupported Type diagnostic。
- C5-M3 队列为空；Pipe `Transformation=Multisection`、`Mode=Frenet`、`Transition=Right corner` 已 expected-backed；Loft Closed / multi-wire / AllowCompound pressure、Pipe Auxiliary / Binormal / scaling / SpineTangent、full sewing MapperHistory 保持 deferred diagnostics。
- C5-M4 队列为空；不发布 selected map mode support，active `AttachmentSupport` / `MapMode` / `AttachmentOffset` / reverse / path parameter 和 downstream attached Datum 引用保持 stable diagnostics。
- C5-M5 本轮关闭 `C5-BLK-501`、`C5-SCOPE-501`、`C5M5-FR-*` 与 `C5M5-FR-BLK-001`，并将 M5 step 重命名为 `【已实现】`。

## remaining deferred / non-goal boundaries

- Revolution / Groove：UpToFirst / UpToLast / UpToFace 的 BRepFeat path、Profile subshape selection、`FuseOrder=FeatureFirst`、Datum/App line axis native oracle。
- Boolean：`Compound` / `Section` 继续作为 `C5-NG-005`，除非后续产品范围显式重开。
- Loft / Pipe：Loft Closed / multi-wire full native parity、explicit subelement selection、full MapperSewing propagation；Pipe Auxiliary / Binormal / Fixed / Round corner / scaling laws / SpineTangent / full front-back sewing history。
- Datum Attachment：GUI Attachment editor、full AttachEngine map-mode solver、selected map modes、AttachmentSupport shadow-sub writeback。
- 全局非目标：不做完整 FreeCAD PartDesign Workbench 迁移，不在 adapter 写业务语义，不用输出顺序 / bbox / fixture 名称修正 topo。

## validation

- `cd /Users/li/Chili3DProject/FreeCAD && git diff --check -- docs/CADCore5.0-PartDesign-高价值剩余语义 cad-core`：通过，无输出。
- `cd /Users/li/Chili3DProject/FreeCAD/cad-core && cmake --build build`：通过，`cad-core-lib`、`cad_core_ffi`、`cad-core` 和 probe target 均已构建。
- `cd /Users/li/Chili3DProject/FreeCAD/cad-core && python3 -m unittest tests.test_p7_features tests.test_expected_fixtures tests.test_adapters`：通过，`Ran 131 tests in 71.927s`，`OK (skipped=16)`。
- `cd /Users/li/Chili3DProject/FreeCAD/cad-core && python3 -m unittest tests.test_mvp tests.test_diagnostics tests.test_feature_flows tests.test_p5_sketch tests.test_p6_topology tests.test_p7_features tests.test_p8_features tests.test_expected_fixtures tests.test_adapters`：通过，`Ran 524 tests in 164.578s`，`OK (skipped=16)`。
- `python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore5.0-PartDesign-高价值剩余语义/C5-M5-Freeze收口主线/工作步骤细分 --format markdown`：通过，空队列。
