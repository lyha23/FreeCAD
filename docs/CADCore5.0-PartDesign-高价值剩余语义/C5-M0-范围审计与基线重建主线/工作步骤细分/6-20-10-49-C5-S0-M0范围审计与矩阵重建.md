# C5-S0 M0 范围审计与矩阵重建

## 目标

复核 C4 freeze 后的 PartDesign remaining boundary，确认 C5 只从 Revolution/Groove、Boolean/Body ownership、Loft/Pipe advanced 和 Datum Attachment 四个具体 owner 进入。

## 必读

- `docs/CADCore5.0-PartDesign-高价值剩余语义/6-20-10-47-CADCore5.0-PartDesign高价值剩余语义总览方案.md`
- `docs/CADCore5.0-PartDesign-高价值剩余语义/矩阵/cadcore5_source_candidates.tsv`
- `docs/CADCore5.0-PartDesign-高价值剩余语义/矩阵/cadcore5_scope_review_matrix.tsv`
- `docs/CADCore5.0-PartDesign-高价值剩余语义/矩阵/cadcore5_blocker_queue.tsv`

## 工作内容

- 校验 C5 全局矩阵和各专题包 package-local 矩阵是否一致。
- 确认每个 pending blocker 都有明确 source authority、cad-core 落点、oracle / diagnostic 路径和验收命令。
- 若发现范围过宽，先拆 owner 和矩阵，不直接进入实现。

## 验收

```bash
cd /Users/li/Chili3DProject/FreeCAD
git diff --check -- docs/CADCore5.0-PartDesign-高价值剩余语义
python3 ~/.codex/skills/goal-step-runner/scripts/step_goal_queue.py docs/CADCore5.0-PartDesign-高价值剩余语义/C5-M0-范围审计与基线重建主线/工作步骤细分 --format markdown
```

## 完成条件

- `C5-BLK-001` / `C5M0-BLK-001` / `C5M0-BLK-002` 关闭或转成更具体 blocker。
- 所有 C5 专题包入口、方案、矩阵和步骤队列可以被下一轮 worker 直接执行。
