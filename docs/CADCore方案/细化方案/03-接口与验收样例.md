# 接口与验收样例

本文件固定 CAD Core 的输入、输出、fixture 和 diagnostics 口径。它不是实现日志；新增能力只在这里补充验收分层和稳定错误码。

## CLI

```bash
cd /Users/li/Chili3DProject/重构Chili/FreeCAD/cad-core
./cad-core recompute fixtures/mvp/rect-pad.json --output out/rect-pad.result.json
```

输入是完整 `DocumentObject graph`，不是局部操作命令。`recompute.objs` 只表示本次计算目标。

CLI 可选把本次 recompute 已计算出的对象写成文件：

```bash
./cad-core recompute fixtures/p8/part-box.json \
  --output out/part-box.result.json \
  --export-object Box \
  --export-format brep \
  --export-file out/part-box.brep
```

当前导出格式为 `brep` / `step` / `stl`，`stl` 可额外传 `--stl-deflection`。导出使用本次请求内的 `TopoDS_Shape`，是 CLI adapter 的文件副作用；BREP / STEP / STL 内容不进入 `DocumentObject graph` 或默认 JSON 状态。

## C ABI

`cad_core_ffi` 是薄 adapter。它应和 CLI 调用同一个 core recompute 入口，返回同一类 JSON 结果和 diagnostics。C ABI 不直接解析 FreeCAD 业务属性，不保存 shape，也不补拓扑命名。

## 输入规范

```json
{
  "Objects": [
    {
      "Name": "Pad",
      "ID": 2,
      "TypeId": "PartDesign::Pad",
      "Properties": {
        "Profile": {
          "PropertyType": "App::PropertyLinkSub",
          "value": "Sketch",
          "SubList": []
        },
        "Type": "Length",
        "Length": 10
      }
    }
  ],
  "recompute": {
    "objs": ["Pad"]
  }
}
```

规则：

- `Objects[]` 中的 `Name` 是运行时引用 key。
- `TypeId` 决定 executor。
- `Properties` 沿用 FreeCAD 属性名。
- `PropertyLink*` 归一为 dependency edge。
- `StableSubList` / `FullSubList` 可与 `SubList` 并存，用于 topo naming 引用恢复。

## 输出规范

结果至少包含：

```text
objects
mesh
subshapes
named_shapes
diagnostics
```

diagnostics 字段口径：

```text
level
code
message
object
property
stage
target
subname
```

常用错误码：

```text
parse_error
missing_target
missing_link_target
cycle_dependency
unsupported_type
unsupported_property
unsupported_geometry
unsupported_subshape_kind
invalid_property_type
invalid_link_value
invalid_placement
invalid_subshape
invalid_length
invalid_angle
open_profile
execution_failed
unsupported_stable_subname
split_stable_subname
deleted_stable_subname
```

## Fixture 分层

| 目录 | 覆盖范围 |
| --- | --- |
| `fixtures/mvp` | P0/P1 基础输入、Pad、基础 diagnostics |
| `fixtures/p2` | FeatureBase、Pocket、Body fuse/cut |
| `fixtures/p3a` | ThroughAll、UpToFace、UpToShape |
| `fixtures/p3b` | Two sides、Symmetric、UpToFirst/Last、taper、inner-wire taper、custom direction、placement |
| `fixtures/p4` | typed property、LinkList/SubList、placement、dependency diagnostics |
| `fixtures/p5` | Sketch profile、BSpline profile、mixed/nested closed-wire face-with-holes、ExternalGeometry、InternalShape、unsupported Sketcher 能力 |
| `fixtures/p6` | NamedShape、ElementMap、stable subname、split/deleted diagnostics、terminal history 跨后续 maker 传播 |
| `fixtures/p7` | Datum、RefineModel Pad standalone / Body AddSub final-result / Fillet / Chamfer / Transformed family replacement、Hole 基础孔 / point / tapered / head-cut / drill-point / cosmetic threaded / fine threaded / UNC / UNF / UNEF / NPT / BSP / BSW / BSF / ISOTyre threaded / ISO metric / UTS / generic fallback thread clearance / HoleCutType 资源表加载 / ThreadDepth 与 DIN76 参数归一化 / ModelThread metric pipe-shell 几何、DressUp、SupportTransform 与链式 SupportTransform、Transformed family、Whole shape、Pattern sketch axis、transformed stable history diagnostics |
| `fixtures/p8` | Part primitive、BREP / STEP / IGES / STL import、CLI BREP / STEP / STL export round-trip、App::Link full-shape / SubList 单/多 subshape compound / LinkGroup source alias subshape / missing target / invalid subshape / ElementCount 折叠数组 / 已物化 ShowElement 子 LinkElement 认领 / LinkSub mapped postfix alias / Link retag terminal history diagnostics、App::LinkElement、App::LinkGroup 显式 ElementList / VisibilityList、Assembly::AssemblyLink、Assembly::AssemblyObject、Part Boolean、BOPTools FeatureXOR、BOPTools BooleanFragments Standard / Split / CompSolid mode |

## 验收规则

- 正常 fixture diagnostics 必须为空。
- 错误 fixture 的 diagnostics code 必须稳定。
- 几何 fixture 优先校验 bbox、volume、topology count、subshape / named shape 语义。
- FreeCAD oracle 不可用时，必须在 fixture 或测试名中明确 known gap。
- 文档、fixture 和 tests 必须同步更新。

### `expected/*.freecad.json` 口径

- `fixtures/<phase>/expected/*.freecad.json` 是成功几何 fixture 的 FreeCAD / oracle golden，不是每个输入 fixture 的一对一索引。
- 错误 fixture 只固定 `diagnostics.code`，不要为了凑数量新增空 `expected` 文件。
- 对 bbox、volume、topology count、mesh summary、named shape 语义做断言的成功几何 fixture，应优先使用同名 expected 文件，避免把几何预期分散硬编码在 Python 测试里。
- expected 文件优先包含 `object`、`reference`、`freecad_version`、`bbox`、`volume`、`topology_counts`；确有需要时再补 `mesh_summary`、`named_shapes` 或 stable subname 相关字段。
- 老 fixture 如果已经存在内联几何断言，也必须迁移成 expected 文件；迁移规则见 `docs/5-30-04-33-CADCore-fixture-expected迁移方案.md`。
- 如果 FreeCAD oracle 暂不可用，只能写清楚 geometry-equivalent `reference` 或 `known_gap`，不能把 cad-core 当前输出伪装成 FreeCAD golden。

当前 expected 覆盖基线：`mvp=1`、`p2=2`、`p3a=5`、`p3b=24`、`p4=7`、`p5=56`、`p6=13`、`p7=64`、`p8=57`。成功几何 fixture 应由 `tests.test_expected_fixtures` 自动发现；错误 fixture 继续留在 diagnostics 矩阵。

## 常用命令

```bash
cd /Users/li/Chili3DProject/重构Chili/FreeCAD/cad-core
cmake --build build
python3 -m unittest tests/test_mvp.py
```
