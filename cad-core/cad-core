#!/usr/bin/env sh
set -eu

here=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
binary="$here/build/cad-core"

if [ ! -x "$binary" ]; then
  echo "cad-core has not been built. Run: cmake -S \"$here\" -B \"$here/build\" && cmake --build \"$here/build\"" >&2
  exit 127
fi

exec "$binary" "$@"

